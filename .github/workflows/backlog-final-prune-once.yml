name: Backlog final prune (one-time)

on:
  push:
    branches:
      - agent/backlog-hygiene-20260730

permissions:
  contents: write

jobs:
  prune:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: agent/backlog-hygiene-20260730

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Prune completed nested records and obsolete containers
        run: node scripts/backlog-final-prune-once.js

      - name: Verify paper-weight guard
        run: node scripts/check-paper-weight.js

      - name: Commit final docs-only cleanup
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add BACKLOG.md BACKLOG_ARCHIVE.md config/paper-weight.json docs/verification/BACKLOG_EDITORIAL_CLEANUP_2026-07-30.md scripts/backlog-final-prune-once.js .github/workflows/backlog-final-prune-once.yml
          git commit -m "docs: prune completed nested backlog records"
          git push origin HEAD:agent/backlog-hygiene-20260730
