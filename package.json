{
  "name": "atlas-workout-updater",
  "version": "1.0.0",
  "description": "Simple Atlas Workout Updater that appends workout data to Google Sheets.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "lint": "node --check index.js && node --check sheets.js && node --check services/analytics.js && node --check services/validation.js && node --check services/vision.js && node --check middleware.js && node --check response.js && node --check services/cache.js",
    "test": "npm run lint"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.3",
    "googleapis": "^121.0.0",
    "multer": "^1.4.5-lts.1",
    "openai": "^4.103.0"
  }
}
