.env -diff
.env.* -diff
.env.example diff
*.key -diff
*.pem -diff
service-account.json -diff
google-credentials.json -diff
credentials*.json -diff

# Self-hosted fonts — binary assets, never line-ending normalized or diffed.
*.woff2 binary -diff
