# Atlas — Backlog Archive

Completed (✅) and cancelled (~~struck~~) backlog items, moved out of [`BACKLOG.md`](./BACKLOG.md) on 2026-06-29 to keep the active backlog focused on open work. Nothing here is actionable — it is a historical record of shipped/closed work (merge cards, resolved findings). Section headers mirror the original backlog.


## Bug-report tooling

- ✅ **Richer one-tap bug capture** `[housekeeping / tooling]` — During the 2026-06-29 live test the owner triaged 18 `Bug_Reports` rows; several root causes had to be INFERRED because the report captured downstream state but not the proximate cause. Enriched what one tap of "Report Bug" captures, so the next live test is diagnosable without the owner typing anything. Added (all client-side, `public/app.js`, all riding inside the existing `Payload JSON` column — **no `Bug_Reports` schema change**): (1) **request + response bodies** for the recent API calls (redacted via the existing secret patterns, truncated to 1000 chars) — e.g. exactly what `/api/parse-workout-text` returned for "Push ups 40 40 40"; (2) **error history** (`recent_errors`, last 12) not just `last_error`, so a poison cascade (one bad set 400ing every save) shows AS a cascade; (3) **client JS error capture** — `window.onerror` + `unhandledrejection` (the silent-lockup class that never reaches `api()`); (4) **UI breadcrumbs** (`action_log`, last 30 taps, capture-phase so a `stopPropagation` handler still logs) — "tapped restore, nothing happened" now shows whether the tap fired; (5) **`ui_state`** — disabled/hidden flags of composer / preview-btn / approve-btn / editors (the "Save greyed out" + "composer won't let me type" class, captured directly not inferred); (6) **`service_worker`** state — controller + waiting SW + atlas cache keys ("a fix didn't take = stale shell"); (7) **device clock + tz + connection** (local-clock-sensitive session-id/date bugs). Bounded: per-call body cap + a total-size guard (`BUG_REPORT_SIZE_BUDGET`) sheds bodies oldest-first so the report always fits the Sheets per-cell limit; recent-calls entries are cloned so trimming never mutates the live buffer. Capture helpers never throw. Server (`services/bugReport.js`) unchanged — new fields auto-ride in `Payload JSON`. Shell `v71→v72`. Tests: `test/unit.test.js` (ring buffers + bounds, error/tap listeners, api body capture + redaction, payload fields, size guard). No write-path / trust-loop / proof-field / schema / parser change.
- ✅ **Bug_Reports summary columns** `[schema — owner-approved]` (owner: "add new heads to the spread sheet") — Promote the most-scannable signals from the enriched one-tap report out of `Payload JSON` into their own grid columns. **Append-only schema change** to the `Bug_Reports` tab — five new columns added at the END (after `Payload JSON`) so historical 9-cell rows stay aligned; never insert/reorder. New columns + their derivation (`services/bugReport.js` `bugReportSummaryFields`, computed server-side from the already-redacted payload, each defaulting to `''` when absent so older clients / the `/bug` path just leave blanks): **Error Count** (len of `recent_errors` — spots a poison cascade), **Last Failed Endpoint** (most recent failing request, e.g. `POST /api/log-workout`), **Last Action** (last UI breadcrumb, e.g. `tap: restore`), **Stale Shell** (`yes` when a SW update is waiting), **UI Blocked** (e.g. `Save disabled, composer disabled`). Server-only — the client already sends the source fields; **no `public/app.js` / shell change**. Tests: `test/unit.test.js` (`Bug_Reports append shape` deepEqual updated to 14 cols + two summary-derivation cases: populated + all-blank). **Owner migration step (one-time, manual — the agent has no Sheets write access and `ensureSheetTab` only writes a header when row 1 is empty):** add the five header labels to row 1 columns **J–N** of the live `Bug_Reports` tab, in this order: `Error Count | Last Failed Endpoint | Last Action | Stale Shell | UI Blocked`. New rows fill under them; old rows stay blank there. **Do NOT reorder columns in the sheet** — append order is contract-bound to `BUG_REPORT_COLUMNS`. No write-path / trust-loop / proof-field / parser change (only the optional `Bug_Reports` diagnostics tab).

- ✅ **Read-only bug-review endpoint** `[tooling]` (owner: "I just want you to have access to read my sheet so when it's time to review the bugs you can just review the sheet") — The agent sandbox has no Sheets credentials and the Google Drive MCP's per-call approval doesn't resolve in the remote/web execution environment, so direct sheet reads were blocked (the owner had to paste rows). Added `GET /api/bug-report` (`index.js`; `config/routes.js` `readOnly:true, writeCapable:false`; auth-gated by the global `/api` API-key guard) — a review feed that returns the most recent `Bug_Reports` rows **newest-first**, mapped to the column names, using the app's existing `getRecentRows` (prod service-account creds; no new credential in the sandbox). The heavy `Payload JSON` cell is dropped by default (the summary columns + note + last error cover triage); `?full=1` includes it, `?limit=N` (1–500, default 50) bounds the read. Read-only: no append, no schema touch, no write/trust-loop/proof-field/parser change. Tests: `test/api-smoke.test.js` (newest-first + column mapping + Payload-JSON dropped by default; `?full=1` includes it; no-API-key → 401). **Owner setup to use it:** add `ATLAS_BASE_URL` (deployed app URL) + `ATLAS_API_KEY` to this environment's variables so a session can `GET $ATLAS_BASE_URL/api/bug-report` and review without pasting. Only the Atlas API key is shared with the sandbox (never Google credentials), and it reaches only this read-only feed.
- ✅ **Verify the bug-review loop end-to-end** `[tooling — owner live]` — **DONE (2026-06-28 PT).** `GET /api/bug-report?limit=50&full=1` → `HTTP 200`, 17 rows, column-mapped, payloads on `?full=1`. Read-feed loop confirmed working. **Env gotcha found + resolved:** `ATLAS_API_KEY` kept collapsing to a single `X` because the key **began with `X#…`** and the environment stores vars in `.env` format where `#` starts a comment, so `ATLAS_API_KEY=X#…` parsed as just `X`. Fix = rotate the key to a value with **no `#`** (ideally no `$` either), set identically on Render + the environment; a *fresh* session then reads it (a running session never re-reads `process.env`). **Full triage of the 17 rows is recorded in [`docs/BUG_TRIAGE_LEDGER.md`](./docs/BUG_TRIAGE_LEDGER.md)** — 11 already shipped (PRs A–E / #678 / B4), 2 fixed pending an owner live re-test, **1 genuinely open** (next line), 3 noise. The ledger is the shared done-vs-open record so parallel sessions don't double-dip; convention = **every fix PR cites its `BUG-…` id in the commit message** and updates the ledger in the same PR.

## QA campaign — 2026-06-26 live gym + composer playtest (B1–B10)

- ✅ **B1 — Idempotent workout save / no duplicate `Log_Cleaned` rows** `[trust-critical]` (this PR) — **Verdict: STILL BROKEN → fixed.** Root cause: `/api/log-workout`'s live path had per-`write_id` replay dedup but — unlike its sibling `/api/complete-workout` — **no row-level composite-key guard** on `Log_Cleaned`. Because `public/app.js` mints a fresh `write_id` on every re-preview (`generateWriteId()`) while reusing the stable `session_id`, a re-previewed/re-approved save (e.g. after a failed effort import forces a re-preview) appended the same rows again under one session_id — the "49 sets / 51,390 lb" duplication. Fix: mirror the proven `getLogCompositeKeys()` row-level dedup onto `/api/log-workout` — skip rows whose `session‖exercise‖set_number` already exists; an all-duplicate re-save appends nothing and replays an idempotent "already logged" response the client recognises. Row-level (not session-level) so legitimate incremental logging still appends. Server-only — no edit to the restricted trust-loop file. Tests: 4 new api-smoke cases (re-submit new write_id appends nothing; partial-duplicate appends only new rows + totals match; replayed write_id appends once; failed-effort re-save no duplicate log rows). No schema / write-path-semantics / proof-field / parser change.
- ✅ **B2 — Canonical active session state** `[trust-critical]` — addressed across two complementary slices.
  - **Slice 1 — plan-card drift (PR #652):** the canonical model (`public/activeSession.js`) and `getCanonicalSession()`/`canonicalSessionRecap()` were already authoritative, with composer/next-up/save-payload/coach-context wired through it (P0 `docs/ACTIVE_SESSION_STATE_DIAGNOSIS.md` lane). The one surface still drifting was the **plan-card banner** (`renderActiveSessionBanner`): it rendered the current step from the raw `activePlannedSession.index` cursor, which lags after a logged set until "Next exercise →" is tapped. Fix: `syncPlannedIndexToCanonical()` reconciles the cursor forward to the canonical current inside the banner render, and `emitSetLogged` refreshes the banner after a log. Forward-only, never mutates the list, no write/preview/parser/schema change. Tests: `test/activeSessionPlanCard.test.js`.
  - **Slice 2 — success-card vs "didn't catch" contradiction (PR #653, this PR):** for a genuinely-unrecognized lift, the composer status showed the failure message "Didn't catch that lift — check the exercise name before saving" while the SAME parsed rows were still captured — `emitSetLogged` (`public/app.js`) buffers them into `sessionLog`/`sessionCompleted` and dispatches a confirmation card, then clears the very `parsedRowsEditor` the warning told the lifter to check. So a single input produced BOTH a green "logged" card AND a red "didn't catch" failure (owner live evidence: "RDLs" carded + warned + saved with muscle group Unknown). Fix: the composer now surfaces ONE consistent name-review advisory that AGREES with the card (`I don't recognize "<name>" — check the exercise name before it's saved.`) instead of a failure message; the set is still captured (no data loss) and the unresolved name is still flagged for correction. Gate (`shouldWarnUnknownLift`, incl. KB/catalog identity) unchanged. No write-path / trust-loop / proof-field / schema / parser change. Tests: `test/activeSessionSurfaceConsistency.test.js` (5 cases) + updated `test/cableFlyIdentity.test.js`. (Shell is network-first; no SW cache bump needed for the composer-copy change.)
  - **Slice 3 — save payload derives from the canonical session (this PR):** the last raw-holder read on the SAVE surface. The end-of-session manual closeout sent `plan_exercises` from the raw `activePlannedSession.exercises` holder (comment: "Fix deferred") while its mid-session sibling already derived from the canonical session via `currentPlannedExercise()`. Fix: the closeout `plan_exercises` now derives from `getCanonicalSession()` through the new pure `planExercisesFromCanonical()` (`public/app.js`) — the same canonical model the card / coach context / preview read, so the save payload can no longer drift from the visible session. Planned-origin only: an off-plan insert (Hammer Curls / Knee Raises) is logged work but never a PRESCRIBED substitution source, so it is excluded from `inferPrescribedPairs`; completed AND pending planned slots are both kept (a fulfilled lift must stay so its logged row is exact-matched/claimed; a pending lift is the legitimate swap source). Behavior-preserving for substitution inference, with a raw-plan fallback only when the canonical model is unavailable. `plan_exercises` rides along in the reused `pendingWrite.payload` to the live `/api/log-workout` call (`public/app.js` approve handler), but server-side it only feeds `inferPrescribedPairs` inside the `test_mode` preview branch (`index.js`) — it never affects the rows written, so this is behavior-preserving on both the preview and live paths. No write-path / trust-loop / proof-field / schema / parser change. Tests: `test/activeSessionSavePayload.test.js` (7 cases).
  - **B2 residual (deferred, follow-ups — overlap B8):** the remaining surfaces that still read divergent holders are squarely B8 and should be wired in their own slices: the composer placeholder (`buildWorkoutPlaceholder`) seeds from the Coach's-Pick suggestion, and `planStepFor`/`getNextExerciseInPlan` fallbacks read the raw session. These are surface-derivation polish (B8 "stale plan cards / composer placeholders") — not the trust-critical contradictions fixed above.
- ✅ **B3 — Failed effort import must not poison the workout save** `[correctness]` (this PR) — **Verdict: PARTIALLY BROKEN → fixed.** Effort & exercise are independent transactions; a failed *optional* effort never blocks or loses the exercise rows, and the copy is specific. Two paths verified against current repo evidence:
  - **Poisoning path (still broken → fixed):** `/api/complete-workout` only degraded gracefully when the screenshot parse THREW (Gemini 429/timeout). When the parse SUCCEEDED but returned invalid/incomplete effort metrics (e.g. missing `activeCalories`), `normalizeAndValidateParsedMetrics` threw and the handler returned a **400 "Parsed metrics validation failed"**, sinking the whole save and **losing the logged sets** — the exact B3 poisoning. Fix: when a screenshot is present AND there are logged sets (`req.file && !effortOnly`), an invalid-metrics validation failure now degrades the SAME way an unreadable screenshot does — save the sets with a blank effort row, set `screenshot_unreadable`/`effort_source: 'screenshot_unreadable'`, and surface the specific owner copy ("I couldn't read effort from the screenshot. I can still save the workout without effort data."). Manual effort and effort-only requests still fail honestly (nothing to fall back to). The frontend already renders this degrade state ("Effort couldn't be read from the screenshot" + warning + previews the sets), so no trust-loop file edit was needed. Server-only; no schema / proof-field / write-path-semantics / parser change.
  - **B1×B3 interaction (already fixed on this branch → regression test added):** the all-duplicate short-circuit on `/api/log-workout` was already guarded with `&& !formattedEffortRow` (commits `17af0ad`, `244ac99`), so a re-save where every log row is a duplicate but a genuinely-new effort row is attached now appends the effort row without re-appending log rows. The missing regression the backlog called for is added: *all log rows duplicate + a new effort row → effort still appends (`effort_rows_written:1`), log rows do not (`log_rows_written:0`)*.
  - **Tests:** 5 new api-smoke cases — invalid-parsed-metrics screenshot degrade (dry-run preview keeps the set + blank effort + specific copy); live invalid-metrics save writes the set with blank effort; effort-only invalid-metrics fails closed (422 + specific copy) and never appends; invalid-metrics degrade discards the rejected screenshot date on a no-manual-date save (convergence with the throw path); B1×B3 effort-appends-when-all-logs-duplicate regression.
- ✅ **B3 follow-up — Manual effort panel had no submit affordance** `[polish]` (this PR) — **Owner live-test gap (2026-06-28).** Found while validating B3's "manual effort can replace failed screenshot state": after typing manual effort, there was no obvious way to submit it — the only trigger was the composer's ↑ button, so the values "just sat there." Fix: an **"Add effort & preview"** button on the effort panel (`public/index.html`) that dispatches the SAME `logger-form` submit the ↑ does (`public/app.js`), so manual effort + the buffered session flow into one preview through the identical preview→approve→write path. Opens no new write path and changes no trust-loop semantics — purely a more discoverable trigger for the existing submit handler. Tests: `test/effortPreviewButton.test.js` (5 structural cases). Network-first shell — no SW cache bump. No schema / write-path / proof-field / parser change.
- ✅ **B5 — Apple Watch screenshot import must preserve the source date** `[correctness]` (this PR) — **Verdict: BROKEN → fixed.** Root cause: the backend resolved the date correctly (manual > screenshot > today) but never labelled which source won, so the preview could not surface a warning when the date silently fell back to today. A June 12 screenshot imported on June 26 was saved as June 26 with no visible indication. Fix: compute `dateSource` ('screenshot' | 'manual' | 'today_fallback') alongside `dateValue` in `/api/complete-workout` (`index.js`) and include it in the response as `date_source`. `renderCompleteWorkoutPreview` (`public/app.js`) now renders: a ⚠️ warning banner for 'today_fallback' ("Date not found in screenshot — saving as {date} (today). If this workout is from a different day, change the date field above and preview again."); a `Date from screenshot: {date}` confirmation for 'screenshot'; a `Date (manual): {date}` confirmation for 'manual'. The `log-date` field is already updated on preview (line 5027) so the owner can correct a wrong date and re-preview — dup-detection then runs against the correct date. No write-path / trust-loop / schema / parser / proof-field change. Tests: `date_source` assertions added to the three existing complete-workout date tests in `test/api-smoke.test.js`.
- ✅ **B6 — Coach's Pick / Blind Spot history correctness** `[trust-critical]` — Root cause: `buildMuscleGroupReadiness` in `services/analytics.js` required `isPositiveFinite(weight)` (weight > 0), silently dropping bodyweight exercises (HNR01 Hanging Knee Raises, KR01 Knee Raises) with weight=0 or null. Core's last-trained date resolved to 76 days ago (last weighted core work) instead of 2 days ago (HNR01 on 2026-06-24), so Core appeared 'fresh' → a blind spot. Fix: removed weight requirement from the readiness filter — it only needed valid reps and a date, since the purpose is "when was this pattern last trained," not "how much was lifted." The `(row.weight || 0)` in volume calculation handles null/0 weight gracefully. Two new tests: bodyweight weight=0 and weight=null rows both register daysSince and non-unknown status for their pattern. All 2906 tests pass.
- ✅ **B7 — Active workout survives app switch / refresh / lock** `[correctness]` — **Verdict: BROKEN → fixed.** Root cause: `restoreSessionSnapshot()` restored `sessionLog` / `sessionCompleted` / `activePlannedSession` on load but showed no user-facing notice and did not persist the `session_id` — so if the app crashed mid-session, the lifter saw no confirmation the session was restored, and a re-preview after restore could generate a different `session_id` (e.g. AM→PM boundary), bypassing the server's row-level dedup. Fix: (1) `saveSessionSnapshot()` now includes the current `#log-session-id` input value so the stable id survives the reload; (2) `restoreSessionSnapshot()` restores it to the input field, then calls `renderResumeNotice(setCount, sets)` which shows a dismissible banner "Session restored — N sets logged (Ex1, Ex2, Ex3)"; (3) `clearSessionSnapshot()` also hides the notice, so it vanishes automatically on successful save or Start Over. The notice element (`#session-resume-notice`) and styles were added to `index.html` / `styles.css`. No write-path / trust-loop / schema / parser / proof-field change.
- ✅ **B8 — Stale plan cards / composer placeholders** `[polish]` — Three surface bugs fixed: (1) `nextUpPlaceholderFromPlan` double-normalized already-normalized `activePlannedSession` entries — `compactPrescription` called `normalizePlanExercise` a second time, mapping `name → liftCode → null`, so the placeholder always returned null and fell back to the expanded per-set format. Fixed with `compactPrescriptionFromNormalized` that reads the already-normalized shape directly (no re-normalization). (2) After save or Start Over, the composer placeholder retained the last Coach's Pick suggestion and `sessionActive` in nav.js stayed true, so generic hints never resumed. Fixed by dispatching `atlas:session-reset` from both `clearSessionSnapshot` call sites (post-save and `startOverWorkout`); nav.js resets `sessionActive = false` on that event; coach-conversation.js resets `lastAnnouncedNextUp = null`. (3) The initial Coach's Pick placeholder (`buildWorkoutPlaceholder`) never filtered already-completed lifts, so after finishing Exercise A the composer could still suggest "Bench … x3" for A. Fixed by reading `getSessionCompleted()` (new read-only accessor on app.js) at placeholder-set time and excluding completed lifts from the suggestion, with full-list fallback when all are complete. No write-path / trust-loop / schema / parser / proof-field change.
- ✅ **B9 — Busy Next Exercise UI** `[polish]` — The `.nextp` prescription card appeared after every set with an orange accent border and bold orange "→ Next" header in display font, drawing the eye during flow logging. Fix: three CSS token swaps — border from `--accent-soft` (orange tint) to `--border` (neutral), `.nextp-h` color from `--accent` (orange) to `--faint` (dim gray), font-weight 500 → 400. Also added a rule for `.next-exercise-handoff` (previously unstyled, inheriting full ink color) setting it to `--muted` at 0.88rem — the "Moving on — next up:" transition line is now consistent with the quieter prescription card. No logic, trust-loop, schema, or write-path change.
- ✅ **B10 — Generic accessory/core coaching** `[polish]` — Root cause: when a lift has no progression_verdict, trend, or readiness_signal (thin history — common for accessories/core), the model had no engine verdict to word but the "take a position" directive still prompted it to fill the void with generic filler ("great work", "keep it up"). Fix: added a THIN-HISTORY RULE to `buildCoachSystemPrompt()` (`services/coach.js`): when all three key signal fields are absent/null, the model may only react to what IS observable (today_sets, effort_verdict if present) and is explicitly forbidden from using named generic-filler phrases. The existing "If a fact is missing, drop that beat" IRON RULE covered numbers; this rule covers the verbal void. No new fields forwarded; the existing null-collapse for absent verdicts is the detection signal. 4 new tests in `test/coachPromptRules.test.js`.


## Bug remediation — 2026-06-29 live gym test (Bug_Reports tab, BUG-20260629)

- ✅ **PR A — [trust-critical] One mis-parsed set must not brick the whole session save** (BUG-20260629-002910, -003028, -003118, -003208) — **shipped (this PR).** "Push ups 40 40 40" routed through `parseUnknownExercise` (Push-Up was absent from EXERCISE_ALIASES), then `parseDaleShorthand` matched the rest as weight=40/reps=40/rir=40. rir=40 violated the hard bounds guard and rejected the ENTIRE session with 400 "Implausible set values rejected". Two fixes: (1) Parser: added Push-Up to EXERCISE_ALIASES with a dedicated bodyweight handler (`parseBareBodyweightReps` — bare space/comma-separated integers ≤100 → bodyweight reps, called only for Push-Up to avoid breaking Knee Raises clarification intent). (2) Resilience: `runCloseout()` now only calls `populateSetRows(buildRowsFromSessionLog())` when the table is empty, so repeat "Log it" calls preserve any user edits; preview catch block parses row indices from the error and applies `.row-error` highlight + "Fix or delete the highlighted row(s)" guidance. Cache bumped v70→v71. Tests: 5 new parser golden tests; 2 new unit source-introspection tests; 1 new api-smoke bounds-rejection test; v71 version pin.
- ✅ **PR B — [trust-critical] Incline/Decline bench logged as plain "Bench Press"** (BUG-20260629-003415/-003450) — **shipped (this PR).** Added two new canonical exercises: `Incline Bench Press` (aliases: incline bench press, incline barbell bench, incline bench, ibp) and `Decline Bench Press` (aliases: decline bench press, decline barbell bench, decline bench). The length-sorted alias matching in `findExerciseInText` now finds "incline bench" (2 words) before bare "bench" (1 word). Guards confirmed: "flat bench"/"bench" still → Bench Press; "incline db press" still → Incline DB Press. Invariant P2: 7 new golden tests added.
- ~~**PR C — [correctness] Restore-session banner tap does nothing**~~ BUG-20260629-003605. **ALREADY FIXED** — Current-State Verification Gate: PR #678 (`restore-banner-swipe-discard-zygm2v`) added a `click` event listener to `wireResumeNoticeGestures` that calls `restoreSessionToView()` on a plain tap (no drag). Existing source-introspection test at `unit.test.js:5454` verifies the path. No code change needed.
- ✅ **PR D — [correctness] "Tricep pulls" not recognized** (BUG-20260629-003720) — **shipped (this PR).** Added `Cable Tricep Pushdown` to EXERCISE_ALIASES with aliases: cable tricep pushdown/pushdowns, tricep pushdown/pushdowns, tricep pulldown/pulldowns, tricep pull/pulls, cable triceps, cable tricep. Invariant P2: 4 new golden tests.
- ✅ **PR E — [polish] Prescription card "→ Next" header renamed to "Next time:"** (BUG-20260629-003820) — **shipped (this PR).** `buildNextPrescription` in `public/coach-conversation.js` rendered a `.nextp` card with "→ Next" header after every logged set. The `rec.recommendation` it displays is always a same-exercise FUTURE prescription (e.g. "Keep 225 lbs for 8 reps next session.") from `/api/recommend/next/{liftCode}` — not a directive to move to a queued exercise. "→ Next" implied a next exercise was queued, confusing the lifter in freestyle mode. Fix: renamed header `'→ Next'` → `'Next time:'`. `coach-conversation.js` is in the SW shell cache (sw.js) so cache bumped v72→v73 (main was already at v72 via #683). Source-introspection test added. No logic / write-path / trust-loop / schema / parser change.
  - ◐ **Freestyle session completion state** `[polish]` — **owner approved 2026-06-29 ("Go"); building in slices.** Investigation finding: most of the original proposal already existed — the review card (rendered after "done"/"log it" via `buildReviewCard`, `coach-conversation.js`) already shows the deterministic recap (exercise-by-exercise list, exercise count, **set count**, **total volume**), the effort grid, the remaining-plan line (`canonicalSessionRecap().remaining`), and a **Save workout** button + Edit affordance. The optional-effort prompt and "no proactive next-session" (B4/PR E) were already done. The genuine gap was that freestyle save was **hidden behind a chat keyword** with no visible affordance.
    - ✅ **Slice 1 — explicit "Finish session" button (shipped, this PR).** Added `#finish-session-btn` (`public/index.html`), a contextual control shown once a set is logged (`atlas:set-logged`) and hidden on reset/save (`atlas:session-reset`) — also surfaced when a restored session is brought into view (`restoreSessionToView`). Clicking it runs the SAME closeout as "done"/"log it" (`handleLogIt` → `runCloseout` → preview → review card), so it opens **no new write path** and the preview→approve→write trust loop is unchanged (nothing saved until the lifter approves the review card). Cache bumped v74→v75. Tests: source-introspection in `test/unit.test.js` + v75 pins.
    - **Slice 2 (deferred, optional) `[polish]`:** add explicit **"Add more"** and **"Discard"** affordances on the review card itself (today "Start over" lives only in the preview panel, and "add more" is implicit = keep logging). File when the owner wants them; not required for the finish flow to work.
- ✅ **PR F — [correctness] "Bicep curls" flagged unknown_exercise** (BUG-20260629 live-test follow-up) — **shipped (this PR).** `bicep curls 30 12/2` parsed its sets correctly but the exercise hit `unknown_exercise` (no `Bicep Curl` in EXERCISE_ALIASES — only `Hammer Curl`/`Leg Curl` existed), so the lifter saw the "I don't recognize 'Bicep Curls'" advisory and it saved with `muscle_group: Unknown`. Same class as PR B/D. Fix: added `Bicep Curl` to EXERCISE_ALIASES with explicit forms (`bicep curls`, `biceps curls`, `bicep curl`, `biceps curl`). Bare `curl`/`curls` deliberately left **unaliased** so unknown "X curl" lifts (Zercher Curl, Jefferson Curl) keep their typed name; length-sort keeps `leg curls`/`hammer curls` winning. Parser-only — no `public/` asset, so no cache bump. Invariant P2: 5 new golden tests (3 bicep resolutions + bare-curl-preserved guard + leg/hammer regression guard). Pairs with the `Bicep Curl` catalog-row data item above for full muscle-group attribution.
- ✅ **PR G — [correctness] Restore-session banner tap dismisses but session looks unrestored** (BUG-20260629-054925) — **shipped (this PR).** Distinct from PR C / #678: that PR correctly wired the **tap** to call `restoreSessionToView()`; this is the next defect down. `restoreSessionToView()` (`public/app.js`) populated the rows and set `parsedRowsEditor.hidden = false`, but `parsedRowsEditor` is a `<details>` (`index.html:197`) — un-hiding it only shows the collapsed **"Edit rows"** summary, so the recovered sets stayed hidden inside the closed disclosure. Owner saw the banner vanish with no visible session (the 9 restored sets were in the buffer and the composer placeholder reflected an active session, but the rows never surfaced). Fix: also set `parsedRowsEditor.open = true` on restore, mirroring the established `details.hidden = false; details.open = true` reveal pattern used for the effort panel. `public/app.js` is a precached shell asset → cache bumped v73→v74. Tests: extended `restore banner: tap-to-view` test with `.open = true` assertion; v74 version pins. No write-path / trust-loop / proof-field / schema / parser change — restore-view visibility only.


## Multi-workout composer (owner 2026-06-25: "log two or more workouts at once")

- ✅ **RIR is OPTIONAL on save (owner 2026-06-25: "log it however" / "it did like it without the rir")** `[correctness]` — `normalizeLogRowObject` (`index.js`) no longer requires `rir`: a set logged with just weight × reps (warm-up or working) saves with a blank RIR cell (column 10 stays present), Atlas never invents a rating. `weight`/`reps` + identity fields remain required so a garbled row is still rejected. Supersedes the #595 warm-up-only relaxation. Progression/PR engines already treat a blank RIR as no-signal (NaN-filtered). Tests updated in `test/api-smoke.test.js` (blank-RIR working + warm-up rows save; missing reps still 400). No proof-field/trust-loop/schema change; slash-notation contract unchanged (RIR still parsed when given).

- ✅ **Engine slice — display-block normalizer** (PR #587) — `public/displayBlockNormalizer.js` (pure UMD): `normalizeDisplayBlocks(text)` splits a stacked display-format paste into per-exercise blocks, each with `{ name, sets:[{weight,reps,rir,warmup,unit}], warmupCount, canonicalText, canonicalTextWorkingOnly }`. It only **restructures** the display format into the canonical per-exercise lines the existing proven parser already accepts (verified by an integration test that feeds each `canonicalText` through `parseWorkoutText`) — it does NOT parse sets itself and never touches the slash-notation contract. Conservative: returns `isDisplayBlock:false` for single-line / slash-notation / ambiguous (set-line-without-header, prose header) input, so existing parsing is untouched. Warm-ups are flagged, never silently dropped. **UNWIRED** — engine only, zero live-path change.
- ✅ **Engine slice 2 — real Atlas paste format** (PR #589) — extended the normalizer's set-line matcher to the live sample: optional space OR `×`/`x` weight↔reps separator, attached `lbs`/`kg` unit, Atlas slash working sets `245lbs 6/2` (RIR captured per set), warm-up markers `· warm-up`/`warmup`/`wu`/`(W)`. Mandatory weight↔reps separator so a bare number can't mis-split. Canonical text round-trips RIR (`245 6/2`). Still **UNWIRED**. 21 unit tests including the exact two-exercise screenshot paste + its `parseWorkoutText` round-trip.

## P0 coach-trust findings — owner live session 2026-06-25 (investigation, not yet fixed)

- ✅ **G2 (F1) — coach reaction under-iterates (one lift, not all)** `[correctness]` — Root cause: `handleSetLogged` only called `getInWorkoutNote` for `exercises[0]`; a stacked Deadlift+Bench entry produced a readback for each lift but coaching prose only for Deadlift. Fix: `for (const ex of exercises.slice(1))` loop in `public/coach-conversation.js` — each additional lift gets its own `[readback → coaching → next-prescription]` block in order. The primary lift's block is byte-identical (no change to single-exercise entries). Attribution: coaching note is prefixed with the lift name (`"Bench Press: <note>"`) so the lifter always knows which reaction is for which lift. Tests: `test/coachConversation.test.js` (G2 + FA structural assertions, existing); 2 new assertions added to `test/unit.test.js` locking chatTurns wiring, per-lift `fetchReaction`, and `buildNextPrescription` per additional lift. Coach-render layer only; no write-path/schema/trust-loop change.
- ✅ **G4 (F4) — coach voice fabricated a unit ("70kg/50kg" for lb data)** `[correctness]`: the coach prompt says weights are lbs with **no units in output** (`services/coach.js:966`) and there is **no kg literal or kg↔lb conversion anywhere in the coach path** — so the LLM **added** the "kg" suffix, a grounding/instruction-adherence violation (the numbers are the right lb values). **FIXED (PR `fix/coach-unit-guard`):** a deterministic post-generation guard `stripFabricatedUnits` removes any weight-unit token (kg/kgs/kilo/kilogram(s)/lb/lbs/pound(s)) that immediately follows a number, applied to the **display voices only** — `generateCoachMessage` (set-reaction), `generatePlanMessage` (plan), `generateChatReply` (chat) — before the prose leaves the server. Deliberately **NOT** at the shared `callGeminiContents` layer, because that layer also serves `compileSessionFromHistory`, whose slash-notation `workout_text` feeds the parser → preview → approve → write path (`public/app.js:4422`) — the guard must stay off any write-feeding text (it's a no-op on valid slash notation anyway; a wiring test locks this). Surgical: keeps the number + sentence (`best of 50kg` → `best of 50`), never touches rep/RIR/set counts, %, dates, durations, cal/HR, letters inside words ("club"), or the chat `PROPOSE_EDIT:` JSON. **The prompt is NOT trusted** — the guard stands alone with the prompt unchanged (the bug existed *because* the model ignored the prompt). Tests: `test/coachUnitGuard.test.js` (strip variants + false-positive guards + compile-notation no-op + display-voices-wired / compile-path-not-wired). No write-path/schema/parser/next-up change; all existing tests unchanged.
- ✅ **P0 — coach claims grounding audit: PR language and session count fabrication** `[trust-critical]` — **INVESTIGATED and closed (this PR).** Full audit of what reaches each voice via `sanitizeFacts` / `sanitizeChatContext`: (a) trend direction, effort verdict, progression verdict, best weight, working range, deviation, readiness, and stall data are ALL forwarded as engine-computed, whitelisted-enum facts; no fabrication risk there. (b) **Gap 1 — PR language not tied to engine verdict:** the prompt said "Do not claim a PR unless the facts say so" but did not IRON RULE it to `progression_verdict.level === 'new_ground'`. The model could see `best_weight` + a slightly higher today-set and declare a PR even when the engine verdict was `in_pocket`. **Fixed:** added an IRON RULE explicitly gating all "personal best / new PR / new record" language on `new_ground`, and clarifying that `best_weight` alone does NOT authorize it. (c) **Gap 2 — session count derived by counting the wrong array:** the evidence_context prompt said "cite the session count" but pointed at `reference_sets[]`, which are SETS not sessions. The model would have miscounted (e.g. 8 sets → "last 8 sessions" when there were 3 sessions). **Fixed:** prompt now directs the model to use `trend.sessions_analyzed` and explicitly forbids counting the `reference_sets` array. Tests: 3 new source-introspection assertions in `test/coachPromptRules.test.js`. **Residual / not-deterministically-guarded:** number precision (rounding `best_weight: 220` to "225") — the existing IRON RULE "never invent or change numbers" covers this by prompt; no deterministic post-generation numeric guard exists (one would require parsing output text, too fragile for this PR). The G4 `stripFabricatedUnits` guard remains the only deterministic post-generation guard (units only).
  - ✅ **Follow-up (G4 review #606): the unit guard is not on every coach voice.** **FIXED (PR #616):** `generateVerdictReaction` now wraps `callGemini` with `stripFabricatedUnits` + `await`. Wiring test extended to assert all four display voices carry the guard while `compileSessionFromHistory` and `callGeminiContents` do not.
- ✅ **Follow-up (G2 review #608): effort-line parity across stacked lifts** `[polish]` `owner-gated coach contract` — **Owner decision 2026-06-28: per-lift effort line.** Each logged lift in a stacked entry now renders its OWN deterministic, engine-backed `effort_note` line (full parity with the per-lift coaching prose), guarded by the same `suppress_generic_prose` check as the primary lift. The slice(1) loop in `public/coach-conversation.js` appends `[card → coaching → effort line → next]` per lift. The contract pin moved from "exactly one effort line" to "one per logged lift" (still never per-set — a single lift's multiple sets collapse to one engine line). Tests in `test/unit.test.js` updated (G2 loop test + set-effort wiring test now expect 2 effort-note elements: primary + slice(1) loop). Coach-render layer only; no write-path/schema/trust-loop change.
  - **Still deferred — session-scoped line de-dup** `[polish]`: **Related (review #608 note 1):** each additional lift makes its own `/api/coach/message` call, whose response can carry a **session-level** line (e.g. `recovery_advisory`, which is session-scoped not per-lift) — so a stacked entry could repeat the same recovery line per lift. Watch at LT-004; if it manifests, de-dupe session-scoped lines to once-per-entry. **Related (review #608 note 2):** if the SAME exercise appears twice in one entry (possible via the display-block path — two same-named blocks), `exercises[0]` and a `slice(1)` member resolve to the same lift and it gets double-coached. Edge case, low priority; de-dupe the coaching loop by canonical lift. Both are separate concerns from the effort-line parity shipped above — file remains until a live repro (LT-004) or owner request.
- ✅ **G5 (F6) — "Today's Workout" card line wrapping** `[polish]`: Deadlift/Bench wrap across ~4 lines; should be one clean line per lift. **Pure display/CSS — no data implication** (confirmed). **FIXED (`fix/recap-card-layout`, 2026-06-26):** `.rv-ex` is a flex row (name `.rv-en` beside sets `.rv-es`); the name had no shrink protection, so a long set list crushed the name column to ~0 width and it wrapped one fragment per line (live "Deadlif/t"). Fix: `.rv-en { flex: 0 0 auto }` (name keeps natural width, one line) + `.rv-es { flex: 1 1 auto; min-width: 0; overflow-wrap: anywhere }` (set column takes remaining space and wraps within itself). CSS-only; shell v59→v60.
- ✅ **RESOLVED — session recap shows duplicated (≈3×) sets after a single whole-workout paste** `[trust-critical / data-integrity]` (owner live 2026-06-26): confirmed fixed by owner (2026-06-26). Not reproducible on re-run. Root was never pinned in code; prime suspects if it recurs: (a) `sessionLog` buffer persisting across un-saved prior attempts via `saveSessionSnapshot`/localStorage; (b) `runCloseout` → `populateSetRows(buildRowsFromSessionLog())` → re-dispatched submit re-running the buffer rebuild. Instrument via Settings → Debug → "Show session state" dump at the moment it appears.

## Coach render-order & completion-flow — owner live session 2026-06-25 (2nd run, investigation)

- ✅ **FA — coach output grouped, not interleaved** `[correctness / UX]` — shipped (PR #610, commit 047680f). `handleSetLogged` now renders `[card → coaching → Next]` per lift in order; session handoff runs once at end. Verified live (LT-005).

## P0 — Active Workout State Unification / Adaptive Workout State — TOP PRIORITY (owner-approved lane, 2026-06-24)

- ✅ **2b decision — skipped-slot representation: RESOLVED = `skipped` is `absent`** (PR #570 review, decided in Sub-PR 2b). The live `skipPlannedExercise` (`public/app.js`) keeps its SPLICE semantics (drop-from-queue): a skipped lift leaves `activePlannedSession.exercises`, so `getCanonicalSession()` (rebuilt from `plannedExerciseEntries()`) simply doesn't see it. The 2b recap surfaces **completed** (logged) + **remaining** (pending, never-done) lifts; a skipped lift is neither logged nor written nor shown as remaining — it's absent, which matches the lifter's intent ("I'm not doing it"). The `activeSession.skipExercise` `status:'skipped'` primitive and the `resolvePlanTargets` skipped branch are retained as correct model primitives for a *future* slice that wants an explicit skipped audit record; they are intentionally dormant in live use today. No new client state holder (no `skippedExercises[]`) is introduced — that would be the only way to show "skipped" vs "absent", and it isn't worth the extra divergence surface now.
- ✅ **Backup / failover LLM provider — DECISION CAPTURED (owner 2026-06-25)** `[correctness / resilience]` `[owner-decision: provider]` — **Decision:** roadmap item captured in BACKLOG (see "Provider resilience and cost-aware LLM routing" in the data-trust section); NOT started. Paid Gemini flash-lite is the interim provider (billing enabled, spend cap set). Full 8-PR failover sequence (error boundary → provider interface → Groq fallback `gpt-4.1-nano` / `llama-3.1-8b-instant` → cost-aware tier router → circuit breaker → observability) is parked **behind the active P0 lane** per owner principle — promote only once P0 is stable in live use. Until then the graceful-degrade (templates / save-without-effort) prevents hard failures.
- ✅ **Mobile PWA test shell (owner 2026-06-25)** `[housekeeping]` — shipped (PR #583). Manifest name → "Atlas"; pull-to-refresh disabled (`overscroll-behavior-y:none`); `beforeunload` warns only on unsaved logged sets; in-progress session (`sessionLog`/`sessionCompleted`/`activePlannedSession`) snapshotted to `localStorage` (12h recency-gated, defensive) on set-logged/plan-start/background/unload, restored on load, cleared on save + Start Over; `docs/MOBILE_TEST_APP.md`. Persistence/resume only — no coaching/workout/trust-loop change.
- ✅ **Coach health diagnostic (owner 2026-06-25: "not sensing any of Atlas' intelligence / can't connect to the coach")** `[correctness]` — shipped (PR #582). The coaching voice is Gemini (`services/coach.js`, model `gemini-2.5-flash-lite`, `GEMINI_API_KEY`); ANY Gemini failure silently degrades to deterministic templates (`coachVoiceTemplates`), which read as "robotic/generic." Added `coach.pingGemini()` + `GET /api/coach/health` (read-only) + a Settings "Test coach connection" button that surfaces `{ configured, model, ok, reason }` — so the owner can see the exact failure (missing key / 401-403 bad key / 404 bad model / 429 quota / timeout) instead of the silent fallback. **OPEN — once the owner runs Test coach and reports the reason, fix the root cause** (likely key/quota/model). Also mirrored the inner-error surfacing to the bodyweight preview catch (PR-581 review note 2).
- ✅ **P0 — "Done, log it." not recognized as closeout + ingestion error hidden (v49 live re-test 2026-06-25)** `[trust-critical]` — shipped (PR #581). (1) `looksLikeLogIt` only matched a whole-string single phrase, so the natural combined command "Done, log it." routed to the coach (coach-down fallback) instead of closing out. Fix: allow an optional leading closeout-ack prefix (done/ok/that's it…, 0–2 times) before the core phrase, with a question guard ("should I log it?" never closes out); mirrored in BOTH `public/app.js` (browser copy) and `services/workoutTextParser.js` (`LOG_IT_PATTERNS`). (2) "Preview failed: Failed to complete workout ingestion" (the `/api/complete-workout` 500) hid its inner cause; the preview catch now surfaces `err.body.details.error` so the next retest reveals WHY. **Still OPEN — the underlying complete-workout 500 root cause** (effort/screenshot save path) awaits the inner `error.message` from a v50 retest; do not guess-fix the server path until the surfaced detail identifies it.
- ✅ **P0 — Deterministic swap didn't fire on an engaged Coach's Pick (v48 live re-test 2026-06-25)** `[trust-critical]` — shipped (PR #580). "Let's do rdls instead of deadlifts" fell through to the coach-down fallback instead of swapping. Two causes: (1) `tryApplyPlanMutation` was gated on a formally-STARTED `activePlannedSession`, but a typed Coach's Pick (`typeSuggestedWorkout`) only sets `coachSuggestionEngaged` + leaves the plan in `lastIntentData` — so no mutable session existed. Fix: `ensureActivePlannedSession()` materializes the engaged suggestion (with its prescription, via `normalizePlanExercise`) into a live session on the FIRST genuine mutation (classify first, so freestyle is untouched). (2) curly apostrophe — "Let's" (’ U+2019, mobile autocorrect) wasn't stripped by the `LEAD` regex, gluing the lead-in onto the substitute ("let's do rdls"). Fix: normalize ’→' at the top of `classifyMutationIntent`. Tests cover both. Shell v48→v49.
- ✅ **P0 — Final closeout/save trust failure (live test 2026-06-25)** `[trust-critical]` — shipped (PR #579). `log it` swallowed silently after a screenshot upload (no bubble/preview/save/error); `done` falsely said "Couldn't find any sets" while logged cards were visible. **Root causes (verified):** (1) the `atlas:preview-ready` listener was `handlePreviewReady(...).catch(() => {})` — a swallowed review-card render error = silent no-op after the composer was cleared; (2) closeout's `sessionLog`-empty fallback used the Gemini `/api/session/compile`, which returns null when the coach is down → false "no sets". **Fixes:** the preview-ready listener now surfaces a visible thread message on render failure (never silent); `handleLogIt` is a never-silent try/catch wrapper around `runCloseout`, which reads the canonical `sessionLog` buffer first, consults `getCanonicalSession().hasLoggedWork` before any "no sets" verdict, and reports a coach-offline compile failure honestly (never a false "no sets" when canonical work exists); screenshot effort-parse failure uses the exact owner copy and never blocks the text closeout; approve-before-write preserved. Full diagnosis: `docs/ACTIVE_SESSION_STATE_DIAGNOSIS.md` (P0 section). **Needs owner v48 live re-confirmation** that the exact silent repro is gone (the swallowed-render hypothesis is verified in code but the precise render-throw trigger wasn't reproduced headless).
- ✅ **`[follow-up]` Barbell loadability snap on substitute prescriptions (discovered PR6, shipped this PR)** `[correctness / polish]`: `POST /api/suggest-substitute` now passes `next_target.weight` through `applyBarbellLoadability` (probe-object pattern: `{ target_weight: next_target.weight, exercise: rec.recommendation }`) and maps the snapped `target_weight` + `loadability_note` back onto `next_target`. Import extended: `applyBarbellLoadabilityToExercises, applyBarbellLoadability` from `barbellLoadabilitySurface`. New test in `test/barbellLoadabilitySurface.test.js` exercises the full weight→target_weight→snap→weight round-trip.

## Trust-Critical Coach Interaction Layer — TOP PRIORITY (diagnosis-first)

- ✅ **P0 follow-up — free-form chat counts as active context** *(shipped #446)*: live testing showed `RIR?` still hit SME in a free-form conversation (no started session/preview → empty client context → gate off). `getChatReply` now also treats an in-progress conversation (`history.length > 0`) as active context. Shell cache v10→v11 (later v11 via #449).
- ✅ **P2 — Extra Work Coach Signal (PR #440)** — *shipped/merged (#440).* Engine fact `extra_work` wired into the coach chat context (on-ask/recovery-gated wording). Follow-ups below remain.
- ✅ **P4 — Session-State Stress Testing** — *shipped (test-only).* `test/sessionStateStress.test.js` runs a messy-input corpus ("rack busy", "I'll do RDL instead", "skip that", "legs are toast", "what now", "how much", "what am I doing next") across the deterministic routing/answering guards together (`isSessionStateQuestion`/`isPlannedLiftQuestion`, `answerBareShorthand`/`answerPlannedLiftQuestion`, `isTirednessExpression`/`buildTirednessRecoveryAnswer`, `classifyMessageIntent`) to prove session state stays prioritized over generic chat/education — LLM-independent (pure helpers). Covers negative controls, the ambiguous-lift clarify guard, the never-hype/never-invent-a-load recovery guarantee, and the AC8 phantom-set floor. No production code change; full suite green (2381).
  - ✅ **Resolved (finding from P4 stress corpus) (2026-06-24)** `[correctness, polish]`: `recoveryRouting.js`'s `FRAMING` list now names specific muscle groups (`quads/hamstrings/hams/glutes/calves/chest/pecs/delts/traps/lats/biceps/triceps/forearms/abs/core`) in addition to the coarse regions, so muscle-specific fatigue slang ("quads are fried", "hamstrings are toast", "chest is cooked") routes to the recovery-aware reply via the `FATIGUE_SLANG + FRAMING` path. The slang requirement still guards against unrelated muscle mentions ("quads day tomorrow", "dead bug for core" → not detected). Coach wording/rendering (PM authority under Escalation Policy v3); pure detector change, no write-path/schema/LLM/trust-loop change. Tests: `recoveryRouting.test.js` (muscle-specific positives + muscle-without-slang negatives). Full suite green (2623).


## Atlas voice & interaction — TOP PRIORITY

- ✅ **PR 3.4 — coach voice reaction** _(complete — merged in [#322](https://github.com/tc5v5s64ym-sketch/atlas-workout-updater/pull/322) @ 962b277)_ — `coach.js` voice layer that words the engine's verdict + rules-engine decisions + per-request context. System prompt now enforces the full VOICE SPEC (not-easily-impressed identity, ≤4 sentences/no hype, rule decisions are final → explain/restate criterion never override, pain → stop coaching load, RIR 0–1 → caution never celebrate grinding, anti-patterns banned, §8 template shapes). New whitelists `sanitizeRuleDecision(s)` + `sanitizeReactionContext`; gate extended via `hasActionableRuleDecision` / `shouldReactToVerdict` (met → quiet UNLESS a caution/reject rule was raised). Words only — every number passes through verbatim. Engine (`computeExpectationVerdict`, rules) untouched. **Merge gate satisfied: owner approved the live example outputs.**

## Near-term

- ✅ **Wire the explicit upper-only request to `build_strength`'s `upperOnly` flag (caller side)** `[correctness]` (owner — 2026-06-25) — *shipped.* follow-up to *PR1 build_strength full-profile default*. The session-generation engine now defaults to a full-profile strength day and produces an upper-only day only when `scoreIntents(..., { upperOnly: true })` is passed; the flag is the explicit-intent seam and is unit-tested directly. **Still unwired on the caller side:** no surface yet sets `upperOnly` from a real user/coach request, so the explicit path isn't reachable end-to-end in production. Smallest next step (read-only, no parser-grammar/write/schema change): thread a query param (`?upperOnly=true` / `?scope=upper`) on the read-only `GET /api/plan/intent-recommendation` route into the `scoreIntents` options, then (separately) let the coach map a natural-language "upper day" request to it. **Note:** the intent-recommendation handler is covered by a brittle 900-char source-window assertion (`P0 wiring 2c` in `test/unit.test.js`) with almost no headroom — adding the param trips it, and that test is in the P0 lane (owner-gated to touch). Widen/relax that window (or move the snap assertion off a fixed char-slice) as part of the same PR. Needed to fully drain **LT-002** (`docs/TEST_QUEUE.md`).
- ✅ **Coach's Pick engagement gate — a displayed suggestion is not an active plan** `[trust-critical]` (owner live test 2026-06-24) — *shipped.* Live testing surfaced that logging straight into the main composer (no Coach's Pick tapped, no session started) was narrated as if mid-plan: after the set reaction Atlas appended "Moving on — next up: <suggested lift>", pre-filled the composer with that lift's prescription, and even fired a `next_move_advisory` toward it. **Root cause:** `loadDashboard()` (app.js) always sets `lastIntentData` to render the home-screen pick, and `plannedExerciseEntries()` treated `lastIntentData`'s recommended intent as the active plan whenever `activePlannedSession` was null — so a *displayed* suggestion was conflated with an *engaged* one (NOT the `/api/plan/today` fallback, which a first investigation wrongly fingered — the handoff fired via `detail.nextPlanned`). **Fix (single chokepoint):** new `coachSuggestionEngaged` flag (app.js, defaults false, no persistence); `plannedExerciseEntries()` returns `[]` for the `lastIntentData` branch unless engaged (an active planned session still always wins). Set true when the lifter taps Coach's Pick (`typeSuggestedWorkout`), false on Freestyle. Fixes every downstream surface at once (handoff, composer pre-fill, reroute queue, `next_move_advisory`, closeout) while leaving the next-*set* card and the preview→approve→write trust loop untouched. Frontend-only; no write-path/schema/parser/proof-field change. SW shell v38→v39. Tests: post-log-identity harness now models the engaged flow + a new gate test (loaded-but-unengaged → empty plan; engage → plan activates); app.js/coach-conversation source-introspection for the gate placement + engage/clear wiring. Full suite green (2566). **Deferred (edge):** the flag persists for the session once engaged — if a lifter finishes a Coach's Pick session and then logs ad-hoc in the SAME session (no reload), the next-up could still narrate; revisit if it surfaces (clear on session save/closeout). Freestyle + reload both clear it today.
- ✅ **FIXED — conversational logging now falls back to the local parser on a flaky/partial backend response (not just a clean timeout)** `[correctness]` (owner — 2026-06-22) — *this PR.* Live symptom: a clearly-typed named set ("Lat Pulldown 175lbs 8/2 8/2 8/2") dropped into chat as "Noted — keep logging" on mobile signal even though the local parser handles it fine. Root cause: `parseWorkoutTextWithBackend` (`public/app.js`) tagged its **no-write-proof failure** (response missing `test_mode`/`sheet_written:false`/`no_write_confirmed`) and its **empty-`log_sets`** failure with `err.noFallback = true`, which made `shouldUseLocalFallback` return false → the local-parser fallback was skipped → the set routed to chat. A clean timeout (AbortError → no status) already fell back; the gap was the *partial/garbled response* path. Fix (frontend-only): both throws still **throw** (a partial response is never trusted), but neither tags `noFallback`, so the caller re-parses with the **pure client-side** local parser instead of dropping the set. Safe because the local parser never writes, the untrustworthy backend response is discarded entirely, and the real write still goes through preview→approve→write. The deliberate non-log answers (clarification/finish_session/effort_capture in `rowsFromBackendParsedWorkout`) **keep** `noFallback`. SW shell v25→v26. Tests: `unit.test.js` source-introspection (no-write-proof + empty-rows throws are fallback-eligible / no `.noFallback =`; clarification intents still tagged) + parser-golden ("Lat Pulldown 175lbs 8/2 8/2 8/2" → 3 clean sets). No parser-grammar / schema / write-path / proof-field / trust-loop change.

- ✅ **FIXED — coach-suggested plan never registered "complete," resurrecting a done lift as next-up** `[correctness]` (owner live 2026-06-22, **confirmed via the #484 "Show session state" dump**) — *this PR.* Root cause (runtime-confirmed, NOT the swap detour — that was a red herring): in `emitSetLogged` (`public/app.js`) `planIsComplete` was computed **only** from `activePlannedSession`, which is `null` in the coach-suggestion flow (logging without "Start Session"). So `planIsComplete` was **always false** there; after the last logged lift the closeout branch never fired and the handoff fell through to the divergent `getNextExerciseInPlan` (`/api/plan/today`) fallback, resurrecting an already-completed lift ("wanted weighted dips again"). The composer mirrored that wrong handoff (it's set from the same `nextEx`), so "stuck on Weighted Dip" was the same one bug. The session-state dump proved the underlying state was correct (`sessionCompleted` had all 6, `remaining: []`) — only the completion *gate* was wrong. Fix (two parts): (1) derive `planIsComplete` from the shared `remainingPlannedExercises()` — `plannedExerciseOrder().length > 0 && remaining.length === 0` — so it's true in BOTH the started and coach-suggested flows; (2) in `handleSetLogged`, compute next-up FIRST and make the `/api/plan/today` fallback **reject a next-up already in `sessionCompleted`** (threaded via `detail.completed`), so a genuine uncompleted next still wins (preserves the E2E "log Bench → next Lat Pulldown" via /api/plan/today) but a divergent fallback can't resurrect a done lift; closeout fires only when nothing is genuinely next. SW shell v24→v25. Tests: `test/postLogIdentity.test.js` (suggestion flow logs all 6 → final `planIsComplete:true`, `nextPlanned:null`; mid-session not prematurely complete) + `unit.test.js` source-introspection (fallback rejects completed; closeout precedence). No parser/schema/write/trust-loop change; `applySessionSubstitution` untouched. The "Show session state" debug dump (#484) stays as a permanent diagnostic.

- ✅ **Next-up + bare-set attach also work when logging against a coach SUGGESTION (no "Start Session")** `[correctness]` (owner — 2026-06-21) — Live evidence: after logging Bench against a coach-suggested "Build Strength" workout, the handoff said "next up: Face Pull" (should be Seated Row) and a bare set sequence still hit "Noted — keep logging". Root cause: `firstUnloggedPlannedLift()` (the source for both next-up and the bare-set parse-context fallback, from #471/#472) read **only** `activePlannedSession`, which is set **only** when the lifter taps "Start Session" (`startPlannedSession`, app.js:1432). Logging straight against a coach suggestion leaves it `null`, so next-up fell back to the buggy `getNextExerciseInPlan` (API-plan map order, no skip-logged) and bare sets had no attach target. Fix: new `plannedExerciseOrder()` (`public/app.js`) returns the visible plan order from the started session **OR** the cached coach-suggested plan (`lastIntentData`'s recommended intent — the same source `currentPlanForChat` already uses); `firstUnloggedPlannedLift()` now walks that and skips `sessionCompleted`. So next-up = the first unlogged lift in the visible order, and a bare sequence attaches to it, in **both** flows. Deterministic (no LLM) → correct even when the coach is down (the screenshots' "Coach is unavailable" no longer blocks the workout flow). SW shell cache v17→v18. Tests: `test/sessionPlanExecutor.test.js` (order source from started OR suggested plan, skip-completed). Full suite green (2262). No write-path / schema / proof-field / approval / parser change.

- ✅ **Coach chat must not present planned work as completed (planned total, engine-computed)** `[correctness/trust]` (owner — 2026-06-21) — Live evidence: a bare "Total?" after discussing Face Pull returned *"You've done 45 reps of Face Pull today."* — the LLM multiplied the plan (3×15) itself and mis-tensed it as completed work the lifter hadn't logged. Two fixes: (1) deterministic `answerTotalRepsQuestion(message, { history, clientContext })` in `services/sessionQuestionAnswer.js` answers a "total" question with the **engine-computed** planned total (sets × reps), worded as planned ("Face Pull today: 45 total reps planned (3 sets × 15)."), resolving the lift from the recent turns so a bare "total?" follow-up works; wired into `POST /api/coach/chat` before Gemini (defers on past-tense/advice/unresolved/missing-sets). (2) New **PLANNED-VS-DONE RULE** in `buildChatSystemPrompt` (`services/coach.js`): `current_plan`/`current_preview`/recommendation numbers are PLANNED targets — never "you've done / you did / you hit", never multiply sets×reps and state the product as performed; only `recent_sessions[*].lift_sets` + `plan_state` reflect logged work. Tests in `test/sessionQuestionAnswer.test.js` (named + bare-follow-up total, planned wording, defers) and `test/coach.test.js` (prompt rule). Full suite green (2235). No write-path / schema / proof-field / parser change; no new LLM call (the total is deterministic).

- ✅ **Recovery trim must not collapse Build Strength below the learned session norm** `[correctness]` (owner — 2026-06-21) — Live evidence: after the recovery-aware density fix, a "push recovering" Build Strength day collapsed to 3 exercises (Bench + Seated Row + Lat Pulldown). Root cause: `build_strength` builds from a narrow pattern list (`['push','pull']`), so trimming the recovering push to one movement left only push(1)+pull(2)=3 — well below the owner's profile (median 5, p75 6). One recovering pattern should reduce ITS volume, not shorten the whole day. Fix (engine-only, drop-only is replaced by *additive* backfill but only from the existing candidate pool): new `backfillToProfileTarget(exercises, trimmedPatterns)` in `services/analytics.js::scoreIntents` tops the session up — when a recovery trim happened and the day is under the **profile median** (`exercises_per_session`, bounded by `session_cap`) — with movements from **rested** patterns (`exForPatterns(readyPatterns)`, dedup by `lift_code`), never the recovering pattern and never extra push. Skipped when there is **no learned profile** (sparse data → generic behaviour) or when **global fatigue is high** (`fatigue.status === 'high'` → a short session is warranted). `build_strength` runs it between `capRecoveringPatternDensity` and `structureSession` (so #466's `capSessionToProfile` still bounds the top end — the target never exceeds `session_cap`), adds a `why_today` line + `volume_shifted_to_ready_patterns` reason code. Tests in `test/analytics-edge.test.js`: recovering push + ready patterns → 4–6 exercises with a rested-pattern backfill (not 3); high global fatigue → short 3-exercise day allowed (no backfill, carries `high_fatigue`). Full suite green (2216). No write-path / schema / proof-field / parser / LLM change. #466 + #467 behaviour intact.
  - **Watch — backfill diversification across ready patterns** `[polish]` (PR #468 Atlas review, non-blocking): `backfillToProfileTarget` draws the top `target*2` lifts across *all* ready patterns by global priority, not balanced per-pattern, so a single ready pattern holding several high-priority lifts (e.g. pull) could absorb the whole top-up rather than diversifying into legs/hinge/core. Not a recovery violation (only recovering patterns are density-capped) and `session_cap` still bounds the total — a design nuance, not a bug. If live results show the backfill clustering on one pattern, round-robin the top-up across rested patterns. No action unless that surfaces live.

- ✅ **In-app build identity shows the deployed PR number** `[housekeeping]` — Owner couldn't tell from a bare SHA whether the live app was the right/current build. `scripts/write-build-info.js` (run at build time via `postinstall`, where `.git` is present) captures the deployed commit's PR number + subject into a gitignored `public/build-info.json`; `services/buildInfo.js` reads/parses it; `/version` now returns `pr` + `commit_subject`; the Settings badge shows `PR #<n> · <sha7> · deployed <time>` (SHA fallback when absent). Read-only diagnostics; degrades to the SHA when build-info is missing (never throws). Tests: `test/buildInfo.test.js` (PR-number parse + reader) + `/version` shape in `api-smoke`. SW cache bumped v13→v14.

## Personalized warm-up ramps learned from logged history (owner idea — 2026-06-21)

- ✅ **[#402](https://github.com/tc5v5s64ym-sketch/atlas-workout-updater/issues/402) Part 1 — Seated Row same-day repeat in recommendations** — Two-part fix in `services/analytics.js` (`scoreIntents`): (1) deduplicate `allRecs` by exercise name after sorting so the same movement logged under multiple lift codes (e.g. ROW01, SR01) appears only once; (2) filter `allRecs` globally to exclude exercises trained yesterday (daysSince=1) before any intent builder runs — covers both `exForPatterns` and `buildIntentSession` paths. Same-day (daysSince=0) is kept so the PR 369 readiness-aware dose reduction still fires. 2 golden-fixture tests added to `test/analytics-edge.test.js`. **Part 2 (save-ready copy UX) remains open** — the suggested workout surface is not yet wired into a route so the exact repro is unclear; deferred below.
- ✅ **#402 dedup: single-word exercise name carve-out** `[housekeeping]` — *shipped (PR #499 + exact-membership follow-up).* The `allRecs` name-dedup in `scoreIntents` (`services/analytics.js`) no longer skips ALL single-word names (a whitespace proxy that let real single-word lifts escape dedup and get double-recommended under multiple lift codes). **Final form:** it skips dedup only for a single-word name that is **exactly** a muscle-group label (`MUSCLE_GROUP_LABELS` set — coarse groups + colloquial muscle taxonomy), so the test-helper `'Chest'`/`'Back'`/`'Lats'`/`'Core'` placeholders stay protected while real single-word lifts dedup to the most-recently-trained code. **The #499-review residual is closed:** the first cut used `classifyMuscleGroup(name) !== null`, which substring-matches the `row`/`ab`/`hip` tokens — so a bare `'Row'` (the exact #402 Seated Row family) still escaped dedup; exact whole-string membership fixes that (`'Row'`/`'Rows'`/`'Squat'`/`'Curl'`/`'Dips'` all dedup; multi-word lifts like `Leg Press` unaffected). Pure engine; no write-path / schema / proof-field / parser / LLM change. Golden tests in `test/analytics-edge.test.js` (Squat + Row dedup to newest; `Leg Press` regression guard; `Chest` placeholder NOT collapsed) + existing #402 tests intact. Full suite green.
- ✅ **Roadmap Step 379 — Reorder session index bugfix.** `[correctness]` Fixed: when a swap/reorder is declared at the current step, `checkAndSuggestSubstitute` (`public/app.js`) now advances `activePlannedSession.index` past the taken exercise (clamped to the plan length, banner re-rendered) right after recording `pendingSubstitution`. Subsequent conversational messages therefore send the next planned slot as `current_exercise` instead of the stale taken lift. The deferred swap is still applied by NAME in `emitSetLogged`, so the cursor move does not affect it; `advancePlannedSession()` is deliberately NOT reused (it clears the pending swap and restarts the logger). Source-introspection regression test added (`test/unit.test.js` "Step 379"). No write-path/schema/LLM change.
- ✅ **Roadmap Step 380 — [#359](https://github.com/tc5v5s64ym-sketch/atlas-workout-updater/issues/359): Historical lift retrieval must use actual logged sets.** `[trust-critical]` Already complete (issue closed-completed; re-listed by the #405 roadmap refill). Verified in `main`: `buildRecentSessions` emits per-set `lift_sets`, `buildChatContext` (`index.js`) forwards them to `/api/coach/chat`, `services/coach.js` HISTORY RULE answers past-workout questions ONLY from `recent_sessions[*].lift_sets` (never prescription/plan/recommendation/benchmark), and `test/coach.test.js` pins lift_sets + RIR-0 fidelity. No code change required.
- ✅ **Roadmap Step 381 — GitHub issue #402A: Recent-lift exclusion / Seated Row identity normalization.** `[trust-critical]` Resolved by PR #404. The exact repro (Seated Row prescribed the day after logging) is fixed by the `scoreIntents` recency exclusion (`daysSince === 1` dropped from `allRecs`) plus dedup-by-name. Identity investigation: in `main` only `seated row → ROW01` exists; the `SR01`/`BOR01` codes named in the issue are hypothetical and not present, so there is no live multi-code split. The only genuine residual (pre-override generated-code history split) is already tracked below as the SESSION_DESIGN AC5a "liftCode history-merge" item. No new code required.
- ✅ **Roadmap Step 382 — GitHub issue #402B: Suggested workout preview must not look save-ready.** `[correctness]` Fixed (PR #410): the save-ready copy was the `nav.js` composer-placeholder rotation ("Say 'log it' to save your session") overwriting the suggestion placeholder, because the rotation only stopped on `atlas:set-logged`. `setWorkoutPlaceholder` now dispatches `atlas:placeholder-owned` whenever coach-conversation sets a contextual placeholder, and `nav.js` suppresses the rotation on it — so a displayed suggested workout (nothing performed) no longer shows save-ready pressure. Frontend-only; source-introspection test added.
- ✅ **Roadmap Step 383 — [#291](https://github.com/tc5v5s64ym-sketch/atlas-workout-updater/issues/291): Deload prescription consolidation (one model).** `[correctness]` **Premise corrected (2026-06-20):** there is NO live contradiction — `computePrescription` is dark (zero callers), there is no "Coach's Pick" UI, the only live deload surface is `suggestDeloads`→insights, and the next-set card's load-cut math is duplicated inline in `index.js` (~1569) and only fires when `Deload_State` is active (which the client never sets). DRY cleanup done: added `computePrescription` to the `deloadProtocols` import and replaced the 9-line inline block with one `computePrescription` call. No behavioral change (path is dark until Step 385).
- ✅ **Roadmap Step 384 — Deload trigger nuance: don't trigger a deload off accessory or deprioritized lifts.** `[correctness]` Fixed (done before 383, re-ordered as the real bug). `annotateStallsForDeload` (`coverageStalls.js`) already existed but was wired only into `scoreIntents`; `suggestDeloads` (`analytics.js`, feeding `/api/coaching/insights`) now applies it and filters out `ignored_for_deload` accessories — so a flat accessory whose muscle is covered by other lifts (Shrugs) no longer surfaces a deload. The Dumbbell-Curl-progressing (40→53) case was already safe: `detectStalls` is estimated-1RM based, so a rising lift is never flagged. Golden test in `test/analytics-edge.test.js`. No write-path/schema/LLM change.
- ✅ **Roadmap Step 385 — [#289](https://github.com/tc5v5s64ym-sketch/atlas-workout-updater/issues/289): Frontend deload lifecycle wiring.** `[correctness]` Lifecycle wired in `public/app.js`: `begin` fires fire-and-forget when a `deload_reset` planned session starts (409 absorbed); `advance` fires after a confirmed live log write when `activePlannedSession.intentId === 'deload_reset'` and not a duplicate-blocked replay; `resolve` fires automatically when `advance` returns `POST_DELOAD_EVALUATION`. None touch `Log_Cleaned`, workout rows, or proof fields. Source-introspection tests in `test/unit.test.js`. The deload state machine is now live.
- ✅ **SESSION_DESIGN AC5 (part b): per-lift load ceiling check** (PR 361) — `services/loadSanity.js` added; `sanitizeLoad` guard wired into `recommendNextSet`; 31 golden-fixture tests all pass. Golden invariant held: Lateral Raise never prescribed above 100 lb. Part (a) — history-merging for old liftCode splits (e.g. rows written before `knownLiftCodeOverrides` was in place may carry a generated code that differs from the canonical one) — is **deferred**: see item below.

## Then

- ✅ **HI-5 — service-worker cache bump** — `CACHE_NAME` bumped to `atlas-shell-v8`; activate handler purges old caches. Owner live-check: hard-refresh on device to confirm new SW activates.
- ✅ **ME-8 — weekly report row shape** — `buildWeeklyReport` now uses `cell(row, idx, key)` helper for all post-filter accesses; object-shaped rows produce correct totals.
- ✅ **HI-3 — parser set-count cap** — `parseWeightRepsSets` path capped on all paths (PR #317).
- ✅ **HI-2 — CSP / inline styles** — inline `style=""` moved to CSS classes; recovery-bar fills driven by `--fill` custom property (PR #315).


### AUDIT.md reconciliation — still-live findings (verified against main 2026-06-20)

- ✅ **ME-4 — client-supplied `volume_calc` trusted when present** `[trust-critical]` — fixed. Both write-path normalizers (`normalizeLogRowObject` for object rows, `logRowArrayToObject` for array rows) now **always** derive `volume_calc` from `weight × reps` server-side and ignore any client-supplied `volume_calc`/`volume`. Column 12 of `Log_Cleaned` can no longer disagree with the logged set. Read-path analytics were never affected (they recompute volume themselves and don't carry the stored column). Regression test in `test/api-smoke.test.js`: a live log-workout with a bogus `volume_calc: 99999` writes `1350` (135 × 10).
- ✅ **HI-4 — numeric/Excel-serial dates produce a wrong date** `[correctness]` (`services/validation.js` `normalizeDate`): the generic `new Date(text)` ran before the Excel-serial branch, so a numeric serial like `45000` parsed as the **year 45000** and the serial branch was unreachable. Fixed by moving the `typeof value === 'number'` Excel-serial conversion **above** the generic `new Date(text)` fallback; `normalizeDate(45000)` now returns `2023-03-15`. Regression test in `test/unit.test.js`. ✅ **Resolved (string form, 2026-06-24)** `[correctness]`: a serial arriving as an all-digit **string** (`"45000"`, how `FORMATTED_VALUE` sheet reads return numbers) was still parsed as the year 45000 by `new Date(text)`. Now an **exactly-5-digit** all-digit string is converted via the shared `excelSerialToIso` helper (range 10000–99999 ≈ 1927–2173 covers every realistic modern date; the 2020s are ~43000–47000), so `normalizeDate('45000')` → `2023-03-15`. A bare 4-digit `"2026"` stays a year and an 8-digit `"20260618"` (YYYYMMDD) is deliberately excluded by the digit count, so neither is misread as a serial. Pure normalizer; no write-path/schema/proof-field/trust-loop change. Regression tests in `test/unit.test.js` (string serial == numeric serial; 4-digit year and 8-digit YYYYMMDD not converted).

## Operational resilience — NEEDS TRIAGE (not yet scoped)

- ✅ **Schema / header-drift validation** `[trust-critical]` — shipped in two PRs. **PR A** (engine): `validateHeaderRow(actualHeader, expectedColumns, aliases)` in `config/sheetContract.js` (pure, alias/casing-tolerant, position-by-position) + `getHeaderRow(tabName)` in `sheets.js`. **PR B** (wiring): a pre-write header guard in `index.js` reads row 1 of the target tab(s) before any live append and **refuses the write with 409 + `sheet_write:'blocked_schema_drift'`, `sheet_written:false`, `no_write_confirmed:true`** on mismatch, releasing the `write_id`. Covers the trust-critical workout-data paths — `Log_Cleaned` (`/api/log-workout` + `/api/complete-workout`) and `Effort` (`/api/complete-workout`). **Deferred (lower priority):** the same guard is not yet applied to `Constraints` (already returns 503 when its tab is missing) or `Deload_State` (system-state, append-only, outside the trust loop); add if those tabs ever drift. Sonnet 4.6 if pursued.
- ✅ **Sheets write retry/backoff** `[correctness]` (`sheets.js` `appendRows`): `appendRows` now wraps the `values.append` call in `retryWithBackoff` (bounded: 1 initial + 3 retries, exponential 500/1000/2000 ms). **Idempotency-safe retry scope:** `values.append` is NOT idempotent and the `write_id` guard lives in `index.js` (dedupes across HTTP requests, not inside this loop), so `isTransientAppendError` retries **only** on errors where Google rejected the request before touching the sheet — HTTP 429/503 and 403 rate-limit/quota reasons. Ambiguous failures (post-send timeouts, 500s) propagate un-retried to avoid a silent double-append. Both helpers are pure (injectable `sleep`) and exported; 5 unit tests in `test/unit.test.js`. **Premise correction:** the original note claimed "`write_id` already guards against a double-append on retry" — true only at the HTTP-request layer, not within a single `appendRows` retry, which is why the retry scope is deliberately narrowed to request-rejected-before-write errors.
- ✅ **Timezone / date handling** `[correctness]` (`services/analytics.js`): the recency math derived "today" from UTC (`todayIso` → `toISOString`) while `date_clean` is stamped in the owner's local day, so `daysSince` / "trained yesterday" could misclassify by a day near midnight — corrupting the recent-lift exclusion (Step 381 / #402). Fixed with a single date-basis helper: `localTodayIso(now, tz)` driven by a new `ATLAS_TIMEZONE` (IANA zone); `todayIso()` now routes through it and `computeFatigueStatus`'s default reference is local-today-anchored. `isoDateAtUtcNoon` (UTC-noon anchoring for DST-safe whole-day diffs) was already correct and is unchanged. **Default unset → UTC (behavior unchanged); the owner sets `ATLAS_TIMEZONE` to their zone to activate alignment** (documented in `.env.example`). Isolated to `services/analytics.js` — no `index.js`/write-path/schema/LLM change. 3 unit tests (UTC default, LA/Tokyo local-day boundary, invalid-zone fallback). **Deferred (same-pattern, optional):** the client/server `getLocalDateString` (`public/app.js`, `services/sessionId.js`, `index.js`) already use device-local time so they align with a correctly-set `ATLAS_TIMEZONE`; no change needed unless the server's local zone is ever used as a stamping fallback in a non-owner zone.

## Housekeeping

- ✅ **Reconcile [`FIX_PLAN.md`](./FIX_PLAN.md)** — all FIX_PLAN items classified (done/open); open items folded into this backlog; FIX_PLAN archived with a banner. Done in this PR.
- ✅ **Harden null-element tolerance in the coach sanitizers** `[correctness]` — Guards already present in `services/coach.js` (`today_sets` via `toSet` line 71, `last_working_sets` inline lines 83–87, `current_preview` inline line 592) and three `[null]`-element regression tests already pass in `test/coach.test.js` (lines 765–793). Fully resolved pre-existing; no PR required. Verified Roadmap Step 378.
- ✅ **Parser substitution priority fix** — `Leg Press` added to `EXERCISE_ALIASES`; `extractSetParagraphs` strips explanatory prose paragraphs (no set tokens) before normalization so the performed exercise header/sets win over skipped/mentioned exercises in substitution sentences. 4 golden tests added.
- ✅ **NF-1 — Playwright browser mismatch in Claude Code remote environment** `[housekeeping]` (Post-P0 QA, 2026-06-26): `@playwright/test` 1.60.0 expects Chromium revision 1223 but the remote environment ships revision 1194 at `/opt/pw-browsers/`. The suite would fail to launch in a fresh session without manual symlink creation. Fixed in PR on branch `claude/post-p0-atlas-qa-ki7qkc`: `playwright.config.js` now reads `PLAYWRIGHT_BROWSERS_PATH` (set by the remote environment) and when present supplies `executablePath: '/opt/pw-browsers/chromium'` (the stable system symlink) to both projects — no impact on local dev or CI where that env var is absent.

## Automation framework follow-ups

- ✅ **Label sync workflow** `[infrastructure]` — `.github/workflows/labels.yml` upserts the risk labels onto the repo from the manifest (github-script; no external action).
- ✅ **Merge-card completeness check** `[infrastructure]` — `.github/workflows/merge-card-check.yml` fails a PR whose body is missing the Atlas Merge Card or still contains template placeholders (`AUTOMATION_PROTOCOL.md` §2).
- ✅ **Auto-label by path** `[infrastructure]` — `.github/workflows/labeler.yml` + `.github/labeler.yml` apply category labels from touched paths (`actions/labeler@v5`). Primary risk label stays a builder decision.

- ✅ **Codex Decision Desk** `[infrastructure]` — `.github/workflows/codex-decision-desk.yml` answers Claude's decision panels so the owner is not asked (`docs/DECISION_ROUTING.md`). Reuses the existing `CLAUDE_CODE_OAUTH_TOKEN` subscription — no new paid API.

- ✅ **Escalation Policy v3 — minimum-escalation, ship-by-default** `[governance]` (owner standing instruction, 2026-06-24) — recorded the owner directive to reduce escalations to the minimum. Coach surface / wording / rendering / frontend / UX is **no longer** an escalation trigger when derivable from governance; the five reserved categories are consolidated to **four** (schema/storage + destructive operations merged into "destructive or irreversible"); coaching **philosophy** is named explicitly as reserved product scope. Encoded across `docs/OWNER_CHECKIN_RULES.md` (authoritative; v2 retained as superseded), `docs/AUTOMATION_PROTOCOL.md` §5, `docs/DECISION_KERNEL.md` (Tier 3), `docs/DECISION_ROUTING.md`, `docs/AGENT_WORKFLOW.md`, and `CLAUDE.md`. Docs-only; no app/schema/write-path/coach behavior change.
- ✅ **Sweep per-item `Sonnet 4.6` model tags now that the builder is Opus-always** `[housekeeping]` — resolved at source: the "Deferred item tags" legend now defines tags by *risk only* and adds a note that the per-item Sonnet/Opus suggestions scattered through older items are **superseded** (read as risk hints, not model selection). Historical plan docs (`FIX_PLAN.md`, `COACH_PLAN.md`, `docs/COACH_NEXT_LEVEL_BUILD_PLAN.md`) are reference-only and left as-is. Surfaced by automated review of #429.
- ~~**CODEX Review as a check**~~ `[infrastructure]` — **STRUCK AS DUPLICATE (owner 2026-06-25): this item is absorbed into the active Codex required-gate proving period already underway. No separate action required; close this entry.**

### Training Intelligence Implementation Series (owner-approved slices)

- ✅ **PR 478 — Exercise Modality Schema** `[correctness]` — *shipped (owner promoted Training Intelligence 478–486, 2026-06-23).* Pure-data module `services/exerciseModality.js`: `modalityFor(name)` returns `{exercise_type, load_type, fatigue_class, effort_metric (+alternates), progression_metric}` (Taxonomy §3), extending the legacy `EXERCISE_TYPES` with `cardio_steady|cardio_interval|circuit|timed_hold|mobility` and reusing `classifyLiftRole`/`costFor` for resistance. Name-pattern detectors (circuit/interval/hold/cardio/locomotion/assisted/added-load/bodyweight) run before the external-weight default so a non-resistance modality is never mis-read as a weighted lift; `isValidModality` guards every field against the controlled vocabularies. REFERENCE-ONLY — no I/O, no wiring, no write-path/schema/parser/LLM change. Golden tests pin the §3 minimum-coverage table + enum validity (`test/exerciseModality.test.js`). Full suite green.
- ✅ **PR 480 — Pure Session Objective Scorer** `[correctness]` — *shipped.* Pure module `services/objectiveScorer.js`: `scoreObjectives(snapshot)` derives the nine §3 term sub-scores per objective (`featuresFor`) from a normalized planning snapshot and ranks/selects via the PR 479 contract. Per-objective rules are the documented initial deterministic defaults (profile axis · goal set · equipment need · time need · taxed-system recovery/fatigue · coverage gaps · pain targets); the two recovery objectives (`recovery_reload`/`deload`) **invert** (they fit a fatigued state and stack ~no fatigue). Read-only/pure — consumes already-derived signals; building the live snapshot is a later wiring slice (no write-path/schema/parser/LLM change). Golden selections pinned (`test/objectiveScorer.test.js`): strength-recovered→strength_progression, cardio intensity-intent fresh→cardio_intervals, the §8 low-recovery downgrade→cardio_base, no-intent→cardio_base, deload-active→deload, poor-sleep→recovery_reload, plus full-ranking + empty-snapshot safety. Full suite green.
- ✅ **PR 481 — Stimulus Governor Fixtures** `[correctness]` — *shipped.* Pure rule-table module `services/stimulusGovernorRules.js`: frozen vocabularies (`PROFILES`, `MODALITY_CATEGORIES`, `PROGRESSION_VERDICTS`, `FATIGUE_SIGNALS`) + `governorRuleFor(profile, modalityCategory)` returning the §5 rule — `effort_interpretation` + reachable verdicts from the modality, RIR-0 handling / progression bias from the profile (strength→RIR0 blocks on heavy compound; hypertrophy→cautioned; general_fitness→reward consistency; bodyweight→fine on strict; cardio modality → RIR irrelevant regardless of profile). Golden tests pin the §5 table row-by-row + vocab membership (`test/stimulusGovernorRules.test.js`). Pure data/lookup; no write-path/schema/parser/LLM change. PR 482 applies a rule to a logged set.
- ✅ **PR 482 — Profile-aware Stimulus Governor** `[trust-critical]` — *shipped (pure + UNWIRED).* `services/stimulusGovernor.js::gradeStimulus({profile, modalityCategory, rir, target_rir, is_heavy_compound, metric_trend})` applies the PR 481 rule to a logged effort → `{effort_interpretation, progression_verdict, fatigue_signal, reason, rule}`. Profile-aware RIR-0 handling (strength heavy-compound RIR0 → hold/high; hypertrophy → hold/elevated caution; general_fitness accessory RIR0 → hold, not rewarded; strength/bodyweight isolation RIR0 → earns progress); under-target reserve → modality progress verdict; at-target → hold; non-RIR modalities (cardio/timed/circuit) judged by `metric_trend` (improved→progress, steady→hold, declined→back_off). Reuses `effortVerdict`; emits a coarse DIRECTION only (never a load number — that stays in `recommendNextSet`). **Read-only and NOT wired** into coach/recommendation/write paths (the `[trust-critical]` concern is the wiring, which is a later separately-gated slice). Golden tests pin the §5 grading + vocab membership (`test/stimulusGovernor.test.js`). No write-path/schema/parser/LLM change.
- ✅ **PR 483 — Live Fatigue Router expansion** `[correctness]` — *shipped (pure + UNWIRED, suggestion-only).* `services/fatigueRouter.js::routeNextMove({justLogged, nextMove, restedAlternative, heavy_lower_block_done, form_breakdown})` generalizes PR 477's pressing reroute to any pattern/modality (Taxonomy §6): repeated-RIR0 → `block_pr` (highest priority); cardio after a heavy lower block → `reduce_intensity`; circuit objective form-breakdown → `reduce_density`; same-muscle/pattern high fatigue → `promote_alternative` (rested antagonist) else `make_optional`; elevated → `reduce`; else `keep`. Form-breakdown must be an objective logged flag (never inferred from reps). Takes already-classified inputs (pattern/muscles/modalityCategory/fatigue_signal) so it stays decoupled — caller computes them via `patternFor`/`musclesFor`/`exerciseModality` + the governor `fatigue_signal`. Frozen `ROUTE_ACTIONS` vocab. Golden tests (`test/fatigueRouter.test.js`). Read-only — NOT wired into the live session/coach path; no write-path/schema/parser/LLM change.
- ✅ **PR 485 — Recovery / Deload Engine** `[trust-critical]` — *shipped (engine-only SELECTION layer, owner-approved scope 2026-06-23).* `services/recoveryDeloadSelection.js::assessRecoveryDeload(signals)` implements the SPE §10.5 recovery decision ladder + §10.2 convergence triggers → `{decision (normal/micro_adjustment/recovery_reload/deload/taper/complete_rest), deload_candidate, recovery_state, deload_style, pain_override, converged_signals, rationale}`. Convergence-based (no deload after one bad session): 2× RIR0 on main lifts / readiness-red 3 days / performance-decline+subjective → deload; ≥2 stacked moderate signals → recovery_reload; one local signal → micro_adjustment; pain-major → independent pivot/stop override; test-date → taper; illness → complete_rest. Profile-aware deload **style descriptors** (§10.4) carry NO numbers — the prescription stays owned by `computePrescription`/`docs/DELOAD_SPEC.md`. Pure/unwired: no parser/slash-notation/schema/write-path/coach-prompt/coach-wording/recommendation/user-facing change. Golden tests pin the §10.7 matrix (`test/recoveryDeloadSelection.test.js`). Full suite green.
