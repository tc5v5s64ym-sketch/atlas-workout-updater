# Atlas — Backlog Archive

Completed (✅) and cancelled (~~struck~~) backlog items, moved out of [`BACKLOG.md`](./BACKLOG.md) on 2026-06-29 to keep the active backlog focused on open work. Nothing here is actionable — it is a historical record of shipped/closed work (merge cards, resolved findings). Section headers mirror the original backlog.

**2026-07-08 (PR-21, governance diet):** a second pass moved every fully-closed, self-contained item (✅/`SHIPPED`/`FIXED`/`RESOLVED`-marked, no nested follow-up/deferred sub-note) out of `BACKLOG.md`. Items that carry any open follow-up, deferred, or not-yet-built sub-note were deliberately left in `BACKLOG.md` even when their parent PR shipped — moving them here would have hidden real open work. `BACKLOG.md` is still larger than the plan's original target size; further reduction needs editorial judgment (splitting a shipped item's prose from its still-open follow-up) that a mechanical pass can't safely do — filed as a `[housekeeping]` follow-up in `BACKLOG.md`.


## Bug-report tooling

- ✅ **Richer one-tap bug capture** `[housekeeping / tooling]` — During the 2026-06-29 live test the owner triaged 18 `Bug_Reports` rows; several root causes had to be INFERRED because the report captured downstream state but not the proximate cause. Enriched what one tap of "Report Bug" captures, so the next live test is diagnosable without the owner typing anything. Added (all client-side, `public/app.js`, all riding inside the existing `Payload JSON` column — **no `Bug_Reports` schema change**): (1) **request + response bodies** for the recent API calls (redacted via the existing secret patterns, truncated to 1000 chars) — e.g. exactly what `/api/parse-workout-text` returned for "Push ups 40 40 40"; (2) **error history** (`recent_errors`, last 12) not just `last_error`, so a poison cascade (one bad set 400ing every save) shows AS a cascade; (3) **client JS error capture** — `window.onerror` + `unhandledrejection` (the silent-lockup class that never reaches `api()`); (4) **UI breadcrumbs** (`action_log`, last 30 taps, capture-phase so a `stopPropagation` handler still logs) — "tapped restore, nothing happened" now shows whether the tap fired; (5) **`ui_state`** — disabled/hidden flags of composer / preview-btn / approve-btn / editors (the "Save greyed out" + "composer won't let me type" class, captured directly not inferred); (6) **`service_worker`** state — controller + waiting SW + atlas cache keys ("a fix didn't take = stale shell"); (7) **device clock + tz + connection** (local-clock-sensitive session-id/date bugs). Bounded: per-call body cap + a total-size guard (`BUG_REPORT_SIZE_BUDGET`) sheds bodies oldest-first so the report always fits the Sheets per-cell limit; recent-calls entries are cloned so trimming never mutates the live buffer. Capture helpers never throw. Server (`services/bugReport.js`) unchanged — new fields auto-ride in `Payload JSON`. Shell `v71→v72`. Tests: `test/unit.test.js` (ring buffers + bounds, error/tap listeners, api body capture + redaction, payload fields, size guard). No write-path / trust-loop / proof-field / schema / parser change.
- ✅ **Bug_Reports summary columns** `[schema — owner-approved]` (owner: "add new heads to the spread sheet") — Promote the most-scannable signals from the enriched one-tap report out of `Payload JSON` into their own grid columns. **Append-only schema change** to the `Bug_Reports` tab — five new columns added at the END (after `Payload JSON`) so historical 9-cell rows stay aligned; never insert/reorder. New columns + their derivation (`services/bugReport.js` `bugReportSummaryFields`, computed server-side from the already-redacted payload, each defaulting to `''` when absent so older clients / the `/bug` path just leave blanks): **Error Count** (len of `recent_errors` — spots a poison cascade), **Last Failed Endpoint** (most recent failing request, e.g. `POST /api/log-workout`), **Last Action** (last UI breadcrumb, e.g. `tap: restore`), **Stale Shell** (`yes` when a SW update is waiting), **UI Blocked** (e.g. `Save disabled, composer disabled`). Server-only — the client already sends the source fields; **no `public/app.js` / shell change**. Tests: `test/unit.test.js` (`Bug_Reports append shape` deepEqual updated to 14 cols + two summary-derivation cases: populated + all-blank). **Owner migration step (one-time, manual — the agent has no Sheets write access and `ensureSheetTab` only writes a header when row 1 is empty):** add the five header labels to row 1 columns **J–N** of the live `Bug_Reports` tab, in this order: `Error Count | Last Failed Endpoint | Last Action | Stale Shell | UI Blocked`. New rows fill under them; old rows stay blank there. **Do NOT reorder columns in the sheet** — append order is contract-bound to `BUG_REPORT_COLUMNS`. No write-path / trust-loop / proof-field / parser change (only the optional `Bug_Reports` diagnostics tab).

- ✅ **Read-only bug-review endpoint** `[tooling]` (owner: "I just want you to have access to read my sheet so when it's time to review the bugs you can just review the sheet") — The agent sandbox has no Sheets credentials and the Google Drive MCP's per-call approval doesn't resolve in the remote/web execution environment, so direct sheet reads were blocked (the owner had to paste rows). Added `GET /api/bug-report` (`index.js`; `config/routes.js` `readOnly:true, writeCapable:false`; auth-gated by the global `/api` API-key guard) — a review feed that returns the most recent `Bug_Reports` rows **newest-first**, mapped to the column names, using the app's existing `getRecentRows` (prod service-account creds; no new credential in the sandbox). The heavy `Payload JSON` cell is dropped by default (the summary columns + note + last error cover triage); `?full=1` includes it, `?limit=N` (1–500, default 50) bounds the read. Read-only: no append, no schema touch, no write/trust-loop/proof-field/parser change. Tests: `test/api-smoke.test.js` (newest-first + column mapping + Payload-JSON dropped by default; `?full=1` includes it; no-API-key → 401). **Owner setup to use it:** add `ATLAS_BASE_URL` (deployed app URL) + `ATLAS_API_KEY` to this environment's variables so a session can `GET $ATLAS_BASE_URL/api/bug-report` and review without pasting. Only the Atlas API key is shared with the sandbox (never Google credentials), and it reaches only this read-only feed.
- ✅ **Verify the bug-review loop end-to-end** `[tooling — owner live]` — **DONE (2026-06-28 PT).** `GET /api/bug-report?limit=50&full=1` → `HTTP 200`, 17 rows, column-mapped, payloads on `?full=1`. Read-feed loop confirmed working. **Env gotcha found + resolved:** `ATLAS_API_KEY` kept collapsing to a single `X` because the key **began with `X#…`** and the environment stores vars in `.env` format where `#` starts a comment, so `ATLAS_API_KEY=X#…` parsed as just `X`. Fix = rotate the key to a value with **no `#`** (ideally no `$` either), set identically on Render + the environment; a *fresh* session then reads it (a running session never re-reads `process.env`). **Full triage of the 17 rows is recorded in [`docs/BUG_TRIAGE_LEDGER.md`](./docs/BUG_TRIAGE_LEDGER.md)** — 11 already shipped (PRs A–E / #678 / B4), 2 fixed pending an owner live re-test, **1 genuinely open** (next line), 3 noise. The ledger is the shared done-vs-open record so parallel sessions don't double-dip; convention = **every fix PR cites its `BUG-…` id in the commit message** and updates the ledger in the same PR.

- ✅ **Recovery/deload session still nudges "add load"** `[correctness]` (BUG-20260629-034034) — **shipped (this PR).** A Coach's Pick Recovery/Pump session replied "Too much left in the tank. Bump coming" — telling the lifter to add load on a deliberate deload day. **Root (refined during the fix):** the visible line is the *deterministic* set-voice (`effortNote`/`renderSetVoice` from `services/setEffortCopy.js`/`coachVoiceRenderer.js`), driven by `analyzeSetSequence`'s `bump` progression verdict — computed **blind to the recovery/deload objective** and rendered server-side regardless of whether Gemini is up. (Slice 6's suppression only covered the client-side LLM-down templated path.) **Fix (source-level, both paths):** (1) new pure `suppressBumpForRecovery(analysis, recoveryActive)` (`services/setEffortSignals.js`) neutralizes ONLY the under-dose `bump` verdict (and its reason code/signal) when a recovery objective is active — `block`/`caution` reads are untouched; wired into `computeSetEffortExtras` (`index.js`) by computing the recovery read UP FRONT (deload active OR a `deload`/`recovery_reload` selection) and gating the analysis before any voice renders, so both `effort_note` and `voiceBase` go quiet on the bump. `classifySeverity` only derives `bump` from the analysis verdict (never from `effort_verdict`), so neutralizing the verdict fully suppresses the deterministic add-load voice. (2) LLM prompt (`buildCoachSystemPrompt`, `services/coach.js`): the `recovery_advisory` rule now **overrides** the `effort_verdict` add-weight steer (same strength as the active-deload rule), so the prose can't independently tell the lifter to add load when the set read easy. (3) Review follow-up — closed the last LLM-path steer: `set_grade` (the Stimulus Governor) graded a high-RIR recovery set as `+load` ("room to add stimulus"); new `holdStimulusForRecovery` downgrades a `+load`/`+reps` grade to `hold` when recovery is active, so no fact forwarded to the model invites adding load. Correctness fix, not a coaching-philosophy change. Tests: 3 new in `test/setEffortSignals.test.js` (bump neutralized on recovery; untouched off-recovery; block/caution never touched) + updated `test/coach.test.js` recovery_advisory prompt assertion. No write-path / trust-loop / proof-field / schema / parser change.

## QA campaign — 2026-06-26 live gym + composer playtest (B1–B10)

- ✅ **B1 — Idempotent workout save / no duplicate `Log_Cleaned` rows** `[trust-critical]` (this PR) — **Verdict: STILL BROKEN → fixed.** Root cause: `/api/log-workout`'s live path had per-`write_id` replay dedup but — unlike its sibling `/api/complete-workout` — **no row-level composite-key guard** on `Log_Cleaned`. Because `public/app.js` mints a fresh `write_id` on every re-preview (`generateWriteId()`) while reusing the stable `session_id`, a re-previewed/re-approved save (e.g. after a failed effort import forces a re-preview) appended the same rows again under one session_id — the "49 sets / 51,390 lb" duplication. Fix: mirror the proven `getLogCompositeKeys()` row-level dedup onto `/api/log-workout` — skip rows whose `session‖exercise‖set_number` already exists; an all-duplicate re-save appends nothing and replays an idempotent "already logged" response the client recognises. Row-level (not session-level) so legitimate incremental logging still appends. Server-only — no edit to the restricted trust-loop file. Tests: 4 new api-smoke cases (re-submit new write_id appends nothing; partial-duplicate appends only new rows + totals match; replayed write_id appends once; failed-effort re-save no duplicate log rows). No schema / write-path-semantics / proof-field / parser change.
- ✅ **B2 — Canonical active session state** `[trust-critical]` — addressed across two complementary slices.
  - **Slice 1 — plan-card drift (PR #652):** the canonical model (`public/activeSession.js`) and `getCanonicalSession()`/`canonicalSessionRecap()` were already authoritative, with composer/next-up/save-payload/coach-context wired through it (P0 `docs/ACTIVE_SESSION_STATE_DIAGNOSIS.md` lane). The one surface still drifting was the **plan-card banner** (`renderActiveSessionBanner`): it rendered the current step from the raw `activePlannedSession.index` cursor, which lags after a logged set until "Next exercise →" is tapped. Fix: `syncPlannedIndexToCanonical()` reconciles the cursor forward to the canonical current inside the banner render, and `emitSetLogged` refreshes the banner after a log. Forward-only, never mutates the list, no write/preview/parser/schema change. Tests: `test/activeSessionPlanCard.test.js`.
  - **Slice 2 — success-card vs "didn't catch" contradiction (PR #653, this PR):** for a genuinely-unrecognized lift, the composer status showed the failure message "Didn't catch that lift — check the exercise name before saving" while the SAME parsed rows were still captured — `emitSetLogged` (`public/app.js`) buffers them into `sessionLog`/`sessionCompleted` and dispatches a confirmation card, then clears the very `parsedRowsEditor` the warning told the lifter to check. So a single input produced BOTH a green "logged" card AND a red "didn't catch" failure (owner live evidence: "RDLs" carded + warned + saved with muscle group Unknown). Fix: the composer now surfaces ONE consistent name-review advisory that AGREES with the card (`I don't recognize "<name>" — check the exercise name before it's saved.`) instead of a failure message; the set is still captured (no data loss) and the unresolved name is still flagged for correction. Gate (`shouldWarnUnknownLift`, incl. KB/catalog identity) unchanged. No write-path / trust-loop / proof-field / schema / parser change. Tests: `test/activeSessionSurfaceConsistency.test.js` (5 cases) + updated `test/cableFlyIdentity.test.js`. (Shell is network-first; no SW cache bump needed for the composer-copy change.)
  - **Slice 3 — save payload derives from the canonical session (this PR):** the last raw-holder read on the SAVE surface. The end-of-session manual closeout sent `plan_exercises` from the raw `activePlannedSession.exercises` holder (comment: "Fix deferred") while its mid-session sibling already derived from the canonical session via `currentPlannedExercise()`. Fix: the closeout `plan_exercises` now derives from `getCanonicalSession()` through the new pure `planExercisesFromCanonical()` (`public/app.js`) — the same canonical model the card / coach context / preview read, so the save payload can no longer drift from the visible session. Planned-origin only: an off-plan insert (Hammer Curls / Knee Raises) is logged work but never a PRESCRIBED substitution source, so it is excluded from `inferPrescribedPairs`; completed AND pending planned slots are both kept (a fulfilled lift must stay so its logged row is exact-matched/claimed; a pending lift is the legitimate swap source). Behavior-preserving for substitution inference, with a raw-plan fallback only when the canonical model is unavailable. `plan_exercises` rides along in the reused `pendingWrite.payload` to the live `/api/log-workout` call (`public/app.js` approve handler), but server-side it only feeds `inferPrescribedPairs` inside the `test_mode` preview branch (`index.js`) — it never affects the rows written, so this is behavior-preserving on both the preview and live paths. No write-path / trust-loop / proof-field / schema / parser change. Tests: `test/activeSessionSavePayload.test.js` (7 cases).
  - **B2 residual (deferred, follow-ups — overlap B8):** the remaining surfaces that still read divergent holders are squarely B8 and should be wired in their own slices: the composer placeholder (`buildWorkoutPlaceholder`) seeds from the Coach's-Pick suggestion, and `planStepFor`/`getNextExerciseInPlan` fallbacks read the raw session. These are surface-derivation polish (B8 "stale plan cards / composer placeholders") — not the trust-critical contradictions fixed above.
- ✅ **B3 — Failed effort import must not poison the workout save** `[correctness]` (this PR) — **Verdict: PARTIALLY BROKEN → fixed.** Effort & exercise are independent transactions; a failed *optional* effort never blocks or loses the exercise rows, and the copy is specific. Two paths verified against current repo evidence:
  - **Poisoning path (still broken → fixed):** `/api/complete-workout` only degraded gracefully when the screenshot parse THREW (Gemini 429/timeout). When the parse SUCCEEDED but returned invalid/incomplete effort metrics (e.g. missing `activeCalories`), `normalizeAndValidateParsedMetrics` threw and the handler returned a **400 "Parsed metrics validation failed"**, sinking the whole save and **losing the logged sets** — the exact B3 poisoning. Fix: when a screenshot is present AND there are logged sets (`req.file && !effortOnly`), an invalid-metrics validation failure now degrades the SAME way an unreadable screenshot does — save the sets with a blank effort row, set `screenshot_unreadable`/`effort_source: 'screenshot_unreadable'`, and surface the specific owner copy ("I couldn't read effort from the screenshot. I can still save the workout without effort data."). Manual effort and effort-only requests still fail honestly (nothing to fall back to). The frontend already renders this degrade state ("Effort couldn't be read from the screenshot" + warning + previews the sets), so no trust-loop file edit was needed. Server-only; no schema / proof-field / write-path-semantics / parser change.
  - **B1×B3 interaction (already fixed on this branch → regression test added):** the all-duplicate short-circuit on `/api/log-workout` was already guarded with `&& !formattedEffortRow` (commits `17af0ad`, `244ac99`), so a re-save where every log row is a duplicate but a genuinely-new effort row is attached now appends the effort row without re-appending log rows. The missing regression the backlog called for is added: *all log rows duplicate + a new effort row → effort still appends (`effort_rows_written:1`), log rows do not (`log_rows_written:0`)*.
  - **Tests:** 5 new api-smoke cases — invalid-parsed-metrics screenshot degrade (dry-run preview keeps the set + blank effort + specific copy); live invalid-metrics save writes the set with blank effort; effort-only invalid-metrics fails closed (422 + specific copy) and never appends; invalid-metrics degrade discards the rejected screenshot date on a no-manual-date save (convergence with the throw path); B1×B3 effort-appends-when-all-logs-duplicate regression.
- ✅ **B3 follow-up — Manual effort panel had no submit affordance** `[polish]` (this PR) — **Owner live-test gap (2026-06-28).** Found while validating B3's "manual effort can replace failed screenshot state": after typing manual effort, there was no obvious way to submit it — the only trigger was the composer's ↑ button, so the values "just sat there." Fix: an **"Add effort & preview"** button on the effort panel (`public/index.html`) that dispatches the SAME `logger-form` submit the ↑ does (`public/app.js`), so manual effort + the buffered session flow into one preview through the identical preview→approve→write path. Opens no new write path and changes no trust-loop semantics — purely a more discoverable trigger for the existing submit handler. Tests: `test/effortPreviewButton.test.js` (5 structural cases). Network-first shell — no SW cache bump. No schema / write-path / proof-field / parser change.
- ✅ **B5 — Apple Watch screenshot import must preserve the source date** `[correctness]` (this PR) — **Verdict: BROKEN → fixed.** Root cause: the backend resolved the date correctly (manual > screenshot > today) but never labelled which source won, so the preview could not surface a warning when the date silently fell back to today. A June 12 screenshot imported on June 26 was saved as June 26 with no visible indication. Fix: compute `dateSource` ('screenshot' | 'manual' | 'today_fallback') alongside `dateValue` in `/api/complete-workout` (`index.js`) and include it in the response as `date_source`. `renderCompleteWorkoutPreview` (`public/app.js`) now renders: a ⚠️ warning banner for 'today_fallback' ("Date not found in screenshot — saving as {date} (today). If this workout is from a different day, change the date field above and preview again."); a `Date from screenshot: {date}` confirmation for 'screenshot'; a `Date (manual): {date}` confirmation for 'manual'. The `log-date` field is already updated on preview (line 5027) so the owner can correct a wrong date and re-preview — dup-detection then runs against the correct date. No write-path / trust-loop / schema / parser / proof-field change. Tests: `date_source` assertions added to the three existing complete-workout date tests in `test/api-smoke.test.js`.
- ✅ **B6 — Coach's Pick / Blind Spot history correctness** `[trust-critical]` — Root cause: `buildMuscleGroupReadiness` in `services/analytics.js` required `isPositiveFinite(weight)` (weight > 0), silently dropping bodyweight exercises (HNR01 Hanging Knee Raises, KR01 Knee Raises) with weight=0 or null. Core's last-trained date resolved to 76 days ago (last weighted core work) instead of 2 days ago (HNR01 on 2026-06-24), so Core appeared 'fresh' → a blind spot. Fix: removed weight requirement from the readiness filter — it only needed valid reps and a date, since the purpose is "when was this pattern last trained," not "how much was lifted." The `(row.weight || 0)` in volume calculation handles null/0 weight gracefully. Two new tests: bodyweight weight=0 and weight=null rows both register daysSince and non-unknown status for their pattern. All 2906 tests pass.
- ✅ **B7 — Active workout survives app switch / refresh / lock** `[correctness]` — **Verdict: BROKEN → fixed.** Root cause: `restoreSessionSnapshot()` restored `sessionLog` / `sessionCompleted` / `activePlannedSession` on load but showed no user-facing notice and did not persist the `session_id` — so if the app crashed mid-session, the lifter saw no confirmation the session was restored, and a re-preview after restore could generate a different `session_id` (e.g. AM→PM boundary), bypassing the server's row-level dedup. Fix: (1) `saveSessionSnapshot()` now includes the current `#log-session-id` input value so the stable id survives the reload; (2) `restoreSessionSnapshot()` restores it to the input field, then calls `renderResumeNotice(setCount, sets)` which shows a dismissible banner "Session restored — N sets logged (Ex1, Ex2, Ex3)"; (3) `clearSessionSnapshot()` also hides the notice, so it vanishes automatically on successful save or Start Over. The notice element (`#session-resume-notice`) and styles were added to `index.html` / `styles.css`. No write-path / trust-loop / schema / parser / proof-field change.
- ✅ **B8 — Stale plan cards / composer placeholders** `[polish]` — Three surface bugs fixed: (1) `nextUpPlaceholderFromPlan` double-normalized already-normalized `activePlannedSession` entries — `compactPrescription` called `normalizePlanExercise` a second time, mapping `name → liftCode → null`, so the placeholder always returned null and fell back to the expanded per-set format. Fixed with `compactPrescriptionFromNormalized` that reads the already-normalized shape directly (no re-normalization). (2) After save or Start Over, the composer placeholder retained the last Coach's Pick suggestion and `sessionActive` in nav.js stayed true, so generic hints never resumed. Fixed by dispatching `atlas:session-reset` from both `clearSessionSnapshot` call sites (post-save and `startOverWorkout`); nav.js resets `sessionActive = false` on that event; coach-conversation.js resets `lastAnnouncedNextUp = null`. (3) The initial Coach's Pick placeholder (`buildWorkoutPlaceholder`) never filtered already-completed lifts, so after finishing Exercise A the composer could still suggest "Bench … x3" for A. Fixed by reading `getSessionCompleted()` (new read-only accessor on app.js) at placeholder-set time and excluding completed lifts from the suggestion, with full-list fallback when all are complete. No write-path / trust-loop / schema / parser / proof-field change.
- ✅ **B9 — Busy Next Exercise UI** `[polish]` — The `.nextp` prescription card appeared after every set with an orange accent border and bold orange "→ Next" header in display font, drawing the eye during flow logging. Fix: three CSS token swaps — border from `--accent-soft` (orange tint) to `--border` (neutral), `.nextp-h` color from `--accent` (orange) to `--faint` (dim gray), font-weight 500 → 400. Also added a rule for `.next-exercise-handoff` (previously unstyled, inheriting full ink color) setting it to `--muted` at 0.88rem — the "Moving on — next up:" transition line is now consistent with the quieter prescription card. No logic, trust-loop, schema, or write-path change.
- ✅ **B10 — Generic accessory/core coaching** `[polish]` — Root cause: when a lift has no progression_verdict, trend, or readiness_signal (thin history — common for accessories/core), the model had no engine verdict to word but the "take a position" directive still prompted it to fill the void with generic filler ("great work", "keep it up"). Fix: added a THIN-HISTORY RULE to `buildCoachSystemPrompt()` (`services/coach.js`): when all three key signal fields are absent/null, the model may only react to what IS observable (today_sets, effort_verdict if present) and is explicitly forbidden from using named generic-filler phrases. The existing "If a fact is missing, drop that beat" IRON RULE covered numbers; this rule covers the verbal void. No new fields forwarded; the existing null-collapse for absent verdicts is the detection signal. 4 new tests in `test/coachPromptRules.test.js`.


## Bug remediation — 2026-06-29 live gym test (Bug_Reports tab, BUG-20260629)

- ✅ **PR A — [trust-critical] One mis-parsed set must not brick the whole session save** (BUG-20260629-002910, -003028, -003118, -003208) — **shipped (this PR).** "Push ups 40 40 40" routed through `parseUnknownExercise` (Push-Up was absent from EXERCISE_ALIASES), then `parseDaleShorthand` matched the rest as weight=40/reps=40/rir=40. rir=40 violated the hard bounds guard and rejected the ENTIRE session with 400 "Implausible set values rejected". Two fixes: (1) Parser: added Push-Up to EXERCISE_ALIASES with a dedicated bodyweight handler (`parseBareBodyweightReps` — bare space/comma-separated integers ≤100 → bodyweight reps, called only for Push-Up to avoid breaking Knee Raises clarification intent). (2) Resilience: `runCloseout()` now only calls `populateSetRows(buildRowsFromSessionLog())` when the table is empty, so repeat "Log it" calls preserve any user edits; preview catch block parses row indices from the error and applies `.row-error` highlight + "Fix or delete the highlighted row(s)" guidance. Cache bumped v70→v71. Tests: 5 new parser golden tests; 2 new unit source-introspection tests; 1 new api-smoke bounds-rejection test; v71 version pin.
- ✅ **PR B — [trust-critical] Incline/Decline bench logged as plain "Bench Press"** (BUG-20260629-003415/-003450) — **shipped (this PR).** Added two new canonical exercises: `Incline Bench Press` (aliases: incline bench press, incline barbell bench, incline bench, ibp) and `Decline Bench Press` (aliases: decline bench press, decline barbell bench, decline bench). The length-sorted alias matching in `findExerciseInText` now finds "incline bench" (2 words) before bare "bench" (1 word). Guards confirmed: "flat bench"/"bench" still → Bench Press; "incline db press" still → Incline DB Press. Invariant P2: 7 new golden tests added.
- ~~**PR C — [correctness] Restore-session banner tap does nothing**~~ BUG-20260629-003605. **ALREADY FIXED** — Current-State Verification Gate: PR #678 (`restore-banner-swipe-discard-zygm2v`) added a `click` event listener to `wireResumeNoticeGestures` that calls `restoreSessionToView()` on a plain tap (no drag). Existing source-introspection test at `unit.test.js:5454` verifies the path. No code change needed.
- ✅ **PR D — [correctness] "Tricep pulls" not recognized** (BUG-20260629-003720) — **shipped (this PR).** Added `Cable Tricep Pushdown` to EXERCISE_ALIASES with aliases: cable tricep pushdown/pushdowns, tricep pushdown/pushdowns, tricep pulldown/pulldowns, tricep pull/pulls, cable triceps, cable tricep. Invariant P2: 4 new golden tests.
- ✅ **PR E — [polish] Prescription card "→ Next" header renamed to "Next time:"** (BUG-20260629-003820) — **shipped (this PR).** `buildNextPrescription` in `public/coach-conversation.js` rendered a `.nextp` card with "→ Next" header after every logged set. The `rec.recommendation` it displays is always a same-exercise FUTURE prescription (e.g. "Keep 225 lbs for 8 reps next session.") from `/api/recommend/next/{liftCode}` — not a directive to move to a queued exercise. "→ Next" implied a next exercise was queued, confusing the lifter in freestyle mode. Fix: renamed header `'→ Next'` → `'Next time:'`. `coach-conversation.js` is in the SW shell cache (sw.js) so cache bumped v72→v73 (main was already at v72 via #683). Source-introspection test added. No logic / write-path / trust-loop / schema / parser change.
  - ◐ **Freestyle session completion state** `[polish]` — **owner approved 2026-06-29 ("Go"); building in slices.** Investigation finding: most of the original proposal already existed — the review card (rendered after "done"/"log it" via `buildReviewCard`, `coach-conversation.js`) already shows the deterministic recap (exercise-by-exercise list, exercise count, **set count**, **total volume**), the effort grid, the remaining-plan line (`canonicalSessionRecap().remaining`), and a **Save workout** button + Edit affordance. The optional-effort prompt and "no proactive next-session" (B4/PR E) were already done. The genuine gap was that freestyle save was **hidden behind a chat keyword** with no visible affordance.
    - ✅ **Slice 1 — explicit "Finish session" button (shipped, this PR).** Added `#finish-session-btn` (`public/index.html`), a contextual control shown once a set is logged (`atlas:set-logged`) and hidden on reset/save (`atlas:session-reset`) — also surfaced when a restored session is brought into view (`restoreSessionToView`). Clicking it runs the SAME closeout as "done"/"log it" (`handleLogIt` → `runCloseout` → preview → review card), so it opens **no new write path** and the preview→approve→write trust loop is unchanged (nothing saved until the lifter approves the review card). Cache bumped v74→v75. Tests: source-introspection in `test/unit.test.js` + v75 pins.
    - **Slice 2 (deferred, optional) `[polish]`:** add explicit **"Add more"** and **"Discard"** affordances on the review card itself (today "Start over" lives only in the preview panel, and "add more" is implicit = keep logging). File when the owner wants them; not required for the finish flow to work.
- ✅ **PR F — [correctness] "Bicep curls" flagged unknown_exercise** (BUG-20260629 live-test follow-up) — **shipped (this PR).** `bicep curls 30 12/2` parsed its sets correctly but the exercise hit `unknown_exercise` (no `Bicep Curl` in EXERCISE_ALIASES — only `Hammer Curl`/`Leg Curl` existed), so the lifter saw the "I don't recognize 'Bicep Curls'" advisory and it saved with `muscle_group: Unknown`. Same class as PR B/D. Fix: added `Bicep Curl` to EXERCISE_ALIASES with explicit forms (`bicep curls`, `biceps curls`, `bicep curl`, `biceps curl`). Bare `curl`/`curls` deliberately left **unaliased** so unknown "X curl" lifts (Zercher Curl, Jefferson Curl) keep their typed name; length-sort keeps `leg curls`/`hammer curls` winning. Parser-only — no `public/` asset, so no cache bump. Invariant P2: 5 new golden tests (3 bicep resolutions + bare-curl-preserved guard + leg/hammer regression guard). Pairs with the `Bicep Curl` catalog-row data item above for full muscle-group attribution.
- ✅ **PR G — [correctness] Restore-session banner tap dismisses but session looks unrestored** (BUG-20260629-054925) — **shipped (this PR).** Distinct from PR C / #678: that PR correctly wired the **tap** to call `restoreSessionToView()`; this is the next defect down. `restoreSessionToView()` (`public/app.js`) populated the rows and set `parsedRowsEditor.hidden = false`, but `parsedRowsEditor` is a `<details>` (`index.html:197`) — un-hiding it only shows the collapsed **"Edit rows"** summary, so the recovered sets stayed hidden inside the closed disclosure. Owner saw the banner vanish with no visible session (the 9 restored sets were in the buffer and the composer placeholder reflected an active session, but the rows never surfaced). Fix: also set `parsedRowsEditor.open = true` on restore, mirroring the established `details.hidden = false; details.open = true` reveal pattern used for the effort panel. `public/app.js` is a precached shell asset → cache bumped v73→v74. Tests: extended `restore banner: tap-to-view` test with `.open = true` assertion; v74 version pins. No write-path / trust-loop / proof-field / schema / parser change — restore-view visibility only.


- ✅ **[data] All five campaign lifts verified present & correct in the production `Exercise_Catalog`** (verified live 2026-06-29 via read-only CSV review). Columns are `Canonical_Name | Muscle_Group | Lift_Code | Original_Variants`. Confirmed rows: `Incline Bench Press | Chest | IBP01`, `Decline Bench Press | Chest | DBP01`, `Cable Tricep Pushdown | Arms | CTP01`, `Romanian Deadlift | Posterior Chain | RDL01` (variants incl. `RDL`, `RDLs` — matches the parser's `RDL` canonical), and `Bicep Curl | Biceps | BC01 | Bicep Curl, Bicep Curls, Biceps Curl, Biceps Curls` (added by owner at row 159). All canonical names match the parser output, so these lifts attribute correctly (no `Unknown`). Notes: (1) the earlier edits were to a **stray** workbook, not the production spreadsheet (identifiers intentionally not recorded in-repo); the catalog is a comprehensive ~239-row pre-built taxonomy, so Incline/Decline/Cable Tricep were already present there. (2) Enrichment is write-time — any sets logged as `Unknown` *before* a row existed won't self-correct; fix `muscle_group` by hand if history matters. (3) Earlier guidance in this file mis-stated the catalog as `Arms`/`Legs` and the wrong column order — corrected here and in `docs/SHEET_CONTRACT.md`. **Open follow-up:** `services/substitutionRecommender.js` references `'Incline Press'` (not `'Incline Bench Press'`) — a pre-existing substitution-suggestion naming inconsistency; correct in a future authorized parser/catalog-naming sweep (does not affect logging/attribution).
- ✅ **[correctness] A single bare bodyweight rep logs as one set — "missed rows" in a multi-exercise paste** (PR pending) — `BUG-20260629-152824` ("I put a bunch of workouts in at once and it missed rows"). Verification gate: the *originally-reported* missed-rows symptom on build `95c5b78` predates **both** #699 (multiline-merge fix) and #700, and maps to the #699 "a second exercise's set merges into the prior lift" bug = **already fixed**. While verifying, found a *separate* still-open trigger: a lone bare rep on an unambiguously-bodyweight lift (`push ups 20`) fell through to a "missing sets" dead-end (`parseBareBodyweightReps` required ≥2 tokens), and because a multi-exercise block is all-clean-or-drop, that one unresolved segment discarded every sibling row. Fixed in `services/workoutTextParser.js` by accepting a single bare rep for the Push-Up path (only caller; Push-Up never carries load, so a lone integer is unambiguously reps). Knee-raises/Dips bare numbers stay ambiguous-by-design (golden-tested) and still ask. Pure parser fix — no write/trust-loop/schema/proof change.
## Multi-workout composer (owner 2026-06-25: "log two or more workouts at once")

- ✅ **RIR is OPTIONAL on save (owner 2026-06-25: "log it however" / "it did like it without the rir")** `[correctness]` — `normalizeLogRowObject` (`index.js`) no longer requires `rir`: a set logged with just weight × reps (warm-up or working) saves with a blank RIR cell (column 10 stays present), Atlas never invents a rating. `weight`/`reps` + identity fields remain required so a garbled row is still rejected. Supersedes the #595 warm-up-only relaxation. Progression/PR engines already treat a blank RIR as no-signal (NaN-filtered). Tests updated in `test/api-smoke.test.js` (blank-RIR working + warm-up rows save; missing reps still 400). No proof-field/trust-loop/schema change; slash-notation contract unchanged (RIR still parsed when given).

- ✅ **Engine slice — display-block normalizer** (PR #587) — `public/displayBlockNormalizer.js` (pure UMD): `normalizeDisplayBlocks(text)` splits a stacked display-format paste into per-exercise blocks, each with `{ name, sets:[{weight,reps,rir,warmup,unit}], warmupCount, canonicalText, canonicalTextWorkingOnly }`. It only **restructures** the display format into the canonical per-exercise lines the existing proven parser already accepts (verified by an integration test that feeds each `canonicalText` through `parseWorkoutText`) — it does NOT parse sets itself and never touches the slash-notation contract. Conservative: returns `isDisplayBlock:false` for single-line / slash-notation / ambiguous (set-line-without-header, prose header) input, so existing parsing is untouched. Warm-ups are flagged, never silently dropped. **UNWIRED** — engine only, zero live-path change.
- ✅ **Engine slice 2 — real Atlas paste format** (PR #589) — extended the normalizer's set-line matcher to the live sample: optional space OR `×`/`x` weight↔reps separator, attached `lbs`/`kg` unit, Atlas slash working sets `245lbs 6/2` (RIR captured per set), warm-up markers `· warm-up`/`warmup`/`wu`/`(W)`. Mandatory weight↔reps separator so a bare number can't mis-split. Canonical text round-trips RIR (`245 6/2`). Still **UNWIRED**. 21 unit tests including the exact two-exercise screenshot paste + its `parseWorkoutText` round-trip.

- ✅ **Live re-test 2026-07-02 (owner, IMG_5441/5442) — bug (1) re-confirmed for a THIRD paste shape; sets LOST. FIXED (partial-log PR, 2026-07-02).** `[trust-critical]` **Fix:** root cause was `normalizeParserText` collapsing newlines BEFORE parsing, so a one-exercise-per-line paste reached `parseLogSets` as a single blob and dead-ended on the generic multi-exercise ask (whose own advice — "put each exercise on its own line" — was therefore unfulfillable). `parseWorkoutText` (`services/workoutTextParser.js`) now runs a multi-line rescue when the collapsed whole-text parse dead-ends (`multiple_exercises_in_input` / `unattributable_trailing_sets`): `parseMultilineLogSets` re-parses per line through the proven single-entry parser (slash notation, ×N/xN, bodyweight, multi-set lines, bare-name headers with context carry) — resolved lines return as `log_sets_multi`, unresolved lines return in `unresolved` with each line's own SPECIFIC ask (**partial-log, owner decision 2026-07-02** — supersedes all-clean-or-drop for newline-separated pastes; inline same-line stacking keeps the G1 refuse-to-merge guardrail pending the owner-gated KB-backed splitter lane). Client (`public/app.js`): partial responses buffer the clean rows and surface "one line needs a check … Which row — seated, bent-over…?"; a none-resolved slash-notation paste surfaces its first specific ask instead of routing to the coach — set notation can no longer sink into a fluent LLM readback over an empty buffer. The rescue runs ONLY on inputs that dead-ended before; every currently-succeeding path is byte-identical. Tests: `test/multilinePartialLog.test.js` (11 golden, incl. the exact owner repro + unicode ×), `test/parserStackedBoundary.test.js` updated to the partial-log contract (hard never-absorb invariant unchanged), 3 client-wiring tests in `test/unit.test.js`. Shell v78→v79. **Owner live re-test:** paste the same five-exercise block → 4 exercises buffer with cards + "Which row…?" ask; reply "seated rows 150 10/2 x3" → attaches; save → 14 rows. _Original finding:_ Paste shape: one exercise per LINE in Atlas slash notation with the `×N` multiplier — `Bench 135 10/5 185 8/2` / `Rows 150 10/2 ×3` / `Dips 10/2 ×3` / `Bicep curls 30 12/2 ×3` / `KR 15/2 ×3`. This is NOT the display-block format PR #591 wired (bare-name headers + `135lbs 10` lines) and NOT the shipped single-line inline (way-2) case. Observed: first paste → fluent LLM readback (all five lifts correctly named, incl. "Knee Raises" from `KR`) that sounded logged but buffered NOTHING; "Done" → nothing; expanded re-paste (one set per line) → generic coach fallback ("Noted — keep logging and say 'log it'"); "Log it" → "Couldn't find any sets in the conversation." Zero rows buffered or saved across the whole exchange. Likely mechanics to verify with /investigate before fixing: bare/ambiguous name-lines (`Rows` is ambiguous-by-design per the G1 extension; `KR` may not be a parser alias) tripping the all-clean-or-drop rule (see the deferred all-clean-or-drop item in "Bug remediation — 2026-06-29"), with the whole block then falling through to the chat LLM instead of a clarification. **Fix requirements (Atlas-principle violations — deterministically-loggable input must not route to the coach; the trust path must not silently discard user intent):** (a) this paste shape parses (or splits per-line into the proven per-exercise parser); (b) an ambiguous chunk surfaces its SPECIFIC clarification — never the generic keep-logging fallback, and never a fluent LLM readback over an empty buffer; (c) the "log it"/"done" empty-buffer copy must not claim "no sets in the conversation" when the user pasted sets that failed routing. **Blocking design question for the owner (already filed as the all-clean-or-drop item): when 4 of 5 lines are clean, may Atlas preview the clean 4 and ask only about the 5th (partial-log), or must it keep all-or-nothing?** The fix's shape depends on this answer.

## P0 coach-trust findings — owner live session 2026-06-25 (investigation, not yet fixed)

- ✅ **G2 (F1) — coach reaction under-iterates (one lift, not all)** `[correctness]` — Root cause: `handleSetLogged` only called `getInWorkoutNote` for `exercises[0]`; a stacked Deadlift+Bench entry produced a readback for each lift but coaching prose only for Deadlift. Fix: `for (const ex of exercises.slice(1))` loop in `public/coach-conversation.js` — each additional lift gets its own `[readback → coaching → next-prescription]` block in order. The primary lift's block is byte-identical (no change to single-exercise entries). Attribution: coaching note is prefixed with the lift name (`"Bench Press: <note>"`) so the lifter always knows which reaction is for which lift. Tests: `test/coachConversation.test.js` (G2 + FA structural assertions, existing); 2 new assertions added to `test/unit.test.js` locking chatTurns wiring, per-lift `fetchReaction`, and `buildNextPrescription` per additional lift. Coach-render layer only; no write-path/schema/trust-loop change.
- ✅ **G4 (F4) — coach voice fabricated a unit ("70kg/50kg" for lb data)** `[correctness]`: the coach prompt says weights are lbs with **no units in output** (`services/coach.js:966`) and there is **no kg literal or kg↔lb conversion anywhere in the coach path** — so the LLM **added** the "kg" suffix, a grounding/instruction-adherence violation (the numbers are the right lb values). **FIXED (PR `fix/coach-unit-guard`):** a deterministic post-generation guard `stripFabricatedUnits` removes any weight-unit token (kg/kgs/kilo/kilogram(s)/lb/lbs/pound(s)) that immediately follows a number, applied to the **display voices only** — `generateCoachMessage` (set-reaction), `generatePlanMessage` (plan), `generateChatReply` (chat) — before the prose leaves the server. Deliberately **NOT** at the shared `callGeminiContents` layer, because that layer also serves `compileSessionFromHistory`, whose slash-notation `workout_text` feeds the parser → preview → approve → write path (`public/app.js:4422`) — the guard must stay off any write-feeding text (it's a no-op on valid slash notation anyway; a wiring test locks this). Surgical: keeps the number + sentence (`best of 50kg` → `best of 50`), never touches rep/RIR/set counts, %, dates, durations, cal/HR, letters inside words ("club"), or the chat `PROPOSE_EDIT:` JSON. **The prompt is NOT trusted** — the guard stands alone with the prompt unchanged (the bug existed *because* the model ignored the prompt). Tests: `test/coachUnitGuard.test.js` (strip variants + false-positive guards + compile-notation no-op + display-voices-wired / compile-path-not-wired). No write-path/schema/parser/next-up change; all existing tests unchanged.
- ✅ **P0 — coach claims grounding audit: PR language and session count fabrication** `[trust-critical]` — **INVESTIGATED and closed (this PR).** Full audit of what reaches each voice via `sanitizeFacts` / `sanitizeChatContext`: (a) trend direction, effort verdict, progression verdict, best weight, working range, deviation, readiness, and stall data are ALL forwarded as engine-computed, whitelisted-enum facts; no fabrication risk there. (b) **Gap 1 — PR language not tied to engine verdict:** the prompt said "Do not claim a PR unless the facts say so" but did not IRON RULE it to `progression_verdict.level === 'new_ground'`. The model could see `best_weight` + a slightly higher today-set and declare a PR even when the engine verdict was `in_pocket`. **Fixed:** added an IRON RULE explicitly gating all "personal best / new PR / new record" language on `new_ground`, and clarifying that `best_weight` alone does NOT authorize it. (c) **Gap 2 — session count derived by counting the wrong array:** the evidence_context prompt said "cite the session count" but pointed at `reference_sets[]`, which are SETS not sessions. The model would have miscounted (e.g. 8 sets → "last 8 sessions" when there were 3 sessions). **Fixed:** prompt now directs the model to use `trend.sessions_analyzed` and explicitly forbids counting the `reference_sets` array. Tests: 3 new source-introspection assertions in `test/coachPromptRules.test.js`. **Residual / not-deterministically-guarded:** number precision (rounding `best_weight: 220` to "225") — the existing IRON RULE "never invent or change numbers" covers this by prompt; no deterministic post-generation numeric guard exists (one would require parsing output text, too fragile for this PR). The G4 `stripFabricatedUnits` guard remains the only deterministic post-generation guard (units only).
  - ✅ **Follow-up (G4 review #606): the unit guard is not on every coach voice.** **FIXED (PR #616):** `generateVerdictReaction` now wraps `callGemini` with `stripFabricatedUnits` + `await`. Wiring test extended to assert all four display voices carry the guard while `compileSessionFromHistory` and `callGeminiContents` do not.
- ✅ **Follow-up (G2 review #608): effort-line parity across stacked lifts** `[polish]` `owner-gated coach contract` — **Owner decision 2026-06-28: per-lift effort line.** Each logged lift in a stacked entry now renders its OWN deterministic, engine-backed `effort_note` line (full parity with the per-lift coaching prose), guarded by the same `suppress_generic_prose` check as the primary lift. The slice(1) loop in `public/coach-conversation.js` appends `[card → coaching → effort line → next]` per lift. The contract pin moved from "exactly one effort line" to "one per logged lift" (still never per-set — a single lift's multiple sets collapse to one engine line). Tests in `test/unit.test.js` updated (G2 loop test + set-effort wiring test now expect 2 effort-note elements: primary + slice(1) loop). Coach-render layer only; no write-path/schema/trust-loop change.
  - **Still deferred — session-scoped line de-dup** `[polish]`: **Related (review #608 note 1):** each additional lift makes its own `/api/coach/message` call, whose response can carry a **session-level** line (e.g. `recovery_advisory`, which is session-scoped not per-lift) — so a stacked entry could repeat the same recovery line per lift. Watch at LT-004; if it manifests, de-dupe session-scoped lines to once-per-entry. **Related (review #608 note 2):** if the SAME exercise appears twice in one entry (possible via the display-block path — two same-named blocks), `exercises[0]` and a `slice(1)` member resolve to the same lift and it gets double-coached. Edge case, low priority; de-dupe the coaching loop by canonical lift. Both are separate concerns from the effort-line parity shipped above — file remains until a live repro (LT-004) or owner request.
- ✅ **G5 (F6) — "Today's Workout" card line wrapping** `[polish]`: Deadlift/Bench wrap across ~4 lines; should be one clean line per lift. **Pure display/CSS — no data implication** (confirmed). **FIXED (`fix/recap-card-layout`, 2026-06-26):** `.rv-ex` is a flex row (name `.rv-en` beside sets `.rv-es`); the name had no shrink protection, so a long set list crushed the name column to ~0 width and it wrapped one fragment per line (live "Deadlif/t"). Fix: `.rv-en { flex: 0 0 auto }` (name keeps natural width, one line) + `.rv-es { flex: 1 1 auto; min-width: 0; overflow-wrap: anywhere }` (set column takes remaining space and wraps within itself). CSS-only; shell v59→v60.
- ✅ **RESOLVED — session recap shows duplicated (≈3×) sets after a single whole-workout paste** `[trust-critical / data-integrity]` (owner live 2026-06-26): confirmed fixed by owner (2026-06-26). Not reproducible on re-run. Root was never pinned in code; prime suspects if it recurs: (a) `sessionLog` buffer persisting across un-saved prior attempts via `saveSessionSnapshot`/localStorage; (b) `runCloseout` → `populateSetRows(buildRowsFromSessionLog())` → re-dispatched submit re-running the buffer rebuild. Instrument via Settings → Debug → "Show session state" dump at the moment it appears.

## Coach render-order & completion-flow — owner live session 2026-06-25 (2nd run, investigation)

- ✅ **FA — coach output grouped, not interleaved** `[correctness / UX]` — shipped (PR #610, commit 047680f). `handleSetLogged` now renders `[card → coaching → Next]` per lift in order; session handoff runs once at end. Verified live (LT-005).

## P0 — Active Workout State Unification / Adaptive Workout State — TOP PRIORITY (owner-approved lane, 2026-06-24)

- ✅ **2b decision — skipped-slot representation: RESOLVED = `skipped` is `absent`** (PR #570 review, decided in Sub-PR 2b). The live `skipPlannedExercise` (`public/app.js`) keeps its SPLICE semantics (drop-from-queue): a skipped lift leaves `activePlannedSession.exercises`, so `getCanonicalSession()` (rebuilt from `plannedExerciseEntries()`) simply doesn't see it. The 2b recap surfaces **completed** (logged) + **remaining** (pending, never-done) lifts; a skipped lift is neither logged nor written nor shown as remaining — it's absent, which matches the lifter's intent ("I'm not doing it"). The `activeSession.skipExercise` `status:'skipped'` primitive and the `resolvePlanTargets` skipped branch are retained as correct model primitives for a *future* slice that wants an explicit skipped audit record; they are intentionally dormant in live use today. No new client state holder (no `skippedExercises[]`) is introduced — that would be the only way to show "skipped" vs "absent", and it isn't worth the extra divergence surface now.
- ✅ **Backup / failover LLM provider — DECISION CAPTURED (owner 2026-06-25)** `[correctness / resilience]` `[owner-decision: provider]` — **Decision:** roadmap item captured in BACKLOG (see "Provider resilience and cost-aware LLM routing" in the data-trust section); NOT started. Paid Gemini flash-lite is the interim provider (billing enabled, spend cap set). Full 8-PR failover sequence (error boundary → provider interface → Groq fallback `gpt-4.1-nano` / `llama-3.1-8b-instant` → cost-aware tier router → circuit breaker → observability) is parked **behind the active P0 lane** per owner principle — promote only once P0 is stable in live use. Until then the graceful-degrade (templates / save-without-effort) prevents hard failures.
- ✅ **Mobile PWA test shell (owner 2026-06-25)** `[housekeeping]` — shipped (PR #583). Manifest name → "Atlas"; pull-to-refresh disabled (`overscroll-behavior-y:none`); `beforeunload` warns only on unsaved logged sets; in-progress session (`sessionLog`/`sessionCompleted`/`activePlannedSession`) snapshotted to `localStorage` (12h recency-gated, defensive) on set-logged/plan-start/background/unload, restored on load, cleared on save + Start Over; `docs/MOBILE_TEST_APP.md`. Persistence/resume only — no coaching/workout/trust-loop change.
- ✅ **Coach health diagnostic (owner 2026-06-25: "not sensing any of Atlas' intelligence / can't connect to the coach")** `[correctness]` — shipped (PR #582). The coaching voice is Gemini (`services/coach.js`, model `gemini-2.5-flash-lite`, `GEMINI_API_KEY`); ANY Gemini failure silently degrades to deterministic templates (`coachVoiceTemplates`), which read as "robotic/generic." Added `coach.pingGemini()` + `GET /api/coach/health` (read-only) + a Settings "Test coach connection" button that surfaces `{ configured, model, ok, reason }` — so the owner can see the exact failure (missing key / 401-403 bad key / 404 bad model / 429 quota / timeout) instead of the silent fallback. **OPEN — once the owner runs Test coach and reports the reason, fix the root cause** (likely key/quota/model). Also mirrored the inner-error surfacing to the bodyweight preview catch (PR-581 review note 2).
- ✅ **P0 — "Done, log it." not recognized as closeout + ingestion error hidden (v49 live re-test 2026-06-25)** `[trust-critical]` — shipped (PR #581). (1) `looksLikeLogIt` only matched a whole-string single phrase, so the natural combined command "Done, log it." routed to the coach (coach-down fallback) instead of closing out. Fix: allow an optional leading closeout-ack prefix (done/ok/that's it…, 0–2 times) before the core phrase, with a question guard ("should I log it?" never closes out); mirrored in BOTH `public/app.js` (browser copy) and `services/workoutTextParser.js` (`LOG_IT_PATTERNS`). (2) "Preview failed: Failed to complete workout ingestion" (the `/api/complete-workout` 500) hid its inner cause; the preview catch now surfaces `err.body.details.error` so the next retest reveals WHY. **Still OPEN — the underlying complete-workout 500 root cause** (effort/screenshot save path) awaits the inner `error.message` from a v50 retest; do not guess-fix the server path until the surfaced detail identifies it.
- ✅ **P0 — Deterministic swap didn't fire on an engaged Coach's Pick (v48 live re-test 2026-06-25)** `[trust-critical]` — shipped (PR #580). "Let's do rdls instead of deadlifts" fell through to the coach-down fallback instead of swapping. Two causes: (1) `tryApplyPlanMutation` was gated on a formally-STARTED `activePlannedSession`, but a typed Coach's Pick (`typeSuggestedWorkout`) only sets `coachSuggestionEngaged` + leaves the plan in `lastIntentData` — so no mutable session existed. Fix: `ensureActivePlannedSession()` materializes the engaged suggestion (with its prescription, via `normalizePlanExercise`) into a live session on the FIRST genuine mutation (classify first, so freestyle is untouched). (2) curly apostrophe — "Let's" (’ U+2019, mobile autocorrect) wasn't stripped by the `LEAD` regex, gluing the lead-in onto the substitute ("let's do rdls"). Fix: normalize ’→' at the top of `classifyMutationIntent`. Tests cover both. Shell v48→v49.
- ✅ **P0 — Final closeout/save trust failure (live test 2026-06-25)** `[trust-critical]` — shipped (PR #579). `log it` swallowed silently after a screenshot upload (no bubble/preview/save/error); `done` falsely said "Couldn't find any sets" while logged cards were visible. **Root causes (verified):** (1) the `atlas:preview-ready` listener was `handlePreviewReady(...).catch(() => {})` — a swallowed review-card render error = silent no-op after the composer was cleared; (2) closeout's `sessionLog`-empty fallback used the Gemini `/api/session/compile`, which returns null when the coach is down → false "no sets". **Fixes:** the preview-ready listener now surfaces a visible thread message on render failure (never silent); `handleLogIt` is a never-silent try/catch wrapper around `runCloseout`, which reads the canonical `sessionLog` buffer first, consults `getCanonicalSession().hasLoggedWork` before any "no sets" verdict, and reports a coach-offline compile failure honestly (never a false "no sets" when canonical work exists); screenshot effort-parse failure uses the exact owner copy and never blocks the text closeout; approve-before-write preserved. Full diagnosis: `docs/ACTIVE_SESSION_STATE_DIAGNOSIS.md` (P0 section). **Needs owner v48 live re-confirmation** that the exact silent repro is gone (the swallowed-render hypothesis is verified in code but the precise render-throw trigger wasn't reproduced headless).
- ✅ **`[follow-up]` Barbell loadability snap on substitute prescriptions (discovered PR6, shipped this PR)** `[correctness / polish]`: `POST /api/suggest-substitute` now passes `next_target.weight` through `applyBarbellLoadability` (probe-object pattern: `{ target_weight: next_target.weight, exercise: rec.recommendation }`) and maps the snapped `target_weight` + `loadability_note` back onto `next_target`. Import extended: `applyBarbellLoadabilityToExercises, applyBarbellLoadability` from `barbellLoadabilitySurface`. New test in `test/barbellLoadabilitySurface.test.js` exercises the full weight→target_weight→snap→weight round-trip.

## Trust-Critical Coach Interaction Layer — TOP PRIORITY (diagnosis-first)

- ✅ **P0 follow-up — free-form chat counts as active context** *(shipped #446)*: live testing showed `RIR?` still hit SME in a free-form conversation (no started session/preview → empty client context → gate off). `getChatReply` now also treats an in-progress conversation (`history.length > 0`) as active context. Shell cache v10→v11 (later v11 via #449).
- ✅ **P2 — Extra Work Coach Signal (PR #440)** — *shipped/merged (#440).* Engine fact `extra_work` wired into the coach chat context (on-ask/recovery-gated wording). Follow-ups below remain.
- ✅ **P4 — Session-State Stress Testing** — *shipped (test-only).* `test/sessionStateStress.test.js` runs a messy-input corpus ("rack busy", "I'll do RDL instead", "skip that", "legs are toast", "what now", "how much", "what am I doing next") across the deterministic routing/answering guards together (`isSessionStateQuestion`/`isPlannedLiftQuestion`, `answerBareShorthand`/`answerPlannedLiftQuestion`, `isTirednessExpression`/`buildTirednessRecoveryAnswer`, `classifyMessageIntent`) to prove session state stays prioritized over generic chat/education — LLM-independent (pure helpers). Covers negative controls, the ambiguous-lift clarify guard, the never-hype/never-invent-a-load recovery guarantee, and the AC8 phantom-set floor. No production code change; full suite green (2381).
  - ✅ **Resolved (finding from P4 stress corpus) (2026-06-24)** `[correctness, polish]`: `recoveryRouting.js`'s `FRAMING` list now names specific muscle groups (`quads/hamstrings/hams/glutes/calves/chest/pecs/delts/traps/lats/biceps/triceps/forearms/abs/core`) in addition to the coarse regions, so muscle-specific fatigue slang ("quads are fried", "hamstrings are toast", "chest is cooked") routes to the recovery-aware reply via the `FATIGUE_SLANG + FRAMING` path. The slang requirement still guards against unrelated muscle mentions ("quads day tomorrow", "dead bug for core" → not detected). Coach wording/rendering (PM authority under Escalation Policy v3); pure detector change, no write-path/schema/LLM/trust-loop change. Tests: `recoveryRouting.test.js` (muscle-specific positives + muscle-without-slang negatives). Full suite green (2623).


## Atlas voice & interaction — TOP PRIORITY

- ✅ **PR 3.4 — coach voice reaction** _(complete — merged in [#322](https://github.com/tc5v5s64ym-sketch/atlas-workout-updater/pull/322) @ 962b277)_ — `coach.js` voice layer that words the engine's verdict + rules-engine decisions + per-request context. System prompt now enforces the full VOICE SPEC (not-easily-impressed identity, ≤4 sentences/no hype, rule decisions are final → explain/restate criterion never override, pain → stop coaching load, RIR 0–1 → caution never celebrate grinding, anti-patterns banned, §8 template shapes). New whitelists `sanitizeRuleDecision(s)` + `sanitizeReactionContext`; gate extended via `hasActionableRuleDecision` / `shouldReactToVerdict` (met → quiet UNLESS a caution/reject rule was raised). Words only — every number passes through verbatim. Engine (`computeExpectationVerdict`, rules) untouched. **Merge gate satisfied: owner approved the live example outputs.**

## Near-term

- ✅ **Wire the explicit upper-only request to `build_strength`'s `upperOnly` flag (caller side)** `[correctness]` (owner — 2026-06-25) — *shipped.* follow-up to *PR1 build_strength full-profile default*. The session-generation engine now defaults to a full-profile strength day and produces an upper-only day only when `scoreIntents(..., { upperOnly: true })` is passed; the flag is the explicit-intent seam and is unit-tested directly. **Still unwired on the caller side:** no surface yet sets `upperOnly` from a real user/coach request, so the explicit path isn't reachable end-to-end in production. Smallest next step (read-only, no parser-grammar/write/schema change): thread a query param (`?upperOnly=true` / `?scope=upper`) on the read-only `GET /api/plan/intent-recommendation` route into the `scoreIntents` options, then (separately) let the coach map a natural-language "upper day" request to it. **Note:** the intent-recommendation handler is covered by a brittle 900-char source-window assertion (`P0 wiring 2c` in `test/unit.test.js`) with almost no headroom — adding the param trips it, and that test is in the P0 lane (owner-gated to touch). Widen/relax that window (or move the snap assertion off a fixed char-slice) as part of the same PR. Needed to fully drain **LT-002** (`docs/TEST_QUEUE.md`).
- ✅ **Coach's Pick engagement gate — a displayed suggestion is not an active plan** `[trust-critical]` (owner live test 2026-06-24) — *shipped.* Live testing surfaced that logging straight into the main composer (no Coach's Pick tapped, no session started) was narrated as if mid-plan: after the set reaction Atlas appended "Moving on — next up: <suggested lift>", pre-filled the composer with that lift's prescription, and even fired a `next_move_advisory` toward it. **Root cause:** `loadDashboard()` (app.js) always sets `lastIntentData` to render the home-screen pick, and `plannedExerciseEntries()` treated `lastIntentData`'s recommended intent as the active plan whenever `activePlannedSession` was null — so a *displayed* suggestion was conflated with an *engaged* one (NOT the `/api/plan/today` fallback, which a first investigation wrongly fingered — the handoff fired via `detail.nextPlanned`). **Fix (single chokepoint):** new `coachSuggestionEngaged` flag (app.js, defaults false, no persistence); `plannedExerciseEntries()` returns `[]` for the `lastIntentData` branch unless engaged (an active planned session still always wins). Set true when the lifter taps Coach's Pick (`typeSuggestedWorkout`), false on Freestyle. Fixes every downstream surface at once (handoff, composer pre-fill, reroute queue, `next_move_advisory`, closeout) while leaving the next-*set* card and the preview→approve→write trust loop untouched. Frontend-only; no write-path/schema/parser/proof-field change. SW shell v38→v39. Tests: post-log-identity harness now models the engaged flow + a new gate test (loaded-but-unengaged → empty plan; engage → plan activates); app.js/coach-conversation source-introspection for the gate placement + engage/clear wiring. Full suite green (2566). **Deferred (edge):** the flag persists for the session once engaged — if a lifter finishes a Coach's Pick session and then logs ad-hoc in the SAME session (no reload), the next-up could still narrate; revisit if it surfaces (clear on session save/closeout). Freestyle + reload both clear it today.
- ✅ **FIXED — conversational logging now falls back to the local parser on a flaky/partial backend response (not just a clean timeout)** `[correctness]` (owner — 2026-06-22) — *this PR.* Live symptom: a clearly-typed named set ("Lat Pulldown 175lbs 8/2 8/2 8/2") dropped into chat as "Noted — keep logging" on mobile signal even though the local parser handles it fine. Root cause: `parseWorkoutTextWithBackend` (`public/app.js`) tagged its **no-write-proof failure** (response missing `test_mode`/`sheet_written:false`/`no_write_confirmed`) and its **empty-`log_sets`** failure with `err.noFallback = true`, which made `shouldUseLocalFallback` return false → the local-parser fallback was skipped → the set routed to chat. A clean timeout (AbortError → no status) already fell back; the gap was the *partial/garbled response* path. Fix (frontend-only): both throws still **throw** (a partial response is never trusted), but neither tags `noFallback`, so the caller re-parses with the **pure client-side** local parser instead of dropping the set. Safe because the local parser never writes, the untrustworthy backend response is discarded entirely, and the real write still goes through preview→approve→write. The deliberate non-log answers (clarification/finish_session/effort_capture in `rowsFromBackendParsedWorkout`) **keep** `noFallback`. SW shell v25→v26. Tests: `unit.test.js` source-introspection (no-write-proof + empty-rows throws are fallback-eligible / no `.noFallback =`; clarification intents still tagged) + parser-golden ("Lat Pulldown 175lbs 8/2 8/2 8/2" → 3 clean sets). No parser-grammar / schema / write-path / proof-field / trust-loop change.

- ✅ **FIXED — coach-suggested plan never registered "complete," resurrecting a done lift as next-up** `[correctness]` (owner live 2026-06-22, **confirmed via the #484 "Show session state" dump**) — *this PR.* Root cause (runtime-confirmed, NOT the swap detour — that was a red herring): in `emitSetLogged` (`public/app.js`) `planIsComplete` was computed **only** from `activePlannedSession`, which is `null` in the coach-suggestion flow (logging without "Start Session"). So `planIsComplete` was **always false** there; after the last logged lift the closeout branch never fired and the handoff fell through to the divergent `getNextExerciseInPlan` (`/api/plan/today`) fallback, resurrecting an already-completed lift ("wanted weighted dips again"). The composer mirrored that wrong handoff (it's set from the same `nextEx`), so "stuck on Weighted Dip" was the same one bug. The session-state dump proved the underlying state was correct (`sessionCompleted` had all 6, `remaining: []`) — only the completion *gate* was wrong. Fix (two parts): (1) derive `planIsComplete` from the shared `remainingPlannedExercises()` — `plannedExerciseOrder().length > 0 && remaining.length === 0` — so it's true in BOTH the started and coach-suggested flows; (2) in `handleSetLogged`, compute next-up FIRST and make the `/api/plan/today` fallback **reject a next-up already in `sessionCompleted`** (threaded via `detail.completed`), so a genuine uncompleted next still wins (preserves the E2E "log Bench → next Lat Pulldown" via /api/plan/today) but a divergent fallback can't resurrect a done lift; closeout fires only when nothing is genuinely next. SW shell v24→v25. Tests: `test/postLogIdentity.test.js` (suggestion flow logs all 6 → final `planIsComplete:true`, `nextPlanned:null`; mid-session not prematurely complete) + `unit.test.js` source-introspection (fallback rejects completed; closeout precedence). No parser/schema/write/trust-loop change; `applySessionSubstitution` untouched. The "Show session state" debug dump (#484) stays as a permanent diagnostic.

- ✅ **Next-up + bare-set attach also work when logging against a coach SUGGESTION (no "Start Session")** `[correctness]` (owner — 2026-06-21) — Live evidence: after logging Bench against a coach-suggested "Build Strength" workout, the handoff said "next up: Face Pull" (should be Seated Row) and a bare set sequence still hit "Noted — keep logging". Root cause: `firstUnloggedPlannedLift()` (the source for both next-up and the bare-set parse-context fallback, from #471/#472) read **only** `activePlannedSession`, which is set **only** when the lifter taps "Start Session" (`startPlannedSession`, app.js:1432). Logging straight against a coach suggestion leaves it `null`, so next-up fell back to the buggy `getNextExerciseInPlan` (API-plan map order, no skip-logged) and bare sets had no attach target. Fix: new `plannedExerciseOrder()` (`public/app.js`) returns the visible plan order from the started session **OR** the cached coach-suggested plan (`lastIntentData`'s recommended intent — the same source `currentPlanForChat` already uses); `firstUnloggedPlannedLift()` now walks that and skips `sessionCompleted`. So next-up = the first unlogged lift in the visible order, and a bare sequence attaches to it, in **both** flows. Deterministic (no LLM) → correct even when the coach is down (the screenshots' "Coach is unavailable" no longer blocks the workout flow). SW shell cache v17→v18. Tests: `test/sessionPlanExecutor.test.js` (order source from started OR suggested plan, skip-completed). Full suite green (2262). No write-path / schema / proof-field / approval / parser change.

- ✅ **Coach chat must not present planned work as completed (planned total, engine-computed)** `[correctness/trust]` (owner — 2026-06-21) — Live evidence: a bare "Total?" after discussing Face Pull returned *"You've done 45 reps of Face Pull today."* — the LLM multiplied the plan (3×15) itself and mis-tensed it as completed work the lifter hadn't logged. Two fixes: (1) deterministic `answerTotalRepsQuestion(message, { history, clientContext })` in `services/sessionQuestionAnswer.js` answers a "total" question with the **engine-computed** planned total (sets × reps), worded as planned ("Face Pull today: 45 total reps planned (3 sets × 15)."), resolving the lift from the recent turns so a bare "total?" follow-up works; wired into `POST /api/coach/chat` before Gemini (defers on past-tense/advice/unresolved/missing-sets). (2) New **PLANNED-VS-DONE RULE** in `buildChatSystemPrompt` (`services/coach.js`): `current_plan`/`current_preview`/recommendation numbers are PLANNED targets — never "you've done / you did / you hit", never multiply sets×reps and state the product as performed; only `recent_sessions[*].lift_sets` + `plan_state` reflect logged work. Tests in `test/sessionQuestionAnswer.test.js` (named + bare-follow-up total, planned wording, defers) and `test/coach.test.js` (prompt rule). Full suite green (2235). No write-path / schema / proof-field / parser change; no new LLM call (the total is deterministic).

- ✅ **Recovery trim must not collapse Build Strength below the learned session norm** `[correctness]` (owner — 2026-06-21) — Live evidence: after the recovery-aware density fix, a "push recovering" Build Strength day collapsed to 3 exercises (Bench + Seated Row + Lat Pulldown). Root cause: `build_strength` builds from a narrow pattern list (`['push','pull']`), so trimming the recovering push to one movement left only push(1)+pull(2)=3 — well below the owner's profile (median 5, p75 6). One recovering pattern should reduce ITS volume, not shorten the whole day. Fix (engine-only, drop-only is replaced by *additive* backfill but only from the existing candidate pool): new `backfillToProfileTarget(exercises, trimmedPatterns)` in `services/analytics.js::scoreIntents` tops the session up — when a recovery trim happened and the day is under the **profile median** (`exercises_per_session`, bounded by `session_cap`) — with movements from **rested** patterns (`exForPatterns(readyPatterns)`, dedup by `lift_code`), never the recovering pattern and never extra push. Skipped when there is **no learned profile** (sparse data → generic behaviour) or when **global fatigue is high** (`fatigue.status === 'high'` → a short session is warranted). `build_strength` runs it between `capRecoveringPatternDensity` and `structureSession` (so #466's `capSessionToProfile` still bounds the top end — the target never exceeds `session_cap`), adds a `why_today` line + `volume_shifted_to_ready_patterns` reason code. Tests in `test/analytics-edge.test.js`: recovering push + ready patterns → 4–6 exercises with a rested-pattern backfill (not 3); high global fatigue → short 3-exercise day allowed (no backfill, carries `high_fatigue`). Full suite green (2216). No write-path / schema / proof-field / parser / LLM change. #466 + #467 behaviour intact.
  - **Watch — backfill diversification across ready patterns** `[polish]` (PR #468 Atlas review, non-blocking): `backfillToProfileTarget` draws the top `target*2` lifts across *all* ready patterns by global priority, not balanced per-pattern, so a single ready pattern holding several high-priority lifts (e.g. pull) could absorb the whole top-up rather than diversifying into legs/hinge/core. Not a recovery violation (only recovering patterns are density-capped) and `session_cap` still bounds the total — a design nuance, not a bug. If live results show the backfill clustering on one pattern, round-robin the top-up across rested patterns. No action unless that surfaces live.

- ✅ **In-app build identity shows the deployed PR number** `[housekeeping]` — Owner couldn't tell from a bare SHA whether the live app was the right/current build. `scripts/write-build-info.js` (run at build time via `postinstall`, where `.git` is present) captures the deployed commit's PR number + subject into a gitignored `public/build-info.json`; `services/buildInfo.js` reads/parses it; `/version` now returns `pr` + `commit_subject`; the Settings badge shows `PR #<n> · <sha7> · deployed <time>` (SHA fallback when absent). Read-only diagnostics; degrades to the SHA when build-info is missing (never throws). Tests: `test/buildInfo.test.js` (PR-number parse + reader) + `/version` shape in `api-smoke`. SW cache bumped v13→v14.

## Personalized warm-up ramps learned from logged history (owner idea — 2026-06-21)

- ✅ **[#402](https://github.com/tc5v5s64ym-sketch/atlas-workout-updater/issues/402) Part 1 — Seated Row same-day repeat in recommendations** — Two-part fix in `services/analytics.js` (`scoreIntents`): (1) deduplicate `allRecs` by exercise name after sorting so the same movement logged under multiple lift codes (e.g. ROW01, SR01) appears only once; (2) filter `allRecs` globally to exclude exercises trained yesterday (daysSince=1) before any intent builder runs — covers both `exForPatterns` and `buildIntentSession` paths. Same-day (daysSince=0) is kept so the PR 369 readiness-aware dose reduction still fires. 2 golden-fixture tests added to `test/analytics-edge.test.js`. **Part 2 (save-ready copy UX) remains open** — the suggested workout surface is not yet wired into a route so the exact repro is unclear; deferred below.
- ✅ **#402 dedup: single-word exercise name carve-out** `[housekeeping]` — *shipped (PR #499 + exact-membership follow-up).* The `allRecs` name-dedup in `scoreIntents` (`services/analytics.js`) no longer skips ALL single-word names (a whitespace proxy that let real single-word lifts escape dedup and get double-recommended under multiple lift codes). **Final form:** it skips dedup only for a single-word name that is **exactly** a muscle-group label (`MUSCLE_GROUP_LABELS` set — coarse groups + colloquial muscle taxonomy), so the test-helper `'Chest'`/`'Back'`/`'Lats'`/`'Core'` placeholders stay protected while real single-word lifts dedup to the most-recently-trained code. **The #499-review residual is closed:** the first cut used `classifyMuscleGroup(name) !== null`, which substring-matches the `row`/`ab`/`hip` tokens — so a bare `'Row'` (the exact #402 Seated Row family) still escaped dedup; exact whole-string membership fixes that (`'Row'`/`'Rows'`/`'Squat'`/`'Curl'`/`'Dips'` all dedup; multi-word lifts like `Leg Press` unaffected). Pure engine; no write-path / schema / proof-field / parser / LLM change. Golden tests in `test/analytics-edge.test.js` (Squat + Row dedup to newest; `Leg Press` regression guard; `Chest` placeholder NOT collapsed) + existing #402 tests intact. Full suite green.
- ✅ **Roadmap Step 379 — Reorder session index bugfix.** `[correctness]` Fixed: when a swap/reorder is declared at the current step, `checkAndSuggestSubstitute` (`public/app.js`) now advances `activePlannedSession.index` past the taken exercise (clamped to the plan length, banner re-rendered) right after recording `pendingSubstitution`. Subsequent conversational messages therefore send the next planned slot as `current_exercise` instead of the stale taken lift. The deferred swap is still applied by NAME in `emitSetLogged`, so the cursor move does not affect it; `advancePlannedSession()` is deliberately NOT reused (it clears the pending swap and restarts the logger). Source-introspection regression test added (`test/unit.test.js` "Step 379"). No write-path/schema/LLM change.
- ✅ **Roadmap Step 380 — [#359](https://github.com/tc5v5s64ym-sketch/atlas-workout-updater/issues/359): Historical lift retrieval must use actual logged sets.** `[trust-critical]` Already complete (issue closed-completed; re-listed by the #405 roadmap refill). Verified in `main`: `buildRecentSessions` emits per-set `lift_sets`, `buildChatContext` (`index.js`) forwards them to `/api/coach/chat`, `services/coach.js` HISTORY RULE answers past-workout questions ONLY from `recent_sessions[*].lift_sets` (never prescription/plan/recommendation/benchmark), and `test/coach.test.js` pins lift_sets + RIR-0 fidelity. No code change required.
- ✅ **Roadmap Step 381 — GitHub issue #402A: Recent-lift exclusion / Seated Row identity normalization.** `[trust-critical]` Resolved by PR #404. The exact repro (Seated Row prescribed the day after logging) is fixed by the `scoreIntents` recency exclusion (`daysSince === 1` dropped from `allRecs`) plus dedup-by-name. Identity investigation: in `main` only `seated row → ROW01` exists; the `SR01`/`BOR01` codes named in the issue are hypothetical and not present, so there is no live multi-code split. The only genuine residual (pre-override generated-code history split) is already tracked below as the SESSION_DESIGN AC5a "liftCode history-merge" item. No new code required.
- ✅ **Roadmap Step 382 — GitHub issue #402B: Suggested workout preview must not look save-ready.** `[correctness]` Fixed (PR #410): the save-ready copy was the `nav.js` composer-placeholder rotation ("Say 'log it' to save your session") overwriting the suggestion placeholder, because the rotation only stopped on `atlas:set-logged`. `setWorkoutPlaceholder` now dispatches `atlas:placeholder-owned` whenever coach-conversation sets a contextual placeholder, and `nav.js` suppresses the rotation on it — so a displayed suggested workout (nothing performed) no longer shows save-ready pressure. Frontend-only; source-introspection test added.
- ◐ **Roadmap Step 383 — [#291](https://github.com/tc5v5s64ym-sketch/atlas-workout-updater/issues/291): Deload prescription consolidation (one model) — PARTIAL.** `[correctness]` **Premise corrected (2026-06-20):** there is NO live contradiction — `computePrescription` is dark (zero callers), there is no "Coach's Pick" UI, the only live deload surface is `suggestDeloads`→insights, and the next-set card's load-cut math is duplicated inline in `index.js` (~1569) and only fires when `Deload_State` is active (which the client never sets). DRY cleanup done for the **next-set-card load-cut math only**: it was consolidated onto the canonical `computePrescription` (added to the `deloadProtocols` import; the 9-line inline block replaced with one `computePrescription` call), no behavioral change (path is dark until Step 385). **Not fully "one model":** the live `suggestDeloads` deload surface (`services/analytics.js`, feeding `/api/coaching/insights`) was NOT folded into `computePrescription` — it remains a separate deload path (see Step 384 below), so the full single-model consolidation #291 describes is still deferred.
- ✅ **Roadmap Step 384 — Deload trigger nuance: don't trigger a deload off accessory or deprioritized lifts.** `[correctness]` Fixed (done before 383, re-ordered as the real bug). `annotateStallsForDeload` (`coverageStalls.js`) already existed but was wired only into `scoreIntents`; `suggestDeloads` (`analytics.js`, feeding `/api/coaching/insights`) now applies it and filters out `ignored_for_deload` accessories — so a flat accessory whose muscle is covered by other lifts (Shrugs) no longer surfaces a deload. The Dumbbell-Curl-progressing (40→53) case was already safe: `detectStalls` is estimated-1RM based, so a rising lift is never flagged. Golden test in `test/analytics-edge.test.js`. No write-path/schema/LLM change.
- ✅ **Roadmap Step 385 — [#289](https://github.com/tc5v5s64ym-sketch/atlas-workout-updater/issues/289): Frontend deload lifecycle wiring.** `[correctness]` Lifecycle wired in `public/app.js`: `begin` fires fire-and-forget when a `deload_reset` planned session starts (409 absorbed); `advance` fires after a confirmed live log write when `activePlannedSession.intentId === 'deload_reset'` and not a duplicate-blocked replay; `resolve` fires automatically when `advance` returns `POST_DELOAD_EVALUATION`. None touch `Log_Cleaned`, workout rows, or proof fields. Source-introspection tests in `test/unit.test.js`. The deload state machine is now live.
- ✅ **SESSION_DESIGN AC5 (part b): per-lift load ceiling check** (PR 361) — `services/loadSanity.js` added; `sanitizeLoad` guard wired into `recommendNextSet`; 31 golden-fixture tests all pass. Golden invariant held: Lateral Raise never prescribed above 100 lb. Part (a) — history-merging for old liftCode splits (e.g. rows written before `knownLiftCodeOverrides` was in place may carry a generated code that differs from the canonical one) — is **deferred**: see item below.

## Then

- ✅ **HI-5 — service-worker cache bump** — `CACHE_NAME` bumped to `atlas-shell-v8`; activate handler purges old caches. Owner live-check: hard-refresh on device to confirm new SW activates.
- ✅ **ME-8 — weekly report row shape** — `buildWeeklyReport` now uses `cell(row, idx, key)` helper for all post-filter accesses; object-shaped rows produce correct totals.
- ✅ **HI-3 — parser set-count cap** — `parseWeightRepsSets` path capped on all paths (PR #317).
- ✅ **HI-2 — CSP / inline styles** — inline `style=""` moved to CSS classes; recovery-bar fills driven by `--fill` custom property (PR #315).


### AUDIT.md reconciliation — still-live findings (verified against main 2026-06-20)

- ✅ **ME-4 — client-supplied `volume_calc` trusted when present** `[trust-critical]` — fixed. Both write-path normalizers (`normalizeLogRowObject` for object rows, `logRowArrayToObject` for array rows) now **always** derive `volume_calc` from `weight × reps` server-side and ignore any client-supplied `volume_calc`/`volume`. Column 12 of `Log_Cleaned` can no longer disagree with the logged set. Read-path analytics were never affected (they recompute volume themselves and don't carry the stored column). Regression test in `test/api-smoke.test.js`: a live log-workout with a bogus `volume_calc: 99999` writes `1350` (135 × 10).
- ✅ **HI-4 — numeric/Excel-serial dates produce a wrong date** `[correctness]` (`services/validation.js` `normalizeDate`): the generic `new Date(text)` ran before the Excel-serial branch, so a numeric serial like `45000` parsed as the **year 45000** and the serial branch was unreachable. Fixed by moving the `typeof value === 'number'` Excel-serial conversion **above** the generic `new Date(text)` fallback; `normalizeDate(45000)` now returns `2023-03-15`. Regression test in `test/unit.test.js`. ✅ **Resolved (string form, 2026-06-24)** `[correctness]`: a serial arriving as an all-digit **string** (`"45000"`, how `FORMATTED_VALUE` sheet reads return numbers) was still parsed as the year 45000 by `new Date(text)`. Now an **exactly-5-digit** all-digit string is converted via the shared `excelSerialToIso` helper (range 10000–99999 ≈ 1927–2173 covers every realistic modern date; the 2020s are ~43000–47000), so `normalizeDate('45000')` → `2023-03-15`. A bare 4-digit `"2026"` stays a year and an 8-digit `"20260618"` (YYYYMMDD) is deliberately excluded by the digit count, so neither is misread as a serial. Pure normalizer; no write-path/schema/proof-field/trust-loop change. Regression tests in `test/unit.test.js` (string serial == numeric serial; 4-digit year and 8-digit YYYYMMDD not converted).

## Operational resilience — NEEDS TRIAGE (not yet scoped)

- ✅ **Schema / header-drift validation** `[trust-critical]` — shipped in two PRs. **PR A** (engine): `validateHeaderRow(actualHeader, expectedColumns, aliases)` in `config/sheetContract.js` (pure, alias/casing-tolerant, position-by-position) + `getHeaderRow(tabName)` in `sheets.js`. **PR B** (wiring): a pre-write header guard in `index.js` reads row 1 of the target tab(s) before any live append and **refuses the write with 409 + `sheet_write:'blocked_schema_drift'`, `sheet_written:false`, `no_write_confirmed:true`** on mismatch, releasing the `write_id`. Covers the trust-critical workout-data paths — `Log_Cleaned` (`/api/log-workout` + `/api/complete-workout`) and `Effort` (`/api/complete-workout`). **Deferred (lower priority):** the same guard is not yet applied to `Constraints` (already returns 503 when its tab is missing) or `Deload_State` (system-state, append-only, outside the trust loop); add if those tabs ever drift. Sonnet 4.6 if pursued.
- ✅ **Sheets write retry/backoff** `[correctness]` (`sheets.js` `appendRows`): `appendRows` now wraps the `values.append` call in `retryWithBackoff` (bounded: 1 initial + 3 retries, exponential 500/1000/2000 ms). **Idempotency-safe retry scope:** `values.append` is NOT idempotent and the `write_id` guard lives in `index.js` (dedupes across HTTP requests, not inside this loop), so `isTransientAppendError` retries **only** on errors where Google rejected the request before touching the sheet — HTTP 429/503 and 403 rate-limit/quota reasons. Ambiguous failures (post-send timeouts, 500s) propagate un-retried to avoid a silent double-append. Both helpers are pure (injectable `sleep`) and exported; 5 unit tests in `test/unit.test.js`. **Premise correction:** the original note claimed "`write_id` already guards against a double-append on retry" — true only at the HTTP-request layer, not within a single `appendRows` retry, which is why the retry scope is deliberately narrowed to request-rejected-before-write errors.
- ✅ **Timezone / date handling** `[correctness]` (`services/analytics.js`): the recency math derived "today" from UTC (`todayIso` → `toISOString`) while `date_clean` is stamped in the owner's local day, so `daysSince` / "trained yesterday" could misclassify by a day near midnight — corrupting the recent-lift exclusion (Step 381 / #402). Fixed with a single date-basis helper: `localTodayIso(now, tz)` driven by a new `ATLAS_TIMEZONE` (IANA zone); `todayIso()` now routes through it and `computeFatigueStatus`'s default reference is local-today-anchored. `isoDateAtUtcNoon` (UTC-noon anchoring for DST-safe whole-day diffs) was already correct and is unchanged. **Default unset → UTC (behavior unchanged); the owner sets `ATLAS_TIMEZONE` to their zone to activate alignment** (documented in `.env.example`). Isolated to `services/analytics.js` — no `index.js`/write-path/schema/LLM change. 3 unit tests (UTC default, LA/Tokyo local-day boundary, invalid-zone fallback). **Deferred (same-pattern, optional):** the client/server `getLocalDateString` (`public/app.js`, `services/sessionId.js`, `index.js`) already use device-local time so they align with a correctly-set `ATLAS_TIMEZONE`; no change needed unless the server's local zone is ever used as a stamping fallback in a non-owner zone.

## Housekeeping

- ✅ **Reconcile [`FIX_PLAN.md`](./FIX_PLAN.md)** — all FIX_PLAN items classified (done/open); open items folded into this backlog; FIX_PLAN archived with a banner. Done in this PR.
- ✅ **Harden null-element tolerance in the coach sanitizers** `[correctness]` — Guards already present in `services/coach.js` (`today_sets` via `toSet` line 71, `last_working_sets` inline lines 83–87, `current_preview` inline line 592) and three `[null]`-element regression tests already pass in `test/coach.test.js` (lines 765–793). Fully resolved pre-existing; no PR required. Verified Roadmap Step 378.
- ✅ **Parser substitution priority fix** — `Leg Press` added to `EXERCISE_ALIASES`; `extractSetParagraphs` strips explanatory prose paragraphs (no set tokens) before normalization so the performed exercise header/sets win over skipped/mentioned exercises in substitution sentences. 4 golden tests added.
- ✅ **NF-1 — Playwright browser mismatch in Claude Code remote environment** `[housekeeping]` (Post-P0 QA, 2026-06-26): `@playwright/test` 1.60.0 expects Chromium revision 1223 but the remote environment ships revision 1194 at `/opt/pw-browsers/`. The suite would fail to launch in a fresh session without manual symlink creation. Fixed in PR on branch `claude/post-p0-atlas-qa-ki7qkc`: `playwright.config.js` now reads `PLAYWRIGHT_BROWSERS_PATH` (set by the remote environment) and when present supplies `executablePath: '/opt/pw-browsers/chromium'` (the stable system symlink) to both projects — no impact on local dev or CI where that env var is absent.

- ✅ **Add Anthropic OAuth token pattern to custom secret scanner** `[housekeeping]` (shipped) — `scripts/check-changed-files-for-secrets.js` gained an `anthropic-oauth-token` rule matching the raw `sk-ant-oat<version>-<token>` format used as `CLAUDE_CODE_OAUTH_TOKEN` in all three automated CI workflows (Claude Code Review, Codex Decision Desk, Atlas Decision Desk) — closes the gap surfaced by the 2026-06-26 CI workflow audit. Catches the raw token under any var name, same approach as the existing `google-ai-api-key` rule. 5 tests added to `test/secret-scan.test.js` (rule exists, flags a committed/leaked token, does not flag the setup-instructions ellipsis in `claude-code-review.yml` or a too-short lookalike); all token values built at runtime so the test file itself never contains a literal matchable token. Scripts-only; no app/schema/write-path change.
## Automation framework follow-ups

- ✅ **Label sync workflow** `[infrastructure]` — `.github/workflows/labels.yml` upserts the risk labels onto the repo from the manifest (github-script; no external action).
- ✅ **Merge-card completeness check** `[infrastructure]` — `.github/workflows/merge-card-check.yml` fails a PR whose body is missing the Atlas Merge Card or still contains template placeholders (`AUTOMATION_PROTOCOL.md` §2).
- ✅ **Auto-label by path** `[infrastructure]` — `.github/workflows/labeler.yml` + `.github/labeler.yml` apply category labels from touched paths (`actions/labeler@v5`). Primary risk label stays a builder decision.

- ✅ **Codex Decision Desk** `[infrastructure]` — `.github/workflows/codex-decision-desk.yml` answers Claude's decision panels so the owner is not asked (`docs/DECISION_ROUTING.md`). Reuses the existing `CLAUDE_CODE_OAUTH_TOKEN` subscription — no new paid API.

- ✅ **Escalation Policy v3 — minimum-escalation, ship-by-default** `[governance]` (owner standing instruction, 2026-06-24) — recorded the owner directive to reduce escalations to the minimum. Coach surface / wording / rendering / frontend / UX is **no longer** an escalation trigger when derivable from governance; the five reserved categories are consolidated to **four** (schema/storage + destructive operations merged into "destructive or irreversible"); coaching **philosophy** is named explicitly as reserved product scope. Encoded across `docs/OWNER_CHECKIN_RULES.md` (authoritative; v2 retained as superseded), `docs/AUTOMATION_PROTOCOL.md` §5, `docs/DECISION_KERNEL.md` (Tier 3), `docs/DECISION_ROUTING.md`, `docs/AGENT_WORKFLOW.md`, and `CLAUDE.md`. Docs-only; no app/schema/write-path/coach behavior change.
- ✅ **Sweep per-item `Sonnet 4.6` model tags now that the builder is Opus-always** `[housekeeping]` — resolved at source: the "Deferred item tags" legend now defines tags by *risk only* and adds a note that the per-item Sonnet/Opus suggestions scattered through older items are **superseded** (read as risk hints, not model selection). Historical plan docs (`FIX_PLAN.md`, `COACH_PLAN.md`, `docs/COACH_NEXT_LEVEL_BUILD_PLAN.md`) are reference-only and left as-is. Surfaced by automated review of #429.
- ~~**CODEX Review as a check**~~ `[infrastructure]` — **STRUCK AS DUPLICATE (owner 2026-06-25): this item is absorbed into the active Codex required-gate proving period already underway. No separate action required; close this entry.**

### Training Intelligence Implementation Series (owner-approved slices)

- ✅ **PR 478 — Exercise Modality Schema** `[correctness]` — *shipped (owner promoted Training Intelligence 478–486, 2026-06-23).* Pure-data module `services/exerciseModality.js`: `modalityFor(name)` returns `{exercise_type, load_type, fatigue_class, effort_metric (+alternates), progression_metric}` (Taxonomy §3), extending the legacy `EXERCISE_TYPES` with `cardio_steady|cardio_interval|circuit|timed_hold|mobility` and reusing `classifyLiftRole`/`costFor` for resistance. Name-pattern detectors (circuit/interval/hold/cardio/locomotion/assisted/added-load/bodyweight) run before the external-weight default so a non-resistance modality is never mis-read as a weighted lift; `isValidModality` guards every field against the controlled vocabularies. REFERENCE-ONLY — no I/O, no wiring, no write-path/schema/parser/LLM change. Golden tests pin the §3 minimum-coverage table + enum validity (`test/exerciseModality.test.js`). Full suite green.
- ✅ **PR 480 — Pure Session Objective Scorer** `[correctness]` — *shipped.* Pure module `services/objectiveScorer.js`: `scoreObjectives(snapshot)` derives the nine §3 term sub-scores per objective (`featuresFor`) from a normalized planning snapshot and ranks/selects via the PR 479 contract. Per-objective rules are the documented initial deterministic defaults (profile axis · goal set · equipment need · time need · taxed-system recovery/fatigue · coverage gaps · pain targets); the two recovery objectives (`recovery_reload`/`deload`) **invert** (they fit a fatigued state and stack ~no fatigue). Read-only/pure — consumes already-derived signals; building the live snapshot is a later wiring slice (no write-path/schema/parser/LLM change). Golden selections pinned (`test/objectiveScorer.test.js`): strength-recovered→strength_progression, cardio intensity-intent fresh→cardio_intervals, the §8 low-recovery downgrade→cardio_base, no-intent→cardio_base, deload-active→deload, poor-sleep→recovery_reload, plus full-ranking + empty-snapshot safety. Full suite green.
- ✅ **PR 481 — Stimulus Governor Fixtures** `[correctness]` — *shipped.* Pure rule-table module `services/stimulusGovernorRules.js`: frozen vocabularies (`PROFILES`, `MODALITY_CATEGORIES`, `PROGRESSION_VERDICTS`, `FATIGUE_SIGNALS`) + `governorRuleFor(profile, modalityCategory)` returning the §5 rule — `effort_interpretation` + reachable verdicts from the modality, RIR-0 handling / progression bias from the profile (strength→RIR0 blocks on heavy compound; hypertrophy→cautioned; general_fitness→reward consistency; bodyweight→fine on strict; cardio modality → RIR irrelevant regardless of profile). Golden tests pin the §5 table row-by-row + vocab membership (`test/stimulusGovernorRules.test.js`). Pure data/lookup; no write-path/schema/parser/LLM change. PR 482 applies a rule to a logged set.
- ✅ **PR 482 — Profile-aware Stimulus Governor** `[trust-critical]` — *shipped (pure + UNWIRED).* `services/stimulusGovernor.js::gradeStimulus({profile, modalityCategory, rir, target_rir, is_heavy_compound, metric_trend})` applies the PR 481 rule to a logged effort → `{effort_interpretation, progression_verdict, fatigue_signal, reason, rule}`. Profile-aware RIR-0 handling (strength heavy-compound RIR0 → hold/high; hypertrophy → hold/elevated caution; general_fitness accessory RIR0 → hold, not rewarded; strength/bodyweight isolation RIR0 → earns progress); under-target reserve → modality progress verdict; at-target → hold; non-RIR modalities (cardio/timed/circuit) judged by `metric_trend` (improved→progress, steady→hold, declined→back_off). Reuses `effortVerdict`; emits a coarse DIRECTION only (never a load number — that stays in `recommendNextSet`). **Read-only and NOT wired** into coach/recommendation/write paths (the `[trust-critical]` concern is the wiring, which is a later separately-gated slice). Golden tests pin the §5 grading + vocab membership (`test/stimulusGovernor.test.js`). No write-path/schema/parser/LLM change.
- ✅ **PR 483 — Live Fatigue Router expansion** `[correctness]` — *shipped (pure + UNWIRED, suggestion-only).* `services/fatigueRouter.js::routeNextMove({justLogged, nextMove, restedAlternative, heavy_lower_block_done, form_breakdown})` generalizes PR 477's pressing reroute to any pattern/modality (Taxonomy §6): repeated-RIR0 → `block_pr` (highest priority); cardio after a heavy lower block → `reduce_intensity`; circuit objective form-breakdown → `reduce_density`; same-muscle/pattern high fatigue → `promote_alternative` (rested antagonist) else `make_optional`; elevated → `reduce`; else `keep`. Form-breakdown must be an objective logged flag (never inferred from reps). Takes already-classified inputs (pattern/muscles/modalityCategory/fatigue_signal) so it stays decoupled — caller computes them via `patternFor`/`musclesFor`/`exerciseModality` + the governor `fatigue_signal`. Frozen `ROUTE_ACTIONS` vocab. Golden tests (`test/fatigueRouter.test.js`). Read-only — NOT wired into the live session/coach path; no write-path/schema/parser/LLM change.
- ✅ **PR 485 — Recovery / Deload Engine** `[trust-critical]` — *shipped (engine-only SELECTION layer, owner-approved scope 2026-06-23).* `services/recoveryDeloadSelection.js::assessRecoveryDeload(signals)` implements the SPE §10.5 recovery decision ladder + §10.2 convergence triggers → `{decision (normal/micro_adjustment/recovery_reload/deload/taper/complete_rest), deload_candidate, recovery_state, deload_style, pain_override, converged_signals, rationale}`. Convergence-based (no deload after one bad session): 2× RIR0 on main lifts / readiness-red 3 days / performance-decline+subjective → deload; ≥2 stacked moderate signals → recovery_reload; one local signal → micro_adjustment; pain-major → independent pivot/stop override; test-date → taper; illness → complete_rest. Profile-aware deload **style descriptors** (§10.4) carry NO numbers — the prescription stays owned by `computePrescription`/`docs/DELOAD_SPEC.md`. Pure/unwired: no parser/slash-notation/schema/write-path/coach-prompt/coach-wording/recommendation/user-facing change. Golden tests pin the §10.7 matrix (`test/recoveryDeloadSelection.test.js`). Full suite green.

## ▶ Active / Now — read this first
- ✅ **Migration PR-A — Exercise Identity Bridge (inert mapping data)** `[correctness]` **(SHIPPED, this PR):** builds the permanent identity bridge that future enrichment-inversion migrations (plan §6 PR-B onward) will consume, with **zero runtime behaviour change** — data + doc + test only, nothing runtime references it. Ships (1) `data/lift_code_bridge.v1.json` — `exercise_id → lift_code`, codes taken verbatim from `services/exerciseEnrichment.js :: knownLiftCodeOverrides` (never invented) and joined to catalog identities via id/name then highest-confidence alias; 14 identities across 12 resolved codes, each recording `resolved_via`/`min_confidence`/`override_names`; (2) `data/muscle_group_bridge.v1.json` — proposed `primary_muscle → Muscle_Group` label for all 17 catalog tokens, each recording how `analytics.classifyMuscleGroup` buckets it **today** (rear_delts→pull, posterior chain→hinge preserved), **UNCALIBRATED** against a live sheet dump; (3) `docs/EXERCISE_IDENTITY_BRIDGE.md`; (4) `test/exerciseIdentityBridge.test.js` proving the bridge is internally faithful to the current engine AND **inert** (no production file references either JSON). **Blockers to future inversion — documented, NOT solved here (per brief):** (a) **`BC01` (generic curl) has no single catalog identity** — the catalog splits curl work across `barbell_curl`/`dumbbell_curl`/`cable_curl`/… so `BC01` cannot be inverted to one `exercise_id` without an owner taxonomy decision (recorded under `unresolved_lift_codes`); (b) **`classifyMuscleGroup("Calves")` returns null** — the analytics `lower` regex matches `calf`, not `calves`; a future inversion writing "Calves" would drop calf work from the lower/recovery pattern (one-token analytics fix, belongs in a behaviour-changing PR); (c) muscle labels need calibration against a live `Muscle_Group` dump + owner decisions on the flagged delt/upper-back/traps/forearms splits; (d) `KR01` `knee raise→hanging_leg_raise` is a 0.70-confidence pairing to confirm with the owner. **Nothing consumes the bridge; the next implementation PR (PR-B) remains owner-gated (plan §8).**
- 📋 **Exercise name / lift-code unification — MIGRATION PLAN (docs-only, planning PR)** `[correctness]` **(SHIPPED, this PR):** scopes the parser↔enrichment↔catalog name unification deferred by PR-14/PR-15, without any behavior/write/sheet/data change. Full plan in [`docs/EXERCISE_NAME_UNIFICATION_MIGRATION_PLAN.md`](./docs/EXERCISE_NAME_UNIFICATION_MIGRATION_PLAN.md): current-state map of the 5 name sources (parser A → `Log_Cleaned` col 3; catalog B; coaching C; enrichment/sheet D → col 4; sheet), the full 32-residual divergence table (A/B/C/D + lift_code + Log_Cleaned impact + proposed canonical + risk), a `primary_muscles→Muscle_Group` bridge proposal and an `exercise_id→lift_code` bridge proposal (both **not applied**), the `--sync-sheet` dry-run plan, a 6-PR safe migration sequence (mapping-data → lift-code bridge → enrichment inversion behind a flag → owner-run sheet sync → parser/catalog rename → optional Log_Cleaned relabel), the regression-test plan, and the owner-decision list. **Key finding:** history + all analytics join on **`lift_code`**, not the display name — so if lift_code is held immutable, unification is mostly a display change with a bounded blast radius, and ~14 of 32 residuals are genuine owner "which lift is this" decisions (Shrug/Row/Dip/Lateral/Hanging-knee-vs-leg/Tricep-pushdown + 2 likely catalog errors: `sldl`→RDL, machine-row identity). **Nothing is implemented; the next implementation PR is owner-gated (§8 of the plan).**
- ✅ **Remediation PR-17 — extract coach/deload/debug/flight/bug-report/schema/health routes into a router** `[housekeeping]` **(SHIPPED, this PR):** the 24 routes of this slice (`/api/coach/{message,chat,ask,health}`, `/api/deload/{status,begin,advance,resolve}`, `/api/debug/{intent-shadow,brain-shadow,intent-observe,config,exercise-match}`, `/api/flight/{recent,ingest}`, `/api/bug-report` GET+POST, `/api/schema/{log,effort,complete-workout}`, `/api/health/{sheets,openai,gemini}`, `/api/coaching/insights`) moved VERBATIM out of `index.js` into `routes/coachOps.js` (`registerCoachOpsRoutes({ getSheetRows })`), same pattern as `routes/reads.js`. Byte-identical paths/handlers; auth/rate-limiters (api/write/flight-ingest)/flight-recorder stay global `app.use('/api', …)` so nothing per-route moved; the shared sheet-rows cache is injected (`getSheetRows`) and every ring/shadow/deload/flight/bug write is service-owned (nothing else injected). The 13 slice-local coach/deload helper functions moved in with their routes. **One subtlety caught by tests:** `getSafeSpreadsheetConfig` needed its index.js **destructuring default** preserved (the test harness stubs `sheets.js` without that export). `config/routes.js` unchanged; four source-introspection guards (coach-chat timeout, stimulus_grade engine-only, two intent-shadow wiring checks) + the route-coverage guard retargeted to scan `routes/coachOps.js`. `index.js` shrank ~1130 lines (now ~3070, down from 4704 pre-PR-16). Full suite (4688) + all endpoint HTTP tests green; 0 eslint errors. **This completes the PR-13→17 remediation batch.**
- ✅ **Remediation PR-16 — extract read/analytics routes into an Express router** `[housekeeping]` **(SHIPPED, this PR):** the 16 read routes (`/api/history/*`, `/api/exercises/*`, `/api/sessions/*` [plural], `/api/summary|progress|report|prs|stalls|volume/muscle-groups|search/sessions|catalog/*`) moved VERBATIM out of `index.js` into a new `routes/reads.js` Express Router (`registerReadRoutes({ getSheetRows })`). Byte-identical paths, handler bodies, and the two `:param` orderings (last-session before `:liftCode`, recent before `:sessionId`). Auth / rate-limiters / flight-recorder are global `app.use('/api', …)` middleware and run before the router regardless of mount position; the shared sheet-rows cache is **injected** so writes still invalidate what the reads see (never duplicated). The slice-exclusive `buildExerciseCatalogEntries` + `catalogCache` moved into the router. `config/routes.js` unchanged (paths identical); the route-coverage + `:param`-ordering guards now also scan `routes/reads.js`. `index.js` shrank ~500 lines. Full suite (4688) + api-smoke HTTP tests green; 0 eslint errors. **Next: PR-17** (coach/deload/debug/flight/health routes).
- ✅ **Remediation PR-15 — Exercise_Catalog becomes a synced export (dry-run sync tool)** `[correctness]` **(SHIPPED, this PR):** `data/exercise_catalog.v1.json` is now the declared **source of truth** and the `Exercise_Catalog` sheet a **synced view**. Added `scripts/catalog-maintenance.js --sync-sheet` — a DRY-RUN reconciliation (pure `planSheetSync`, unit-tested) that reads the JSON source + the live sheet and prints the identity diff (source exercises absent from the sheet; sheet rows with no source entry) and **NEVER writes** (applying the sync stays an owner-run action). Documented in the script header + `docs/SHEET_CONTRACT.md` ("source of truth & sync"). **Scope note (premise adjustment, evidence-based):** the brief's step-1 — "enrichment resolves canonical/lift_code/muscle_group from the JSON first" — is **not achievable without changing write-path output**: the JSON has **no `lift_code` and no `muscle_group`** (only fine `primary_muscles`, a different vocabulary from the sheet's granular `Muscle_Group` labels), and its canonical `name`s differ from the sheet/history for the grandfathered residuals (flipping them rewrites `Log_Cleaned`). So enrichment still reads the sheet at write time (output byte-identical; enrichment tests + the PR-06 truth audit + `EXERCISE_TRUTH_ALLOWLIST` all unchanged). **Deferred follow-ups** (filed here): (a) invert `exerciseEnrichment.js` to read the JSON — needs a `primary_muscles → Muscle_Group` mapping, a JSON `lift_code`/exercise_id↔lift_code bridge, and an owner-run sheet sync first (history-affecting); (b) once enrichment sources from JSON, repoint the pending-exercise "add to Exercise_Catalog to lock it" copy (`public/app.js`/`src/app/app.js` `renderUnknownExerciseSuggestions`) to the JSON + maintenance script (kept pointing at the sheet for now, since that is still what enrichment reads). Full suite green.
- ✅ **Remediation PR-14 — parser aliases sourced from data** `[trust-critical]` **(SHIPPED, this PR):** the parser's three hardcoded alias tables (`EXERCISE_ALIASES` / `AMBIGUOUS_ALIASES` / `CONTEXTUAL_ALIASES`) moved out of `services/workoutTextParser.js` into **`data/parser_aliases.v1.json`**, which the parser loads at module load. **Zero behavior diff** — the JSON was generated verbatim from the live tables (byte-identical content AND order; order is load-bearing: EXERCISE_ALIASES length-sort tie-breaks by insertion order, CONTEXTUAL_ALIASES is longest-first by list order with no runtime sort). Committed shadow-parity guard `test/parserAliasParity.test.js` holds an independent frozen reference and asserts byte-identical tables + end-to-end resolution parity for every alias; the full golden corpus + fuzz stay green byte-identical; `exerciseTruthAudit` (which imports the exported tables) is unaffected and the `EXERCISE_TRUTH_ALLOWLIST` stays exact. **Scope note (premise adjustment):** the brief's aspiration to *regenerate parser aliases from the catalog* is in tension with "zero behavior diffs" — the catalog uses different canonical names than the parser for ~30 grandfathered residuals (`docs/verification/EXERCISE_TRUTH_ALLOWLIST.json`), and adopting them would change `Log_Cleaned` output (history). So PR-14 does the safe half (aliases become data, single-sourced + audited) and the parser keeps its own history-critical canonicals; full parser↔catalog **name unification remains deferred** (it is a history-affecting migration needing owner/data-migration sign-off, tracked by the truth audit). **Deferred `[housekeeping]`:** 5 load-bearing dispatch/output literals remain in the parser (`Hanging Knee Raises`/`Push-Up` bodyweight-rep handling; the `Dips (Weighted)`→`Dips` relabel) — these select parse BEHAVIOR, not alias resolution, so they stay as logic rather than move to the alias data. Full suite green (4684).
- ✅ **Remediation PR-13 — orphan-module triage + "wired-or-deleted" CI guard** `[housekeeping]` **(SHIPPED, this PR):** added `scripts/check-wired-modules.js` — it builds the production module graph from the real entrypoints (`index.js`/`sheets.js`/`middleware.js`, `scripts/*`, `config/**`, the frontend entry) **plus the declarative capability-manifest `module.file` strings** (which the orchestrator resolves at runtime, so a require-only graph would false-positive them), and fails on any `services/*.js` unreachable from production that isn't in `config/wiring-allowlist.json`. The allowlist has two categories: **staged** (needs `expires` + `roadmap`; an expired entry fails CI so staging can't be permanent; ≤ 8) and **testOnly** (production-unreachable by design, e.g. the PR-05/06 audit tooling). Wired into CI (`npm run check:wiring` step + `test/wiredModules.test.js`). **Current-state note:** the brief assumed "~16 deletable orphans," but a prior sweep already deleted the truly-dead ones — the 14 remaining were deliberate tested build-aheads, so the triage RETIRED 5 with no named wiring plan (`movementPatternsModule`, `planActionClassifier`, `evidenceTiersModule`, `balanceSignal`, `missedLiftHistory` + tests) and allowlisted 8 with active-roadmap homes (dated 2026-12-31). See the resolved dark-module disposition item below. Full suite green; 0 eslint errors.
- ✅ **PR-12 — workout plan presentation & rendering consistency** `[correctness]` **(FIXED, this PR):** the live-validation session surfaced the chat "build me a workout" flow presenting the SAME workout twice, with the two copies drifting apart. Root causes, all in the `handleChatMessage` → `propose_plan_edit` path: **(1) rendered twice** — the chat system prompt contradicted itself (one rule told the model to enumerate "each exercise on its own line" in prose, another said the app renders the structured block, so both showed). Fix (`services/coach.js`): the prose commits the plan through the structured `PROPOSE_PLAN_EDIT` block only — a single representation, never two. **(2) rendered block lost prescription** — `appendWorkoutPlan`/`formatPlanSetLine` only drew a set line when BOTH weight and reps were present, so a load-less accessory (Face Pull with reps/sets/RIR but no snapshot weight) collapsed to a bare "Face Pull". Fix (`coach-conversation.js`): render whenever reps are known; a load-less set reads "{reps} reps/{rir}" (RIR still never dropped); the directive now always carries the coach's own sets/reps/RIR (only a weight is snapshot-gated). **(3) two presentation models** — the block re-mapped `edit.exercises` independently of the normalized model `app.js` stored on `activePlannedSession`. Fix: `applyProposedPlanEdit` returns the applied model and the chat block renders that same array, so the block, the active-session banner, and the store cannot drift. No parser/logging/save-path/workout-engine/state-store change; SW cache v117→v118. Regression coverage in `test/workoutPresentationConsistency.test.js`.
- ✅ **Live find 2026-07-07 (owner gym session, app `4b5a4ad`) — Flight Recorder starved the Sheets write quota and 500'd a real Save** `[trust-critical]` **(FIXED, this PR):** with `ATLAS_FLIGHT_RECORDER` ON, `recordApiFlow` appended **one row per served API request** to the `Flight_Recorder` tab. A gym session fires dozens of API calls/min, so those appends exhausted Google's *"Write requests per minute per user"* quota (60/min); the owner's `POST /api/log-workout` live append then failed with `429 Quota exceeded` → a **500 Save** (Render log: `[sheets.js] Transient append error on "Flight_Recorder" … Quota exceeded`). Telemetry must never be able to knock out the trust path. Fix: `services/flightRecorder.js` now **buffers** events and flushes them in **one** `appendRows` call per ~2 s window (or at a 100-row cap), collapsing a burst into a single write request; new `flushFlightRecorder()` for graceful-shutdown/tests. Not a Phase 0.9 regression (that code was untouched). Repro pinned in `test/flightRecorderApiFlow.test.js` (40 events → ≤2 appends). **Owner note:** re-Save your session (quota resets each minute; with the recorder off it also clears) — the 500'd workout likely did not land in `Log_Cleaned`.

## Atlas Flight Recorder — replayable real-session transcript (owner-approved design 2026-07-05)
- ✅ **PR-FR1 — schema + contract + pure core (no writes)** `[housekeeping]` — **shipped (PR #856).** `Flight_Recorder` added to `optionalSheetTabs` (`config/sheetContract.js`); `flightRecorderColumns` (18-col JSON-in-cells contract) added to `config/columns.js`; new pure `services/flightRecorder.js` (`buildFlightRow`/`buildFlightRows`, capped newest-first in-memory ring, `redactFlightEvent` reusing `services/bugReport.js`'s `redactBugPayload`, flag gate default-OFF, event taxonomy). **UNWIRED** — nothing appends to Sheets, opens a route, or touches any runtime path yet. Tests: `test/flightRecorder.test.js` (row/contract parity, secret redaction + truncation, blank/junk handling, ring cap, flag gate, TOTAL on hostile input). No write-path/trust-loop/proof-field/parser change.
- ✅ **Live-validation follow-up — API-event session linkage** `[correctness]` — **shipped (this PR).** Server-side `api_response` events had blank `flight_session_id`/`seq`/`device_id`, making the transcript hard to replay. Fix: the client sends `x-atlas-flight-session`/`x-atlas-device-id` on every app `/api` call (`flightRecorder.requestHeaders()` spread into `app.js` `api()` — `{}` and byte-identical when the recorder is off), the FR2 middleware already reads them, and `recordApiFlow` derives a server-side per-session monotonic `seq` (blank when unlinked; explicit seq still wins). Scoped to Flight Recorder API-event linkage only — **not** the FR5 Brain_Shadow/Intent_Shadow `correlation_id`. Test-first (`test/flightRecorderApiFlow.test.js` seq/linkage + `test/flightRecorderClient.test.js` requestHeaders/app.js-wiring). Shell v109→v110. No write-path/proof-field/trust-loop/parser change; best-effort preserved.
- ✅ **PR-FR4 — Settings → Debug UX** `[polish]` — **shipped (this PR).** A "Flight Recorder (debug)" card in Settings (`public/index.html`) wired entirely from `public/flightRecorder.js` (runtime exports `markIssue`/`getSessionId` + `wireDebugUi`; **no `app.js` handler edits** — only the shell-version bump v108→v109 + SW cache): shows enabled state + current `flight_session_id`, **Refresh** renders the last-20 events from `GET /api/flight/recent`, **Copy transcript** puts the ring JSON on the clipboard, **Mark issue** drops a `bug_marker` event (flushed promptly). Degrades gracefully when the flag is off. Proofs: `test/flightRecorderClient.test.js` (markIssue/getSessionId inert-when-off + TOTAL; index.html controls present; client wires the buttons + emits `bug_marker`) + the shell-cache guardrail (v109). Full suite 4692/4692. No trust-tab/proof-field/trust-loop/parser change.
- ✅ **Flight-review evidence extractor (READ-ONLY)** `[housekeeping]` — **shipped.** New `scripts/flight-review.js`: a read-only exporter that pulls `Flight_Recorder` / `Brain_Shadow` / `Intent_Shadow` / `Log_Cleaned` / `Bug_Reports`, groups by `flight_session_id`, correlates the shadow lanes by captured_at time-window and workout rows by session_id (date fallback), and writes `outputs/flight-review/<timestamp>.{json,md}` (gitignored). Flags six anomaly classes (repeated idle coach messages, 4xx/5xx, missing session ids, repeated completed exercises, session-confirm mismatch, coach-not-answering) as triage candidates with evidence. Uses the Sheets **read-only** scope (mirrors `scripts/export-sheets-backup.js`) — never writes, never touches the trust path, changes no runtime behavior. Offline `--from-dir=<backup>` mode + `--sheet`/`--window-mins` flags. Tests: `test/flightReview.test.js` (parser drift, grouping, correlation, each detector, end-to-end render). **Follow-ups (deferred, NOT built — evidence/planning only per task scope):** (1) MVP shadow correlation is time-window heuristic (coarse; a precise join waits on **PR-FR5** `correlation_id`); (2) `Log_Cleaned` date-window fallback over-attach — **partially addressed (PR #868):** `correlateLogs` now prefers `session_id` matches and only engages the same-day date-window fallback when NO session_id resolved, and `correlateByTime` tags shadow lanes `route+time` vs `time`. **Tradeoff noted (review #868):** once any session_id resolves, same-day rows with a blank/absent session_id are now dropped rather than surfaced as weaker `date_window` context — fine for owner-facing evidence, but if a future session-boundary edge case wants those low-confidence rows back, revisit here; a precise correlation_id join still waits on **PR-FR5**; (3) anomaly heuristics read coach text from `ui_snapshot_json`/`decision_summary_json` candidate keys — revisit if the recorder's snapshot field names change.

## Housekeeping — pre-existing contract gap discovered during Flight Recorder work
- ✅ **`[housekeeping]` `Intent_Shadow` is not declared in `optionalSheetTabs`** — **RESOLVED (PR #874, 2026-07-06):** `'Intent_Shadow'` added to the optional list alongside its siblings, plus a new guard suite `test/manifest-module-files.test.js` pinning that every non-missing capability's `module.file`/`module.export` resolves (it caught and fixed the stale `safety → classify` export, now `classifyTrafficLight`). Original note: (`config/sheetContract.js`) even though `services/intentShadow.js` best-effort appends to it (`SHADOW_TAB = 'Intent_Shadow'`). `Brain_Shadow` IS declared; `Intent_Shadow` was missed when the shadow persistence shipped. Harmless (the append is best-effort and the tab is created by hand per the owner setup note), but the contract-status surface (`buildSheetContractStatus`) never reports it. Add `'Intent_Shadow'` to the optional list in a small housekeeping PR. Left out of PR-FR1 to keep that PR to one concern (Flight Recorder only).

- ✅ **`[housekeeping]` Dark-module disposition — RESOLVED by Remediation PR-13 (wired-or-deleted guard).** The three "await decision, no named wiring plan" modules — `services/evidenceTiersModule.js`, `services/movementPatternsModule.js` (overlapped the live `movementPattern.js` map), `services/planActionClassifier.js` (superseded for the live need by `public/planMutationIntent.js`) — were **RETIRED (deleted with their tests)** under the guard's wired-or-deleted rule; git history preserves them if a future capability wants them. Two further homeless build-aheads with no named wiring plan were retired the same way: `services/balanceSignal.js`, `services/missedLiftHistory.js`. The remaining intentional build-aheads with a **named active-roadmap home** are now **allowlisted with a 2026-12-31 expiry** in `config/wiring-allowlist.json` (Session Planning Engine: `objectiveScorer`/`objectiveScoring`/`sessionCloseout`; CIL: `autoregulationModule`/`starterProgramModule`/`intermediateProgramModule`/`populationCapsModule`; the APPROVED `deterministicCoachRenderer`) — an expired entry now **fails CI**, so each must be wired or deleted by then. `services/exerciseTruthAudit.js` is recorded as permanent `testOnly` tooling (production-unreachable by design). Enforced by `scripts/check-wired-modules.js` + `test/wiredModules.test.js` + a CI step; the earlier sweep's three deletions (`workoutDisplayFormatter`/`suggestedWorkout`/`autoregulation`) stand.

## Bug remediation — 2026-07-02 owner live test (B5 date regression on the conversation surface)
- ✅ **[trust-critical] Vision model hallucinated the YEAR on a yearless screenshot header — 12 rows SAVED dated 2020-06-28 (live incident, 07-02 11:57, IMG_5453/5459). FIXED (plausibility-guard PR, 2026-07-02); sheet cleanup is owner-side.** The B5 fix correctly let the screenshot date win — which exposed that the vision model, given a yearless "June 28" watch header, returned `2020-06-28` as a "visible and unambiguous" date (June 28 is a Sunday in both 2020 and 2026 — it weekday-matched a wrong year). The card truthfully showed "Date from screenshot: 2020-06-28" (visibility worked) but a screenshot-sourced date had no correction affordance, and the owner saved: `Log_Cleaned` rows 1597–1608 (Bench 170×10@5 ×3, Hammer Curls 30×15@4 ×3, OHP 90×12@5 ×3, Seated Row 145×13@4 ×3) + the `20200628-AM-01` Effort row are dated six years in the past. **Shipped (this PR):** (1) deterministic server guard `isPlausibleScreenshotDate` (`index.js`) — a screenshot date >2 days ahead or >400 days back is REJECTED: never drives the save/session_id/effort row (gated at the resolution AND the effort-row builder), `date_source` falls back to `today_fallback`, and the response carries `screenshot_date_rejected` so the card words it honestly ("The screenshot's date reads 2020-06-28, which doesn't look right — saving as <today> unless you set the real date:") with the picker; (2) the same window in the client closeout check (`isConfidentScreenshotDate`, `public/app.js`); (3) vision prompt hardening (`services/vision.js`): the year must be printed — never inferred from the weekday — else `date: null`; (4) an ACCEPTED screenshot date is now also correctable on the card ("Wrong date? Fix it" reveals the shared picker row, which keeps the PR #795 session-id clear). Tests: 3 route-level guard tests (`test/api-smoke.test.js`, incl. manual-date-still-wins + rejected-date-never-reaches-the-effort-row; the pre-existing screenshot-date test de-time-bombed onto relative dates), prompt rule pinned (`test/vision.test.js`), 3 client introspection blocks (`test/unit.test.js`). Shell v81→v82. **OWNER DATA CLEANUP (production sheet, owner-side — hand edit):** in `Log_Cleaned`, rows 1597–1608: change column A `2020-06-28` → the session's real date (2026-06-28) and column B `20200628-AM-01` → `20260628-AM-01`; in `Effort`, find the `20200628-AM-01` row and make the same two edits (or delete both sets of rows and re-import once the fix deploys). **Owner live re-test:** re-import the same screenshot → expect the ⚠️ "reads 2020-06-28, which doesn't look right" warning + picker (if the model still returns 2020), or the plain no-date fallback (if the prompt fix holds); either way the save cannot land in 2020.
- ✅ **[correctness] Effort-screenshot save preview never shows the workout date — or the today-fallback warning — on the conversation surface (B5 regression)** — **FIXED (B5 conversation-surface PR, 2026-07-02).** The fix found a SECOND, worse defect while building: the screenshot-preview branch (`public/app.js`) always sent the auto-filled `#log-date` default (today) as an explicit `date` form field — and a manual date beats the screenshot date server-side (`resolveWorkoutDate`), so the screenshot's own date could NEVER win on this path and `date_source` read `'manual'`, suppressing even the today-fallback warning. **Shipped:** (1) the screenshot preview now sends `date` ONLY when the owner actually typed one (`logDateManuallyEntered`, RC2's flag; mirrors the RC5 blank-session_id pattern) — blank lets the server resolve screenshot-date → today-fallback and report an honest `date_source`; preview↔write parity holds because `pendingWrite.date` captures the server-RESOLVED date, which approve re-sends. (2) `emitCoachPreview` → `atlas:preview-ready` now carries `dateInfo { date, source }` from both preview renderers, and `buildReviewCard` (`public/coach-conversation.js`) renders it: header date falls back to the resolved date on effort-only cards (never dateless), `screenshot`/`manual` render a quiet confirmation line, and `today_fallback` renders the ⚠️ warning **plus an inline date input + "Use this date"** that sets `#log-date` via a real `input` event (latching `logDateManuallyEntered`) and re-dispatches the logger-form submit — re-running the SAME preview flow so payload, session id, and card re-resolve on the corrected date (read-only; the approve gate is unchanged). Plain set-log previews pass `source: null` → no notice, unchanged look. Tests: 3 introspection blocks in `test/unit.test.js` (date gating incl. manual-effort-path-unchanged; both renderers pass date+source; card renders notice + correction wiring). Shell v79→v80. **Owner live re-test:** re-import the July-28 screenshot → card shows "⚠️ No date found on the screenshot — saving as <today>" with a date picker; pick 2026-06-28 → "Use this date" → card re-previews with "Date: 2026-06-28" and a matching session id; save → Effort row lands on 06-28. _Original finding:_ Owner live test 07-02 (checklist B5): attached an Apple Watch screenshot dated July 28 (IMG_5125); the in-thread review card showed Duration / cals / HR + "Effort only — no sets logged this session" with **no date and no warning** — "it didn't seem to understand" the screenshot's date, and there is no way to see or correct the resolved date before approving. **Root cause:** the original B5 fix (`renderDateSourceNotice` + `date_source: manual|screenshot|today_fallback`) renders into the legacy `#preview-content` panel, but the conversation surface hides that panel (`previewPanel.hidden = true`, `public/app.js` ~5599) and makes the in-thread review card the single review surface — and `emitCoachPreview` (`public/app.js` ~3944) forwards only `rows/liftCodes/effortOnly/effort/substitutions`, not `data.date`/`data.date_source`, while `buildReviewCard` (`public/coach-conversation.js`) derives its date solely from `rows[0][0]`, which is empty on an effort-only save. So whether the server used the screenshot date or silently fell back to today, the card shows neither. Compounding — **owner confirmed 07-02: the screenshot header reads "July 28" with NO year** — ambiguous by design under the vision prompt's strict rule (`services/vision.js`: return a date only when visible AND unambiguous, as YYYY-MM-DD), so the silent today-fallback IS the live behavior, and the fix must include the B5 "ambiguous date asks" affordance (surface the fallback + a working correction path), not just date visibility. **Smallest safe fix (display-only; no write-path/trust-loop/schema/parser change):** thread `date` + `date_source` through `emitCoachPreview` → `atlas:preview-ready` → `buildReviewCard`, and render the resolved date plus the existing `renderDateSourceNotice` copy (⚠️ today-fallback warning / "Date from screenshot: X") on the review card. Same-PR design point: the conversation surface has no date-correction affordance for an effort-only save (the `#log-date` field lives on the hidden legacy form) — the warning copy must point at a correction path that actually works there. Touches `public/app.js` (event payload only) + `public/coach-conversation.js` (render) — high-risk-file discipline applies (tiny PR, focused diff, tests).

## Live-retest harness (manual, owner-driven) — owner-approved roadmap
- ✅ **Slice 1 (PR #715) — manual workflow + scenario catalogue** `[housekeeping]` (shipped) — `.github/workflows/live-retest.yml` (`workflow_dispatch` only) + `scripts/live-retest.js`. Lines up a known *fixed-pending-live-retest* bug (`bug-20260629-153258`, `-153312`, `-003505`, `-002945`) and prints bug id / purpose / input / expected / forbidden (sourced from `docs/BUG_TRIAGE_LEDGER.md`). Validates inputs, fails on unknown scenario, **defaults to dry-run / no-write**. Not a merge gate, not in branch protection, not a replacement for owner judgment.
- ✅ **Slice 2 (PR #716) — read-only browser bootstrap** `[housekeeping]` (shipped) — `--dry-run-only false` now launches Playwright Chromium, opens the deployed Atlas `/app/`, verifies the shell loaded (`body[data-surface]`), locates the composer (`#workout-text`), confirms the write button (`#approve-btn`) is **present and disabled** (observed, never clicked), captures a full-page screenshot to `live-retest-artifacts/` (gitignored; uploaded as a CI artifact), and exits. **READ-ONLY**: no composer fill, no preview/approve click, no write endpoint, no Sheets write. The workflow installs Chromium only when a non-dry-run bootstrap is requested. Dry-run remains the default and is unchanged. No runtime app change. Locally verified against `tests/e2e/static-server.js`.
- ✅ **Slice 3 (PR #717) — scenario navigation (composer + preview, never Save)** `[correctness]` (shipped) — the read-only browser run now drives the scenario through the UI: each scenario carries a `navigation` descriptor; composer-type scenarios populate `#workout-text` with the scenario text and submit the **preview** flow only (a `test_mode` dry-run — no write), capturing `02-composer` / `03-preview` screenshots and the post-preview thread state. The restore-banner scenario (`-003505`) is tagged `manual` (a UI-state action, not composer-driven) and cleanly skips composer navigation. **Hard read-only guard: only `#workout-text` and `#preview-btn` are ever touched; `#approve-btn` is observed-and-confirmed-disabled, never enabled or clicked** — re-asserted after the preview. No write endpoint, no Sheets write, no runtime app change. Verified locally against `tests/e2e/static-server.js` (composer populated, preview navigated, approve never clicked, exit 0).
- ✅ **Slice 4 (PR #718) — read-only assertion engine** `[correctness]` (shipped) — each scenario gains an `assertion: { forbidden:[...], expected:[...] }`; after navigation, `assertScenario` compares the captured post-preview thread text against the patterns and emits a verdict — **FAIL** (a forbidden/bug pattern reappeared → exit 2), **PASS** (no forbidden + expected present, or a real response when no expected patterns), **INCONCLUSIVE** (no forbidden but expected missing — e.g. an unauthenticated run), **MANUAL** (restore-banner, no automatable assertion). Writes a `<scenario>-result.json` artifact (verdict + per-pattern matched + thread excerpt + screenshot list) alongside the screenshots. Pure string/regex comparison over already-captured text — no extra navigation, **no writes**. Verified locally: `-153258` → PASS (forbidden absent; thread shows the correct fixed reply), `-002945` → INCONCLUSIVE (needs an authed parse), `-003505` → MANUAL.
- ✅ **Slice 5 (PR #719) — scenario-library expansion + reporting** `[correctness]` (shipped) — the catalogue now covers **all five** 🟡 yellow `BUG_TRIAGE_LEDGER` rows: added `bug-20260629-204817` (recovery-bump recurrence — multi-step Coach's-Pick flow, tagged `manual` for now). New **`all`** mode runs every scenario in sequence and prints a verdict summary table + writes `summary.json` (PASS/FAIL/INCONCLUSIVE/MANUAL tally); `all` and `-204817` added to the workflow `scenario` choices. Also folded in the two #718 review tighten-ups: (a) the result manifest now lists only screenshots that exist on disk (a MANUAL scenario lists just `01-loaded`); (b) the coach-fallback rows gained a positive `expected` pattern (`re-?type` / `add it to the preview`) so an unauthenticated error toast reads **INCONCLUSIVE**, not a false PASS. Still strictly read-only — never Save, no writes. Verified locally (`all` → PASS=2/MANUAL=2/INCONCLUSIVE=1) + `test/live-retest-assert.test.js` (8 unit tests over the verdict matrix).

## Deterministic Coach Voice Renderer — ◐ slice 1 (pure engine + tests) shipped; wiring deferred
- ✅ **Slice 1 — pure engine + tests (this PR).** `services/deterministicCoachRenderer.js`: `renderCoachEvent(eventType, input)` → `{ message, evidenceCited }` for all 17 event types, each with ≥2 deterministically-chosen phrasing variants (stable djb2 hash, no randomness), conclusion-first, ≤3 sentences. NEVER invents a number — only interpolates values present in the input (or stock phrases); `coach_unavailable` is confident output, not an apology; `save_failed` reassures the sets are kept + is actionable. Pure/stateless, no route/LLM/write change. 15 unit tests in `test/deterministicCoachRenderer.test.js` incl. the hard guarantee (every output digit must appear in the input — full-input subset + empty-input zero-digits), determinism, ≤3-sentence bound, evidenceCited ↔ number-present, variant coverage, and no-throw on malformed input.

## New-user onboarding + working-weight discovery (B8) — engine layer complete; UX (PR-O4) deferred to owner direction
- ✅ **PR-O2 — onboarding session-template builder** `[correctness]` (engine, pure, shipped) `services/onboardingSessionPlan.js::buildOnboardingSessionPlan({ availableEquipment, lifts })` emits the spec §3 full-body calibration plan (3 sessions, 8 reps @ 2 RIR across squat/push/pull/hinge, widening to a vertical push + one isolation in S2/S3), selecting a lift variant per pattern from available equipment. Numbers are engine-owned: start hints from `buildWorkingWeightProtocol` (70% / "Start conservative"), ramps from `buildWarmupRamp` offered **only once a working weight exists** (F7), per-lift `calibration_status` from PR-O1's `calibrationStatusFor` (no majority gate). Fixtures F1–F3, F7 pinned in `test/onboardingSessionPlan.test.js`. No write path/schema/route/LLM change.
- ✅ **PR-O5 — questionnaire → template assignment router** `[correctness]` (engine, pure, shipped) `services/onboardingRouter.js::routeOnboarding({ goalId, trainingLevel, availableEquipment, daysPerWeek, logEntries, lifts })` routes questionnaire answers to: `cold_start_calibration` (no history → calibration plan), `beginner_lp` (beginner → first recommended starter program), `intermediate_531` (531_wave model → 5/3/1 program id), `advanced_block` (block-intermediate → phase structure), `advanced_dup` (dup-patterns → session types by daysPerWeek). 59 tests. Completes the engine layer for roadmap PR 9.

## Test-coverage hardening — make Atlas safer without building new behaviour (owner-approved 2026-06-29)
- ✅ **PR3 — `services/vision.js` provider-dispatch + parse-pipeline tests** `[housekeeping]` (shipped) — `getProviderConfig` env permutations were already covered in `test/vision.test.js`; the real gap was the orchestration in `parseWorkoutScreenshot` (lines 124–230). New `test/vision-parse.test.js` injects fake `openai` / `@google/genai` SDKs before loading the real `vision.js` and covers: provider dispatch (openai vs gemini), image payload shaping (data URL / inline data + mime type via `getMimeTypeFromPath`), `extractTextOutput` (both `output_text` and `output[].content[]` paths), fence-strip + JSON-parse + `normalizeParsedMetrics` into the 7-field contract, no-text / invalid-JSON error paths, model selection (`ATLAS_LLM_MODEL`), and the **no-silent-fallback** contract (a Gemini failure never constructs OpenAI and vice-versa — CLAUDE.md). `vision.js` now 100% line / 100% func / ~93% branch. Test-only; no source change.
- ✅ **PR5 — CI coverage report (non-blocking)** `[housekeeping]` (shipped) — added a `coverage-report` job to `.github/workflows/ci.yml` that runs `node --test --experimental-test-coverage` and writes the summary (overall + key source files: index.js / sheets.js / vision.js / coach.js / analytics.js / workoutTextParser.js) to the GitHub Actions run summary. **Report-only, `continue-on-error: true` — never gates a merge** (owner decision: a global threshold now would be a false sense of safety since the high overall % masks low-coverage critical files, and a strict per-file threshold would fail immediately). A hard gate is intentionally deferred until the coverage-hardening PRs settle — owner's later target: global lines 95% / branches 80% / functions 85%, no per-file threshold yet, exclude generated/coverage output (and `scripts/` only if separately justified).

- ✅ **read-endpoint smoke coverage (`index.js` read path)** `[housekeeping]` (shipped) — five GET endpoints had **zero** tests; now covered in `test/api-smoke.test.js` against the existing `logRows`/`exerciseCatalogRows` fixtures (read-only, no write path): `/api/exercises/:liftCode` (aggregated detail — known lift + empty-aggregate unknown lift), `/api/volume/muscle-groups` (summary + `days` validation 400s), `/api/search/sessions` (filtered result), `/api/catalog/exercises` (parsed entries), `/api/catalog/search` (query filter + `q`-required 400), `/api/health/sheets` (required/optional tab presence). 7 tests. `index.js` was the standout remaining gap (62% branch / 78% line); these fully-uncovered read handlers (874–934, 980–1052, 1056–1076) are now exercised. Test-only; `index.js` unchanged.
- ✅ **`recommendationPolicy.js` progression-branch coverage** `[housekeeping]` (shipped) — it was the lowest-branch-covered service (~79%). New `test/recommendation-policy-branches.test.js` drives the uncovered goal-policy progression paths through the public `recommendExercisePrescription()` (pure, no stubs): estimated-1RM base-weight derivation (no previous weight), hypertrophy `add_set` (volume-below-target + reps below range), `conditioning_fat_loss` density progression, `mixed` main-lift `add_load` at top of range, the `general_health` default-fallback rep progression, and unknown-goal→`general_health` normalization. 6 tests. Lifted `recommendationPolicy.js` to ~97% line / ~88% branch / 100% func. Test-only; source unchanged.
- ✅ **`coach.js` Gemini network-path coverage** `[housekeeping]` (shipped) — the existing `coach.test.js` covers the pure sanitize/prompt builders but never exercised the actual Gemini call path (`callGeminiContents` + the `generate*` wrappers) because it doesn't stub `fetch`. New `test/coach-gemini.test.js` stubs the global `fetch` (no network) to drive: success (text returned), non-OK status, empty-output → the three `callGeminiContents` outcomes; the unconfigured guard (no `GEMINI_API_KEY`) throwing before any fetch; `generateCoachMessage`/`generatePlanMessage`/`pingGemini`/`generateChatReply` success; and `generateVerdictReaction`'s `shouldReactToVerdict` gate (null + no call on a quiet "met" set vs a "beat" verdict that calls). 10 tests. Lifted `coach.js` to ~96% line / ~93% func (the previously-uncovered network/degradation ranges 527–557, 570–586, 1016–1046, 1259–1270 now exercised). Read-only voice; no write path; `coach.js` unchanged.

## One-Brain Coaching Engine — ARCHITECTURE LOCKED, build sequence (owner-gated promotion to active)
- ✅ **Avoid the index.js double Sheets read** (shipped) — the hybrid attach now calls `assembleState({ asOf, readers: { ...defaultReaders(), getLogRows: async () => allLog } })`, reusing the already-fetched rows while keeping the real deload/profile readers. `stateAssembly` exposes `defaultReaders`; a single-read test guards it. (PR #755 review.)
- ✅ **empty-string RIR → `0`** (shipped) — `liftPrescription.normRow` now coerces empty/blank/missing weight/reps/rir cells to `NaN` via a `_num` helper (was `Number('')===0`), so an unlogged RIR degrades to clarification rather than a false at-failure read that suppressed load increases. Tests in `test/liftPrescription.test.js`. (PR #757 review.)
- ✅ **`underloaded` scenario now reachable on the shadow path** (shipped) — `liftPrescription.js` gained a pure `isOverperforming(rows, liftCode, asOf, set?)` (last working set's actual reps vs `expectedPerformanceModule.assessExpectedPerformance` expected reps at ±10% of the set load; `false` when <3 qualifying sessions — the conservative default). `prescribeLift` computes it and threads it to `classifyScenario`, and `coachRunners.scenarioClassifierRunner` reuses the same helper — so the `underloaded`/`increase_load` branch is reachable from real data (precedence intact: injury > plateau > fatigue > overperforming > improving). ✅ **Hinge regression added** — `sessionGenerator` test: a hinge lift (Romanian Deadlift) with held-weight/rising-rep (overperforming) history → `underloaded` → `increase_load` → `target_weight` steps by the 5 lb lower-body increment (135 → 140), exercising `bodyRegion → computeLoadStep`; plus `prescribeLift`/`isOverperforming` tests for the overperforming→underloaded path. This is the consumer-path regression that was unconstructible in PR #761 (increase_load was unreachable then). (PR #757 / #761 review.)
- ✅ **`_liftRows` recomputed per helper** (shipped 2026-07-02) — `liftPrescription.js` now has internal `_*Ctx` variants (`_deriveLiftStateCtx`/`_derivePlateauCtx`/`_lastWorkingSetCtx`/`_isOverperformingCtx`); `prescribeLift` builds the normalized/filtered ctx ONCE and shares it across the pipeline (was 4× per call). Public signatures unchanged (coachRunners' direct calls unaffected); a source-guard test pins the once-only rule. (PR #757 review.)
- ✅ **`IntentEnvelope.asOf` strict ISO-8601** (done in PR-3) — `services/intentEnvelope.js` now requires a strict ISO-8601 date-time regex (rejects `"June 30 2026"` / bare `"2026-06-30"`), not just `Date.parse`. Resolved the PR #748 reviewer note.
- ✅ **Trust-contract: key-aware matching** (done, before PR-5) — `services/coachingDecision.js` rule 4 is now **key-aware**: each prescribed payload number must be echoed under its corresponding `explanation_inputs` key (`explanation_inputs[field]` for single-value payloads; `explanation_inputs.blocks[i][field]`, order-aligned, for workout). A right value under the wrong key or an incidental match no longer satisfies the contract. Also fixed `block_prescribed_numbers` to use the real block field `reps` (was `target_reps`, which never matched, so rep targets escaped the check). Landed before the orchestrator wires a number to the LLM (PR-5), as gated. From PR #750 review.

## Coach Intelligence Layer — follow-ups (PR #721, scaffold)
- ✅ **Exercise ontology: dangling `substitutions`/`progressions`/`regressions` references — mostly resolved (PR 2)** `[housekeeping]` — PR 2 expanded the ontology to 52 exercises; the previously-dangling refs for `front-squat`, `leg-press`, `belt-squat`, `dumbbell-bench-press`, `trap-bar-deadlift`, `lat-pulldown`, `romanian-deadlift`, `chin-up`, `cable-row`, `face-pull`, `barbell-row`, and many others are now live exercises with their own files. **One remaining dangling ref:** `bench-press.json` `substitutions` lists `machine-chest-press` which does not exist in the ontology. Acceptable — no consuming module yet. Add `machine-chest-press.json` in a future exercise-expansion pass, or when the consuming module requires it.

- ✅ **UserStateModule: per-user per-lift state substrate (roadmap PR 6, shipped)** — `services/userStateModule.js` aggregates Log_Cleaned rows into `{ liftStates, muscleVolume, adherence }`. 43 tests. Depends on IntensityModule (PR 3) + VolumeAssessmentModule (PR 4). No runtime consumer yet. Unblocks roadmap PR 14 (ExpectedPerformance/Plateau composite) and PR 18 (Memory architecture).

- ✅ **ExpectedPerformanceModule: composite expected-performance + plateau assessment (roadmap PR 14, shipped)** — `services/expectedPerformanceModule.js` wraps `computeExpectedPerformance` (structured-entry adapter) + plateau/stall detection + composite `assessLift` with readiness adjustment. 35 tests. Depends on IntensityModule, FatigueAssessmentModule (PR 11/12), UserStateModule (PR 6). No runtime consumer yet. Unblocks roadmap PR 15 (Autoregulation) and PR 18 (Memory architecture, now fully unblocked).

- ✅ **MemoryModule: long-term trends store + entity resolution (roadmap PR 18, shipped)** — `services/entityResolutionModule.js` maps raw exercise names / colloquialisms → canonical exercise IDs (aliases + progressions from ontology; priority: exact id > name > alias > progression). `services/memoryModule.js` is the Brian-layer composite: wraps `detectTrend` + `detectPatterns` + `detectMissedLifts` + entity resolution into `{ queryTrend, queryPatterns, buildMemorySnapshot, resolveExercise, listExerciseIds }`. `buildMemorySnapshot(logRows, opts)` sweeps all lifts present in the rows and returns `{ liftsEncountered, liftTrends, liftPatterns, missedLifts }`. 51 tests. No runtime consumer yet. Unblocks roadmap PR 19 (ConfidenceModule).

- ✅ **Periodization & peaking engine (roadmap PR 22, shipped)** — `config/coaching/periodization/` (3 configs: block-intermediate, dup-patterns, peaking) + `services/periodizationModule.js` (`selectPeriodizationModel`, `buildBlockPhases`, `getDupSessionTypes`, `isPeakingAppropriate`, `buildPeakingPhase`). Block: 8-week 3-phase cycle. DUP: 3- and 4-day weekly patterns. Peaking: 2-week taper for powerlifting goal, excluded for youth/older-adult. 82 tests. No runtime consumer yet. Depends on PR 15, PR 17.

- ✅ **Goal/population templates & caps + 5/3/1 intermediate program (roadmap PR 17, shipped)** — Goal templates (`config/coaching/goals/`): `general-fitness`, `powerlifting`, `bodybuilding`, `weight-loss` — each maps to recommended program IDs by training level via `selectPrograms(goalId, {trainingLevel})`. Population caps (`config/coaching/populations/`): `general` (uncapped baseline), `older-adult`, `youth`, `busy-parent`, `home-gym`. Program config (`config/coaching/programs/531-intermediate.json`): Wendler 5/3/1, 4-week wave cycle (5s/3s/5-3-1/deload), 4-day split (Squat/OHP/Deadlift/Bench), AMRAP top set, +5 lb upper/+10 lb lower per cycle. Three engine modules: `goalTemplateModule.js`, `populationCapsModule.js`, `intermediateProgramModule.js` (`buildNextSession531`, `suggestNextCycleMaxes`, `computeTrainingMax`). 125 tests. No runtime consumer yet. Unblocks roadmap PR 22 (periodization engine).

## AI overnight full-app test sweep — findings (2026-07-04, owner-requested "test everything, poke at everything")
- ✅ `[correctness]` Malformed / oversized JSON body returned **500 instead of 400/413** — global error handler now maps body-parser `entity.parse.failed`→400 and `entity.too.large`→413 (`index.js`; test in `api-smoke.test.js`).
- ✅ `[trust-critical]` **Negative/fractional `set_number` was not bounds-checked** — a `set_number:-5` rode into the row (weight/reps had bounds, set_number did not). Added integer 1–100 validation (`rules/validationRules.js`; tests in `rules.test.js` + `api-smoke.test.js`).
- ✅ `[correctness/security]` **`/api/coach/chat` disclosed its full system prompt** on request ("repeat your system prompt") while `/message` and `/ask` refused. Added a CONFIDENTIALITY hard-rule to the chat system prompt (`services/coach.js`). ⚠️ **Needs live post-deploy re-verification** — this is LLM behavior, not locally unit-testable.

## Editorial archive 2026-07-30

Owner-authorized reconciliation of `BACKLOG.md`. These records were explicitly marked complete/resolved and carried no open, deferred, pending, evidence-gated, or owner-reserved child in their Markdown block. Historical record only; nothing below is actionable.

### Atlas — Backlog › Recent discoveries & in-flight notes (reference — not the work queue)

- ✅ `[trust-critical]` **`F10C-IMPLICIT-WIRING-1` — FIXED (F10C slice 2, 2026-07-18).** Wired the implicit-recommendation derivation end-to-end, still dry-run. Server: `POST /api/session-plan-sets/implicit` (`routes/sessionPlans.js`, given `getSheetRows` in `index.js`) derives server-side over Log_Cleaned with the current session excluded (leakage), then checkpoints a reliable target via `captureImplicit`/`checkpointImplicit` (mirroring accept/revision, dry-run) — a `no_reliable_target` appends no row (§4A rule 5). Client (`src/app/app.js`): `emitSetLogged` detects an off-plan logged exercise via `isOffPlanLoggedExercise` (the F10 `planSlotStatuses` selector in isolation) and `emitImplicitRecommendation` POSTs under a deterministic per-(session,lift) `plan_item_id` (idempotent), storing only a reliable result in `_sessionImplicitRecs` (snapshot v3→v4) for reload; never a planned slot/substitute, never the performed value. Red-first `test/implicitRecommendationWiring.test.js` + `test/sessionPlanSetsRoutes.test.js` + `test/store.test.js`. `services/implicitRecommendation.js` left the wiring allowlist. Full suite 5689 pass. Canonical card: F10C (COMPLETE) in [`docs/ATLAS_V1_EXECUTION_PLAN.md`](./docs/ATLAS_V1_EXECUTION_PLAN.md).

  - ✅ `[housekeeping]` **`REVIEW-LIVE-1` — FIXED (F09C, 2026-07-17).** Added `npm run atlas:review-live` (`scripts/atlas-review-live.js`): auto-selects the newest owner session, joins Flight Recorder / Intent Shadow / Brain Shadow / Session Plans / Log / Effort, detects build changes, and reports PASS/FAIL/UNKNOWN per trust criterion (UNKNOWN = missing evidence, never a false green). Read-only; fixtures in `test/atlasReviewLive.test.js` incl. the v141-shaped only-server-rows failure.

  - ✅ `[correctness]` **`AUTH-DURABLE-1` — RESOLVED, no defect (F09D proof-only close, 2026-07-17).** The re-prompt was traced (F04C, live-validated on v137) to expected loss of site data (clearing Safari history/website data clears the session), NOT a persistence regression. The session is durable: 120-day HttpOnly/Secure/SameSite=Lax cookie that renews after 60 days; a transport failure never becomes a key prompt. Added durable-lifetime regression pins in `test/session.test.js`; no production-code change.

  - ✅ `[correctness]` **`PLAN-EXEC-1` — FIXED (F09E, 2026-07-17).** Render-layer completeness: a bodyweight lift (engine `weight:0`) rendered a meaningless `0lbs 15/?` → now `BW — 15 reps ×3` (grouped, explicit set count), distinct from a load-omitted accessory (`weight:null` → `15 reps/2`); an exercise with no confident rep target (`reps==null`) rendered a bare name → now a clarify prompt. The engine already emits set/rep/RIR targets and the accepted plan already retains them in session state (`normalizePlanExercise`), so no engine/state change was needed. Red-first tests in `test/workoutPresentationConsistency.test.js`; weighted `225lbs 5/2` unchanged.

  - ✅ `[trust-critical]` **`PLAN-COACH-SPLIT-1` — FIXED (F09F, 2026-07-17).** The visible plan and the live recommendation engine disagreed; Atlas prescribed one target then treated a different performed target as though it had always been the plan (actual silently became "plan"). Root cause was a read-time provenance gap in the deterministic coach-answer layer (`services/sessionQuestionAnswer.js`), not a stored write-back: `targetFromContext` echoed `current_preview` (performed) values as prescriptions, and the `recommendNextSet` engine fallback was worded identically to a frozen-plan value. Fixed so every answer's target is explicitly the **accepted plan**, a **labeled revised next-set recommendation**, or **"no reliable target available"** — preview resolves lift identity but never prescribes numbers; the engine may fill genuinely-missing guidance for a real plan lift; context outranks the engine where an accepted-plan target exists. Six red-first cases in `test/sessionQuestionAnswer.test.js`. **F09F ≠ F10**: the target-value selector is distinct from F10's `plan_item_id` completion-identity selector, so F10 keeps its place before the ledger cards. The full stored recommendation-vs-actual ledger remains **F10A–F10E**.

  - ✅ `[correctness]` **`PR-CLAIM-1` — FIXED (F09H, 2026-07-17).** "That was a PR!" was treated as workout-set logging, then as a coaching-note save request; a failed `/api/coaching-notes` write returned 503. A four-surface map found the parser already refuses to parse a PR claim as a set ("pr" resolves to no alias) and the note-service is already isolated (client `try/catch`; no logging/closeout path calls it — the 503 could not interrupt logging). The real defect: a PR claim could become a coaching note. Fixed deterministically — a new pure `looksLikePrClaim(message)` classifier (`services/coach.js`) + a route-level suppressor (`routes/coachOps.js`) drop any `propose_note`/`propose_constraint` for a self-reported PR/personal-best claim (a training GOAL that mentions a PR is not a claim and still proposes), plus a note-proposing prompt exclusion. PR status stays engine-owned (`progressionVerdict`/`lift_prs`), so a typed claim manufactures nothing. Red-first `test/prClaimGuard.test.js` + integration `test/api-smoke.test.js` + prompt pin.

  - ✅ `[correctness]` **`SIDECAR-DATE-1` — FIXED (F09I, 2026-07-17).** `POST /api/coaching-notes`, `POST /api/constraints`, and `Deload_State` `deload_start_date` now stamp the owner's LOCAL day via the existing canonical `localTodayIso(ATLAS_TIMEZONE)` helper instead of a raw UTC slice — so an evening-Pacific write is no longer dated tomorrow. No historical rewrite; UTC fallback until `ATLAS_TIMEZONE=America/Vancouver` is set in prod (owner env action). Red-first route tests + month/year + DST unit coverage.

  - ✅ `[trust-critical]` **`UNDER-TARGET-1` — truthfulness FIXED (F09J, 2026-07-17).** Atlas claimed a lift was "under target 5 of the last 5 sessions" with **no stored historical prescription**; the detector is benchmark-based, not plan-versus-actual, so "missed/under target/failed the plan" is untruthful without a stored plan. Fixed: the chat CHALLENGE MODE prompt block (`services/coach.js` `buildChatSystemPrompt`) now frames a `consistent_underperformance` signal as a `BENCHMARK/TREND comparison`, `NOT a missed plan`, forbids "under target"/"missed target"/"failed the plan"/"beat expectations", and supplies honest replacement wording ("below your recent benchmark" / "below your established range"). No deterministic string carried the old phrasing; the valid prescribed-RIR `effort_verdict` path is untouched. Prompt-contract tests in `test/coachPromptRules.test.js`. Full plan-aware assessment still arrives in **F10E**.

  - ✅ `[housekeeping]` **`playwright.config.js` remote-env executablePath shim — FIXED (with F09B, 2026-07-17).** Top-level `use.executablePath` is not a valid option on @playwright/test 1.60 (must be `use.launchOptions.executablePath`), so in the remote container Playwright looked for a `chromium_headless_shell` revision that isn't pre-installed and every e2e spec failed at browser launch. Moved the override into `launchOptions`; CI never sets `PLAYWRIGHT_BROWSERS_PATH` (it runs `playwright install`), so the change is a no-op there. Local e2e now runs against the pre-installed `/opt/pw-browsers/chromium`.

- ✅ **Session_Plans PR-I — production-validated + mid-plan Done RESOLVED (2026-07-11).** The full client capture lane (PR-D…PR-I) is live and **production-validated**: an automated live-write canary proved `accept` (3 `plan_accepted` rows) + `completed` + `substituted` (with `performed_lift_code`) + `skipped` + `finalized` closeout all persist to the real `Session_Plans` tab under one identity; follow-up controlled tests proved **idempotent replay** (0 new rows), **invalid-payload rejection** (400, 0 rows), and an **abandoned** lifecycle (`plan_accepted` + `abandoned` closeout, 2 rows). Canary rows were administratively removed; `ATLAS_SESSION_PLANS_WRITE` is enabled for real shadow capture. The earlier **mid-plan "Done with this exercise" reachability** fork is **RESOLVED by PR #974** (owner-approved "target last-logged item"): `services/planCompletion.js` selects the most recently logged still-unresolved accepted item, so the explicit `completed` boundary is reachable mid-plan (identity strictly by `plan_item_id`; a substituted slot completes the original id; logging/cursor movement never auto-complete). No open sub-item.

   - ✅ **`[trust-critical]` PARSER BUG — silent set MISATTRIBUTION — FIXED 2026-07-03** (same day it was found; the `todo` reproducers in `test/parserFuzz.test.js` are now HARD assertions plus two new same-line guards). Root cause was two stacked holes in `parseLogSets`: (1) the G1 trailing guard required EVERY word of an inter-set run to be name-like, so one notation/alias word ("press" is a contextual-alias word, "x3") poisoned the run and let "… x3 Zzqx Press 185 5/2" absorb silently — now `some(isNameWord)`, strictly more conservative; (2) no LEADING guard existed — slash sets BEFORE the matched name ("Zzqx Press 185 5/2 Bench 200 5/2") were vacuumed into the found lift — now the G1 leading analogue dead-ends them with `unattributable_trailing_sets`, which also routes newline pastes into the existing multi-line rescue where each line resolves correctly (the unknown name becomes its own flagged freeform entry; the #820 check-name chip covers the surface). All previously-clean grammars verified byte-identical (single/multi-set/prose-noise/2-line carry/known-multi); suite 4515/4515; browser sweep clean. Original finding record follows for history: an unknown exercise name on a set-bearing line can have its sets absorbed by a DIFFERENT, known lift — worse than dropping, it corrupts the wrong lift's history at save. Repro A (collapse path): `"Zzqx Press 185 5/2 - solid\nBench 200 5/2"` → log_sets for Bench Press with BOTH sets (185 under Bench), zero warnings — the newline collapse treats the unknown leading name as noise and only one KNOWN exercise exists, so the multi-exercise dead-end (which would trigger the line-by-line rescue) never fires. Repro B (rescue path): `"Leg Press …\nRDL 225 5/2 x3\nZzqx Press 185 5/2 - solid"` → RDL grows a phantom 185 set via the rescue loop's carry-forward (parseLogSets with activeExercise attaches the line's sets to the carried lift when the line's own name fails). Fix direction: a set-bearing line with its own leading name must never merge into a different exercise — unresolvable names go to `unresolved` (rescue) and the collapse path must dead-end into the rescue when an unknown leading name precedes a known one. High-risk `services/workoutTextParser.js` surgery — do it as a focused fresh-session PR with G1 regression coverage, not as a tail-of-session patch.

     - ✅ **PR-3 `[correctness]` — short-gap restart posture — SHIPPED (PR #842):** `scoreIntents` (`services/analytics.js`) now surfaces a severity-independent `todays_read.days_since_last_session` (from `assessLayoff`, emitted for every gap, not just 7+ day layoffs), and a new pure `shortGapRestartNote(gapDays)` prepends a reentry line — "Back after {N} days off — settle in with clean, controlled reps." — to the recommended TRAINING intent's `why_today` at a 3–6 day gap, so the PR-1 opener leads with it (engine-*true*, not invented). WORDING ONLY: no prescribed number changes, and the 7/14/28 `assessLayoff` make-up-volume caps are untouched (a 7+ day gap still takes the layoff path; short_session/test_progress/recovery keep their own framing). Tags the intent `short_gap_restart`. Tests: `test/short-gap-restart.test.js` (window guard + 5-day integration + sub-3-day & 7+-day controls). No write-path/schema/trust-loop/routing-rank change; server-side engine only (no shell bump).

   - ✅ **C3 shadow COVERAGE gap — most typed messages were never observed — FIXED (chokepoint-observe PR)** `[product-evidence]`: owner ran many shadow sessions, `/api/debug/intent-shadow` showed only 2 entries. Root cause: `observeChatMessage` fired at ONE place — server-side inside `/api/coach/chat` — but the composer routes typed text through ~8 deterministic lanes that each return before any chat call (set logs → parse/preview, session/artifact asks → local, log-it → compile, **plan mutations + identity corrections → client-only, no network at all**, modality → `/api/log-modality`), and even chat-reaching messages hit `/api/coach/ask` (SME) first and return before `/api/coach/chat` when answered. So the log only ever saw the residual free-form questions ("What's next"). Fix: observation moved to the ONE composer chokepoint — a fire-and-forget `observeComposerText` at the top of the `logger-form` submit handler POSTs every free-text submission to a new auth-gated, observe-only `POST /api/debug/intent-observe` (declared in `config/routes.js`), which forwards to `observeChatMessage`; the redundant `/api/coach/chat` call was removed (single observation point, no double-count). Still observe-only, off the reply/write path, server no-ops when the flag is off. Programmatic canned `routeMessageToCoach` calls (pattern-board/nav) are intentionally not observed (not free-text typed). Shell v103→v104. Review #838 fix: the observe POST uses a BARE authenticated fetch (not `api()`) so a dropped observation never lands in the `atlasLastError`/request-history that bug reports capture — observation stays truly invisible. **Known-harmless limitation (#838 review note):** a few flows re-dispatch a synthetic `submit` with the user's text still in the composer ("Log as new session", compile re-submit, manual-effort), so the same text can occasionally be observed twice — fine for diagnostics, and the blank-guard no-ops the empty-composer re-dispatches. (Persistence to a Sheet tab so review needs no debug JSON — SHIPPED next, see below.)

   - ✅ **Owner directive (2026-07-03, live screenshots): plans render STACKED, never as prose lines** — the chat's plan reply rendered "RDL — 230 × 8 reps × 3 sets @ RIR 2" linear prose; the owner wants the Coach's-Pick stacked block (bold name, one set per line) everywhere a plan renders. Shipped: (1) applied chat plan edits (`replace_plan`/`add_exercises`) render `appendWorkoutPlan` from the edit's own deterministic numbers under the reply; (2) the chat prompt instructs the model NOT to enumerate the plan in prose (the app owns the render); (3) `looksLikeSessionRequest` gained suggest/recommend phrasings ("what workout would you suggest for today", "recommend me a session") so those asks land on the canonical stacked pick in the first place.

   - ✅ **Live bug (owner 2026-07-03, 11:23): an alias log against a CHAT-created plan never advanced the plan — FIXED** `[correctness / trust]` — logged "Rdl 240 8/3 ×3"; the handoff said "next up: Romanian Deadlift" and the composer stayed pointed at it. Three stacked gaps: applied `PROPOSE_PLAN_EDIT` plans carry names only (no lift codes) so the code bridge was one-sided; the exercise datalist held canonical names only (no variants, so "RDL" resolved no code client-side); and the enrichment dry-run is best-effort (null on a slow network). Fix (`public/app.js`): the datalist now includes catalog VARIANTS (value=variant, label=code, deduped) so `liftCodeFromCatalog('RDL')`→`RDL01`; `resolveCompletedIdentity` bridges codes TWO-SIDED (entry code falls back to the catalog by canonical/name) and gains the ≥2-word word-subset tier (mirrors `planMutationIntent`). Bonus: the logger typeahead now suggests variants.

   - ✅ **Live find (owner 2026-07-03, 11:51) — typo'd identity correction missed the relabel → DUPLICATE sets in the review — FIXED (identity-correction typo-tolerance PR)** `[trust-critical]`: "Slslp 70 15/2 ×4" buffered with the check-name chip; the correction "Sorry slslp is single leg seated leg prep" (typo: 'prep') missed the deterministic identity-correction matcher and fell to Gemini (which chatted, no relabel); "I did 70 15/2 ×4" then logged 4 NEW sets under the plan step — the review carried BOTH Slslp ×4 and Single-Leg Seated Leg Press ×4 (owner warned to Edit-rows before saving). **Shipped:** (1) `identityCorrection.js` gains the "X is Y" grammar (optional sorry/oops/actually cue; is/was/means/should-be connectors; demonstratives stay with the cue-gated that-was patterns, questions/numeric sides never fire) returning `{ from, to }`; (2) `tryApplyIdentityCorrection` (`public/app.js`) resolves `from` against the DISTINCT buffered lift names (exact → singular → substring → word-subset/typo, newest-first — an "X is Y" whose X isn't buffered falls through to the coach untouched) and relabels that WHOLE group wherever it sits in the log; (3) the `to` side falls back from the catalog tiers to a new ≥2-word word-subset/typo tier against PLAN names then catalog names (per-word: exact / singular-equal / ≥4-char shared-3-prefix edit-distance ≤2 — 'prep' meets 'press', 'pull' never meets 'push', 'leg' never meets 'lat'), refusing on ambiguity at each source. Legacy that-was corrections byte-identical (trailing-run relabel, catalog-first). Tests: classifier + 5 wiring cases incl. the live repro. Shell v100→v101.

   - ✅ **Owner feedback (2026-07-03, 11:32) — opening message briefing — SHIPPED (opening-briefing PR)** `[polish/UX]`: the "Last time: OHP 115×8" opener is replaced by a deterministic engine briefing. `loadDashboard` (`public/app.js`) now builds the strings from the SAME startup fetches it already runs (`/api/plan/intent-recommendation` + `/api/progress/summary`) and dispatches `atlas:glance-ready` with `{ headline, facts }`: headline `"{Weekday}. Today's read: {label}."` (from `todays_read.recommended_label`) and a facts line = `buildConsistencyText` + `buildPatternBriefing` (freshest/overdue patterns, most-recovered/overdue first, up to two named, `''` when none). `coach-conversation.js`'s `renderCoachOpening` is now an `atlas:glance-ready` listener that upgrades `#coach-opening` and reveals the new `#coach-facts` line — no second network call, LLM-free, guarded on the hero still being visible. Degrades to the unchanged default tagline + guide when nothing meaningful arrives (no key/offline/cold-start → no dispatch). Shell v101→v102. Tests: opener redesign (greeting still synchronous + fetch-free; briefing renders app.js strings, no fetch/LLM; app.js headline/facts/degradation; `buildPatternBriefing` golden). No write-path/trust-loop/proof-field/parser change.

   - ✅ **Owner directive (2026-07-03, 11:40 screenshot): in-workout chrome collapses to the pin row** — the Coach-plan card (Next exercise → / End session) took too much space from the thread. Now: the session pin is the ONE always-visible row with a chevron; tapping it (or Enter/Space — role=button, aria-expanded) expands the plan card as a dropdown; the glance strip hides while a session is live (`body.session-active`). Card state resets when the session ends.

   - ✅ **Live find (owner session 2026-07-03): bodyweight sets rendered "0×20"** — Hanging Knee Raises readback tile showed `0×20`; the review-card set line and the opener's 0-weight edge had the same hole. FIXED same day: shared `hasLoad()` guard in `public/coach-conversation.js` (weight null/''/0 = bodyweight → "20 reps"), matching the opener's established reps-only convention.

### Atlas — Backlog › Soul Plan S7 / LT-011 reassure voice — live FAIL + deterministic fix, re-validation pending (2026-07-12)

  - ✅ **LT-011 live re-validation — PASS (2026-07-12, deployed `e94bfbb`/`pr:998`).** Re-ran the seven `POST /api/coach/chat` probes against production: explicit discouragement reaches reassure 3/3 (zoom-out + one move) even with the standing challenge pattern; the ordinary turn returns to challenge; the multi-clause-negation case reaches reassure 3/3; "bench isn't stuck anymore" does not (challenge); recovery (`source:engine`) and safety/pain (3/3 safety-first) outrank reassure. **S7 / LT-011 closed** (`docs/TEST_QUEUE.md`).

### Atlas — Backlog › Deep-review audit — 2026-07-07 — open findings

- [x] **CLIENT-1** `[trust-critical]` **(C)** — ✅ **FIXED (PR-0D, `claude/atlas-remediation-v2-phase-0-yxfp1t`)**: every saved review card now binds its write's `log_appended_range` at `markReviewSaved` time; on a newer save, older cards' live Undo is stripped from the thread, and `window.atlasUndoLastWrite(expected)` refuses at the request layer if the bound identity no longer matches the current `lastWrite` (new `window.atlasCurrentWriteIdentity()` bridge getter feeds the binding). Server undo contract (W4/W5/W7) untouched — client-targeting fix only. Minefield Map §5/§6.2 updated (intentional change to the frozen `atlasUndoLastWrite` surface). Repro + guard + strip + single-save-undo pinned in `tests/e2e/undo-stale-card.spec.js`. *(was: every saved review card kept a live "Undo" calling the global undo-last; tapping an older card deleted the newest write — `src/app/coach-conversation.js:381-389`, `src/app/app.js:6762-6796`.)*

- [x] **PARSE-1** `[trust-critical]` **(E)** — ✅ **FIXED (PR-0E, `claude/atlas-remediation-v2-phase-0-yxfp1t`)**: on the LOG path, `findExerciseInText` now rejects an anywhere-position alias match when a genuine variant qualifier immediately precedes it (`aliasIsVariantOfAnotherLift` — leading phrase only / no set-group before / preceding token name-like and not a framing/note word), so `front squat 225 5/2` → **Front Squat** (unknown-exercise path, `needs_catalog_review`), goblet/close-grip/stiff-leg likewise — never the silent base lift. Strict guard is gated to `parseLogSets` (question resolution stays lenient); natural-language logs (`do bench 225`, `Skipped warmup bench …`, `then bench …`) and at-start compounds (`incline bench`) still resolve. Golden pins in `test/parser-golden.test.js` (PARSE-1 block); full suite green. Unlisted qualifiers still defer to PR-14 (catalog-generated alias table). *(was: anywhere-match collapsed variants with zero warnings — `services/workoutTextParser.js:151`.)*

- [x] **PARSE-4** `[correctness]` **(E)** — ✅ **FIXED (F05, 2026-07-16).** Mixed-notation input no longer silently drops unconsumed set groups. A route-level gate (`services/fullConsumptionGate.js`, wired on `POST /api/parse-workout-text`) downgrades to `needs_clarification` when the raw text mixes 2+ set-notation families (slash / `x\d+@` / `\d+ for \d+`) — the shape where first-sub-parser-wins drops a group — without touching parser grammar. `bench 185 5/2 3x8@165` now asks.

- [x] **PARSE-5** `[correctness]` **(E)** — ✅ **FIXED (F05, 2026-07-16).** `bench 3x10 @2` no longer logs a 2 lb bench: the same gate refuses a barbell `NxM@W` form with `W ≤ 10` (a lifter's RIR band) when the parser emitted that tiny weight, asking "weight or RIR?" The `@`-weight grammar and the dumbbell `@RIR` reading are unchanged.

- [x] **WRITE-1** `[correctness]` **(C)** — ✅ **FIXED (F02, 2026-07-16, PR #1018).** `/api/complete-workout` now binds and returns the exact Sheets append proof (`logAppendedRange` from `logResponse.data.updates.updatedRange`) and verifies it, so closeout saves are verifiable and undoable per Invariant W3; an idempotent retry keeps an unverified proof fail-closed. Red-first route + unit tests. *(was: the closeout live write discarded append responses and returned no `logAppendedRange`, contra Invariant W3 — `index.js:3794-3895`.)*

- [x] **WRITE-2** `[correctness]` **(P)** — ✅ FIXED (F03, 2026-07-16). The closeout route reuses the server-minted `session_id` stamped in the prior idempotency record (new read-only `peekWrite`) instead of re-minting on retry, and the duplicate-session hard-stop now also covers a reused minted id, so the composite-key (Log) + duplicate-session (Effort) dedupes catch the replay. Red-first route + unit tests.

- [x] **WRITE-3** `[correctness]` **(C)** — ✅ FIXED (F03, 2026-07-16). `services/idempotency.js` tags in_progress records rehydrated from disk and `beginWrite` downgrades a stale (`>5min`) rehydrated reservation to retryable — not only at load — closing the ≤24h post-crash wedge. Red-first unit test.

- [x] **INFRA-1** `[correctness]` **(E)** — ✅ **RESOLVED (PR-22, 2026-07-08), by eliminating the class rather than patching the guard.** `public/` is no longer committed at all (gitignored build output, regenerated via a `prestart` npm hook), so there is nothing for a drift guard to miss — the whole "forgot to commit a new build file, ships green, 404s in prod" failure mode is structurally impossible now. `git diff --exit-code -- public/` replaced with a build-output-existence check in CI.

- [x] **CLIENT-3** `[correctness]` **(P)** — ✅ **ADDRESSED by F07 (CLIENT-3, shipped 2026-07-16, PR #1033):** a module-level flow-sequence token (`previewRequestSeq`, bumped by `invalidatePreview`) now drops a stale / out-of-order dry-run/preview response before it can overwrite a newer `pendingWrite` + review card. Canonical card: **F07** in [`docs/ATLAS_V1_EXECUTION_PLAN.md`](./docs/ATLAS_V1_EXECUTION_PLAN.md). *(was: no staleness guard on `pendingWrite`; an out-of-order transport-retried dry-run response overwrote a newer preview → approved write dropped the last exercise — `src/app/app.js:6325,6357,6402`.)*

- [x] **CLIENT-4** `[correctness]` **(P)** — ✅ **FIXED (F08, 2026-07-16, canonical card in [`docs/ATLAS_V1_EXECUTION_PLAN.md`](./docs/ATLAS_V1_EXECUTION_PLAN.md)).** On `/api/complete-workout` the Log rows now adopt the resolved canonical session date (`withCanonicalSessionDate` stamps `dateValue` onto each row before `enrichAndFormatLogRows`), so a screenshot-dated or backdated closeout can no longer date the Effort row on the screenshot date while the Log rows keep today's auto-fill. Server-authoritative — applies to the dry-run preview and the live write; red-first api-smoke parity tests (prior-day / month / year boundary / timezone-edge / invalid-rejected / same-day + backdated manual / live-write). *(was: screenshot-with-editor-rows save wrote Log rows dated today + an Effort row dated the screenshot date in one session — `src/app/app.js`; `index.js` complete-workout path.)*

- [x] **WRITE-5** `[correctness]` **(P, design challenge)** — ✅ FIXED (F04, 2026-07-16). `sheets.js` `isTransientAppendError` no longer retries an ambiguous 503 on `values.append` (treated like 500 / post-send timeout); only pre-write rejections (429 / 403 quota) retry in-request. An ambiguous 503 propagates and recovery defers to the upstream `write_id` idempotency + composite-key/effort-session dedupes (at-most-once, hardened by F02/F03). Red-first: 503-non-retryable + at-most-once, 429-still-retryable; the prior 503-retryable pin flipped.

- [x] **WRITE-6** `[polish]` — ✅ **RESOLVED (LT-008 hardening, owner-approved).** Malformed truthy `test_mode` values (`1`, `"yes"`, `"on"`) silently went LIVE. Added `isAmbiguousTestMode()` (`index.js`): a `test_mode` that is not omitted, a real boolean, or the string `"true"`/`"false"` (case/space-insensitive) now returns 400 on all four write handlers (`/api/log-workout`, `/api/complete-workout`, `/api/bodyweight`, `/api/log-modality`). INVARIANT W2 ("absent = live") is preserved — only ambiguous values fail closed. Owner sign-off recorded in session. Tests in `test/api-smoke.test.js`.

- [x] **LT-008 write-path input hardening** `[correctness]` — ✅ **SHIPPED (owner-approved full-auto).** Safe live break test (`/api/*`, dry-run only, zero real writes) found the write path silently accepting impossible input. Fixed in one PR alongside WRITE-6: (F1) `/api/log-workout` now rejects an unparseable `date` via `normalizeWorkoutDateStrict()` instead of writing it verbatim into `date_clean` (matches the format-guard `/api/log-modality` already had; also rejects calendar-impossible dates like `2026-13-45`); (F2) `reps` now integer-checked in `rules/validationRules.js` (`3.7` reps → 400), mirroring the existing `set_number` guard; (F4) exercise name capped at 120 chars; (F5) `/api/history/recent?limit=` clamped to `[1,200]` (was raw `Number()`, so `99999` read the whole log and `-5` sliced from the wrong end). Tests in `test/rules.test.js` + `test/api-smoke.test.js`.

### Atlas — Backlog › Deep-review audit — 2026-07-07 — open findings › PR-10 live-session regression addendum — disposition (2026-07-08)

- [x] **ADD-1** `[correctness]` — ✅ **ALREADY FIXED — regression-locked (this PR, `claude/add-1-restored-session-pin`)**. Verification gate confirmed `discardRestoredSession` already clears the restored pin from **both state and UI**: `clearSessionSnapshot()` hides `#session-resume-notice` (the "Session restored — N sets logged (X)" pin) AND removes the persisted snapshot; `endPlannedSession()` clears the active plan + re-renders (hides) its banner; the buffers (`sessionLog`/`sessionCompleted`/`sessionSavedLog`), closeout state, and `#log-session-id` are cleared, and `atlas:session-reset` is broadcast. New `test/restoredSessionDiscard.test.js` drives the real `discardRestoredSession` and asserts the pin is hidden, buffer/plan/session_id cleared, snapshot removed, and reset dispatched — no stale pin. Test-only; no runtime change.

- [x] **ADD-2** `[correctness]` — ✅ **FIXED (this PR, `claude/add-2-spoken-skip-state`)**: a spoken negation-style skip now becomes real session state. `src/app/planMutationIntent.js` gained (a) a **negation-skip** grammar rule (`"no [more] X"`), (b) a **reason-clause peel** for a leading reason before a `so/but/then/,` connector (so *"My lower back is a bit sore so no RDLs for me today"* classifies), (c) `cleanName` now strips **stacked session-scope fillers** (`"rdls for me today"` → `"rdls"`), and (d) `resolvePlanTargets` gained a **lift-code stem tier** so `"rdls"` reaches the *Romanian Deadlift* slot via `RDL01`→`rdl` (its name has no `rdl` substring). `tryApplyPlanMutation` (`src/app/app.js`) now threads `liftCode` into the fallback plan-entries so the resolver can use it. End-to-end proof in `test/planSkipWiring.test.js` (drives the real `tryApplyPlanMutation`, asserts the RDL slot leaves `activePlannedSession`) plus pure classifier/resolver cases in `test/planMutationIntent.test.js`. A non-matching `"no X"` (e.g. *"no thanks"*) still falls through to the coach. Full suite green (4710).

- [x] **ADD-7** `[correctness]` — ✅ **FIXED (PR-11, `claude/pr-11-conversation-hardening`)**: the Flight Recorder's `latestCoachMessage()` reported `coach_message:""` because it read the last `.coach-msg` even when transiently empty (a receipt-only block, a suppressed message, or a bubble mid-typewriter). It now walks back to the last **non-empty** bubble (preserves the last valid message per addendum #7) and returns `null` (never `""`) when none exists; `finalizeSetVoice` also normalizes an empty/whitespace model reply to `null` so `/api/coach/message` never serializes `message:""`. Pinned in `test/flightRecorderCoachMessage.test.js`.

### Atlas — Backlog › Deep-review audit — 2026-07-07 — open findings › PR-11 — conversation reliability & plan-mutation hardening (2026-07-08)

- [x] **PR11-2 — plan-mutation intent** `[correctness]` — ✅ FIXED: "swap next workout for dips" removed Dips instead of making Dips next. `classifyMutationIntent` now distinguishes destination/source/removal/substitution — positional targets ("next/current exercise/one") resolve to the current slot, destination-only swaps ("swap to/for X") substitute into the current slot, and "remove/take out/get rid of X" classify as skip (`src/app/planMutationIntent.js`, `src/app/app.js`, `test/planMutationIntent.test.js`).

- [x] **PR11-3 — duplicate recap** `[polish]` — ✅ FIXED: the readback tile listed identical sets individually; a shared `groupConsecutiveSets` now collapses them to "225×5 RIR2 ×3" in both the readback and the review card (`src/app/coach-conversation.js`, `test/coachConversation.test.js`).

- [x] **PR11-4 — blank coach message** `[correctness]` — ✅ FIXED: see ADD-7 above.

### Atlas — Backlog › Coaching reactions — 2026-07-11 (every logged lift gets one concise reaction; history-aware facts)

  - ✅ **[correctness] Harden the Sheets-down fallback so a forged `progression_history` / `rec.progression_verdict` can't survive (CODEX review note, PR #988) — shipped.** `routes/coachOps.js` now fails closed on the enrichment `catch` path, mirroring the `layoff` / `athlete_identity` / `engineNewGround` discipline: `progression_history` is captured from a SUCCESSFUL enrichment and ALWAYS overwritten (engine value, or null on a Sheets read/enrichment failure), and on a genuine enrichment failure the client's unconfirmed `rec.progression_verdict` is nulled — so neither a forged checkpoint nor a forged verdict can reach the prompt in the Sheets-down window. The successful enrichment path is unchanged (engine value flows, `rec` untouched); endpoint stays read-only; no Sheets schema / write path / GATE A / coaching-policy change. Tests (red-first): `test/coach-message-block.test.js` (a simulated Sheets-read failure nulls a client-forged `progression_history` + `rec.progression_verdict`; the successful path is preserved) + updated the `athlete_identity` source-pin to tolerate the extra field.

### Atlas — Backlog › Bug remediation — 2026-07-02 owner live re-test (recovery-session scold, 2nd recurrence of BUG-20260629-204817)

  - [x] **Owner live re-test — PASSED 2026-07-02 10:58** (post-merge #788): owner ran the same Recovery/Pump session shape. OHP 90×12@RIR5 → "Dialled in — that landed right on target" + "Next time: On target — keep 90 and chase 13 reps next set" (load held); Seated Row 145×13@RIR4 → on target. No "add weight" / "below your range" scold, no load bump. Lane closed.

### Atlas — Backlog › Multi-workout composer (owner 2026-06-25: "log two or more workouts at once")

    - ✅ **RESOLVED (PR #593)** — review note PR #590 #2: `analytics.recommendNextSet`'s immediate bump anchor (`lastSet`/`priorSet`) now prefers **working sets** (`workingRows = rows.filter(!isWarmupNote)`, falling back to all rows only if every row is a warm-up), so a warm-up logged *last* can't drive "increase to X". Behavioral control-paired test added.

    - ✅ **RESOLVED (PR #593)** — review note PR #590 #3: `sessionBestByLift` + `historicalBestByLift` now skip note-tagged warm-ups, so a heavy tagged feeler no longer registers a false personal record; the PR baseline is working sets only.

    - ✅ **RESOLVED (PR #593)** — review note PR #593: the last two LIVE PR readers also skip tagged warm-ups — `detectRecentPrs` (GET `/api/prs/recent`) filters them at the row level, and `buildWeeklyReport`'s weekly-PR block skips them in both the this-week and prior-week best maps. With these, **all** progression/trajectory/PR surfaces (benchmark, expected-perf, coachMemory, trendDetector, recommendNextSet band+anchor, sessionBest/historicalBest, detectRecentPrs, weekly report) exclude note-tagged warm-ups; volume still counts them.

### Atlas — Backlog › Coach render-order & completion-flow — owner live session 2026-06-25 (2nd run, investigation)

  - **RC1 — preview ≠ write (silent row drops).** The preview showed 7 exercises / 30 sets / 33,885 lb, but the sheet saved only **22** Log_Cleaned rows for `20260628-PM-01`, with **Bench Press dropped entirely** and Seated Row written ×3 (preview showed ×6). Root: the `/api/log-workout` row-level composite-key dedup (`session‖exercise‖set_number`) ran ONLY on the live write; the `test_mode` dry-run returned the RAW `formattedLogRows`, so the preview promised rows the live write then silently skipped (`index.js`). **✅ FIXED (this PR — preview-write parity):** extracted `partitionLogRowsByExisting(formattedRows, existingLogKeys)` and applied it in BOTH the dry-run preview and the live write (shared code = guaranteed parity), so `log_rows_preview` now shows exactly the rows that will be appended and surfaces `skipped_duplicates`. Tests: `test/api-smoke.test.js` "FB parity: …" (preview applies dedup; preview count == live write count). Server-only; no trust-loop/proof-field/schema/parser change.

### Atlas — Backlog › Atlas voice & interaction (historical lane — voice work now governed by the Soul Plan under the V1 execution plan)

    - ✅ **WIRING GAP — skip-notation path** _(resolved [#338](https://github.com/tc5v5s64ym-sketch/atlas-workout-updater/pull/338))_: parser now extracts `prescribed: [{exercise, reason}]` from inline "X skipped - reason." sentences; frontend passes it through to the `/api/log-workout` dry-run payload so `buildSubstitutionPreviews` fires.

      - ✅ **Plan-prescribed wiring** _(resolved PR 341)_: `services/planMatcher.js` → `inferPrescribedPairs` infers substitution pairs from `activePlannedSession` when the user didn't write an explicit skip sentence. `index.js` test_mode block accepts `plan_exercises` from the payload and merges inferred pairs into `prescribedList` before calling `buildSubstitutionPreviews`. `public/app.js` attaches `plan_exercises` from `activePlannedSession.exercises` to both the session preview payload and the mid-session substitution check.

      - ✅ **`planMatcher` identity is name-only — lift_code guard** — fixed. `inferPrescribedPairs` now runs a Step 1b `lift_code` identity pass after the exact-name pass: equal non-null `lift_code` (case-insensitive) marks the planned lift fulfilled even when names differ (plan `"Bench Press"` vs logged `"Barbell Bench Press"`), so Step 2 no longer mislabels the same lift as a substitution. Differing codes / one-sided codes fall through to the existing name/pattern logic unchanged. Dry-run only — no data risk. 5 golden tests added; full suite green.

        - ✅ **Surfaced into the coach voice (was dark)** — `scoreSubstitutionQuality`'s tier is now attached in `buildSubstitutionPreviews` (`index.js`) → `sub.quality`, bounded by `sanitizeSubstitution` (enum `excellent|acceptable|poor|unknown`, else null), and worded by the reaction + verdict-reaction prompts per owner-approved rules: **poor → volunteer** what muscle/intent the swap missed; acceptable → one-line tradeoff; excellent → brief acknowledgement; and **never call a swap good / say it "counts" unless the engine tier is excellent/acceptable** (absent/unknown → no quality judgement). Owner-approved wordings in `COACH_VOICE_VALIDATION.md`. 3 coach tests + the existing suite (2086) green.

        - ✅ **Resolved (decision, PR #439) — `computePrimaryMuscleOverlap` directionality is the correct semantics, consciously accepted.** It returns the fraction of *prescribed* primaries covered by the *logged* set — which is exactly the question substitution quality asks: "did what you actually did cover what was planned?" The direction is fixed by the real scenario (prescribed = the plan, logged = what was performed); the "reverse" pairing never occurs in use. So the asymmetry is not a bug and a symmetric overlap (Jaccard) would be *wrong* here (it would dilute "logged covers prescribed" with prescribed-not-in-logged). Gate from #431 closed: the tier is now surfaced (PR #439) with this intent documented; no code change needed.

    - ✅ **Owner-approved Standard-tier wording (voice reference)** — `docs/COACH_VOICE_VALIDATION.md` "Extra-work voice" section (owner-approved 2026-06-20, Owner-Review Pack #1) pins the proactive rule (volunteer; Standard threshold `extra ≥ 2` or any unprogrammed exercise; silent on `has_extra:false`), the engine-fact ceiling (set counts + names only — no fabricated fatigue/effort), and the D1–D4 wordings. This is the merge gate for the wiring PR's coach tests; docs-only, no production code.

  - ✅ **Engine slice (deterministic guard, unwired):** `services/messageIntent.js` — pure `classifyMessageIntent(rawText, { hasResolvedLift, hasSetTokens })` → `{ isQuestion, suppressLog, reason }` where `reason` ∈ `question | unresolved_lift | null`. Encodes the floor: a real resolved set always logs (even if it also asks something — the "Crushed 235x8, should I go up?" both-case); a pure question is answered and logs nothing; an unresolved lift is not saved (ask which one). `isQuestionText` is conservative — a bare past-tense log ("did 225x5") is not a question, and openers are `\b`-anchored so they can't fire on lift names ("arnold press" ≠ "are"). 11 golden tests in `test/messageIntent.test.js`, including the documented 2026-06-16 "Didn't you suggest 225 5/2 x3" phantom-set bug. No parser/save-path/LLM edit.

  - ✅ **Wiring — rule (a), the question→phantom bug (owner-scoped):** `parseWorkoutText` (`services/workoutTextParser.js`) now applies `classifyMessageIntent` after `parseLogSets`; when the message reads as a question and did NOT resolve to a real set, it returns `intent:'question'` (`warnings:['logged_nothing_question']`) instead of letting `parseUnknownExercise` fabricate a logged set. The frontend already treats any non-`log_sets` intent as "show message, build no rows" (`rowsFromBackendParsedWorkout`, `public/app.js:2467`), so **no `app.js`/`index.js` edit was needed** — no phantom row reaches the write path. Safety property: a genuinely resolved set (real lift + tokens) is never suppressed, so the both-case ("Bench 235 8/2, should I go up?") still logs. 6 golden tests in `test/parser-golden.test.js` (incl. the exact 2026-06-16 bug). Full suite 2041 pass.

### Atlas — Backlog › Near-term

  - ✅ **(1) session-volume profile engine** — `services/sessionVolumeProfile.js::buildSessionVolumeProfile(logRows, { today, windowDays=120, minSessions=6 })` returns the lifter's own `{ exercises_per_session, compounds_per_session, accessories_per_session, session_cap (75th pct), sessions_analyzed, confidence }` (median distinct exercises/session split by `classifyLiftRole`), or `null` below `minSessions` (→ keep generic caps). Pure, read-only, unwired. Motivated by a live "Build Strength" day showing 1 compound + 5 accessories (felt bloated). Golden tests in `test/sessionVolumeProfile.test.js`.

    - ✅ **`fix_blind_spots` AC1 brief vs the cap — fixed** `[polish]` (2026-06-24): the brief now recomputes covered patterns from the **final** exercise list (after `structureSession`'s role-order + density cap + lower-body cap + dose) instead of `buildIntentSession`'s pre-structure `session.coveredPatterns`, so a capped/dropped accessory can never leave a coarse pattern named in the brief without a matching exercise. `scheduledFresh` filters against `finalCoveredPatterns` (mapped via `allRecs` lift_code → pattern). Invariant guard test in `test/analytics-edge.test.js` (every overdue pattern surfaced in the brief has a same-pattern exercise in the final session). Full suite green (2600). No write-path / schema / proof-field / parser / LLM change.

    - ✅ **`capRecoveringPatternDensity` passes the exercise muscle_group** `[polish]` — *shipped.* `capRecoveringPatternDensity`'s `roleRank` (`services/analytics.js`) now calls `classifyLiftRole(ex.exercise, ex.muscle_group)` instead of an empty group, so when a recovering pattern is thinned to its highest-priority movement a keyword-less accessory is ranked as `accessory` (and dropped before a real lift) rather than name-only `secondary`. Completes the muscle_group threading across the engine's session consumers. Pure engine; no write-path / schema / proof-field / parser / LLM change. Source-introspection guard test in `test/analytics-edge.test.js`. Full suite green (2385).

### Atlas — Backlog › Then › AUDIT.md reconciliation — still-live findings (verified against main 2026-06-20)

  - ✅ **ME-5 — unbounded `/api/complete-workout` body** `[correctness/security]` (2026-06-24) — two guards added: (1) multer `limits.fieldSize = 512 KB` so the `log_rows_json` *field* (not a file, so `fileSize` never bounded it) can't reach `JSON.parse` as a multi-megabyte string; (2) after the array check, reject `parsedLogRows.length > 200` with a 400 (`standardError`, temp file cleaned up) so an oversized array can't drive unbounded row-by-row enrichment (DoS). 200 is a generous ceiling (a real session is tens of rows); 512 KB is far above a real payload. Write-path-adjacent route, but **guard-only** — no change to the write logic, `test_mode`/proof fields, enrichment, or trust loop. The error middleware also now maps multer's `LIMIT_FIELD_VALUE` (an over-`fieldSize` field) to a clean **413**, matching the existing `LIMIT_FILE_SIZE` path instead of falling through to a generic 500 (PR #553 review). api-smoke tests: a 201-row payload → 400 (`200-row limit`); a >512 KB field → 413; both with **no append**. Full suite green (2600).

  - ✅ **ME-9 — `propose_edit` numbers validated server-side (defense-in-depth)** `[trust-critical]` (2026-06-24) — `isValidEditSchema` (`services/coach.js`) now validates any **present** `weight`/`reps`/`rir` in an `add_set`/`update_set` proposal via new `editNumbersValid`: weight (lb) finite & `0 < w ≤ 2000`, reps integer `1..100`, rir finite `0..10` — rejecting NaN/Infinity/negative/zero/string/out-of-range. Each field is checked **only when present** (a bare `add_set` and a partial `update_set` stay valid — the client still bounds index vs visible rows and the write path re-parses; this is an added guard, not a replacement). A reply proposing an absurd number is stripped to prose with `propose_edit:null` and never reaches the approval gate. Reclassified autonomous (Escalation Policy v2: strengthens the trust contract, doesn't amend an INVARIANT). Tests in `test/coach.test.js` (valid numbers pass on each action; garbage weight/reps/rir rejected; back-compat for absent fields; end-to-end parser drop). Full suite green. No write-path / schema / proof-field / trust-loop change.

  - ✅ **ME-10 — vision `ATLAS_LLM_MODEL` honored on OpenAI** `[housekeeping]` (2026-06-24) — `getProviderConfig`'s OpenAI branch now returns `model: (ATLAS_LLM_MODEL || 'gpt-4.1-mini').trim()` and `parseWithOpenAI` uses `config.model` (was hardcoded `gpt-4.1-mini`). Documented default unchanged, so behavior is identical unless the env var is set. Reclassified autonomous (Escalation Policy v2: makes an already-documented env mechanism work — correctness, not a model-selection change; the default model is untouched). Tests in `test/vision.test.js` (default + override). Suite green.

  - ✅ **ME-11 — vision JSON mode + fence strip + abort timeout (both providers)** `[correctness]` (2026-06-24) — OpenAI Responses call adds `text:{format:{type:'json_object'}}` and now strips code fences defensively before parse; Gemini call adds `config:{responseMimeType:'application/json'}`. Both run through new `withVisionTimeout(fn, ms)` which threads a real `AbortSignal` into the SDK (`{signal}` for OpenAI, `config.abortSignal` for Gemini), surfaces a clear "timed out" error, and always clears the timer. Timeout is vision-appropriate (default 30s, `ATLAS_VISION_TIMEOUT_MS` override) rather than the coach's 8s — image parsing is heavier, and an over-aggressive cap would false-fail normal screenshots (the goal is bounding a hung call, not racing a fast one). Reclassified autonomous (defensive parsing/reliability, not product scope). Tests: fence strip, timeout abort + clear error, non-timeout error passthrough, signal threading. Suite green.

  - ✅ **ME-12 — `dotenv` hard `require` at load** `[housekeeping]` (2026-06-24) — `index.js:1-2` no longer hard-requires `dotenv` (`const dotenv = require('dotenv'); dotenv.config();`), which would crash module load if `dotenv` were absent. Now `try { require('dotenv').config(); } catch { /* env injected by the host */ }` — dotenv is a local-dev convenience only; Render injects env vars directly. Source-introspection guard test in `test/unit.test.js` (no hard top-level require; `dotenv.config()` wrapped in try/catch). Full suite green (2598). No write-path / schema / proof-field / trust-loop change.

  - ✅ **New nit — `previewTestRows` hardcodes `/session-2026/`** `[housekeeping]` (2026-06-24) — generalized the test-row filter in `previewTestRows` (`services/analytics.js`) from the literal `/session-2026/i` to `/session-20\d\d/i`, so the admin-only read diagnostic (`POST /api/admin/preview-test-rows`) keeps matching `session-20YY`-style test/manual session IDs past 2027 (the `/test/i` notes + session_id branches are unchanged). Read-only diagnostic — no write-path / schema / proof-field / trust-loop / coach change. Regression test added in `test/unit.test.js` (session-2027 + session-2030 ids matched).

  - ✅ **New nit — zero `timeout-minutes` across the CI workflows** `[housekeeping]` (2026-06-24) — added a per-job `timeout-minutes` to every job across all 8 workflows (11 jobs): `ci.yml` (unit-tests 15 / secret-scan 10 / e2e-tests 20 / render-smoke 10), `claude-code-review` 20, `atlas-decision-desk` 15, `codex-decision-desk` 15, `monitoring` 15, `labeler` 10, `labels` 10, `merge-card-check` 10. A hung fetch/step can no longer run to GitHub's 6h default. CI-config only — no app/source/test change; YAML validated, every job covered.

### Atlas — Backlog › Future epic

  - ✅ **Resolved (PR 362)** — `facts.deviation`: `classifyDeviation` is now called with the top-weight set from `todaySets` + `computeExpectedPerformance` output in `services/liveIntelligence.js::enrichCoachFacts`, and `POST /api/coach/message` enriches the facts server-side before handing to `sanitizeFacts`. 13 golden-fixture tests in `test/liveIntelligence.test.js`.

  - ✅ **Resolved (PR 363)** — System-prompt instructions added for `deviation` wording and RIR anti-fabrication: model must derive effort from `effort_verdict` only (RIR 2 is NOT failure); deviation instruction names all four verdicts and forbids single-session fatigue calls; `last_working_sets` added as a valid historical grounding source. 11 prompt-rule tests in `test/coachPromptRules.test.js`.

  - ✅ **Resolved (PR 362)** — `facts.evidence_context`: assembled from `computeBenchmark` + `computeExpectedPerformance` basis in `enrichCoachFacts`. `reference_sets` and `date_range` are empty/null for now (expectedPerformance does not expose its session list); only `benchmark` + `confidence` are populated. Sufficient for the coach to cite a working weight and confidence level.

  - ✅ **Resolved (PR 362)** — `facts.working_weight`: `resolveWorkingWeight` called in `enrichCoachFacts`; result merged into `rec.working_weight` before `sanitizeFacts` runs.

  - ✅ **Resolved (PR 362)** — `facts.trend`: `detectTrend` called in `enrichCoachFacts`; result merged into `rec.trend`.

  - ✅ **Resolved (PR 358a)** — `plan_completed` is now tracked in `sessionCompleted` (client-side, per-session) and forwarded in `routeMessageToCoach` as `context.plan_completed`. The server can now compute `plan_state` including `isComplete` from real completed exercise data. Session buffer cleared alongside `sessionLog` on save and startOver.

  - ✅ **Resolved (Roadmap Step 359)** — `services/planActionClassifier.js`: deterministic plan-action classifier. `classifyPlanAction({planned, completed, loggedName, loggedLiftCode, skippedName})` returns `{action, prescribedExercise, skippedAhead}` where action ∈ `on_plan | reorder | substitute | add | skip`. Matching: exact name (case-insensitive) then lift_code for on_plan/reorder; `inferPrescribedPairs` (pattern/region) for substitute; anything else is add. Pure function, no I/O. 22 regression tests (all pass). Does not modify parser, confirmation-card, or session-closeout paths.

  - ✅ **Resolved (Roadmap Step 360)** — Session closeout: `services/sessionCloseout.js` (`computeCloseout`) detects when all planned exercises are complete. `emitSetLogged` in `public/app.js` computes `planIsComplete` and includes it in the `atlas:set-logged` event. `handleSetLogged` in `public/coach-conversation.js` renders a session wrap-up message ("That's your plan done — N exercises complete. Say \"done\" to save.") and updates the composer placeholder when the plan is complete. Pure engine logic is testable via the service; 15 regression tests (all pass). Does not trigger a write — the user must still say "done" or take a screenshot.

  - ✅ **Resolved (Roadmap Step 364 / GitHub PR #366)** — Out-of-order closeout trust bug: plan [A,B,C] completed as A→C→B showed "next up: C" after B because `getNextExerciseInPlan('B')` returned C and the `else if (planIsComplete)` guard was never reached. Fix: moved the `planIsComplete` check BEFORE `getNextExerciseInPlan` so closeout always wins when the plan is done, regardless of completion order. Uses `detail.planIsComplete` already emitted by `emitSetLogged`. `isPlanComplete` convenience wrapper added to `services/sessionPlanExecutor.js`; 5 regression tests added to `test/sessionPlanExecutor.test.js`.

  - ✅ **Resolved (PR 364)** — `repeated_substitution` now fires in production: `services/substitutionHistory.js::buildSubstitutionHistory` infers substitution events from `Log_Cleaned` rows (modal-lift heuristic with three false-positive guards: MIN_SESSIONS_FOR_USUAL=3, DOMINANCE_RATIO=2.0 rotation guard, returns-later program-change guard, co-occurrence accessory guard); `buildChatContext` in `index.js` calls it and passes the result as `substitutionHistory` (per-lift filtered, case-insensitive) to every `detectPatterns` call. 14 golden-fixture tests in `test/substitutionHistory.test.js`, including integration test confirming `repeated_substitution` fires with 3+ substitutions, plus accessory co-occurrence regression test.

  - ✅ **Resolved (PR 367)** — `reason_codes` field added to every intent emitted by `scoreIntents` in `services/analytics.js`: short machine-readable snake_case strings (e.g. `high_fatigue`, `well_rested`, `lower_overdue`, `stall_detected`, `goal_alignment`) that tell callers *why* the intent was scored without requiring them to parse `why_today` prose. Goal-alignment post-processing appends `goal_alignment` when a positive bonus fires. `buildSuggestedWorkout` now exposes `topIntent.reason_codes` instead of `topIntent.why_today` in its output. 4 new tests in `test/suggestedWorkout.test.js` verify: codes match snake_case pattern (≤30 chars), `well_rested` fires when gap ≥ 2 days, `goal_alignment` fires under matching goal. Direct `scoreIntents` test in `test/analytics-edge.test.js` verifies `fix_blind_spots` carries `*_overdue` codes.

  - ✅ **Resolved (PR 368)** — Trend-aware recommendation scoring: `scoreIntents` now computes `downwardLifts = allRecs.filter(r => r.e1rm_trend === 'down')`. When 2+ push/pull compounds trend down: `build_strength` score −15 and adds `multiple_trending_down` reason_code; `test_progress` score −15 and adds `multiple_trending_down`. `recovery_pump` gets +15 bonus and `multiple_trending_down` code. `short_session` gets +8 bonus and `multiple_trending_down` code. 2 new tests in `test/analytics-edge.test.js` verify: `build_strength` scores lower on declining data than ascending, and `recovery_pump` scores higher on declining data than flat.

  - ✅ **Resolved (PR 369)** — Readiness-aware dose adjustment: `scoreIntents` now applies `applyReadinessDose()` to the exercise list for the four training intents (`build_strength`, `build_muscle`, `fix_blind_spots`, `balanced`). When a muscle group pattern is `fatigued` (recovery < 0.36 — typically trained in the last day or so), exercises for that pattern have `target_sets` reduced by 1 (min 2) and receive `readiness_note: 'volume_reduced_fatigued'`. Recovery/test/deload intents (`recovery_pump`, `short_session`, `test_progress`, `deload_and_reset`) are unaffected — they already have special volume handling. 2 new tests in `test/analytics-edge.test.js` verify: fatigued push exercises in `build_strength` have reduced sets and the `volume_reduced_fatigued` note; non-fatigued exercises carry no readiness note.

  - ✅ **Resolved (PR 370)** — Coach Confidence Layer: every exercise object emitted by `scoreIntents` (via `exForPatterns` in `services/analytics.js`) and by `buildIntentSession` (in `services/sessionBuilder.js`) now carries `confidence_factors: { sessions, data_age_days, trend, lift_confidence }`. `sessions` = number of distinct sessions analyzed; `data_age_days` = days since the lift was last trained; `trend` = e1RM trend string (`'up'|'down'|'flat'|'insufficient_data'`); `lift_confidence` = overall engine confidence (`'high'|'medium'|'low'|'none'`). Also added to the `test_progress` intent's direct exercise builder (which builds from `trendingFresh` without going through `exForPatterns`). `recovery_pump` and `short_session` inherit `confidence_factors` via `...ex` spread. 2 new tests in `test/analytics-edge.test.js`: shape test (every exercise in every intent carries the required fields with correct types) and data-quality test (6-session bench fixture produces `sessions=6`, `trend='up'`, valid `lift_confidence` string).

  - ✅ **Resolved (Roadmap Step 371)** — Both legacy trend fields removed from `sanitizeFacts`: `e1rm_trend` (old short-window string, superseded by `trend`) and `sessions_analyzed` (redundant with `trend.sessions_analyzed`). System prompt updated to name `trend` as the authoritative e1RM trajectory signal; `coachPromptRules.test.js` updated with a test confirming the rule. Both removals address the same step-351 ambiguity.

  - ✅ **PR 1.1** — `services/muscleCoverage.js`: `musclesFor` / `liftsForMuscle`, 17-muscle taxonomy, pattern-based, pure data (31 tests).

  - ✅ **PR 1.2** — `services/muscleVolume.js`: `weeklyMuscleVolume`, rolling-window volume per muscle, direct 1.0 + indirect 0.5 credit (16 tests).

  - ✅ **PR 1.3** — `services/movementPattern.js`: `patternFor`; 14-pattern vocabulary (squat/hinge/push/pull/isolation/trunk/carry/other); pattern-based, pure data (20 tests).

  - ✅ **Hold Point 1** — owner reviews coverage map + per-muscle volumes against real log before behavior changes. _(Sonnet → Opus 4.8 after hold)_

  - ✅ **PR 2.1** — Coverage-aware stall/deload: `coverageStalls.annotateStallsForDeload`; accessories downgraded when primary muscles covered by other lifts; `weeklyMuscleVolume` extended with `today`/`excludeLiftCode` options (929 tests).

  - ✅ **Hold Point 2** — live-app test confirmed: Face Pull correctly feeds deload (rear_delts 1.5 eff. sets < 2.0 threshold); OHP main stall feeds independently. Coverage logic verified correct.

  - ✅ **PR 3.1** — `services/underCoverage.js`: `computeUnderCoverage`; per-muscle status (`under`/`adequate`/`optimal`) + reason string vs. MEV-style target ranges (950 tests).

  - ✅ **PR 3.2** — Surface under-coverage in coaching chat: `muscle_gaps` (sorted by severity) wired into `buildChatContext` + `sanitizeChatContext`; LLM nudges 1–2 under-served muscles when asked what to train (952 tests).

  - ✅ **Hold Point 3** — gap nudges reviewed and approved in live conversations. Wording reads naturally; style confirmed.

  - ✅ **PR 3.3 — expectation verdict engine** — `computeExpectationVerdict` in `analytics.js`; emits `{ outcome, why, prescribedRir, actualRir, rirDelta }` per set; outcome ∈ beat/met/fell_short/swap; rirDelta sign convention: negative = pushed harder; golden fixtures for all 4 outcomes (29 tests). Pure data, nothing surfaces it yet.

  - ✅ **PR 3.5 — swap detection + working-weight finder** — `detectSwap` (case-insensitive name comparison, null-safe) + `buildWorkingWeightProtocol` (calibration protocol; 70% reference anchor → nearest 5 lb startHint; "Start conservative" when no reference); feeds `computeExpectationVerdict` swapped flag; pure data (36 tests).

  - ✅ **PR 3.x — systemic-cost tier** — `services/liftCost.js`: `costFor`; HIGH/MEDIUM/LOW by name pattern; pure data (16 tests).

  - ✅ **PR 3.x — balance signal** — `services/balanceSignal.js`: `computeBalanceSignal`; antagonist volume-ratio engine for 4 pairs (horizontal push:pull, vertical push:pull, anterior:posterior, quad:hamstring) → `{ pair, aSets, bSets, ratio, status, reason }`; wide bands; pure data (39 tests).

  - ✅ **PR 4.0** — `services/profileGoal.js`: `getProfileGoal()` reads `ATLAS_PROFILE_GOAL` env var, normalises through goal vocabulary (aliases included), returns null when absent; wired into `/api/recommendation/preview` as stored-goal fallback behind per-request `?profileGoal=` (23 tests).

  - ✅ **PR 4.1** — Goal- + coverage-aware ranking: `scoreIntents` enriched with `goal` option + under-coverage signal; GOAL_BONUS table for 8 goals; coverage-gap bonus for `fix_blind_spots` and hypertrophy `build_muscle`; `build_strength` includes lower/hinge when goal=strength + legs fresh; `/api/plan/intent-recommendation` wired to `getProfileGoal()`; `options.underCoverage` override for test isolation (15 tests).

  - ✅ **PR 4.2 — session builder** — `services/sessionBuilder.js`: `buildWarmupRamp` (50 %/70 %/85 % ramp, priming-flagged), `isBlockedPair` (fine-grained pattern + HIGH-cost guard; blocks Deadlift+RDL, allows Squat+Deadlift), `buildIntentSession` (anchor → support → balance; de-dup by name + code; isolation cap ≤1 per muscle; balance slot from full allRecs pool); wired into `fix_blind_spots`, `build_muscle`, `balanced` in `analytics.js`; AC1 fixed (focus/why_today filtered to scheduled patterns only); 47 tests.

### Atlas — Backlog › Operational resilience — NEEDS TRIAGE (not yet scoped)

- **Render cold start kills the first request after idle — diagnosed live 2026-07-02** `[correctness]` — Owner live re-test (IMG_5450, 9:27): the first screenshot attach of the session died with **"Preview failed: Load failed"** (iOS Safari's transport-level fetch failure). Diagnosed with an authed dry-run against production: the first `/api/complete-workout` request took **36s wall-clock with only ~3s of server processing** (cold instance spin-up + a slow first vision call); the next three requests took 1.4–5.9s. Mobile Safari gives up on a hanging upload → "Load failed." The trust loop held (nothing saved, honest failure) but the copy is unhelpful and the pattern WILL recur — the owner's usage (idle between gym sessions) means the first interaction of most sessions risks a cold instance. Mitigations (a)+(b) ✅ SHIPPED 2026-07-02 (composer-first Phase 0b): dry-run-only retry-once in `api()` + the honest waking-up copy at both Preview-failed surfaces; (c) remains an owner infra decision. Original options: (a) **client retry-once on transport failure for DRY-RUN previews only** — safe (test_mode is idempotent, no write; the live write must NEVER auto-retry) and closes most of the visible failure; (b) friendlier network-failure copy ("The server was waking up — try again" instead of "Load failed"); (c) keep-alive ping or a paid Render tier (owner infra decision). Related-but-separate: the LLM provider-resilience item below (#586) covers provider failures; this is instance cold start.

### Atlas — Backlog › Test-coverage hardening — make Atlas safer without building new behaviour (owner-approved 2026-06-29)

  - ✅ **PR2d — `/api/constraints` 503-until-tab-exists** `[trust-critical]` (shipped) — the one real gap: constraints had GET/POST/validation/idempotency/manifest tests but no test for the documented 503-until-Constraints-tab-exists path. Added to `test/api-smoke.test.js` (+ a `hideConstraintsTab` flag in the fake `getSpreadsheetTabs`, mirroring `hideModalityLogTab`/`hideDeloadStateTab`): asserts 503 + "Constraints tab not found" message + nothing written, AND that the 503 path releases the `write_id` (`failWrite`) so a retry after the tab exists is not a duplicate and writes exactly once. Verified `index.js:1988` already releases the write_id — no bug. Test-only; no `index.js` change.

  - ✅ **PR4a — frontend trust-loop proof-gate tests** `[correctness]` (shipped) — `test/trustLoopProof.test.js` gives the three pure trust-loop functions in `public/app.js` their first BEHAVIORAL coverage (previously only source-string matched in `unit.test.js`): `previewProofFromResult` (correct `result.data` vs nested `result.data.data` per mode + timestamp), `pendingWriteHasPreviewProof` (the approve gate — accepts a valid dry-run proof for manual/modality/screenshot/effort-only; rejects missing write/proof, `test_mode!==true`, `sheet_written!==false`, `no_write_confirmed!==true`, and `sheet_write!=='skipped'` — the live-write guard), and `pendingBodyweightHasPreviewProof`, plus a round-trip (a captured dry-run proof always satisfies the gate). Functions extracted via `new Function` (same technique as the `activeSession*` tests). Test-only; `app.js` not modified.

### Atlas — Backlog › Housekeeping

- **Close obsolete PR [#288](https://github.com/tc5v5s64ym-sketch/atlas-workout-updater/pull/288)** (superseded by [#290](https://github.com/tc5v5s64ym-sketch/atlas-workout-updater/pull/290)). ✅ Already closed 2026-06-16.

- ~~**API key in `localStorage`** `[housekeeping]`~~ — ✅ **ADDRESSED (F04C, 2026-07-16).** The durable owner session (`docs/OWNER_SESSION.md`) exchanges the key for a signed HttpOnly `atlas_session` cookie and removes the raw key from `localStorage` once sessions are enabled (`ATLAS_SESSION_SECRET`). JS can no longer read the credential, closing the XSS-exposure vector. The header path remains only for trusted scripts and the bounded migration window.

- [x] **JSON `/api/log-workout` `log_rows.length` upper bound** `[housekeeping]` — ✅ **RESOLVED (LT-008 hardening).** The `MAX_LOG_ROWS` (200) cap now guards `log_rows.length` on the JSON handler too, so the write/verify/undo ceiling is bounded by one constant end-to-end (a >200-row JSON write is rejected 400 up front instead of appending un-verifiable/un-undoable rows). Test in `test/api-smoke.test.js`.

### Atlas — Backlog › Multi-style training support — PLANNING (owner-gated, not active) › Training Intelligence Implementation Series (owner-approved slices)

  - ✅ **PR-A (engine, GitHub #478)** — pure `services/setEffortSignals.js` (`analyzeSetSequence`/`assessNextMoveConflict`) + fixtures, unwired. *Merged.*

  - ✅ **Wiring slice 1 — pure adapter** *(shipped)*: `services/trainingIntelligenceAdapter.js` — `profileForGoal(goal)` (TRAINING_GOALS → governor/scorer PROFILE) + `modalityCategoryFor(name)` (exercise → governor/router MODALITY_CATEGORY). Bridges live data to the engine vocabularies so later slices can feed `stimulusGovernor`/`fatigueRouter`/`recoveryDeloadSelection`/`objectiveScorer` from real signals. Pure/unwired; no I/O/schema/write/LLM change. Tests: `test/trainingIntelligenceAdapter.test.js`. Full suite green.

  - ✅ **Wiring slice 2 — profile-aware governor grade on the set-reaction** *(shipped)*: `computeSetEffortExtras` (`index.js`, read-only `/api/coach/message` set path) attaches a `set_grade` fact = `gradeStimulus({ profile: profileForGoal(getProfileGoal()), modalityCategory: modalityCategoryFor(exercise), rir: hardest logged RIR, target_rir, is_heavy_compound: analysis.is_compound })` → `{ profile, effort_interpretation, progression_verdict, fatigue_signal }`. Additive read-only fact riding along every response path (incl. LLM-down); does NOT change the existing `voiceBase`/message (slice 4 words it). Best-effort; null for non-weighted/cardio input. No write-path / proof-field / schema / parser / LLM-prompt change. Tests: api-smoke real-route (`set_grade` shape + cardio→null). Full suite green.

  - ✅ **Wiring slice 3 — voice the governor grade** *(shipped)*: the set-reaction coach now WORDS `set_grade`. `index.js` threads the slice-2 grade onto the coach facts as `stimulus_grade` (set path only); `services/coach.js` whitelists it (`sanitizeStimulusGrade` — bounded to the §481 PROFILES / PROGRESSION_VERDICTS / FATIGUE_SIGNALS enums) and `buildCoachSystemPrompt` gains one narrow VOICE rule: respect the profile-aware read (hold/back_off → don't push; high fatigue → flag recovery; **general_fitness → don't celebrate grinding to failure**), consistent with `effort_verdict`, never invent a number. Read-only `/api/coach/message`; no write-path / schema / parser change; no public-asset/SW change. Tests: `coach.test.js` (prompt rule + `sanitizeStimulusGrade` + `sanitizeFacts` thread-through). **PR 484 core delivered: the new governor fact is wired (slice 2) and voiced (slice 3).**

  - ✅ **Wiring slice 5 — voice the recovery/deload SELECTION (conservative)** *(shipped, owner-authorized 2026-06-23)*: the set-reaction coach now WORDS the convergence-based recovery decision (PR 485 `assessRecoveryDeload`) with deliberately cautious language. A tiny adapter (`deriveRecoverySignals` in `index.js`) maps ONLY already-computed live verdicts — `trend` (declining + non-low confidence → `performance_decline`) and `readiness_signal` (`likely_fatigue` → `subjective_fatigue`; `possible_fatigue` → the milder `loads_feel_hard`) — into the engine's signal snapshot; **no new deload math.** The engine's own convergence thresholds under-trigger (one signal never converges → silent). `computeSetEffortExtras` attaches `recovery_advisory` ONLY for the recovery-oriented decisions (`deload`/`recovery_reload`); SILENT for normal/micro_adjustment (too weak) and taper/complete_rest (no live test-date/illness signal — never fabricated), and SUPPRESSED when a deload is already active (the existing `deload` fact owns that voice). `services/coach.js` whitelists it (`sanitizeRecoveryAdvisory`, bounded to `RECOVERY_DECISIONS`, drops everything but deload/recovery_reload, no numbers) + one CAUTIOUS prompt rule ("worth considering" / "recovery may be the smarter play" / "hold the line"; never "you need a deload"; don't shame effort; never also say add-load; invent no numbers). Read-only `/api/coach/message` set path; **no write-path / proof-field / schema / parser / recommendation / modality-logging / public-asset / SW change.** Tests: `coach.test.js` (cautious prompt rule + `sanitizeRecoveryAdvisory` keep/drop/clamp + `sanitizeFacts` thread-through) + `api-smoke.test.js` (real route: strong→deload, moderate→recovery_reload, single signal→silent, active-deload→suppressed, no-contradiction-with-fatigue-router). Engine golden matrix (`recoveryDeloadSelection.test.js`) untouched/green. Full suite green (2529).

    - ✅ **Wiring slice 7 — LLM-down voicing of the `stimulus_grade` (governor verdict)** `[trust-critical]` *(shipped 2026-06-24)*: the deterministic fallback opener now defers to the profile-aware governor grade so it never invites progression the engine is holding. Two pure helpers in `public/coachVoiceTemplates.js` — `governorOverridesProgressionInvite(grade)` (true when `progression_verdict` ∈ `hold`/`back_off` or `fatigue_signal` ∈ `elevated`/`high`) + `templatedGovernorHoldLine(grade)` (no-number, conclusion-first hold line: firm "back off" for back_off/high-fatigue; general_fitness → "that's enough for your goal, no need to chase more"; else "repeat, not a jump"). `coachOpener` (`public/coach-conversation.js`) consults them ONLY for the progression-invite raw verdicts (`easy`/`far_easy`), fed `data.set_grade` from the server response (present on the Gemini-erroring path). Resolves the slice-3 mismatch (e.g. an `easy` set for a `general_fitness` lifter → opener no longer says "room to add load" when the governor holds). Frontend-only; no server/engine/schema/write-path/proof-field/trust-loop/LLM-prompt change. SW shell v39→v40. Tests: `coachConversation.test.js` (both helpers: override predicate truth table + hold-line wording, general_fitness goal line, no-number guarantee, null when not overriding) + `unit.test.js` source-introspection (opener takes the grade, gates to progression-invite levels, calls both helpers, call site passes `data.set_grade`). Full suite green (2621).

  - ✅ **Slice 1 — weighted/RIR set-feedback voice (deterministic control of meaning)** — *shipped #488 (owner-approved 2026-06-22).* New pure `services/coachVoiceRenderer.js` (`renderSetVoice` + `findForbiddenContradictions`): turns the set-effort engine output (`analyzeSetSequence`/`assessNextMoveConflict`) + the recommendation effort verdict into a structured `voice` ({ primary_line, secondary_line, severity, reason_codes, suppress_generic_prose, contradictions }). Deterministic precedence: block (compound redline/rep-drop → HOLD) → caution (isolation RIR0) → bump (high-RIR underdose) → on_target praise → neutral. Wired **only** into the read-only `/api/coach/message` set path: `finalizeSetVoice` **suppresses the LLM prose server-side** whenever a non-neutral signal is present or the prose trips the contradiction guard (redline/rep-drop/pressing-yellow → no progression hype; underdose → not "on target"; warmup → never sandbagged). Frontend (`public/coach-conversation.js`) prefers `voice.primary_line` when `suppress_generic_prose` and skips the now-redundant separate effort line. LLM prompt untouched; parser/schema/write-path/trust-loop untouched; plan routing stays suggestion-only. SW shell v26→v27.

  - ✅ **Slice 1b — on-target determinism + "didn't catch that lift" false warning** — *shipped (owner live retest 2026-06-23).* (1) Live test showed the **on-target** path still let a long generic/LLM paragraph through ("Solid work today, right on target with RIR 2…"). Fix: `renderSetVoice` makes `on_target` a deterministic, prose-suppressing state (`suppress_generic_prose:true`) so the weighted/RIR lane shows the short Atlas line ("Dialled in — that landed right on target."); the server already nulls the prose and the frontend already prefers `voice.primary_line`. `hard` is split into its own severity that keeps the praise floor but lets the LLM add tone (preserves prior behavior; no contradiction risk). The next-recommendation card and plan/handoff lines are untouched; neutral/no-signal unchanged. (2) Secondary: "Cable Fly 30 12/0" parsed + showed a card but `rowsFromWorkoutInput` still raised "Didn't catch that lift" because the parser's internal alias map is narrower than the catalog (`parseUnknownExercise` → `unknown_exercise`). Fix: new pure `shouldWarnUnknownLift(warnings, name, catalogLookup)` gates the warning on the **catalog** — only warns when the lift is unknown to the catalog too (truly unresolved). No parser-grammar/schema/write-path/trust-loop change. SW shell v27→v28. Tests: `test/coachVoiceRenderer.test.js` (on-target primary-line / long-prose suppression / incline live input / Cable-Fly no-warning + isolation caution).

  - ✅ **Slice 4b — `POST /api/log-modality` trust-loop write route** *(shipped, autonomous within authorized 486 scope)*: persists a recognized non-slash modality to `Modality_Log`. The **engine owns the structured fields** — the route re-recognizes the raw `text` server-side via `recognizeModalityInput` (client never supplies the numbers) and normalizes units through the new pure `services/modalityLogRow.js` (`toModalityLogRow`/`normalizeModalityRecord`): `duration_min`→sec, `distance_km`→m, `work_sec`→`duration_sec`, `work_distance_m`→`distance_m`, `elapsed` mm:ss→sec (+ noted), `sets`→`rounds`, circuit `movements`→notes — directly resolving the #516 reviewer's record↔column divergence note. **Trust loop honored exactly like `/api/log-workout`:** `test_mode:true` → dry-run preview (`sheet_written:false`, `no_write_confirmed:true`, normalized `modality_row_preview`); `test_mode` absent/false → live write (the approve→write step after the slice-3 dry-run preview), `write_id`-gated idempotency (`beginWrite`/`completeWrite`/`failWrite`), **503 until the optional tab exists** (mirrors `Constraints`). A slash set → 422 (resistance path never hijacked). **Never touches `Log_Cleaned`/`Effort` or their proof-field semantics.** Registered in `config/routes.js` (`writeCapable:true`) + the write rate-limiter. Tests: `test/modalityLogRow.test.js` (unit normalization across all four modalities, unit conversions, 200-char notes cap) + `test/api-smoke.test.js` (dry-run no-write + proof fields; live append of the normalized row; idempotent duplicate appends once; 503-until-tab; slash→422; live requires write_id). Full suite green (2512).

  - ✅ **Slice 4c — `/api/log-modality` write-path hardening (review #517)** *(shipped)*: folds the two non-blocking #517 review observations — (1) a light `YYYY-MM-DD` format check on `date` (it lands verbatim in `Modality_Log`, so reject a malformed value rather than persist it → 400, no write); (2) an explicit `if (!row)` guard before the append (defense-in-depth — `toModalityLogRow` returns null only for an unknown modality, unreachable today, but the invariant is now loud → 422 rather than a silent bad write). Tests: `test/api-smoke.test.js` (malformed date → 400, writes nothing). Full suite green (2513).

  - ✅ **Slice 5 — frontend trust-loop wiring (`public/app.js`)** *(shipped, owner-authorized 2026-06-23)*: makes modality logging usable end-to-end in the app. A cardio/interval/circuit/timed-hold composer input (which the slash parser rejects) now routes through a new `tryPreviewModality` → `/api/log-modality` **dry-run preview** (`test_mode:true`), renders exactly what will be written into the existing `#preview-panel`, and stages a `pendingWrite{mode:'modality'}` gated by the dry-run no-write proof (`hasLogModalityNoWriteProof`). The existing `#approve-btn` is the **only** write trigger — its new `mode:'modality'` branch posts the live `/api/log-modality` (write_id from the dry-run, no `test_mode`) and requires `sheet_write:'success'`. **Mirrors and does not alter the slash trust loop:** the hook lives only in the coach-fallback branch (input the slash parser rejected), so strength-set logging is untouched; the slash `log-workout` approve branch is byte-unchanged; preview→approve→write and proof gating are preserved; fail-closed (no proof → friendly message, nothing staged). No schema / parser-contract / coach-wording / recommendation change; `coach-conversation.js` untouched. SW shell v30→v31. Tests: `test/unit.test.js` source-introspection (dry-run-only preview, single approve-gated write, slash branch unchanged, modality tried before coach, proof gating) + existing api-smoke server coverage (dry-run no-write; write requires write_id). Full suite green (2514). **✅ Owner live-validated (2026-06-24): `Modality_Log` tab created; cardio preview correct; "Write to Google Sheets" succeeded; row appeared correctly in `Modality_Log`; strength logging unaffected. Persistence path validated.**

    - ✅ **Bug-fix (live audit 2026-06-23, PR 1 of the audit plan) — added-load not entering session state:** added-load sets were logged mid-session but absent from the save (only `Bench` persisted). **Root cause (client-side):** slice 2b fixed only the BACKEND parser (`services/workoutTextParser.js`); the CLIENT local fallback parser in `public/app.js` (`splitWorkoutLine`/`parseSetSegment`) still couldn't parse `+25` — `splitWorkoutLine("Dips +25 8/2")` returned `null`, so `rowsFromWorkoutInput()` threw and the submit handler routed the input to the coach (which voices a plausible readback but NEVER buffers into `sessionLog`). `log it` (`buildRowsFromSessionLog`) therefore wrote only the deterministically-parsed lifts. The server write path drops nothing (verified). **Fix:** mirror the #526 anchored strip `/(^|\s)\+(\d)/ → '$1$2'` into the local `parseWorkoutText` line loop, BEFORE `splitWorkoutLine`, so added-load buffers on the offline/fallback + `/api/session/compile` re-parse paths too. SW shell v33→v34. Tests: `unit.test.js` source-introspection (local strip present, anchored, before split). Bug #2 (post-save "log it" reports logged with no new rows) shares this root cause — the post-save set now buffers once it parses.

    - ✅ **Audit PR 2 (trust hardening) — a malformed SET no longer silently becomes coach chat** *(shipped)*: the masking that hid Bug #1. `rowsFromBackendParsedWorkout` now carries the parser verdict on the thrown error (`parsedIntent`, `recognizedExercise`); the submit-handler coach-fallback intercepts an UNAMBIGUOUS failed slash-log — `needs_clarification` + a recognized exercise + slash-set notation (`\d+/\d+`) in the input — and surfaces the parser's format hint ("… Check the format, e.g. 'Bench 225 5/2'.") instead of routing to the coach. Deliberately narrow so it can NEVER suppress a real question or a bare partial: a question (no recognized exercise / no slash) and "Bench 225" (no slash → coach still asks conversationally) are unaffected. Read-only; no write-path/schema/parser-grammar/modality change. SW shell v34→v35. Tests: `unit.test.js` source-introspection (verdict carried; guard requires recognized-exercise + slash; fires before modality/coach; never calls the coach itself). Full suite green (2538).

    - ✅ **Audit PR 3 (post-save UX) — "log it" with nothing new no longer implies a save** *(shipped)*: Bug #2's UX/safety tail. In `handleLogIt`, an EMPTY `sessionLog` AND a prior save this session (`lastWrite` set) now short-circuits with "Nothing new to log since your last save — log some sets first." (status `warn`), BEFORE the `/api/session/compile` fallback. This stops the post-save "log it" from (1) emitting an LLM "session logged / great work" closeout that implied a fresh write, and (2) recompiling the already-saved chat history — which risked re-writing sets already on the sheet. New sets logged after a save still buffer (`sessionLog` non-empty → the structured-buffer branch wins first), so a genuine second save is unaffected. Read-only guard; no write-path/schema/parser/modality change. SW shell v35→v36. Tests: `unit.test.js` source-introspection (nothing-new guard is `lastWrite`-gated, returns, precedes the compile fallback, and the `sessionLog` branch still precedes it). Full suite green (2539). **Live-audit plan (PR 1–3) complete.**

### Atlas — Backlog › One-Brain Coaching Engine — ARCHITECTURE LOCKED, build sequence (owner-gated promotion to active)

- ✅ **PR-GATEA1 — evidence provenance (SHIPPED)** `[correctness]` — the blocker repair. New pure `services/evidenceProvenance.js` classifies every shadow event `athlete_ui | synthetic | unknown` + `evidence_eligible`, **fail-closed** (only a positively-identified genuine athlete-UI request in production is eligible; test_mode/CI/Playwright/sim/canary/smoke and non-production runtime → synthetic; untagged/missing/malformed → unknown; classification never uses volume/timestamps and never changes a served response). Three columns appended to the END of `Brain_Shadow` (16→19) and `Intent_Shadow` (13→16); old rows read back as `unknown`. **First-party UI provenance (not just a client marker):** the real UI sends `x-atlas-request-origin: athlete_ui` (or `playwright` under `navigator.webdriver`) via `src/app/api.js` + the intent-observe body, and the SERVER verifies browser-enforced same-origin fetch metadata (`Sec-Fetch-Site: same-origin` + host-matched `Origin`/`Referer`) before accepting `athlete_ui` — a bare header on a direct API/script call → `unknown`. Honest first-party provenance to exclude automated/direct-API traffic, **not** authentication. Smoke marks `smoke`; sim reuses `x-atlas-simulation`. Wired at the three coach-engine gates + intent-observe. `docs/ONE_BRAIN_PROMOTION_CRITERIA.md` §3 now defines the 50-event floor as `evidence_eligible=TRUE ∧ evidence_class=athlete_ui`; old/unknown/synthetic never count. Post-merge owner window-reset runbook: `docs/verification/GATEA1_WINDOW_RESET_RUNBOOK.md`. No training-sheet / write-path / Render / served-response change. Tests: `test/evidenceProvenance.test.js`, `test/shadowProvenance.test.js`, `test/gateA1Wiring.test.js` (+ recorder/route regressions).

   - ✅ **PR-5a — pure Coach Orchestrator module** (shipped) — `services/coachOrchestrator.js`: `orchestrate({ envelope, snapshot, runners })` resolves the intent via `capabilityManifest.resolve`, runs complete/partial capabilities in dependency order via **injected runners** (skips `status:missing`, records them in `provenance.skipped`; a throwing runner is skipped, never crashes), and packages a valid `CoachingDecision` — answered when a runner supplies a typed payload and confidence isn't `ask`, else `clarification_needed`. **Rule-free coordinator:** imports only the manifest + contract + state-assembly helpers, no Brain decision module (module computation lives in the injected runners/adapters); guard test pins this. Honest about the keystone gap — `best_workout` resolves to clarification (session_generator missing) while `progression_review` can answer (progression is `partial`). Folds in the State Assembly `getProfile` fix (returns null when no goal, so `provenance.reads` is accurate). 13 tests (`test/coachOrchestrator.test.js`): guards, always-valid output, answered + clarification paths, provenance accounting (run ∪ skipped = plan), trust-contract honored, throwing-runner degradation, rule-free guard. Pure — no I/O/LLM/route/index.js touch.

     - ✅ **Brian serve-eligibility rail** (owner-approved 2026-07-05; shipped same day) — a safety rail so `brian` SERVE mode cannot expose a Coach's Pick built on a not-yet-runner-complete capability stack. `services/coachEnginePromotion.js` gained `isBrianServeEligible(decisionType)` (a coarse decision-type allowlist: `progression` IS serve-eligible; `workout`/Coach's Pick is NOT yet), checked in `planBrianOverride` (no-op — progression stays eligible) and `planBrianPickOverride` (workout → `reason:'not_serve_eligible'` → brian falls back to legacy). **HYBRID is untouched** — the Orchestrator still fully composes and attaches `result.brian`, so divergence-vs-legacy observation continues; only the user-facing SERVE path for `brian` is gated. The deload proof moved off the (now-refused) brian DRIVE path to two non-user-served levels: a `buildSession` raw-shape unit test (`test/sessionGenerator.test.js`) and the hybrid-shadow HTTP test (`test/onebrain-brian-route-drive.test.js`, `data.brian` observes the deload-reduced pick without serving it). Regression added: `brian` does not serve `best_workout`; the progression route still DRIVES (rail does not block the live path).

     - ✅ **Resolver hardening — the four PR #809 review notes** (shipped 2026-07-02) — (a) the pass-through branch enum-cases equipment like every other branch; (b) intersect-branch drops now carry `ignored: equipment_stored_avoid_narrowed` provenance; (c) the equipment-conflict rule is settled and documented in-module: *stored avoids narrow per-item, unless narrowing would leave nothing usable — an explicit request is present-tense consent, so on a total conflict the request stands, recorded* (`equipment_conflict_request_wins`); (d) a total stored avoid with no request leaves the `equipment` key ABSENT with `ignored: equipment_all_avoided_default_used` — an empty `[]` is never emitted, because `sessionGenerator` treats `[]` as falsy and would fall back to the FULL default set, inverting the stored intent (the fifth-review catch). 3 new tests.

     - ✅ **Shadow-path wiring** (shipped 2026-07-02) — `stateAssembly` hydrates `constraints_active` (Constraints tab via the injected-reader pattern: lazy `getConstraints` default reader, `_safeRead` degrade-to-`[]`, `knownKeys` addition) and `coachRunners` gained the `constraint_resolver` adapter (stored rows + envelope constraints → `resolveConstraints`; the manifest's `session_generator depends_on constraint_resolver` orders it first) with `sessionGeneratorRunner` consuming the RESOLVED constraints (raw-envelope fallback when the resolver yields nothing). End-to-end test proves a Constraints-tab "avoid Bench Press" now drops the bench block from the generated workout — a saved rule shapes what the coach GENERATES, not just what it says. Note: on the (gated-off) hybrid/brian attach the Constraints read is a new single lazy read via `defaultReaders` — not a double read (that route never read the tab); revisit only if the hybrid-flag observation shows it matters.

    - ✅ **Just-logged anchor guard** (shipped 2026-07-03) — `planBrianOverride` gained an optional `context.justLoggedSet` guard: in-workout, the just-logged set is not in the sheet yet (session-level save), so Brian's snapshot-derived progression decision is stale by exactly that set — the legacy response anchored on the fresh set always wins (`engine_source.reason: 'just_logged_anchor'`; deload still outranks everything). Route + module tests pin it.

### Atlas — Backlog › Coach Intelligence Layer — follow-ups (PR #721, scaffold)

- ✅ **StarterProgramModule: StrongLifts 5×5 + GZCLP templates + deterministic session runner (roadmap PR 7, shipped)** — `config/coaching/programs/stronglifts-5x5.json` + `config/coaching/programs/gzclp.json` + `services/starterProgramModule.js`. Pure deterministic `buildNextSession(programId, state)` runner: A/B cycle for SL5×5 (LP with hold/deload), Day 1–4 cycle for GZCLP (T1 tier escalation + T2 scheme switching). 59 tests. No runtime consumer yet. Unblocks roadmap PR 9 (full cold-start onboarding flow) and PR 17 (goal/population templates).
  - **PR 9 consumer note — GZCLP T1 weight at stall tiers** `[housekeeping]` — `buildNextSession` always returns `rawWeight` unchanged when `tierIndex > 0` (stall tier). In standard GZCLP a *successful* AMRAP at a stall tier (6×2+, 10×1+) still adds weight while staying at that tier. The module cannot distinguish success-vs-fail at a stall tier from `state` alone — the caller must store the new (incremented) weight in `currentWeights` after a success and the unchanged weight after a fail. PR 9 (or whichever PR wires `buildNextSession`) must drive weight changes on stall-tier success itself and must not infer weight movement from `progressionNote`. (Reviewer note PR #739 claude[bot].)

- ✅ **AutoregulationModule: e1RM-driven, readiness-aware load prescription (roadmap PR 15, shipped)** — `services/autoregulationModule.js`: `computeCurrentE1RM` (best e1RM from last 3 sessions, warm-up excluded), `detectLPGraduation` (plateau-based LP graduation check), `autoregulateLoad` (readiness-adjusted load prescription with plate rounding). 41 tests. No runtime consumer yet. Unblocks roadmap PR 17, PR 22, PR 23.
  - ✅ **`autoregulateLoad` `effectiveRIR` clamped to `MAX_RIR=4` (PR #741 fix)** — reviewer found that `targetRIR ≥ 2` + low readiness (`+2`) would push `effectiveRIR` to 5, causing `percentOf1RM` to throw and `recommendedWeight` to be `null` despite available e1RM. Fixed by clamping `effectiveRIR = Math.min(rir + readinessAdjustment, 4)` before `targetWeightForRIR`. New test `effectiveRIR clamped to 4` added (42 total). (Reviewer note PR #741 claude[bot].)

### Atlas — Backlog › Soul Plan PR-B1 — mode gaps the coaching-mode enum makes visible (2026-07-10)

    - **✅ UPDATE (2026-07-10) — B5a challenge voice VALIDATED live (LT-009 PASS).** Agent-performed live validation against production (owner full-auto authorization) using a REAL qualifying lift (Bench Press — genuine `consistent_underperformance`, 5/5 sessions ≥5% below benchmark; no manufactured history). 9/9 criteria PASS: challenge selected; pattern + numbers named ("under target in 5 of the last 5 sessions"); one question; no lecture; held the line after one pushback; not harsh; no profanity; no implied write; recovery precedence beats challenge (tiredness routed to recovery). Recorded as `docs/TEST_QUEUE.md` LT-009. **This unblocks `detectDiscouragement`→reassure to be proposed as its own separate PR** (its inputs are all live from logs; needs NO plan persistence).

  - ✅ **SHIPPED (task #8, per #950 review) `[correctness]` — phrase-scoped negation.** `discouragementSignal` negation was **whole-string** — a negator anywhere before a discouragement term suppressed the whole message (e.g. "I don't feel weak but bench is stuck" read as negated → no fire). Now **phrase-scoped**: `_isNegatedAt` neutralizes only a discouragement phrase within a bounded preceding window (negator + ≤2 words), so a negated clause no longer swallows a genuine discouragement in another clause — the "…but bench is stuck" half now fires (`frustration`). Existing pure-negation suppressions preserved (single-phrase "bench isn't stuck anymore" still silent). Red-first fixtures added to `test/discouragementSignal.test.js`; full suite green. Live re-validation folded into TEST_QUEUE LT-011.

### Atlas — Backlog › Session_Plans persistence lane (Decision Desk #952, Option A — owner-approved 2026-07-10)

- ✅ **PR-A (SHIPPED) — schema contract + pure event builders + idempotency.** `config/columns.js` `sessionPlansColumns` (owner-approved order) + `Session_Plans` optional tab (`config/sheetContract.js`, 503 until it exists) + `services/sessionPlanEvents.js` (pure `buildPlanAcceptedEvents` / `buildItemOutcomeEvent` / `buildSessionCloseoutEvent`, frozen vocab, deterministic `idempotency_key` over the event's semantic identity — never the timestamp — so retries collapse). No Sheets I/O, no wiring, no drift detection. Staged in the wiring allowlist (expires 2026-10-31; cap 10→11 for this window) until PR-B/PR-C wire it. Tests: `test/sessionPlanEvents.test.js` (contract order, frozen vocab, per-event shape, substituted-requires-performed-code, canonical-code-only, idempotency determinism + retry-safety + distinct-event divergence). Documented in `CLAUDE.md` (13-column tab contract). **`closeout_status` vocab (`finalized | abandoned`) is PR-A's proposal** — the owner froze `event_type`+`outcome`; confirm/adjust `closeout_status` before PR-C.

  - ✅ **AUTHORIZED (owner, 2026-07-10) — Session_Plans live-capture wiring slice** (owner will create the tab with the exact 13-col header order). **One narrow PR, tests first, NO schema change, NO drift detection in this PR, never `Log_Cleaned`; use the existing builders/writer/reader/enums/idempotency contract.** Requirements:
    - **Feature flag `ATLAS_SESSION_PLANS_WRITE` (default `0`/OFF).** Writer disabled unless explicitly on.
    - **Validate the sheet header EXACTLY before permitting writes.** If the tab is missing OR the header differs from `sessionPlansColumns` (position-by-position), **disable the writer + emit a clear diagnostic** — never guess column positions.
    - **Keep `recorded_at` mandatory/non-empty** (the #959-review invariant: the Sheets API strips trailing empty cells, so an empty `recorded_at` would make a valid row read as truncated → fail-closed drop; the writer already stamps `_nowIso()` — pin it with a test).
    - **Capture ONLY deterministic lifecycle events — no inference:** (1) `plan_accepted` — only after the user EXPLICITLY accepts the plan or begins executing an accepted plan (NOT every proposal/draft); one event per accepted item. (2) `item_outcome` — only from an authoritative session action (`completed`/`skipped`/`substituted`; substituted preserves `planned_lift_code` + records `performed_lift_code`); never infer completion from chat wording. (3) `session_closeout` — `finalized` only after explicit close; `abandoned` only after explicit cancel/discard; **never infer `abandoned` from inactivity/timeout/browser-close/server-restart/missing data.**
    - **Reliability:** every event uses the deterministic idempotency key; retries don't duplicate; revision collisions fail closed; **a Session_Plans sidecar failure must NOT corrupt/duplicate the main workout save** — emit an explicit persistence-failure signal and leave plan-history-dependent detectors UNAVAILABLE rather than pretending memory was captured; **do not make user-facing "plan remembered" claims unless the append succeeded.**
    - **Required tests:** accepted plan appends expected rows · duplicate accepted-plan retry idempotent · completed appends one outcome · skipped appends one outcome · substituted preserves planned + performed codes · explicit finalized closeout · explicit abandoned closeout · inactivity does NOT create abandoned · missing tab fails safely · wrong header order fails safely · writer disabled by default · writer enabled performs the append · Session_Plans failure does NOT corrupt the main write · reader folds newly captured events correctly.
    - **Rollout:** merge with the flag OFF → deploy → dry-run/header verification → enable for ONE controlled real session only after owner confirmation → inspect raw rows + reader output → **do NOT enable `skipped_pattern_streak`/`plan_deviation` until that canary passes.** Wiring removes the three Session_Plans allowlist entries. **Sequenced AFTER `detectDiscouragement`→reassure** (owner queue 2026-07-10).

    - ✅ **PR-D (SHIPPED — this) — docs/spec only.** Client lifecycle map + authority classification (authoritative/ambiguous/non-authoritative), client-held accepted-plan contract, the three explicit endpoints (`/api/session-plans/accept|outcome|closeout`) with exact payloads/responses, acceptance semantics (explicit actions only; the primary Coach's Pick needs the smallest explicit accept affordance — do NOT redefine the implicit `ensureActivePlannedSession` promotion or the engaged flag as acceptance), closeout semantics (finalized only from explicit finish/end; abandoned only from explicit discard; implicit `endPlannedSession` paths excluded; never infer), rollout PR-D→PR-I, threat/trust review, and the **client-generated deterministic-ID recommendation**. **No production code. Stops for owner approval before PR-E.**

    - ✅ **PR-E (SHIPPED) — server protocol.** Three authenticated, feature-flagged sidecar endpoints (`routes/sessionPlans.js` + `config/routes.js` entries, `writeCapable:true` but Session_Plans-only): `POST /api/session-plans/accept|outcome|closeout`. New `services/sessionPlanCapture.js` owns the flag gate (`ATLAS_SESSION_PLANS_WRITE`, default OFF → `{status:'disabled'}`, no Sheets access), **exact position-by-position header validation** (missing tab → `tab_missing`; any positional diff → `header_mismatch`; never guesses columns), the existing idempotent writer/builders, and **total failure isolation** (every path returns a `captured:false` envelope, never throws → a Session_Plans failure can't corrupt/duplicate/block the main workout save). Proof envelope `{status, captured, written, skipped, plan_version, reason}`; `captured:true` only on a real append or idempotent skip. Routes validate required fields + opaque `pv_`/`pi_` ID shape + frozen vocabularies (400 on bad requests). **No client calls (PR-F); never `Log_Cleaned`/`Effort`; no drift.** Wired `sessionPlanStore`+`sessionPlanEvents` (dropped their allowlist entries; `sessionPlanReader` stays staged). Tests-first: `test/sessionPlanCapture.test.js` (18) + `test/sessionPlansRoutes.test.js` (11) + api-smoke manifest + route-coverage scan; full suite green (5060).

    - ✅ **PR-F (SHIPPED) — accepted-plan identity + "Start this plan" button.** New pure/DI `src/app/planAcceptance.js` (opaque `pv_`/`pi_` UUID minting via `crypto.randomUUID`/`getRandomValues` — never timestamp/`Math.random`; `buildAcceptedItems` with canonical-code validation + no partial snapshot; `acceptCopy` — only `captured===true` licenses memory language; `runAcceptance` orchestrator). A distinct **"Start this plan"** button on the Coach's-Pick plan card (`coach-conversation.js appendStartThisPlanButton`) is the v1 authoritative acceptance boundary; app.js `acceptDisplayedPlan` is a thin adapter (mint identity → store the immutable accepted snapshot → persist → start the workout → non-blocking `/api/session-plans/accept` POST). The workout starts regardless of flag/sidecar; a sidecar failure preserves the local snapshot and never re-mints; double-tap guarded (`_acceptInFlight` + button disable) → one revision. `store.js` hydrate extended so an **accepted** plan-only snapshot survives reload with identity intact (an unaccepted plan-only snapshot still never auto-resumes). Drafts stay ephemeral; implicit Coach's-Pick materialization / engaged flag / generic "Start Session" are NOT acceptance. No item outcomes, no closeout, no drift, no profanity, no main-write change. SW cache v121→v122 (+`planAcceptance.js` precached). Tests-first: `test/planAcceptance.test.js` (14) + `test/planAcceptancePersistence.test.js` (3) + `test/planAcceptanceWiring.test.js` (7) + the flipped resume-guard assertion; full suite 5086 green.

      - ✅ **PR-G1 (SHIPPED) — identity propagation + skipped + substituted.** Acceptance now tags each `exercises[]` slot with its immutable `plan_item_id` (`planAcceptance.js` — items[i] ↔ exercises[i]; survives reload/reorder; substitution keeps the original id on the replacement slot). New pure/DI `src/app/planOutcome.js` (`resolveItemForOutcome` by `plan_item_id` ONLY — never lift-code/name/position; duplicate lift codes unambiguous; `buildOutcomePayload` fails closed on unaccepted/missing-id/missing-performed; `runOutcome` non-blocking, never throws). `app.js` emits at the explicit handlers: `skipPlannedExercise` (captures id before splice → `skipped`) and `applySessionSubstitution` (keeps original id + records `performed_lift_code` → `substituted`), POST `/api/session-plans/outcome`, gated on `plan.accepted`. NO completed, NO closeout, NO drift, NO inference from logged sets, no main-write change. SW cache v122→v123 (+`planOutcome.js`). Tests-first: `test/planOutcome.test.js` (11) + `test/planOutcomeWiring.test.js` (7) + acceptance/persistence tagging assertions; full suite 5106.

      - ✅ **PR-G2 (SHIPPED) — explicit `completed`.** A "Done with this exercise" button on the current-exercise banner (`renderActiveSessionBanner`) is the ONLY authoritative completed boundary. `planOutcome.js` now permits `completed` (no performed code). `app.js completeCurrentPlanItem` marks the item's outcome `completed` locally (no re-complete guard), persists (survives reload), emits `completed` for the exact `plan_item_id` (non-blocking), and advances — regardless of the sidecar. The button is shown only for the current accepted item with **evidence of ≥1 performed set** (`currentItemHasPerformedSet` reads the derived completion buffer) and **not already completed**; the CLICK — not the evidence — is authoritative. Double-tap guarded (disable-on-click + outcome check); logging a set / "Next" / chat text NEVER emit completed (single emit site, pinned by test). No typed "done with X" command. SW cache v123→v124. Tests-first: `planOutcome.test.js` (completed payload + unknown-outcome reject) + `planCompleteWiring.test.js` (4); full suite 5111.

    - ✅ **PR-H (SHIPPED) — explicit closeout capture.** New pure/DI `src/app/planCloseout.js` (`buildCloseoutPayload` fails closed on non-accepted/missing-id/invalid-status; frozen `finalized|abandoned`; `runCloseout` non-blocking, never throws). `app.js emitPlanCloseout` (gated on `plan.accepted`) wired at **exactly four explicit affordances**: `finalized` ← banner "End session" button + `#finish-session-btn` "Finish session"; `abandoned` ← `#start-over-btn` "Start over" + restore-banner trash discard. Emitted BEFORE the state-clearing call so `session_id`/`plan_version` are in scope. **Never inferred** — the implicit `endPlannedSession` cleanup paths (auto-end, post-save reset) and "Next" auto-advance do NOT emit (pinned by test). `finalized` = explicitly closed (not "all items completed"); `abandoned` = explicitly discarded. Retries reuse the same event identity; double-clicks collapse (server idempotency); a sidecar failure never blocks/corrupts the local close/discard. No outcomes/drift/plan-replacement/main-write change. SW cache v124→v125. Tests-first: `planCloseout.test.js` (7) + `planCloseoutWiring.test.js` (7, incl. the implicit-path no-emit guard); full suite 5125.

### Atlas — Backlog › Soul Plan PR-B4 — split into safe slices (2026-07-10, PM authority)

- ✅ **B4-1 (this PR) — additive mode + register forwarding.** Compute `selectCoachMode → computeCelebrationScarcity → grantRegister` server-side on the set/block reaction path (zero extra Sheets reads — reuses the enrichment `allLog`); forward `coach_mode` + `register` as whitelisted engine facts via `sanitizeFacts`/`sanitizeChatContext` (enums imported from the modules, stimulusGovernor lockstep pattern). **`profanity_ok` is deliberately NOT forwarded** — the permission waits for its suppressor. Prompt unchanged, `finalizeCoachVoice` unchanged → **zero behavior change** (the model receives two new facts it is not yet told to use). The three modules are now wired (allowlist entries removed; cap reverted 11→8). Tests: `test/registerWiring.test.js` + silent-path byte-identity via the unchanged `coach-message-block` suite.

- ✅ **B4-2 (SHIPPED) — persona register-interpretation block** (`services/coachPersonaCore.js`): the REGISTER block now spells out what routine/elevated/max sound like and what `casual_ok`/`humor_ok` license (wording only; register sets tone only and never overrides a grounding rule; no profanity introduced in this slice), extending the PR-A2 once-per-prompt tests.
