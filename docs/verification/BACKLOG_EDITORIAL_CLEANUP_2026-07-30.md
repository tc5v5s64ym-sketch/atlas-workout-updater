# BACKLOG editorial cleanup — 2026-07-30

## Result

- Completed blocks archived: **134**
- `BACKLOG.md` lines: **1273 → 1126**
- Paper-weight cap: **1273 → 1126**
- Runtime/code behavior changed: **no**

## Safety rule used

A list-item block moved only when its own first line carried a strong completion marker (`✅`, `[x]`, or a struck-through resolved/fixed marker) and the whole block contained no open/deferred/pending/evidence/owner-gated signal. Anything ambiguous stayed in `BACKLOG.md`.

## Archived blocks

- ✅ `[trust-critical]` **`F10C-IMPLICIT-WIRING-1` — FIXED (F10C slice 2, 2026-07-18).** Wired the implicit-recommendation derivation end-to-end, still dry-run. Server: `POST /api/session-plan-sets/implicit` (`routes/sessio…
- ✅ `[housekeeping]` **`REVIEW-LIVE-1` — FIXED (F09C, 2026-07-17).** Added `npm run atlas:review-live` (`scripts/atlas-review-live.js`): auto-selects the newest owner session, joins Flight Recorder / Intent Shadow / Brain …
- ✅ `[correctness]` **`AUTH-DURABLE-1` — RESOLVED, no defect (F09D proof-only close, 2026-07-17).** The re-prompt was traced (F04C, live-validated on v137) to expected loss of site data (clearing Safari history/website dat…
- ✅ `[correctness]` **`PLAN-EXEC-1` — FIXED (F09E, 2026-07-17).** Render-layer completeness: a bodyweight lift (engine `weight:0`) rendered a meaningless `0lbs 15/?` → now `BW — 15 reps ×3` (grouped, explicit set count), d…
- ✅ `[trust-critical]` **`PLAN-COACH-SPLIT-1` — FIXED (F09F, 2026-07-17).** The visible plan and the live recommendation engine disagreed; Atlas prescribed one target then treated a different performed target as though it …
- ✅ `[correctness]` **`PR-CLAIM-1` — FIXED (F09H, 2026-07-17).** "That was a PR!" was treated as workout-set logging, then as a coaching-note save request; a failed `/api/coaching-notes` write returned 503. A four-surface …
- ✅ `[correctness]` **`SIDECAR-DATE-1` — FIXED (F09I, 2026-07-17).** `POST /api/coaching-notes`, `POST /api/constraints`, and `Deload_State` `deload_start_date` now stamp the owner's LOCAL day via the existing canonical `l…
- ✅ `[trust-critical]` **`UNDER-TARGET-1` — truthfulness FIXED (F09J, 2026-07-17).** Atlas claimed a lift was "under target 5 of the last 5 sessions" with **no stored historical prescription**; the detector is benchmark-ba…
- ✅ `[housekeeping]` **`playwright.config.js` remote-env executablePath shim — FIXED (with F09B, 2026-07-17).** Top-level `use.executablePath` is not a valid option on @playwright/test 1.60 (must be `use.launchOptions.exec…
- ✅ **Session_Plans PR-I — production-validated + mid-plan Done RESOLVED (2026-07-11).** The full client capture lane (PR-D…PR-I) is live and **production-validated**: an automated live-write canary proved `accept` (3 `pla…
- ✅ **`[trust-critical]` PARSER BUG — silent set MISATTRIBUTION — FIXED 2026-07-03** (same day it was found; the `todo` reproducers in `test/parserFuzz.test.js` are now HARD assertions plus two new same-line guards). Root …
- ✅ **PR-3 `[correctness]` — short-gap restart posture — SHIPPED (PR #842):** `scoreIntents` (`services/analytics.js`) now surfaces a severity-independent `todays_read.days_since_last_session` (from `assessLayoff`, emitted…
- ✅ **C3 shadow COVERAGE gap — most typed messages were never observed — FIXED (chokepoint-observe PR)** `[product-evidence]`: owner ran many shadow sessions, `/api/debug/intent-shadow` showed only 2 entries. Root cause: `…
- ✅ **Owner directive (2026-07-03, live screenshots): plans render STACKED, never as prose lines** — the chat's plan reply rendered "RDL — 230 × 8 reps × 3 sets @ RIR 2" linear prose; the owner wants the Coach's-Pick stack…
- ✅ **Live bug (owner 2026-07-03, 11:23): an alias log against a CHAT-created plan never advanced the plan — FIXED** `[correctness / trust]` — logged "Rdl 240 8/3 ×3"; the handoff said "next up: Romanian Deadlift" and the …
- ✅ **Live find (owner 2026-07-03, 11:51) — typo'd identity correction missed the relabel → DUPLICATE sets in the review — FIXED (identity-correction typo-tolerance PR)** `[trust-critical]`: "Slslp 70 15/2 ×4" buffered wit…
- ✅ **Owner feedback (2026-07-03, 11:32) — opening message briefing — SHIPPED (opening-briefing PR)** `[polish/UX]`: the "Last time: OHP 115×8" opener is replaced by a deterministic engine briefing. `loadDashboard` (`publi…
- ✅ **Owner directive (2026-07-03, 11:40 screenshot): in-workout chrome collapses to the pin row** — the Coach-plan card (Next exercise → / End session) took too much space from the thread. Now: the session pin is the ONE …
- ✅ **Live find (owner session 2026-07-03): bodyweight sets rendered "0×20"** — Hanging Knee Raises readback tile showed `0×20`; the review-card set line and the opener's 0-weight edge had the same hole. FIXED same day: sh…
- ✅ **LT-011 live re-validation — PASS (2026-07-12, deployed `e94bfbb`/`pr:998`).** Re-ran the seven `POST /api/coach/chat` probes against production: explicit discouragement reaches reassure 3/3 (zoom-out + one move) even…
- [x] **CLIENT-1** `[trust-critical]` **(C)** — ✅ **FIXED (PR-0D, `claude/atlas-remediation-v2-phase-0-yxfp1t`)**: every saved review card now binds its write's `log_appended_range` at `markReviewSaved` time; on a newer sa…
- [x] **PARSE-1** `[trust-critical]` **(E)** — ✅ **FIXED (PR-0E, `claude/atlas-remediation-v2-phase-0-yxfp1t`)**: on the LOG path, `findExerciseInText` now rejects an anywhere-position alias match when a genuine variant qu…
- [x] **PARSE-4** `[correctness]` **(E)** — ✅ **FIXED (F05, 2026-07-16).** Mixed-notation input no longer silently drops unconsumed set groups. A route-level gate (`services/fullConsumptionGate.js`, wired on `POST /api/par…
- [x] **PARSE-5** `[correctness]` **(E)** — ✅ **FIXED (F05, 2026-07-16).** `bench 3x10 @2` no longer logs a 2 lb bench: the same gate refuses a barbell `NxM@W` form with `W ≤ 10` (a lifter's RIR band) when the parser emitt…
- [x] **WRITE-1** `[correctness]` **(C)** — ✅ **FIXED (F02, 2026-07-16, PR #1018).** `/api/complete-workout` now binds and returns the exact Sheets append proof (`logAppendedRange` from `logResponse.data.updates.updatedRan…
- [x] **WRITE-2** `[correctness]` **(P)** — ✅ FIXED (F03, 2026-07-16). The closeout route reuses the server-minted `session_id` stamped in the prior idempotency record (new read-only `peekWrite`) instead of re-minting on r…
- [x] **WRITE-3** `[correctness]` **(C)** — ✅ FIXED (F03, 2026-07-16). `services/idempotency.js` tags in_progress records rehydrated from disk and `beginWrite` downgrades a stale (`>5min`) rehydrated reservation to retryab…
- [x] **INFRA-1** `[correctness]` **(E)** — ✅ **RESOLVED (PR-22, 2026-07-08), by eliminating the class rather than patching the guard.** `public/` is no longer committed at all (gitignored build output, regenerated via a `…
- [x] **CLIENT-3** `[correctness]` **(P)** — ✅ **ADDRESSED by F07 (CLIENT-3, shipped 2026-07-16, PR #1033):** a module-level flow-sequence token (`previewRequestSeq`, bumped by `invalidatePreview`) now drops a stale / out-…
- [x] **CLIENT-4** `[correctness]` **(P)** — ✅ **FIXED (F08, 2026-07-16, canonical card in [`docs/ATLAS_V1_EXECUTION_PLAN.md`](./docs/ATLAS_V1_EXECUTION_PLAN.md)).** On `/api/complete-workout` the Log rows now adopt the re…
- [x] **WRITE-5** `[correctness]` **(P, design challenge)** — ✅ FIXED (F04, 2026-07-16). `sheets.js` `isTransientAppendError` no longer retries an ambiguous 503 on `values.append` (treated like 500 / post-send timeout); on…
- [x] **WRITE-6** `[polish]` — ✅ **RESOLVED (LT-008 hardening, owner-approved).** Malformed truthy `test_mode` values (`1`, `"yes"`, `"on"`) silently went LIVE. Added `isAmbiguousTestMode()` (`index.js`): a `test_mode` tha…
- [x] **LT-008 write-path input hardening** `[correctness]` — ✅ **SHIPPED (owner-approved full-auto).** Safe live break test (`/api/*`, dry-run only, zero real writes) found the write path silently accepting impossible inp…
- [x] **ADD-1** `[correctness]` — ✅ **ALREADY FIXED — regression-locked (this PR, `claude/add-1-restored-session-pin`)**. Verification gate confirmed `discardRestoredSession` already clears the restored pin from **both sta…
- [x] **ADD-2** `[correctness]` — ✅ **FIXED (this PR, `claude/add-2-spoken-skip-state`)**: a spoken negation-style skip now becomes real session state. `src/app/planMutationIntent.js` gained (a) a **negation-skip** grammar…
- [x] **ADD-7** `[correctness]` — ✅ **FIXED (PR-11, `claude/pr-11-conversation-hardening`)**: the Flight Recorder's `latestCoachMessage()` reported `coach_message:""` because it read the last `.coach-msg` even when transie…
- [x] **PR11-2 — plan-mutation intent** `[correctness]` — ✅ FIXED: "swap next workout for dips" removed Dips instead of making Dips next. `classifyMutationIntent` now distinguishes destination/source/removal/substitution —…
- [x] **PR11-3 — duplicate recap** `[polish]` — ✅ FIXED: the readback tile listed identical sets individually; a shared `groupConsecutiveSets` now collapses them to "225×5 RIR2 ×3" in both the readback and the review card …
- [x] **PR11-4 — blank coach message** `[correctness]` — ✅ FIXED: see ADD-7 above.
- ✅ **[correctness] Harden the Sheets-down fallback so a forged `progression_history` / `rec.progression_verdict` can't survive (CODEX review note, PR #988) — shipped.** `routes/coachOps.js` now fails closed on the enrichm…
- [x] **Owner live re-test — PASSED 2026-07-02 10:58** (post-merge #788): owner ran the same Recovery/Pump session shape. OHP 90×12@RIR5 → "Dialled in — that landed right on target" + "Next time: On target — keep 90 and ch…
- ✅ **RESOLVED (PR #593)** — review note PR #590 #2: `analytics.recommendNextSet`'s immediate bump anchor (`lastSet`/`priorSet`) now prefers **working sets** (`workingRows = rows.filter(!isWarmupNote)`, falling back to all…
- ✅ **RESOLVED (PR #593)** — review note PR #590 #3: `sessionBestByLift` + `historicalBestByLift` now skip note-tagged warm-ups, so a heavy tagged feeler no longer registers a false personal record; the PR baseline is work…
- ✅ **RESOLVED (PR #593)** — review note PR #593: the last two LIVE PR readers also skip tagged warm-ups — `detectRecentPrs` (GET `/api/prs/recent`) filters them at the row level, and `buildWeeklyReport`'s weekly-PR block …
- **RC1 — preview ≠ write (silent row drops).** The preview showed 7 exercises / 30 sets / 33,885 lb, but the sheet saved only **22** Log_Cleaned rows for `20260628-PM-01`, with **Bench Press dropped entirely** and Seated …
- ✅ **WIRING GAP — skip-notation path** _(resolved [#338](https://github.com/tc5v5s64ym-sketch/atlas-workout-updater/pull/338))_: parser now extracts `prescribed: [{exercise, reason}]` from inline "X skipped - reason." sen…
- ✅ **Plan-prescribed wiring** _(resolved PR 341)_: `services/planMatcher.js` → `inferPrescribedPairs` infers substitution pairs from `activePlannedSession` when the user didn't write an explicit skip sentence. `index.js` …
- ✅ **`planMatcher` identity is name-only — lift_code guard** — fixed. `inferPrescribedPairs` now runs a Step 1b `lift_code` identity pass after the exact-name pass: equal non-null `lift_code` (case-insensitive) marks the …
- ✅ **Surfaced into the coach voice (was dark)** — `scoreSubstitutionQuality`'s tier is now attached in `buildSubstitutionPreviews` (`index.js`) → `sub.quality`, bounded by `sanitizeSubstitution` (enum `excellent|acceptabl…
- ✅ **Resolved (decision, PR #439) — `computePrimaryMuscleOverlap` directionality is the correct semantics, consciously accepted.** It returns the fraction of *prescribed* primaries covered by the *logged* set — which is e…
- ✅ **Owner-approved Standard-tier wording (voice reference)** — `docs/COACH_VOICE_VALIDATION.md` "Extra-work voice" section (owner-approved 2026-06-20, Owner-Review Pack #1) pins the proactive rule (volunteer; Standard th…
- ✅ **Engine slice (deterministic guard, unwired):** `services/messageIntent.js` — pure `classifyMessageIntent(rawText, { hasResolvedLift, hasSetTokens })` → `{ isQuestion, suppressLog, reason }` where `reason` ∈ `question…
- ✅ **Wiring — rule (a), the question→phantom bug (owner-scoped):** `parseWorkoutText` (`services/workoutTextParser.js`) now applies `classifyMessageIntent` after `parseLogSets`; when the message reads as a question and di…
- ✅ **(1) session-volume profile engine** — `services/sessionVolumeProfile.js::buildSessionVolumeProfile(logRows, { today, windowDays=120, minSessions=6 })` returns the lifter's own `{ exercises_per_session, compounds_per_…
- ✅ **`fix_blind_spots` AC1 brief vs the cap — fixed** `[polish]` (2026-06-24): the brief now recomputes covered patterns from the **final** exercise list (after `structureSession`'s role-order + density cap + lower-body c…
- ✅ **`capRecoveringPatternDensity` passes the exercise muscle_group** `[polish]` — *shipped.* `capRecoveringPatternDensity`'s `roleRank` (`services/analytics.js`) now calls `classifyLiftRole(ex.exercise, ex.muscle_group)`…
- ✅ **ME-5 — unbounded `/api/complete-workout` body** `[correctness/security]` (2026-06-24) — two guards added: (1) multer `limits.fieldSize = 512 KB` so the `log_rows_json` *field* (not a file, so `fileSize` never bounded…
- ✅ **ME-9 — `propose_edit` numbers validated server-side (defense-in-depth)** `[trust-critical]` (2026-06-24) — `isValidEditSchema` (`services/coach.js`) now validates any **present** `weight`/`reps`/`rir` in an `add_set`…
- ✅ **ME-10 — vision `ATLAS_LLM_MODEL` honored on OpenAI** `[housekeeping]` (2026-06-24) — `getProviderConfig`'s OpenAI branch now returns `model: (ATLAS_LLM_MODEL || 'gpt-4.1-mini').trim()` and `parseWithOpenAI` uses `con…
- ✅ **ME-11 — vision JSON mode + fence strip + abort timeout (both providers)** `[correctness]` (2026-06-24) — OpenAI Responses call adds `text:{format:{type:'json_object'}}` and now strips code fences defensively before p…
- ✅ **ME-12 — `dotenv` hard `require` at load** `[housekeeping]` (2026-06-24) — `index.js:1-2` no longer hard-requires `dotenv` (`const dotenv = require('dotenv'); dotenv.config();`), which would crash module load if `dote…
- ✅ **New nit — `previewTestRows` hardcodes `/session-2026/`** `[housekeeping]` (2026-06-24) — generalized the test-row filter in `previewTestRows` (`services/analytics.js`) from the literal `/session-2026/i` to `/session-…
- ✅ **New nit — zero `timeout-minutes` across the CI workflows** `[housekeeping]` (2026-06-24) — added a per-job `timeout-minutes` to every job across all 8 workflows (11 jobs): `ci.yml` (unit-tests 15 / secret-scan 10 / e…
- ✅ **Resolved (PR 362)** — `facts.deviation`: `classifyDeviation` is now called with the top-weight set from `todaySets` + `computeExpectedPerformance` output in `services/liveIntelligence.js::enrichCoachFacts`, and `POST…
- ✅ **Resolved (PR 363)** — System-prompt instructions added for `deviation` wording and RIR anti-fabrication: model must derive effort from `effort_verdict` only (RIR 2 is NOT failure); deviation instruction names all fou…
- ✅ **Resolved (PR 362)** — `facts.evidence_context`: assembled from `computeBenchmark` + `computeExpectedPerformance` basis in `enrichCoachFacts`. `reference_sets` and `date_range` are empty/null for now (expectedPerforma…
- ✅ **Resolved (PR 362)** — `facts.working_weight`: `resolveWorkingWeight` called in `enrichCoachFacts`; result merged into `rec.working_weight` before `sanitizeFacts` runs.
- ✅ **Resolved (PR 362)** — `facts.trend`: `detectTrend` called in `enrichCoachFacts`; result merged into `rec.trend`.
- ✅ **Resolved (PR 358a)** — `plan_completed` is now tracked in `sessionCompleted` (client-side, per-session) and forwarded in `routeMessageToCoach` as `context.plan_completed`. The server can now compute `plan_state` incl…
- ✅ **Resolved (Roadmap Step 359)** — `services/planActionClassifier.js`: deterministic plan-action classifier. `classifyPlanAction({planned, completed, loggedName, loggedLiftCode, skippedName})` returns `{action, prescrib…
- ✅ **Resolved (Roadmap Step 360)** — Session closeout: `services/sessionCloseout.js` (`computeCloseout`) detects when all planned exercises are complete. `emitSetLogged` in `public/app.js` computes `planIsComplete` and in…
- ✅ **Resolved (Roadmap Step 364 / GitHub PR #366)** — Out-of-order closeout trust bug: plan [A,B,C] completed as A→C→B showed "next up: C" after B because `getNextExerciseInPlan('B')` returned C and the `else if (planIsCo…
- ✅ **Resolved (PR 364)** — `repeated_substitution` now fires in production: `services/substitutionHistory.js::buildSubstitutionHistory` infers substitution events from `Log_Cleaned` rows (modal-lift heuristic with three f…
- ✅ **Resolved (PR 367)** — `reason_codes` field added to every intent emitted by `scoreIntents` in `services/analytics.js`: short machine-readable snake_case strings (e.g. `high_fatigue`, `well_rested`, `lower_overdue`, `…
- ✅ **Resolved (PR 368)** — Trend-aware recommendation scoring: `scoreIntents` now computes `downwardLifts = allRecs.filter(r => r.e1rm_trend === 'down')`. When 2+ push/pull compounds trend down: `build_strength` score −15…
- ✅ **Resolved (PR 369)** — Readiness-aware dose adjustment: `scoreIntents` now applies `applyReadinessDose()` to the exercise list for the four training intents (`build_strength`, `build_muscle`, `fix_blind_spots`, `balan…
- ✅ **Resolved (PR 370)** — Coach Confidence Layer: every exercise object emitted by `scoreIntents` (via `exForPatterns` in `services/analytics.js`) and by `buildIntentSession` (in `services/sessionBuilder.js`) now carries…
- ✅ **Resolved (Roadmap Step 371)** — Both legacy trend fields removed from `sanitizeFacts`: `e1rm_trend` (old short-window string, superseded by `trend`) and `sessions_analyzed` (redundant with `trend.sessions_analyzed`).…
- ✅ **PR 1.1** — `services/muscleCoverage.js`: `musclesFor` / `liftsForMuscle`, 17-muscle taxonomy, pattern-based, pure data (31 tests).
- ✅ **PR 1.2** — `services/muscleVolume.js`: `weeklyMuscleVolume`, rolling-window volume per muscle, direct 1.0 + indirect 0.5 credit (16 tests).
- ✅ **PR 1.3** — `services/movementPattern.js`: `patternFor`; 14-pattern vocabulary (squat/hinge/push/pull/isolation/trunk/carry/other); pattern-based, pure data (20 tests).
- ✅ **Hold Point 1** — owner reviews coverage map + per-muscle volumes against real log before behavior changes. _(Sonnet → Opus 4.8 after hold)_
- ✅ **PR 2.1** — Coverage-aware stall/deload: `coverageStalls.annotateStallsForDeload`; accessories downgraded when primary muscles covered by other lifts; `weeklyMuscleVolume` extended with `today`/`excludeLiftCode` optio…
- ✅ **Hold Point 2** — live-app test confirmed: Face Pull correctly feeds deload (rear_delts 1.5 eff. sets < 2.0 threshold); OHP main stall feeds independently. Coverage logic verified correct.
- ✅ **PR 3.1** — `services/underCoverage.js`: `computeUnderCoverage`; per-muscle status (`under`/`adequate`/`optimal`) + reason string vs. MEV-style target ranges (950 tests).
- ✅ **PR 3.2** — Surface under-coverage in coaching chat: `muscle_gaps` (sorted by severity) wired into `buildChatContext` + `sanitizeChatContext`; LLM nudges 1–2 under-served muscles when asked what to train (952 tests).…
- ✅ **Hold Point 3** — gap nudges reviewed and approved in live conversations. Wording reads naturally; style confirmed.
- ✅ **PR 3.3 — expectation verdict engine** — `computeExpectationVerdict` in `analytics.js`; emits `{ outcome, why, prescribedRir, actualRir, rirDelta }` per set; outcome ∈ beat/met/fell_short/swap; rirDelta sign conventio…
- ✅ **PR 3.5 — swap detection + working-weight finder** — `detectSwap` (case-insensitive name comparison, null-safe) + `buildWorkingWeightProtocol` (calibration protocol; 70% reference anchor → nearest 5 lb startHint; "Sta…
- ✅ **PR 3.x — systemic-cost tier** — `services/liftCost.js`: `costFor`; HIGH/MEDIUM/LOW by name pattern; pure data (16 tests).
- ✅ **PR 3.x — balance signal** — `services/balanceSignal.js`: `computeBalanceSignal`; antagonist volume-ratio engine for 4 pairs (horizontal push:pull, vertical push:pull, anterior:posterior, quad:hamstring) → `{ pair, aS…
- ✅ **PR 4.0** — `services/profileGoal.js`: `getProfileGoal()` reads `ATLAS_PROFILE_GOAL` env var, normalises through goal vocabulary (aliases included), returns null when absent; wired into `/api/recommendation/preview` a…
- ✅ **PR 4.1** — Goal- + coverage-aware ranking: `scoreIntents` enriched with `goal` option + under-coverage signal; GOAL_BONUS table for 8 goals; coverage-gap bonus for `fix_blind_spots` and hypertrophy `build_muscle`; `b…
- ✅ **PR 4.2 — session builder** — `services/sessionBuilder.js`: `buildWarmupRamp` (50 %/70 %/85 % ramp, priming-flagged), `isBlockedPair` (fine-grained pattern + HIGH-cost guard; blocks Deadlift+RDL, allows Squat+Deadlift…
- **Render cold start kills the first request after idle — diagnosed live 2026-07-02** `[correctness]` — Owner live re-test (IMG_5450, 9:27): the first screenshot attach of the session died with **"Preview failed: Load fai…
- ✅ **PR2d — `/api/constraints` 503-until-tab-exists** `[trust-critical]` (shipped) — the one real gap: constraints had GET/POST/validation/idempotency/manifest tests but no test for the documented 503-until-Constraints-ta…
- ✅ **PR4a — frontend trust-loop proof-gate tests** `[correctness]` (shipped) — `test/trustLoopProof.test.js` gives the three pure trust-loop functions in `public/app.js` their first BEHAVIORAL coverage (previously only so…
- **Close obsolete PR [#288](https://github.com/tc5v5s64ym-sketch/atlas-workout-updater/pull/288)** (superseded by [#290](https://github.com/tc5v5s64ym-sketch/atlas-workout-updater/pull/290)). ✅ Already closed 2026-06-16.…
- ~~**API key in `localStorage`** `[housekeeping]`~~ — ✅ **ADDRESSED (F04C, 2026-07-16).** The durable owner session (`docs/OWNER_SESSION.md`) exchanges the key for a signed HttpOnly `atlas_session` cookie and removes the …
- [x] **JSON `/api/log-workout` `log_rows.length` upper bound** `[housekeeping]` — ✅ **RESOLVED (LT-008 hardening).** The `MAX_LOG_ROWS` (200) cap now guards `log_rows.length` on the JSON handler too, so the write/verify/u…
- ✅ **PR-A (engine, GitHub #478)** — pure `services/setEffortSignals.js` (`analyzeSetSequence`/`assessNextMoveConflict`) + fixtures, unwired. *Merged.*
- ✅ **Wiring slice 1 — pure adapter** *(shipped)*: `services/trainingIntelligenceAdapter.js` — `profileForGoal(goal)` (TRAINING_GOALS → governor/scorer PROFILE) + `modalityCategoryFor(name)` (exercise → governor/router MOD…
- ✅ **Wiring slice 2 — profile-aware governor grade on the set-reaction** *(shipped)*: `computeSetEffortExtras` (`index.js`, read-only `/api/coach/message` set path) attaches a `set_grade` fact = `gradeStimulus({ profile: …
- ✅ **Wiring slice 3 — voice the governor grade** *(shipped)*: the set-reaction coach now WORDS `set_grade`. `index.js` threads the slice-2 grade onto the coach facts as `stimulus_grade` (set path only); `services/coach.js…
- ✅ **Wiring slice 5 — voice the recovery/deload SELECTION (conservative)** *(shipped, owner-authorized 2026-06-23)*: the set-reaction coach now WORDS the convergence-based recovery decision (PR 485 `assessRecoveryDeload`)…
- ✅ **Wiring slice 7 — LLM-down voicing of the `stimulus_grade` (governor verdict)** `[trust-critical]` *(shipped 2026-06-24)*: the deterministic fallback opener now defers to the profile-aware governor grade so it never i…
- ✅ **Slice 1 — weighted/RIR set-feedback voice (deterministic control of meaning)** — *shipped #488 (owner-approved 2026-06-22).* New pure `services/coachVoiceRenderer.js` (`renderSetVoice` + `findForbiddenContradictions`…
- ✅ **Slice 1b — on-target determinism + "didn't catch that lift" false warning** — *shipped (owner live retest 2026-06-23).* (1) Live test showed the **on-target** path still let a long generic/LLM paragraph through ("Sol…
- ✅ **Slice 4b — `POST /api/log-modality` trust-loop write route** *(shipped, autonomous within authorized 486 scope)*: persists a recognized non-slash modality to `Modality_Log`. The **engine owns the structured fields** …
- ✅ **Slice 4c — `/api/log-modality` write-path hardening (review #517)** *(shipped)*: folds the two non-blocking #517 review observations — (1) a light `YYYY-MM-DD` format check on `date` (it lands verbatim in `Modality_L…
- ✅ **Slice 5 — frontend trust-loop wiring (`public/app.js`)** *(shipped, owner-authorized 2026-06-23)*: makes modality logging usable end-to-end in the app. A cardio/interval/circuit/timed-hold composer input (which the s…
- ✅ **Bug-fix (live audit 2026-06-23, PR 1 of the audit plan) — added-load not entering session state:** added-load sets were logged mid-session but absent from the save (only `Bench` persisted). **Root cause (client-side)…
- ✅ **Audit PR 2 (trust hardening) — a malformed SET no longer silently becomes coach chat** *(shipped)*: the masking that hid Bug #1. `rowsFromBackendParsedWorkout` now carries the parser verdict on the thrown error (`par…
- ✅ **Audit PR 3 (post-save UX) — "log it" with nothing new no longer implies a save** *(shipped)*: Bug #2's UX/safety tail. In `handleLogIt`, an EMPTY `sessionLog` AND a prior save this session (`lastWrite` set) now short…
- ✅ **PR-GATEA1 — evidence provenance (SHIPPED)** `[correctness]` — the blocker repair. New pure `services/evidenceProvenance.js` classifies every shadow event `athlete_ui | synthetic | unknown` + `evidence_eligible`, **fa…
- ✅ **PR-5a — pure Coach Orchestrator module** (shipped) — `services/coachOrchestrator.js`: `orchestrate({ envelope, snapshot, runners })` resolves the intent via `capabilityManifest.resolve`, runs complete/partial capabil…
- ✅ **Brian serve-eligibility rail** (owner-approved 2026-07-05; shipped same day) — a safety rail so `brian` SERVE mode cannot expose a Coach's Pick built on a not-yet-runner-complete capability stack. `services/coachEngi…
- ✅ **Resolver hardening — the four PR #809 review notes** (shipped 2026-07-02) — (a) the pass-through branch enum-cases equipment like every other branch; (b) intersect-branch drops now carry `ignored: equipment_stored_av…
- ✅ **Shadow-path wiring** (shipped 2026-07-02) — `stateAssembly` hydrates `constraints_active` (Constraints tab via the injected-reader pattern: lazy `getConstraints` default reader, `_safeRead` degrade-to-`[]`, `knownKey…
- ✅ **Just-logged anchor guard** (shipped 2026-07-03) — `planBrianOverride` gained an optional `context.justLoggedSet` guard: in-workout, the just-logged set is not in the sheet yet (session-level save), so Brian's snapsho…
- ✅ **StarterProgramModule: StrongLifts 5×5 + GZCLP templates + deterministic session runner (roadmap PR 7, shipped)** — `config/coaching/programs/stronglifts-5x5.json` + `config/coaching/programs/gzclp.json` + `services/s…
- ✅ **AutoregulationModule: e1RM-driven, readiness-aware load prescription (roadmap PR 15, shipped)** — `services/autoregulationModule.js`: `computeCurrentE1RM` (best e1RM from last 3 sessions, warm-up excluded), `detectLP…
- **✅ UPDATE (2026-07-10) — B5a challenge voice VALIDATED live (LT-009 PASS).** Agent-performed live validation against production (owner full-auto authorization) using a REAL qualifying lift (Bench Press — genuine `consis…
- ✅ **SHIPPED (task #8, per #950 review) `[correctness]` — phrase-scoped negation.** `discouragementSignal` negation was **whole-string** — a negator anywhere before a discouragement term suppressed the whole message (e.g.…
- ✅ **PR-A (SHIPPED) — schema contract + pure event builders + idempotency.** `config/columns.js` `sessionPlansColumns` (owner-approved order) + `Session_Plans` optional tab (`config/sheetContract.js`, 503 until it exists)…
- ✅ **AUTHORIZED (owner, 2026-07-10) — Session_Plans live-capture wiring slice** (owner will create the tab with the exact 13-col header order). **One narrow PR, tests first, NO schema change, NO drift detection in this PR…
- ✅ **PR-D (SHIPPED — this) — docs/spec only.** Client lifecycle map + authority classification (authoritative/ambiguous/non-authoritative), client-held accepted-plan contract, the three explicit endpoints (`/api/session-p…
- ✅ **PR-E (SHIPPED) — server protocol.** Three authenticated, feature-flagged sidecar endpoints (`routes/sessionPlans.js` + `config/routes.js` entries, `writeCapable:true` but Session_Plans-only): `POST /api/session-plans…
- ✅ **PR-F (SHIPPED) — accepted-plan identity + "Start this plan" button.** New pure/DI `src/app/planAcceptance.js` (opaque `pv_`/`pi_` UUID minting via `crypto.randomUUID`/`getRandomValues` — never timestamp/`Math.random`…
- ✅ **PR-G1 (SHIPPED) — identity propagation + skipped + substituted.** Acceptance now tags each `exercises[]` slot with its immutable `plan_item_id` (`planAcceptance.js` — items[i] ↔ exercises[i]; survives reload/reorder;…
- ✅ **PR-G2 (SHIPPED) — explicit `completed`.** A "Done with this exercise" button on the current-exercise banner (`renderActiveSessionBanner`) is the ONLY authoritative completed boundary. `planOutcome.js` now permits `co…
- ✅ **PR-H (SHIPPED) — explicit closeout capture.** New pure/DI `src/app/planCloseout.js` (`buildCloseoutPayload` fails closed on non-accepted/missing-id/invalid-status; frozen `finalized|abandoned`; `runCloseout` non-bloc…
- ✅ **B4-1 (this PR) — additive mode + register forwarding.** Compute `selectCoachMode → computeCelebrationScarcity → grantRegister` server-side on the set/block reaction path (zero extra Sheets reads — reuses the enrichme…
- ✅ **B4-2 (SHIPPED) — persona register-interpretation block** (`services/coachPersonaCore.js`): the REGISTER block now spells out what routine/elevated/max sound like and what `casual_ok`/`humor_ok` license (wording only;…

## Deliberately retained

- Every unchecked checklist item.
- Every shipped parent that still contains an explicit follow-up, deferred item, watch, owner decision, evidence gate, or live re-test requirement.
- All unmarked prose, even when it looks historical, because deleting it safely requires a separate explicit editorial judgment rather than a completion-marker heuristic.

## Second pass — historical re-section

- Open/deferred blocks retained: **272**
- `BACKLOG.md` lines: **1126 → 826**
- Historical remainder preserved intact in `BACKLOG_ARCHIVE.md`: **yes**
- Current Phase-4 carryovers added explicitly: **yes**
- Runtime/code behavior changed: **no**

The second pass did not delete the old prose. It archived the entire historical remainder verbatim, then rebuilt the active file from action-shaped unchecked/deferred/owner-gated bullets. Any ambiguous item was retained rather than guessed complete.

## Final pass — nested completion and stale-container prune

- Completed/superseded blocks archived: **8**
- Live child blocks promoted: **12**
- `BACKLOG.md` lines: **826 → 813**
- Historical text discarded: **no**
- Runtime/code behavior changed: **no**
