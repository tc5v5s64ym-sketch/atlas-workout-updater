# Atlas Soul Plan v1 — Personality, Voice & Soul: the build queue with prompts

> **Status:** Owner-adopted build plan · **v1.1 (2026-07-08)** — revised per the independent execution review. Companion to the planning brief `docs/ATLAS_SOUL_READINESS_PLAN.md`; this file is the executable version — one paste-ready **PROMPT** per PR, in order, in the style of `docs/REMEDIATION_PLAN_V2.md`.
> **v1.1 changes (review verdict: keep, reordered):** (1) NEW hard gate **LT-007** — live validation of current-session truth (the #925–#930 session-tally/capture-vocabulary/completion-claim fixes shipped 2026-07-08 with unit tests but NO live-session proof; `docs/TEST_QUEUE.md` has no card). LT-007 runs at the very next gym session and is a **blocking gate for PR-B4 and everything after it** — current-session truth outranks personality. It does not block A1/A2/A7 builds (zero-behavior-change consolidation + pure state work). (2) **PR-A7 (Athlete Identity) promoted** to immediately after PR-A2 — the "he knows me" ingredient is the highest felt-value work and its only dependency is the persona core's cite-never-invent rule. (3) PR-A4 moves ahead of PR-A3 (A4 is unblocked; A3 waits on the owner's AC8-b call). Everything else holds, including the two parallel clocks.
> **Sources of authority:** the owner buddy-coach brief (persona direction settled: the logbook-keeper), the coach-voice architecture review lane, `docs/COACH_MOMENTS_ENGINE.md`, `docs/ONE_BRAIN_PROMOTION_CRITERIA.md`, `docs/OWNER_CHECKIN_RULES.md`.
> **Implementation model:** no builder model is mandatory and model choice is not a gate. Risk tags remain review-intensity hints. **OWNER GATE** blocks below are real decision stops.

**How to use this doc:** work top to bottom. Give one **PROMPT** block to a fresh implementation session, let it open one PR under the active workflow (`CLAUDE.md`): deterministic checks green, clean-context cold review, risk-triggered ChatGPT Atlas Contract Review, and merge per that contract — owner-reserved Soul / phase-transition PRs stop for Dale. One concern per PR — adjacent discoveries get filed in `BACKLOG.md`, not built. Items marked **OWNER** are Dale's, not the agent's. The two clocks run in parallel: flip the A-0 flags first, then build while the shadow evidence accumulates.

**Voice authority:** the coach persona, register calibration, profanity ceiling, challenge/warmth/humor, AC8(b), goals storage, and the Set C corpus are governed by `docs/ATLAS_VOICE_RATIFICATION_V1.md` — do not re-litigate them inside this plan. PRs A2, A3, B2, B6, B7, and B8a consume that document as owner-approved source of truth once ratified.

**Sequencing map (v1.1):**
`A-0 (owner, today)` + `LT-007 (owner, next gym session — hard gate for B4+)` → `A1 → A2 → A7` → `A4` → `A3 (when AC8-b is decided)` → *[2–4 gym weeks of hybrid shadow]* → `GATE A (owner)` → `A5 (+ 2-week burn-in)` → `A6` → `B-0 (owner sitting — any time)` → `B1 → B2 → B3` (pure, safe during the window/burn-in) → **[LT-007 must be PASS]** → `B4` → `B5a → B5b` → `B6 lane (needs A5 live + B-0 forks)` → `B7` → `B8a/B8b`. `12B` runs on its own clock after burn-in and blocks nothing in Part B.

---

## Soul completion & Post-Soul handoff contract

> **Added 2026-07-12 (docs-only).** This section makes "when is Soul done, and what happens next" explicit and evidence-checkable. It is authoritative for Soul completion and for the handoff to `docs/POST_SOUL_V1_FINISHING_PLAN.md`.
> **Soul is NOT declared complete by this section.** It records the *criteria* and the *current remaining* items from repository evidence. Marking Soul complete is a separate, later action taken only when every **required** criterion below is recorded done.

### How to read this contract

Each item is classified into exactly one category:

- **Required exit criterion** — Soul is not complete until this is recorded done. These gate the handoff.
- **Optional work** — improves Atlas but never blocks Soul completion.
- **Intentionally deferred work** — consciously postponed; explicitly *not* required for Soul completion.
- **Evidence-gated work** — cannot be declared done by building alone; needs live/observation evidence recorded (a `docs/TEST_QUEUE.md` LT-### card, or an observation window).
- **Owner-gated decision** — an owner check-in under `docs/OWNER_CHECKIN_RULES.md`; an agent may not self-close it.

A single item may carry an evidence-gated or owner-gated tag *in addition to* being required (e.g. a required criterion whose proof is a live card).

### Required Soul exit criteria — and their current state (repository evidence, 2026-07-12)

| # | Required criterion (the plan's finish: "one persona, the athlete's whole story, an engine-granted voice") | State | Evidence |
|---|---|---|---|
| **S1** | One persona, one voice path (PR-A2 persona core; PR-A6 retire the duplicate verdict-reaction voice) | ✅ recorded complete | `services/coachPersonaCore.js`; PR-A2 review-pass 2026-07-09 (`BACKLOG.md`); PR-A6 retirement (#978, `test/verdictReactionRetired.test.js`). |
| **S2** | The athlete's whole story reaches the voice (PR-A7 athlete identity; PR-B8a structured goals) | ✅ **recorded complete — via the owner-confirmed no-goals path** | Athlete-identity half ✅ live (`services/athleteIdentity.js`, PR-A7). Structured-goals reader + plumbing ✅ (`services/athleteGoals.js`, PR-B8a #980). **S2 has two valid completion paths:** **(a)** the owner seeds real goal rows on the production `Constraints` surface and Atlas live-reads/forwards them (no invented data), **or** **(b)** the owner explicitly confirms a **"no active goals"** state **and** the empty-goals behavior is verified safe. **Path (b) is satisfied (2026-07-12):** the owner confirmed no active training goals and asked that none be invented, and the empty-state is **verified** — no `goal` rows → `readAthleteGoals` → `[]` → `sanitizeAthleteGoals` → `null` → the coach sees `athlete_goals` absent; persona-core rule 7 + the chat prompt's goal bullet forbid inventing a goal or the stale `bench_press 215` example; there is **no** code fallback; and workout planning / set-reaction never consume goals, so absence changes nothing. Pinned by `test/athleteGoals.test.js` + `test/athleteGoalsForwarding.test.js` (empty/`[]`/malformed → `null`; `sanitizeChatContext({athlete_goals:[]}).athlete_goals === null`). If the owner later adopts goals, path (a) applies via the deferred **Goal management surface** (`BACKLOG.md`). |
| **S3** | Credibility floor: never celebrate an unlogged set (PR-A4 AC8-c); refuse-and-ask on an unresolved lift (PR-A3 → D7(a)) | ✅ recorded complete | AC8(c) celebration lock-in guard (referenced in the PR-A6 note); D7(a) `services/unresolvedLiftGate.js` (#957, Decision Desk #942). |
| **S4** | Current-session truth proven live (LT-007 hard gate) | ✅ **PASS** (2026-07-09) | `docs/TEST_QUEUE.md` LT-007 (7/7), PR #933. *Evidence-gated — satisfied.* |
| **S5** | Engine-granted voice wired: mode + register into the live voice (PR-B1/B2/B3 pure modules; PR-B4 wiring) | ◐ **PARTIAL — remaining required work** | Modules ✅ (`coachMode.js`, `registerPermissions.js`, `celebrationScarcity.js`). B4-1/B4-3 set/block path + profanity suppressor ✅ (`BACKLOG.md` "PR-B4 split"), profanity **staged OFF** (`ATLAS_COACH_PROFANITY`). **Required-open:** (a) the chat-path **register** wiring (B4-4 register), (b) completion of the `selectCoachMode` input set at the set-reaction call site, (c) **LT-010** routine-vs-earned mode/register validation **with profanity OFF**. **Profanity activation and its live validation are NOT part of S5** — they are optional, separately owner-gated work (see below) and never block Soul completion or the handoff. |
| **S6** | Engine-triggered challenge is real and lives on the live path (PR-B5a sandbag challenge) | ✅ build + live PASS | #954 on the #953 chat coach-mode foundation; `docs/TEST_QUEUE.md` LT-009 **PASS** (2026-07-10). |
| **S7** | Engine-triggered reassurance is real (PR-B5b) and its live behavior is confirmed | ✅ **recorded complete — LT-011 PASS (re-validated live 2026-07-12)** | Wiring #961/#963; precedence (Owner Decision 1). The 2026-07-12 live run found a defect (reassure came back as challenge — `memory_patterns` challenge fuel forwarded in reassure mode); **fix #998** suppresses it. **Re-validated live against the deployed fix (`e94bfbb`/`pr:998`):** explicit discouragement reaches reassure 3/3 (zoom-out + one move) even with the standing challenge pattern; the ordinary turn returns to challenge; the multi-clause-negation case reaches reassure 3/3; "bench isn't stuck anymore" does not (challenge); recovery (`source:engine`) and **safety/pain (3/3 safety-first)** outrank reassure. `docs/TEST_QUEUE.md` LT-011 = PASS. |

**Remaining required exit criterion (open as of 2026-07-12):**

1. **S5 — finish the engine-granted voice:** (a) wire the chat-path register (B4-4 register), (b) complete the `selectCoachMode` input set at the set-reaction call site, and (c) record **LT-010** — routine-vs-earned coach mode/register behavior, verified from flight-recorder evidence, **with profanity OFF** — as PASS. *(evidence-gated: LT-010's required, profanity-OFF portion.)* **Profanity is out of scope for S5:** its activation and live validation are optional, separately owner-gated (see "Optional work" and "Owner-gated decisions").

**S2** was resolved via the owner-confirmed no-goals path (2026-07-12); **S7** is now closed — LT-011 re-validated live to PASS against the deployed fix (`e94bfbb`). S1, S2, S3, S4, S6, and S7 are recorded complete. **Only ONE required criterion remains (S5); once it is recorded complete, Soul is complete and the Post-Soul handoff fires.**

### Optional work (never blocks Soul completion)

- **Profanity register activation + its live validation (separately owner-gated).** The `casual/humor` register and the profanity **suppressor** ship with S5 (staged OFF), but turning profanity **on** is optional and requires a **separate, explicit owner approval**: `ATLAS_COACH_PROFANITY` **remains OFF unless separately and explicitly approved.** Its live check is the **optional profanity portion of LT-010**, run only after that authorization. **This never blocks Soul completion or the Soul→Post-Soul handoff**, and an un-run profanity portion does not prevent LT-010 from passing its required (profanity-OFF) Soul gate.
- **PR-B8b — Tone dial** (owner-facing calibration surface). The plan marks it "optional — build when B4 has lived a few weeks." Convenience for a single-user app.
- De-templated `ack_only` variety and richer arc wording (`BACKLOG.md` coaching-reactions follow-ups) — owner-gated coach-wording polish, not a correctness gap.

### Intentionally deferred work (consciously not required for Soul completion)

- **PR-B5a Part 2b — drift-driven user-facing challenge.** Part 1 (`services/driftSignal.js` #949) and Part 2a (live dark read + shadow computation, `services/driftShadow.js` #976, default OFF) shipped; the athlete-visible drift→challenge consumption waits on shadow observability + explicit authorization. Sandbag challenge (S6) already satisfies the challenge mode.
- **PR-12B — delete the superseded legacy engine lanes.** Runs on its own clock after the One-Brain burn-in; the plan states it "blocks nothing in Part B."

### Evidence-gated work

- **LT-010** (required: routine-vs-earned mode/register with profanity OFF; profanity portion optional) — the remaining open Soul live card (S5). **LT-011** (reassure) — **PASS** (re-validated live 2026-07-12, S7 closed).
- **GATE A — One-Brain promotion observation window.** PR-GATEA1 (evidence provenance, #981) and PR-GATEA2 (read-only eligible-window scorecard, #982) shipped. **The fresh eligible-event numerical floor is already met** — current production carries well over 50 eligible `athlete_ui` / `evidence_eligible=TRUE` rows, and the synthetic / `FALSE` / smoke-provenance proof exists. GATE A is **not complete**; the remaining gap is (1) **varied genuine activity across multiple real workouts and days** (the §3 variety bar, not raw count), (2) the **production scorecard**, (3) a **human safety/quality review**, and (4) the **explicit owner promotion decision** (`docs/ONE_BRAIN_PROMOTION_CRITERIA.md` §5, §8). This is a **parallel, owner-reserved lane** that does not block Soul completion (owner decision 2026-07-12).

### Owner-gated decisions

- **GATE A / PR-A5 — promote One-Brain to primary** (owner-reserved, `docs/OWNER_CHECKIN_RULES.md` criterion 2 / `docs/ONE_BRAIN_PROMOTION_CRITERIA.md`). Evidence-gated *and* owner-gated. The burn-in sign-off that gates PR-12B is downstream of it.
- **PR-B6 — Coach Moments Engine lane** (gated on PR-A5 live + the §10 forks, which are ratified as D6). The lane opener is owner-reserved.
- **Flipping `ATLAS_COACH_PROFANITY` on** — a **separate, explicit owner approval**, **not** part of S5. It remains OFF unless separately approved; its live check is the optional profanity portion of LT-010, run only after that authorization. Never a Soul-completion or handoff blocker.
- Any change to coaching philosophy / voice — already ratified as D1–D8 in `docs/ATLAS_VOICE_RATIFICATION_V1.md`; not re-litigated.

> **Parallel-clock note — the "one brain" question (OWNER-RESOLVED 2026-07-12).** The Soul Plan runs **two clocks in parallel**: the voice/soul build and the One-Brain promotion clock (GATE A → PR-A5 → burn-in → PR-12B). PR-B4+ is gated on **LT-007**, *not* on promotion; only PR-B6 (Moments) and PR-12B sit behind PR-A5.
> **Owner decision (2026-07-12): One-Brain promotion is NOT a blocking Soul-completion criterion.** Soul is complete when all required Soul voice/persona criteria and required live validations (**S1–S7**) are recorded PASS. Optional, intentionally-deferred, and separately evidence-gated work — including GATE A / One-Brain promotion — does **not** block Soul completion or the Soul→Post-Soul handoff.
> **GATE A → PR-A5 remains an active parallel, evidence-gated, owner-reserved lane.** It continues to block **any capability whose own specification explicitly requires One-Brain promotion** (e.g. PR-B6 Coach Moments, which sequences after promotion; PR-12B legacy-lane deletion, which is gated on the post-promotion burn-in). **Promotion must never occur automatically** — it is an explicit owner decision under `docs/ONE_BRAIN_PROMOTION_CRITERIA.md` §8. **Neither Soul completion nor the document-driven handoff constitutes a One-Brain promotion decision.** The required set is **S1–S7**; the handoff waits only on the one open required criterion (S5 as of 2026-07-12 — S7 closed, LT-011 PASS).

### The handoff rule (Soul → Post-Soul)

1. **While any required Soul exit criterion (S1–S7) remains open, Soul retains execution priority.** The active queue stays the required Soul work; an agent selecting the next task picks the open Soul criterion, not the finishing plan.
2. **Once every required Soul exit criterion is recorded complete**, the normal agent work-selection process — `AGENTS.md` backlog/roadmap discipline → `docs/DECISION_KERNEL.md` document precedence → `docs/ACTIVE_ROADMAP.md` — must select **`docs/POST_SOUL_V1_FINISHING_PLAN.md`** as the next governed phase, **without requiring another owner prompt.** The roadmap and docs index are already wired to point there (this PR).
3. **"Automatic handoff" is document-driven work selection only.** It creates **no** trigger, reminder, scheduled job, check-in, watcher, or background task. An agent proceeds because the governance documents route it there when Soul is done — nothing fires on a clock. (The Post-Soul entry gate then applies its own current-state verification before any finishing build begins.)
4. **The handoff is not a promotion decision, and Post-Soul may activate while GATE A remains open.** Per the 2026-07-12 owner decision, the Post-Soul phase can become active with One-Brain promotion still pending — but it must **carry GATE A visibly as an unresolved owner gate** (see `docs/POST_SOUL_V1_FINISHING_PLAN.md`), and any POST item whose spec requires promotion stays blocked until the owner promotes. Selecting the finishing plan never promotes the Brain.

---

## PART A — GETTING TO SOUL-READY

### A-0 — OWNER ACTION — Start the evidence clock (~10 min, Render dashboard, not a PR)

**Why (plain terms):** the Brain can only earn promotion by shadowing real gym decisions, and the flag is still off. Every day it stays off is a day added to the calendar. While you're in the dashboard, also finish the idempotency-disk follow-up from PR-04 — the restart-safe write shield is inert until the disk exists.

**Checklist (owner, on Render):**
1. Env var `ATLAS_COACH_ENGINE=hybrid` (shadow-only; user responses stay byte-identical to legacy — pinned by tests).
2. Env var `ATLAS_INTENT_ROUTER=shadow` (intent classifier observation, fire-and-forget).
3. Recommended: `ATLAS_BRAIN_SHADOW_PERSIST=1` (durable `Brain_Shadow` tab so evidence survives redeploys — this is the owner nod the backlog gates it on).
4. PR-04 follow-up: attach a persistent disk; set `ATLAS_IDEMPOTENCY_FILE=/var/data/atlas-idempotency.json`.
5. Then train normally. Variety > count: multiple lifts, upper + lower, in-workout traffic, planning-time requests. Target: 50+ real events (100 preferred) per `docs/ONE_BRAIN_PROMOTION_CRITERIA.md` §3.

---

### LT-007 — OWNER ACTION — Live validation: current-session truth in coach chat (next gym session, not a PR) · **HARD GATE for PR-B4+**

**Why (plain terms):** the fixes that give the coach authoritative today-facts (per-exercise session tally, capture vs saved vocabulary, session-identity number keying, completion-claim gating — #925–#930) shipped with unit tests but have never been proven at a real session. If Atlas can't reliably answer "what did I do today," no amount of personality survives contact. Current-session truth outranks soul work — this gate makes that enforceable. It does NOT block the A1/A2/A7 builds (they change no session-fact behavior); it blocks the moment the voice starts getting richer (PR-B4 onward).

**Checklist (owner, one real session — file as TEST_QUEUE card LT-007 with sub-checks):**
1. Mid-session, ask the coach: sets logged for a specific lift; the weights just used; total working sets so far; "what did I just do" — every answer must match `session_tally` exactly, no hedging that the tally is unavailable.
2. Log an off-plan exercise, then ask what's planned vs extra — planned flags must be right.
3. Perform a substitution, then ask what was done — the coach reports the lift that was actually logged, not the earlier suggestion.
4. With sets captured but NOT yet saved, confirm the coach uses capture vocabulary (never claims a write happened); after save + verify, confirm it flips.
5. Say "I'm done" with plan items remaining — no completion praise; say it with the plan genuinely complete — appropriate closeout.
6. Any failure: file the finding, fix-PR it (trust-critical lane), and re-run before B4. PASS recorded in `docs/TEST_QUEUE.md` is the gate artifact.

---

### PR-A1 — File the coach-voice architecture review · **Opus 4.8** · `[housekeeping]` (docs-only)

**Why (plain terms):** the voice-architecture findings (two personas in code, 41 KB of duplicated prompt rules, the missing athlete story) currently live only in chat. This puts them under governance so every PR below builds from a committed document, not memory.

**PROMPT:**
> Docs-only PR on a fresh branch. Execute the already-drafted coach-voice architecture review lane exactly as specified in its plan: (1) Write `docs/COACH_VOICE_ARCHITECTURE_REVIEW_2026-07-09.md` in the repo's review-doc style (cf. `docs/AUDIT_2026-07-07_DEEP_REVIEW.md`) containing the split verdict — trust layer durable (deterministic mode selection via `coachNoteTier`/`shouldReactToVerdict`/`finalizeCoachVoice`, whitelist grounding via `sanitizeFacts`/`sanitizeChatContext`, degradation ladder, behavior tests); personality layer prompt-dependent and fragmenting (two personas: `services/coach.js:48` wired "sharp, encouraging" vs `:1258` unwired logbook-keeper; ~41 KB across 5 inline prompt builders + 6 deterministic copy sources; prompt-text-pinned tests; doc↔code drift vs `COACH_PERSONALITY.md`); user-state layer missing (no athlete identity summary forwarded — `detectRecentPrs`/`buildProgressSummary` computed in `services/analytics.js` but never reach a coach prompt; no drift/discouragement signals). Spot-check and cite every file:line claim against the current tree before committing — the retest sweep (#923–#930) has grown `services/coach.js` since the review was drafted; update line numbers and note the new inline RULE blocks (SESSION-TALLY, SESSION-IDENTITY) as live evidence of the fragmentation finding. (2) Append to `BACKLOG.md` per backlog discipline: persona-core extraction (awaiting the PR-A2 prompt in `docs/SOUL_PLAN_V1.md` — this plan is the authorization); persona doc↔code drift reconciliation; athlete-identity facts object; engine-triggered challenge + reassurance modes; the unwired `generateVerdictReaction` voice as a standing decision point. (3) Update `docs/DOCS_INDEX.md` (review doc = Active). Constraints: zero code changes; `npm test` untouched and green; diff = the new doc + BACKLOG.md + DOCS_INDEX.md only. Per CLAUDE.md, do not self-author any of the proposed PRs from this review — they run from this plan's own prompts.

---

### PR-A2 — Persona Core extraction — one coach, one file · **Opus 4.8** · `[correctness]`

**Why (plain terms):** the code contains two coaches, and users hear the wrong one — a generic cheerleader instead of the not-easily-impressed logbook keeper every governance doc describes. This PR merges the seven scattered copies of "who Atlas is" into one owner-ratified file that every voice surface shares, and stops the 104 KB prompt file's rule sprawl before Part B multiplies it.

**OWNER GATE (5 minutes, at PR review):** read the final persona core text before merge. It decides which coach every user hears. The wording is Tier-1 derivable from ratified docs (`docs/DECISION_KERNEL.md`), but the persona IS coaching philosophy — eyeball it.

**PROMPT:**
> Build the Persona Core extraction specified in `docs/COACH_VOICE_ARCHITECTURE_REVIEW_2026-07-09.md` §4 (authorized by `docs/SOUL_PLAN_V1.md` PR-A2). One concern: consolidate persona/shared voice rules; NO engine, sanitizer, gate, signal, or wiring changes. (1) TESTS FIRST: write failing tests asserting (a) each of the five prompt builders in `services/coach.js` (set-reaction, chat, verdict-reaction, plan, compile) emits the persona core exactly once at the top of its system prompt; (b) the shared-rule assertions currently pinned per-builder in `test/coachPromptRules.test.js` and `test/coach.test.js` (IRON RULE on numbers, thin-history filler-phrase ban, anti-hype list, plain-text-only, never-writes) hold against the core so they apply to ALL five voices — today only set + verdict carry some of them; (c) a golden test that the core text forbids the named filler phrases and the anti-pattern vocabulary (`services/coach.js:1293-1295` list: pet names, "beast mode"/"crushing it", emoji, attendance praise). (2) Create `services/coachPersonaCore.js` exporting one frozen persona + iron-rule block. Persona wording: reconcile to the doc-specified voice — the direct training partner who keeps the logbook, not easily impressed, never a hype man, quiet when routine, conclusion-first, contractions, concrete unrounded numbers, judges only against the lifter's own history — synthesized from `COACH_PERSONALITY.md`, `docs/CONVERSATION_CONTRACT_V1.md`, and the owner-approved corpus in `docs/COACH_VOICE_VALIDATION.md`. The wired `:48` "sharp, encouraging" identity line is superseded; the `:1258` unwired identity is the source of truth. (3) All five builders import + prepend the core; delete only the per-builder lines the core now owns; per-voice SIGNAL rules (SESSION-TALLY RULE, verdict outcome rules, etc.) stay exactly where they are. (4) Review-only pass (no edits, file findings in BACKLOG): list where the six deterministic copy sources (`services/coachVoiceRenderer.js`, `services/deterministicCoachRenderer.js`, `services/setEffortCopy` path, `src/app/coachVoiceTemplates.js`, `VERDICT_VARIANTS`/`coachOpener` in `src/app/coach-conversation.js`) contradict the core, so a later pass can reconcile copy. Acceptance: new tests green; full suite green with zero behavior-diff on deterministic paths (byte-identical templated copy); prompt-builder diffs show consolidation only; PR body flags the persona text block for the owner's read. Explicit non-goals: do NOT wire the verdict-reaction voice (that is PR-A6); do NOT touch `finalizeCoachVoice`, sanitizers, or any route.

---

### PR-A7 — Athlete Identity Facts object — the coach gets your story · **Opus 4.8** · `[correctness]` — *promoted in v1.1: builds immediately after PR-A2*

**Why (plain terms):** the difference between "strong set" and "strong set — up from the 185 you started at in March" is most of the buddy feeling, and it's pure deterministic data the app already has but never hands to the voice. After this PR, the coach knows your tenure, your dated PRs, your streaks, and your gaps — and is tested to cite them, never invent them.

**PROMPT:**
> Build the Athlete Identity Facts object per `docs/COACH_VOICE_ARCHITECTURE_REVIEW_2026-07-09.md` recommendation (c) and the owner buddy-coach brief §8 (authorized by `docs/SOUL_PLAN_V1.md` PR-A7). (1) TESTS FIRST from fixtures: pure `services/athleteIdentity.js` — `buildAthleteIdentity(logRows, { asOf })` computing from 12-col `Log_Cleaned` rows (reuse `services/liftPrescription.js` row-normalization helpers; inject `asOf`, never `new Date()`, per the BACKLOG time-bomb note): `tenure_months` + `first_session_date`; per-lift dated PR history (top-3 best working sets with dates + `current_best`, e.g. Bench: 185 @2026-03-14 → 205 @2026-05-30 → 225 @2026-07-01); consistency (current weekly streak, sessions/week over trailing 8 weeks); `longest_gap_days` + `days_since_last_session`; recent milestones (last 60-day `new_ground` events by lift). Fixture cases: rich multi-year history, thin history (<3 sessions → mostly-null object, never fabricated values), gap-heavy history, single-lift history. (2) Sanitizer: extend the frozen-whitelist pattern in `services/coach.js` `sanitizeFacts`/`sanitizeChatContext` — every forwarded identity field explicitly whitelisted, strings injection-stripped, mirroring `test/coach.test.js:29-53`. (3) Forward to BOTH the set-reaction and chat context assemblies as `athlete_identity` (chat already carries history context; the set voice gains its first longitudinal facts). (4) Prompt rule in the Persona Core (PR-A2's file): history may be CITED, never INVENTED — any date/weight/streak claim must appear verbatim in `athlete_identity`; add a `coachPromptRules`-style test asserting the rule text + a behavior test that thin-history input yields no historical claims (THIN-HISTORY rule extended, not duplicated). (5) Read budget: identity computes from the log rows the routes already fetch — assert zero additional Sheets reads per `docs/READ_BUDGET.md` pattern (`test/sheets-adapter-reads.test.js`). Acceptance: module + sanitizer + forwarding + prompt-rule tests green; full suite green; no schema change, no new reads, no write path, no gate/tier changes. Non-goals: goals object (PR-B8a), scarcity (PR-B3), any register/mode work. Read-budget guard: if any voice path lacks the log rows to compute `athlete_identity` without a new Sheets read, do not add a read silently — compute identity only where rows already exist and file the missing surface as a follow-up, or stop and report the tradeoff; no hot-route read increase without explicit review.

---

### PR-A4 — AC8 rule (c) lock-in: never celebrate an unlogged set · **Opus 4.8** · `[trust-critical]` (mostly tests)

**Why (plain terms):** #925 already stopped the coach praising a finished workout that isn't actually saved. This PR proves — with tests that will fail forever after if anyone regresses it — that no celebration can fire anywhere unless a set was genuinely logged. Every celebratory sentence Part B will ever write stands on this.

**PROMPT:**
> Close AC8 rule (c) ("never celebrate a set that wasn't actually logged" — BACKLOG deferred item) as a guard-test sweep + gap fixes. (1) Enumerate every path that can emit celebratory/completion copy: the set-reaction prompt path and `shouldReactToVerdict` gating in `services/coach.js`; `coachNoteTier`/`batchNoteFacts` note paths; the deterministic renderers (`services/coachVoiceRenderer.js`, `src/app/coachVoiceTemplates.js`); client celebration copy in `src/app/coach-conversation.js`; the closeout flow; the "I'm done" plan_state praise gate from #925. (2) For each, add a behavior test asserting no praise/celebration/PR-language output when the triggering input is: a suppressed `question` intent, a `needs_clarification`/unresolved result, a conversation-only (unsaved) set, or an empty `session_tally` — reuse the existing harness patterns in `test/coach-message-block.test.js` and `test/verdict-reaction.test.js`. (3) Reassert the two existing iron gates alongside (PR language requires `progression_verdict.level === 'new_ground'` — `test/coachPromptRules.test.js:168-178` pattern; celebration requires an earned `beat`). (4) If the sweep finds a real gap (a celebration keyed off anything other than a logged set), fix it with the smallest diff and its own failing-test-first. Acceptance: the sweep's path inventory is recorded in the PR body; new guards green; full suite green; any copy-source findings that are cosmetic go to BACKLOG, not this diff. No prompt-builder restructuring (PR-A2 owns that), no new signals, no write path.

---

### PR-A3 — AC8 rule (b): unresolved-lift refusal · **Opus 4.8** · `[correctness]`

**OWNER GATE (decide before pasting — or batch into B-0):** when a message carries set tokens but an unrecognizable lift name (`"zercher thrust 95 8/2"`), Atlas should: **(Option 1) refuse-and-ask** — "didn't catch that lift — which one?", logs nothing until answered (matches the AC8 spec; risk: an intended set requires one extra exchange); or **(Option 2) keep flag-and-log** — today's catalog-review flow (never loses a set; risk: garbage names enter the log pipeline). Record the choice in BACKLOG, then paste the prompt with the option named.

**Why (plain terms):** this is the last open rule of the credibility floor: the coach must never act on a lift it didn't actually understand.

**PROMPT:**
> Wire AC8 rule (b) per the owner decision recorded in BACKLOG.md ("AC8 — phantom-set suppression", deferred rule-b item): OPTION __ [owner fills: 1 refuse-and-ask / 2 keep-flag-and-log]. The deterministic classifier already exists — `services/messageIntent.js` `classifyMessageIntent` returns `reason:'unresolved_lift'`; today a non-question unrecognized lift with set tokens still logs via the `unknown_exercise`/`needsCatalogReview` flow. If Option 1: in `services/workoutTextParser.js` (where rule (a) is already wired after `parseLogSets`), route `unresolved_lift` to a `needs_clarification` intent with a `unresolved_lift` warning and the ask copy, so no row reaches the preview/write path; preserve the both-case safety property (a genuinely resolved lift + set tokens ALWAYS logs, even alongside a question) and the multi-line rescue behavior (a known lift on another line still logs; only the unresolved line asks). If Option 2: ship the current behavior as pinned golden tests + a BACKLOG note closing rule (b) as owner-decided, no code change. Tests first either way: extend `test/messageIntent.test.js` + `test/parser-golden.test.js` with the zercher-thrust case, the both-case, a multi-line mixed case, and the rule-(a) question cases unchanged byte-for-byte. High-risk file (`workoutTextParser.js`): focused fresh-session PR, G1 + parserFuzz regression suites must stay green, zero existing parser tests modified. No write-path, schema, or trust-loop change — this PR only ever REDUCES what reaches the preview.

> **STATUS (2026-07-10) — SHIPPED as D7(a), with a corrected wiring point (Decision Desk #942).** The original prompt's premise — wire inside `services/workoutTextParser.js` with "zero existing parser tests modified" — **failed the verification gate**: the parser's alias table is deliberately narrower than the Exercise KB, so a parser-level refusal over-refuses **catalog-known** lifts (Front Squat, Cable Fly, chin ups…) and flips 13 goldens. Owner verdict: **D7(a) refuse-and-ask**, wired at the **per-line parser orchestration boundary** — a new pure `services/unresolvedLiftGate.js` applied in the `/api/parse-workout-text` route (`index.js`) **after** the parser resolves recognized lines and **with the KB resolver injected**, **before** the client assembles preview rows. A set line neither the parser NOR the KB recognizes → `needs_clarification` + zero rows (`unresolved_lift`); KB-known parser-narrow lifts keep logging; mixed input keeps the known rows and withholds only the unknown line; a recognized lift + a trailing question still logs. KB→parser decoupling held (KB never enters the parser); **parser goldens untouched** (the gate lives outside `parseWorkoutText`); tests at the unit (`test/unresolvedLiftGate.test.js`) + route (`test/api-smoke.test.js`) level, failing-first. Read-only dry-run path — no write/proof-field/schema/trust-loop change.

---

### GATE A — OWNER DECISION — One-Brain promotion review (not a PR)

**Why (plain terms):** the Brain has been auditioning in shadow since A-0. This is you reading its scorecard and deciding, per decision type, whether it drives production.

**Process (owner, ~1 sitting):** run `docs/ONE_BRAIN_PROMOTION_CRITERIA.md` end-to-end — window floor met (50+ real varied events)? Zero unsafe recommendations, zero orchestrator errors, ≥90% agreement on comparable decisions within tolerance (±5 lb upper / ±10 lb lower / ±1 rep), every `ok:false` explainable? Evidence surfaces: `GET /api/debug/brain-shadow`, the `Brain_Shadow` tab, the Hybrid Compare card. Decide per decision type: `progression` first; `workout`/Coach's Pick stays behind the serve rail until its block-level divergence metric ships (filed follow-up). Outcomes: **promote → PR-A5**; **extend the window** (never lower the bar); or **send back for fixes** (file findings, fix PRs, re-observe).

---

### PR-A5 (= remediation PR-12A) — Promote One-Brain with a one-switch rollback · **Opus 4.8** · `[trust-critical]`

**Why (plain terms):** the Brain earned it at GATE A; now it drives the promoted decision types for real — with the old engine kept fully intact behind a single switch, so one bad session means one env-var flip back, not a rollback deploy.

**PROMPT:**
> Execute remediation PR-12A per `docs/REMEDIATION_PLAN_V2.md` Phase 2 and GATE A's recorded per-decision-type outcome (BACKLOG entry: date, evidence summary, promoted types). Promote by configuration, not deletion: `ATLAS_COACH_ENGINE=brian` becomes the documented production mode for the GATE-A-promoted decision types via the existing `services/coachEnginePromotion.js` machinery; `SERVE_ELIGIBLE_DECISION_TYPES` reflects exactly the owner's decision (expanding it further is itself owner-gated per `docs/ONE_BRAIN_PROMOTION_CRITERIA.md`). Legacy lanes stay fully intact and reachable (`legacy`/`hybrid` byte-identical — pinned); every ineligible/failed/clarification/deload path still falls back to legacy with `engine_source` provenance. Deliverables: (1) tests pinning promoted-type drive + every fallback branch at route level across all three gates (`/api/recommend/next/:liftCode`, `/api/plan/today`, `/api/plan/intent-recommendation`), extending `test/coachEnginePromotion.test.js` + `test/api-smoke.test.js`; (2) the burn-in checklist from the promotion-criteria doc committed as `docs/verification/ONE_BRAIN_BURNIN_12A.md` with the ≥2-weeks / ≥4-real-sessions gate and rollback procedure (one env var, expected behavior after flip); (3) `.env.example` + `docs/DEPLOYMENT_GUIDE.md` document `ATLAS_COACH_ENGINE` legal values and the rollback; (4) BACKLOG updated: PR-12A shipped, burn-in open, PR-12B gated on it. Consider (do not silently ship) the two deferred flip items in BACKLOG's One-Brain section — fire-and-forget shadow observation off the reply path, and the `Deload_State` double-read dedupe: if either is needed for acceptable live latency, build it HERE as part of the flip with its own tests; otherwise record why not. No write path, no schema, no trust-loop, no LLM-path change — this PR only changes which deterministic engine sources the numbers.

**OWNER — burn-in (calendar):** ≥2 weeks / ≥4 real sessions on `brian`, checklist clean, recorded in BACKLOG. One-var rollback if anything smells wrong. PR-A6/A7 and B1–B3 build during this window.

---

### PR-A6 — Verdict-reaction voice disposition: one set-reaction path · **Opus 4.8** · `[correctness]`

**Why (plain terms):** the well-specified logbook-keeper reaction voice has sat in the code, fully written and never connected, while a duplicate does the job. With the Persona Core live, fold what's uniquely good about it into the one real path and delete the duplicate — ending the "two coaches" era in code, not just in prompts.

**PROMPT:**
> Resolve the standing decision point from `docs/COACH_VOICE_ARCHITECTURE_REVIEW_2026-07-09.md` (BACKLOG: "unwired generateVerdictReaction voice"): consolidate to ONE set-reaction voice path. Default disposition (owner-ratified in `docs/SOUL_PLAN_V1.md`): FOLD — migrate into the wired set-reaction path anything the unwired voice (`services/coach.js` ~`buildVerdictReactionSystemPrompt`, gate `shouldReactToVerdict` ~`:1395`) has that the wired path lacks, then retire the duplicate builder. Inventory first, in the PR body: compare the two builders rule-by-rule now that PR-A2 centralized the shared persona — expected unique content includes the don't-cave-on-pushback rule (~`:1275`), the react-on-rule-raised gate (caution/reject e.g. pain, RIR 0–1 grind), and the quiet-on-clean-met gate (verify against `test/verdict-reaction.test.js` — some of this may already be live). Migrate each unique rule with a behavior test; keep `shouldReactToVerdict`'s semantics byte-identical where already runtime-tested; then delete `buildVerdictReactionSystemPrompt` + its dead callers, and update the PR-13 wired-or-deleted guard expectations. If the inventory reveals the fold is NOT safe in one PR (e.g. the wired path structurally can't express a gate), STOP and report per CLAUDE.md rather than forcing it — the fallback disposition (wire the unwired voice behind its gate) is a different PR and needs an owner ping. Acceptance: one set-reaction builder remains; don't-cave + rule-raised + quiet-on-met all runtime-tested on the LIVE path; full suite green; `test/coach-message-block.test.js` tier wiring unchanged. No engine/gate-logic changes beyond the migration; no new signals.

---

### PR-12B — Delete the superseded legacy lanes *(after burn-in clears)* · **Opus 4.8** · `[trust-critical]`

**Why (plain terms):** the fire-the-old-coach PR. Only after the new one has demonstrably held the room for two-plus weeks. Runs on its own clock; nothing in Part B waits for it.

**PROMPT:**
> Execute remediation PR-12B per `docs/REMEDIATION_PLAN_V2.md` Phase 2, gate satisfied: PR-A5 live ≥2 weeks / ≥4 real sessions with `docs/verification/ONE_BRAIN_BURNIN_12A.md` clean, recorded in BACKLOG (verify before touching anything; if the record is absent, STOP). Delete the legacy decision lanes the promoted Brain superseded — for the promoted decision types only: the `planBrianOverride` compatibility fallbacks collapse to Brain-primary, the corresponding `scoreIntents`/`recommendNextSet` consumer paths retire per the analytics.js-migration endgame note (BACKLOG One-Brain item 8), and the `ATLAS_COACH_ENGINE` tri-state simplifies only if all its consumers are promoted (otherwise document the residual matrix). Un-promoted surfaces (coach chat/message, set reaction if not yet promoted) keep their legacy paths untouched. Every deletion: name the superseding engine path + its test in the PR body; behavior tests move from "legacy byte-identical" pins to "Brain-primary" pins in the same diff. Full suite + e2e green; `brainShadow` observation stays wired (it now measures the primary engine). This is the highest-blast-radius PR in the plan — smallest possible diff per lane, and if any lane resists clean deletion, ship what's clean and file the remainder rather than forcing one heroic diff.

---

## PART B — THE SOUL BUILD

### B-0 — OWNER CALIBRATION SITTING (one batch, not a PR — do any time, ideally this week)

**Why (plain terms):** every taste decision the build will ever need, settled in one sitting so the lane never stalls waiting on you. PM drafts, you rule.

**Decision list (record outcomes in BACKLOG, one line each):**
1. **Profanity ceiling** — permitted at all? If yes, confirm the brief's rules: top intensity rung only, engine-certified rare events only (new_ground, earned comeback beat), never in safety/pain/correction/uncertainty, scarcity cap (proposal: not twice in the same week).
2. **Register calibration** — pick the default on the spectrum: "Goddamn. 315." ←→ "That's new ground — 315." Warmth default, challenge firmness, humor allowance (low-stakes moments only per the brief).
3. **Moments Engine §10 forks** (`docs/COACH_MOMENTS_ENGINE.md`): F1 goal-proximity unprompted?, F2 frequency dial default, F3 teaching-moment default, F4 cross-lift narrative.
5. **AC8(b)** if not already decided at PR-A3.
5. **Corpus ratification** — confirm the Set A/B pattern: PM drafts the buddy-register golden corpus (PR-B7), owner edits/approves, corpus becomes the regression standard.
6. **Goals storage nod** (for PR-B8a): Constraints-tab row vs a new small tab (light schema decision).

### PR-B1 — Coaching-mode enum — the engine names every moment · **Opus 4.8** · `[correctness]` (pure, unwired)

**Why (plain terms):** today "when the coach speaks, and in what spirit" is real but implicit, scattered across tiers, verdicts, and triggers. This gives it one name in one place — silent, nod, praise, celebrate, correct, challenge, reassure, and so on — decided by the engine, so the model never gets to choose how a moment is framed.

**PROMPT:**
> Pure-module PR (unwired, one concern): `services/coachMode.js` per the buddy-coach brief §5 layer 3, authorized by `docs/SOUL_PLAN_V1.md` PR-B1. Export a frozen `COACH_MODES` enum — `silent | nod | note | praise | celebrate | correct | challenge | reassure | educate | refuse | safety` — and pure total `selectCoachMode(facts)` → `{ mode, evidence }` mapping EXISTING deterministic triggers onto it (no new detection logic in this PR): `note_trigger:'form_safety'` → safety (highest precedence — fires even when verdicts look on-plan, per the tested behavior); engine `rule_decisions` reject/hold → refuse; caution → correct; `effort_verdict` fell_short / far_easy and warn-grade substitutions → correct; `progression_verdict.level==='new_ground'` (scarcity input arrives in PR-B3; accept an optional `scarcityClear` param, default true) → celebrate; earned `beat` → praise; `memory_patterns.consistent_underperformance` → challenge; layoff-return state → reassure; `/api/coach/ask`-class SME context → educate; `coachNoteTier` ack_only/routine clean-met → silent; remaining note-worthy tiers → note/nod per `services/coachNoteTier.js` semantics. Precedence pinned by table-driven tests: safety > refuse > correct > challenge > reassure > celebrate > praise > educate > note > nod > silent. `evidence` echoes the exact fact fields that justified the mode (key-aware, trust-contract style). Fixture tests: every mode reachable from a realistic fact shape; a clean met set with no rule raised maps to silent (the crown jewel, cross-referenced to `test/verdict-reaction.test.js` semantics); unknown/empty facts → silent, never a throw. No wiring, no route, no prompt, no I/O, no LLM. File in BACKLOG: the mode gaps this enum makes visible (chat drift → PR-B5a; discouragement → PR-B5b).

### PR-B2 — Register permissions — the volume knob the model can't touch · **Opus 4.8** · `[correctness]` (pure, unwired)

**Why (plain terms):** how excited the coach gets is never the model's call. The engine computes a volume level — routine, elevated, or max — plus whether casual language and (if you allowed it) profanity are licensed, from the moment type, how recently Atlas last celebrated, and your B-0 settings. The model receives permissions; it never grants itself any.

**PROMPT:**
> Pure-module PR (unwired): `services/registerPermissions.js` per the buddy-coach brief §5 layer 4, authorized by `docs/SOUL_PLAN_V1.md` PR-B2, encoding the B-0 owner calibration recorded in BACKLOG [paste the recorded values]. Export pure total `grantRegister({ mode, scarcity, ownerPrefs })` → `{ intensity: 'routine'|'elevated'|'max', casual_ok, humor_ok, profanity_ok }`, with the owner calibration living as DATA in `config/coaching/register.calibration.json` (single source, schema-validated like `config/coaching/contracts/*` — defaults + ceilings + scarcity caps), not prose. Iron invariants as table-driven tests: `casual_ok` true by default (casual is the register, not a reward) EXCEPT safety/refuse/pain contexts → false; `humor_ok` only in low-stakes modes (nod/note/educate/closeout-class), never correct/challenge/safety/reassure; `profanity_ok` ⟹ `intensity==='max'` ∧ mode==='celebrate' ∧ scarcity clear ∧ owner ceiling permits — unreachable in safety/pain/correction/uncertainty by construction; `max` only for celebrate (and, if owner-enabled, comeback-beat praise); safety mode forces `{routine, casual_ok:false, humor_ok:false, profanity_ok:false}`; every input combination returns a valid grant (total function, garbage → most-conservative grant). Property test: enumerate mode × scarcity × pref-corner combinations and assert no path grants profanity outside the certified cell. No wiring, no prompt, no I/O. Non-goal: scarcity computation (PR-B3 supplies it; accept it as an input here).

### PR-B3 — Celebration scarcity, derived not stored · **Opus 4.8** · `[correctness]` (pure, unwired)

**Why (plain terms):** big reactions stay rare because rarity is the feature. Instead of a new database field, Atlas derives "when did I last get genuinely excited" from the training log itself — new-ground events are already in there.

**PROMPT:**
> Pure-module PR (unwired): `services/celebrationScarcity.js` per the buddy-coach brief §5 layer 5, authorized by `docs/SOUL_PLAN_V1.md` PR-B3. Export pure `computeCelebrationScarcity(logRows, { asOf, caps })` → `{ scarcityClear, last_new_ground: {lift, date}|null, new_ground_last_7d, new_ground_last_30d }`, deriving new-ground history from 12-col `Log_Cleaned` rows using the SAME per-lift best-detection semantics as the live `new_ground` verdict path in `services/analytics.js` (import/reuse the existing helper — do not re-derive PR detection with different math; if no reusable helper exists, extract one byte-identically first, in this PR, analytics-migration style). `caps` defaults from `config/coaching/register.calibration.json` (PR-B2's file; owner cap e.g. max 1 top-rung celebration per rolling 7 days). Injected `asOf`, no `new Date()`. Fixture tests: two new-ground days back-to-back → second computes `scarcityClear:false` (B2 will downgrade celebrate→praise); a 30-day drought clears; multi-lift same-day new grounds count once per the cap's unit; empty/thin history → `scarcityClear:true` with null last event; zero extra Sheets reads (derives from rows already fetched). No wiring, no persistence, no schema. Non-goal: consuming the value (PR-B4).

### PR-B4 — Wire mode + register into the live voice · `[trust-critical]` — *the big wiring PR*

**Why (plain terms):** the switch-on moment. The three pure modules (mode, permissions, scarcity) plug into the real coach paths, and the persona core learns what each volume level sounds like. From this PR on, every sentence the coach speaks carries an engine-granted mood and volume — and prose that outruns its grant gets suppressed by the same deterministic ladder that already polices contradictions.

**PROMPT:**
> The mode/register wiring PR, authorized by `docs/SOUL_PLAN_V1.md` PR-B4. GATE CHECK FIRST: `docs/TEST_QUEUE.md` LT-007 (current-session truth live validation) must be recorded PASS — if absent or failed, STOP and report; this PR may not build. Prereqs merged: PR-A2 (persona core), PR-A7 (identity facts), PR-B1/B2/B3 (pure modules). One concern: wiring + the register-interpretation prompt block; no new detection logic, no new modes. (1) In the server voice paths (set-reaction and chat context assembly in `services/coach.js` / `routes/coachOps.js`, and the closeout path), compute `selectCoachMode(facts)` → `computeCelebrationScarcity(...)` → `grantRegister(...)`, and forward `coach_mode` + `register` as sanitizer-whitelisted fields (extend `sanitizeFacts`/`sanitizeChatContext` + their frozen-enum imports so the vocabularies can't drift — enums imported FROM `coachMode.js`/`registerPermissions.js`, matching the stimulusGovernor pattern). (2) `mode==='silent'` must resolve BEFORE any LLM fetch, preserving the zero-fetch-on-quiet-set behavior (`test/coach-message-block.test.js:109-117` class) — assert byte-identical quiet behavior. (3) Persona core (services/coachPersonaCore.js) gains the register-interpretation block: what routine/elevated/max sound like; what `casual_ok`/`humor_ok` license; the profanity rules verbatim from `config/coaching/register.calibration.json` (cite the config as source in a comment; do not fork the values into prose). The model NEVER chooses intensity — the block states it words the granted level only. (4) Extend `finalizeCoachVoice` (`routes/coachOps.js:332-343`) suppression: a register violation (detectable deterministically — profanity tokens without `profanity_ok`; celebration/PR vocabulary when mode ∉ {celebrate, praise}) triggers the existing deterministic-fallback ladder exactly like a reason-code contradiction; add the profanity token list beside the existing anti-pattern list. (5) Tests: whitelist round-trip for both new fields incl. injection-stripping; a seeded `max`+`profanity_ok` grant appears ONLY with the full gate chain (property test reusing PR-B2's matrix through the wired path); prompt carries the register block exactly once (extend the PR-A2 once-per-prompt tests); suppression fires on a seeded violation and degrades to the renderer line, HTTP 200, never a write; deload/pain/safety fact shapes produce routine-only grants end-to-end. Acceptance: full suite + e2e green; TEST_QUEUE entries filed for live validation at the next real session (mode/register visible in the flight recorder for review). Non-goals: drift/discouragement signals (B5), Moments (B6), any copy rewrite of the deterministic renderers.

### PR-B5a — Engine-triggered challenge: the drift signal · **Opus 4.8** · `[correctness]`

**Why (plain terms):** "brave enough to challenge when you're drifting" is in the north star, but chat currently has no tripwire for it — the model just improvises. This builds the deterministic tripwire (skipped-pattern streaks, chronic plan deviation, persistent sandbagging) so pushback always arrives with the receipts, asked as a question, not a lecture.

**PROMPT:**
> Two-part PR authorized by `docs/SOUL_PLAN_V1.md` PR-B5a. Part 1, pure: `services/driftSignal.js` — `detectDrift(logRows, plannedVsCompleted, memoryPatterns, { asOf })` → `{ drifting:bool, kind: 'skipped_pattern_streak'|'plan_deviation'|'sandbag_persistence', evidence }` with conservative, tested thresholds (proposal, tune in fixtures: same movement pattern skipped ≥3 consecutive planned sessions; ≥50% planned-lift deviation across 3 consecutive sessions; `consistent_underperformance` persisting across ≥2 detection windows). Reuse the Missed-Lift planned-vs-completed memory (Roadmap Step 365) and `memory_patterns` — no new storage. Fixture tests: each kind fires on its shape; a legitimate deload/layoff window does NOT read as drift (guard on deload state + `days_since_last_session`); thin history never fires. Part 2, wiring: `detectDrift` output feeds `selectCoachMode` (challenge mode gains its chat trigger — update `coachMode.js` mapping + precedence tests) and the evidence forwards through the whitelist. Persona core challenge block: Motivational-Interviewing shape — state the pattern with its numbers, then ASK (the sandbag scenario's "what's going on — the knee, or just not feeling it?" register), never lecture; and the don't-cave rule (migrated in PR-A6) applies when the user pushes back. Behavior tests: a drifting fixture through the chat path yields challenge mode with evidence attached; a user-disagrees follow-up turn keeps the position (prompt-rule test); no drift → no challenge. Full suite green. Non-goal: discouragement (B5b).

> **STATUS (2026-07-10) — Part 1 SHIPPED; Part 2 dependency graph corrected (premise finding #951, owner-directed follow-up).** Part 1 (`services/driftSignal.js` + fixtures) merged (#949). Part 2 does NOT reuse the "Missed-Lift planned-vs-completed memory" as the PROMPT assumed — verified against `main`, that memory (`opts.missedLiftHistory`) is **never populated on any live path**, because Atlas persists only *logged* history and past **plans are ephemeral client state**. Corrected wiring plan:
> - **Prerequisite RESOLVED — chat-path coach-MODE foundation** (`services/chatCoachMode.js`, wired into `buildChatContext`): the chat voice now carries an engine `coach_mode`, the seam Part 2 attaches `challenge` to. (Register still deferred — B4-4.)
> - **`sandbag_persistence` → challenge: BUILDABLE now** on that seam (it derives from logged rows via `memory_patterns`/`detectPatterns`). Do not ship a sandbag-only partial B5a unless a revised plan authorizes the split.
> - **`skipped_pattern_streak` + `plan_deviation`: PREREQUISITE RESOLVED (2026-07-11).** The persisted plan-history data layer is live: Decision Desk #952 → the Session_Plans event-sourced sidecar (PR-D…PR-I), **production-validated** (accept / completed / substituted / skipped / finalized + abandoned closeouts + idempotent replay + invalid-payload rejection all proven live against the real tab; canary rows administratively removed; `ATLAS_SESSION_PLANS_WRITE` now enabled for real shadow capture).
> - **PR-B5a Part 2a SHIPPED — live dark read + shadow drift computation.** The finalized-only fold (`services/sessionPlanReader.js` `readFinalizedPlannedVsCompleted`) feeds `detectDrift` through a gated, TTL-cached, fail-closed live read adapter (`services/driftShadow.js`), wired **dark** into the chat path behind `ATLAS_DRIFT_SHADOW` (default OFF; OFF ⇒ zero Session_Plans reads). It emits a structured `[drift-shadow]` diagnostic to the existing observation surface only — **nothing reaches the athlete**: the drift result is never passed to the LLM, never changes `selectCoachMode`, never triggers challenge, never alters copy or session execution, and the reply never blocks on the read. Note: `patternFor` is name-based, so lift-code history maps to `other/needsReview` — `skipped_pattern_streak` cannot fire from the current code-only history (the safe "unknown pattern" floor), so shadow surfaces only `plan_deviation` / `sandbag_persistence` for now.
> - **STILL DEFERRED — user-facing challenge consumption.** Part 2b (drift → `selectCoachMode` challenge trigger + the persona challenge block + the athlete-visible pushback) is NOT built here; it waits on observability from the shadow lane and an explicit authorization. No threshold tuning from production data yet.

### PR-B5b — Engine-triggered reassurance: the discouragement seed · **Opus 4.8** · `[correctness]`

**Why (plain terms):** when you type "bench is stuck, I feel like I'm going backwards," the zoom-out ("squat's up 15 since June; you haven't missed a Monday in two months") shouldn't be model improvisation — it should be the engine handing over the exact facts that make it true. Deliberately the smallest honest version: explicit frustration phrases plus stall context, not a psychology model.

**PROMPT:**
> Two-part PR authorized by `docs/SOUL_PLAN_V1.md` PR-B5b, mirroring PR-B5a's shape. Part 1, pure: `services/discouragementSignal.js` — extend the `isTirednessExpression` seed pattern (chat route) into a conservative `detectDiscouragement(messageText, context)` → `{ discouraged:bool, kind:'frustration'|'negative_self_talk'|'stall_frustration', matched }`: explicit-phrase classes only (\b-anchored, lift-name-collision-proof like `isQuestionText`), boosted to stall_frustration when `stalls` context co-occurs. NOT the 16-state research model (`docs/research/.../07-the-feel-of-atlas.md` stays non-governing) — explicit language only, no sentiment inference. Golden tests: firing phrases, near-miss non-firing phrases ("tired" routes to the existing recovery read, not here), lift-name false-positive guards. Part 2, wiring: feeds `selectCoachMode` → reassure (precedence per PR-B1); the reassure evidence pack assembles the ZOOM-OUT facts deterministically — per-lift trends, `athlete_identity` streaks/tenure (PR-A7), the stalled lift's own history — whitelisted like everything else. Persona core reassure block: acknowledge in one clause, zoom out with the athlete's own numbers, one concrete next move (rep-scheme change class), never "believe in yourself" filler (extend the banned-filler list); honest-verdict rule holds — reassurance never fabricates progress (thin-history → say less, not warmer). Behavior tests: frustrated message + stall context through chat yields reassure mode carrying trend/streak evidence; the same message with no supporting positive facts yields the honest variant (no fabricated zoom-out — prompt-rule test); recovery-read routing for tiredness unchanged byte-for-byte. Full suite green.

> **STATUS (2026-07-10) — Part 1 SHIPPED; Part 2 unblocked to build (owner-directed follow-up).** Part 1 (`services/discouragementSignal.js` + golden fixtures) merged (#950). Part 2's prerequisite — a coaching mode on the chat voice for `reassure` to attach to — is now RESOLVED by the chat-path coach-mode foundation (`services/chatCoachMode.js`). So `detectDiscouragement` → reassure with the deterministic zoom-out pack is **buildable** once authorized (all its inputs — trends, `athlete_identity`, the stalled lift's history — are live from logs; unlike drift's two plan-history kinds, reassure needs **no** plan persistence). Precedence to preserve when wired: the route already resolves tiredness/recovery **before** the LLM, so reassure must never override that path. ✅ Review carry-over RESOLVED — **B5b Part 2 — clause-scope `NEGATED`** in `discouragementSignal` (per #950 review): negation is now **phrase-scoped** (`_isNegatedAt`, negator + ≤2 words), so a negated clause no longer suppresses a genuine discouragement in another clause ("I don't feel weak but bench is stuck" now fires); pure single-phrase negations still suppress. Red-first fixtures added; live re-validation folded into TEST_QUEUE LT-011.

### PR-B6 — Coach Moments Engine, Phase 1 lane *(multi-PR, spec-governed)* · **Opus 4.8** · `[trust-critical]`

**Why (plain terms):** the seven earned moments — PR, progression callback, quality streak, stall broken, goal proximity, teaching, comeback — with a praise budget and an anti-repetition ledger, deployed shadow-first exactly like the Brain earned its job. The spec already exists and governs; this prompt opens the lane rather than re-specifying it.

**Gate check (owner, before pasting):** PR-A5 live (Moments spec §9 sequences after One-Brain promotion) ✚ B-0 forks F1–F4 recorded in BACKLOG. Goal-proximity (one of the seven) additionally needs PR-B8a — build it last within the lane or after B8a.

**PROMPT (lane opener — expect the agent to propose the PR split, then execute one at a time):**
> Open the Coach Moments Engine Phase 1 build lane per `docs/COACH_MOMENTS_ENGINE.md` (Active, governing) — gates satisfied: One-Brain PR-12A live (verify the BACKLOG record) and the §10 owner forks F1–F4 decided (read the recorded decisions; if any is missing, STOP and list them). First deliverable, docs-only: read the spec end-to-end and file in BACKLOG a proposed PR split honoring one-concern-per-PR — expected shape: (1) pure moment detectors on deterministic history (consuming `athleteIdentity`, `celebrationScarcity`, verdict/stall/streak facts; each moment M1–M7 with fixture tests incl. its honesty rules), (2) the praise-economy budget + anti-repetition ledger per spec (note its storage decision — if the ledger needs persistence beyond derive-from-log, that is a light owner schema nod; flag it, don't ship it silently), (3) shadow-first wiring (moments computed and recorded — brainShadow-style ring/observability — NEVER surfaced), (4) the surfacing PR, mode/register-integrated (a moment proposes; `selectCoachMode`/`grantRegister` still decide framing and volume; `finalizeCoachVoice` still suppresses), gated on the owner reviewing the shadow record. Wait for owner approval of the split, then execute PR 1. Iron rules from the spec bind every PR in the lane: honesty contract (no moment fires without its evidencing facts), praise economy (budget-capped), scarcity respected, per-set silence untouched. Non-goals anywhere in the lane: goal-proximity before PR-B8a; any write-path or schema change without its nod.

### PR-B7 — Golden corpus + voice regression harness · **Opus 4.8** · `[correctness]`

**Why (plain terms):** turn your taste into a permanent test. You approve a corpus of "this is Atlas / this is not Atlas" examples across the sixteen situations; from then on, the deterministic halves (which moment, which volume) are asserted by tests forever, and a manual script grades sampled live wording against the rubric. Also begins retiring the brittle tests that pin prompt text instead of behavior.

**PROMPT:**
> Two-part PR authorized by `docs/SOUL_PLAN_V1.md` PR-B7, after the B-0 corpus ratification (owner-approved buddy-register corpus recorded — extend `docs/COACH_VOICE_VALIDATION.md` with Set C following the Set A/B ratification pattern; if Set C is not yet owner-approved, deliver Part 1 as the DRAFT corpus PR for owner review and STOP). Part 1, corpus: encode the sixteen owner-brief scenarios (ordinary set → genuine celebration) as data fixtures — each with input facts, expected `coach_mode`, expected `register` grant, required-fact citations, and in-character/out-of-character exemplar lines. Part 2, harness: (a) deterministic tests driving `selectCoachMode` + `grantRegister` (+ scarcity) over every corpus fixture — behavior assertions, extending the tier/gate test pattern; (b) every prompt surface carries the persona core exactly once (already pinned in PR-A2 — extend to any surface added since); (c) begin migrating `test/coachPromptRules.test.js` prompt-TEXT pins to behavior/core-level assertions where PR-A2/B4 made that possible — migrate what's clean, file the remainder in BACKLOG (do not weaken any guard in the process: a pin is only removed when a behavior test covers the same failure); (d) `scripts/voice-validation-live.js` — a manual, advisory script that samples live LLM outputs for corpus scenarios and scores them against the rubric (registers violations, filler phrases, uncited numbers), printed for owner review, NEVER a CI gate (model output is nondeterministic). Full suite green; corpus doc + fixtures + harness in one coherent diff.

### PR-B8a — Structured goals · **Opus 4.8** · `[correctness]` *(needs the B-0 storage nod)*

**Why (plain terms):** "chasing 215 on bench" is the one identity fact the log can't derive. A tiny structured goal object unlocks goal-proximity moments and callbacks that know what you're actually training toward.

**PROMPT:**
> Build the minimal structured-goals object per the buddy-coach brief §3 (missing entirely: long-term goals), authorized by `docs/SOUL_PLAN_V1.md` PR-B8a with the B-0 storage decision recorded in BACKLOG: [Constraints-tab row convention | new small tab] — if absent, STOP and ask. Schema (smallest honest version): `{ lift_code, target_weight, target_reps?, set_date, note? }`, read-only from the coach's perspective (goal EDITING is a later surface; seed the owner's current goals — 215 Bench, 215 Back Squat — via the chosen storage by hand or a one-off script, not app code). Pure `services/athleteGoals.js` reader + validator following the `constraintResolver` stored-row mapping pattern; hydrated via `stateAssembly`'s injected-reader pattern (lazy default reader, degrade-to-empty, `knownKeys` updated); forwarded through the sanitizer whitelist as `athlete_goals`; persona-core rule: goal proximity may be stated only from `athlete_goals` + current e1RM/best facts (cite-never-invent extended). Tests: reader fixtures incl. malformed rows degrade-to-skip; whitelist round-trip; a goal-proximity prompt-rule test (claims require both the goal and the current number present). If the B-0 decision was a new tab: declare it in `config/sheetContract.js` optional tabs, read-only, no write path. Zero new reads on hot routes if the chosen storage is the Constraints tab (already hydrated); otherwise document the read in `docs/READ_BUDGET.md`.

### PR-B8b — Tone dial (owner-facing calibration surface) · **Opus 4.8** · `[polish]` *(optional — build when B4 has lived a few weeks)*

**Why (plain terms):** your B-0 taste settings, adjustable from the app instead of a PR. For a single-user app this is a convenience, not a need — which is why it's last.

**PROMPT:**
> Surface the owner tone preferences (backlog: "chattiness dial, tone, celebration sensitivity") as a Settings card, authorized by `docs/SOUL_PLAN_V1.md` PR-B8b. Scope: read/write a small preferences object — `{ chattiness: brief|normal|chatty, register_ceiling: professional|casual|full, celebration_sensitivity: low|normal|high }` — persisted client-side first (localStorage, matching the hybridCompare pattern) and sent per-request as `ownerPrefs` into the existing `grantRegister` input (PR-B2's `ownerPrefs` parameter — the module already consumes it; this PR only supplies live values with the config-file values as defaults). `register_ceiling` can only LOWER the config ceiling, never raise it above `config/coaching/register.calibration.json` (invariant test). `chattiness` maps onto the existing `coachNoteTier`/`isBriefTier` verbosity machinery — reuse, don't fork. Tests: prefs round-trip; ceiling-only-lowers property; brief chattiness produces the existing brief-tier behavior byte-identically; absent prefs = config defaults everywhere (no behavior change until the owner touches the dial). No server persistence, no schema, no write path. Non-goal: per-context overrides, multi-user anything.

---

## Owner touchpoint index (everything that's yours, in order)

1. **A-0** — flip the Render flags + idempotency disk (today, ~10 min).
2. **LT-007** — one real gym session validating current-session truth (next workout; hard gate for PR-B4+).
3. **PR-A2 merge** — five-minute read of the persona core text.
4. **AC8(b)** — refuse-and-ask vs flag-and-log (before PR-A3, or inside B-0).
5. **GATE A** — the promotion review sitting (when the shadow window fills).
6. **Burn-in sign-off** — after ≥2 weeks / ≥4 sessions on `brian` (gates 12B).
7. **B-0** — the calibration sitting: profanity, register, §10 forks, corpus method, goals storage (any time; unblocks all of Part B).
8. **B6 lane** — approve the Moments PR split; review the shadow record before surfacing.
9. **B7** — ratify the Set C corpus.
10. **Later, when its divergence metric ships** — promote the `workout` decision type past the serve rail.

*End of plan. When every box above is checked, Atlas has one brain, one persona, the athlete's whole story, and an engine-granted voice — and the soul is no longer a metaphor, it's a test suite.*
